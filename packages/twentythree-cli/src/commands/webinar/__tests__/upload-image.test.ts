import { describe, it } from 'vitest'

describe('WebinarUploadImage', () => {
  it.todo('uses tokenFieldName live_id (not upload_token)')
  it.todo('sends type via extraFields')
  it.todo('prints admin URL with webinar ID')
})
