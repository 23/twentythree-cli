import { describe, it } from 'vitest'

describe('WebinarList', () => {
  it.todo('renders table with ID, Title, Status, Date, Private columns')
  it.todo('uses live_id ?? photo_id for ID column')
  it.todo('returns JSON with webinar breadcrumb')
})
