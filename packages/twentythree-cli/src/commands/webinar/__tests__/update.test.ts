import { describe, it } from 'vitest'

describe('WebinarUpdate', () => {
  it.todo('maps --title flag to body.name field')
  it.todo('enters interactive mode when no flags')
  it.todo('validates webinar ID is numeric')
})
