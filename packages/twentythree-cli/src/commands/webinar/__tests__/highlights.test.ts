import { describe, it } from 'vitest'

describe('WebinarHighlights', () => {
  it.todo('renders table with Type, Start, End, Absolute Start columns')
  it.todo('passes photo_id when --video-id provided')
})
