import { describe, it } from 'vitest'

describe('WebinarRepeat', () => {
  it.todo('sends POST /live/repeat with live_id and schedule_start_time')
  it.todo('prints new webinar ID and admin URL')
  it.todo('--date flag is required')
})
