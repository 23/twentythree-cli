import { describe, it } from 'vitest'

describe('CategoryUpdate', () => {
  it.todo('sends POST /album/update with only provided flags')
  it.todo('enters interactive mode when no metadata flags provided')
  it.todo('validates album_id is numeric')
})
