import { describe, it } from 'vitest'

describe('CategoryDelete', () => {
  it.todo('prompts for confirmation including domain name')
  it.todo('sends POST /album/delete with album_id')
  it.todo('skips confirmation in --json mode')
})
