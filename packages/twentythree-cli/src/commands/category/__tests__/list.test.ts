import { describe, it } from 'vitest'

describe('CategoryList', () => {
  it.todo('renders table with ID, Title, Hidden, Created columns')
  it.todo('returns JSON with ok, data, summary, breadcrumbs')
})
