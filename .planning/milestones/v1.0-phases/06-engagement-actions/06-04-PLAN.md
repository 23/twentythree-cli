---
plan: "06-04"
phase: "06-engagement-actions"
type: execute
wave: 1
depends_on: []
title: "Player commands (list, update, delete, embed, embed-versions, styles)"
goal: "All 6 player commands work including embed code generation via stdout"
requirements: [PLY-01, PLY-02, PLY-03, PLY-04, PLY-05, PLY-06]
files_modified:
  - packages/twentythree-cli/src/commands/player/list.ts
  - packages/twentythree-cli/src/commands/player/update.ts
  - packages/twentythree-cli/src/commands/player/delete.ts
  - packages/twentythree-cli/src/commands/player/embed.ts
  - packages/twentythree-cli/src/commands/player/embed-versions.ts
  - packages/twentythree-cli/src/commands/player/styles.ts
autonomous: true
must_haves:
  truths:
    - "player list renders a table of players (POST form endpoint)"
    - "player update modifies player settings"
    - "player delete removes a player after confirmation"
    - "player embed extracts embed_code from JSON and writes to stdout (D-4)"
    - "player embed-versions lists available embed versions for an object"
    - "player styles lists available player styles"
  artifacts:
    - path: "packages/twentythree-cli/src/commands/player/list.ts"
      provides: "player list (POST form pagination)"
    - path: "packages/twentythree-cli/src/commands/player/update.ts"
      provides: "player update"
    - path: "packages/twentythree-cli/src/commands/player/delete.ts"
      provides: "player delete with confirmation"
    - path: "packages/twentythree-cli/src/commands/player/embed.ts"
      provides: "embed code extraction + stdout.write (D-4)"
    - path: "packages/twentythree-cli/src/commands/player/embed-versions.ts"
      provides: "embed versions listing"
    - path: "packages/twentythree-cli/src/commands/player/styles.ts"
      provides: "player styles listing"
  key_links:
    - from: "player/embed.ts"
      to: "/player/embed"
      via: "D-4: extract data.embed_code from JSON, stdout.write"
      pattern: "embed_code.*process\\.stdout\\.write"
    - from: "player/list.ts"
      to: "/player/list"
      via: "POST form body pagination (Pitfall 2)"
      pattern: "apiClient\\.POST.*player/list"
---

<objective>
Implement all 6 player commands for the TwentyThree CLI.

Purpose: Enables users to manage video players, generate embed codes, and browse player styles and versions.
Output: 6 command files in src/commands/player/
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-engagement-actions/06-CONTEXT.md
@.planning/phases/06-engagement-actions/06-RESEARCH.md

@packages/twentythree-cli/src/commands/video/list.ts
@packages/twentythree-cli/src/commands/video/get.ts
@packages/twentythree-cli/src/commands/video/update.ts
@packages/twentythree-cli/src/lib/base-command.ts
@packages/twentythree-cli/src/lib/output.ts
@packages/twentythree-cli/src/lib/term-map.ts
@packages/twentythree-cli/src/lib/pagination.ts

<interfaces>
From src/lib/output.ts:
- renderTable(headers: string[], rows: string[][]): Table
- formatJsonOutput(opts: { ok: boolean, data: any, summary: string, breadcrumbs: any[] }): object
- formatApiError(error: unknown): string
- EXIT_ERROR = 1
- EXIT_CANCELLED = 2

From src/lib/pagination.ts:
- fetchAllPages<T>(fetcher: (page, size) => Promise<{data: T[], total_count: number}>): Promise<T[]>
</interfaces>
</context>

<tasks>

<task id="06-04-01" title="Player list, update, delete commands">
  <name>Player list, update, delete commands</name>
  <read_first>
    - packages/twentythree-cli/src/commands/video/list.ts -- list command pattern
    - packages/twentythree-cli/src/commands/video/update.ts -- POST form update pattern
    - packages/twentythree-cli/src/lib/output.ts -- renderTable, EXIT_CANCELLED
    - packages/twentythree-cli/src/lib/pagination.ts -- fetchAllPages signature
    - .planning/phases/06-engagement-actions/06-RESEARCH.md -- Pitfall 2 (player/list is POST), player API params, player update sparse types
  </read_first>
  <action>
Create 3 player command files.

**player/list.ts (PLY-01):** POST form to `/player/list`.
- CRITICAL PITFALL 2: `/player/list` is POST, not GET. Pagination params go in the form body.
- Flags: `--source` (string, optional).
- Use `fetchAllPages` adapted for POST body:
  ```
  const players = await fetchAllPages<any>(async (page, size) => {
    const { data, error } = await this.apiClient.POST('/player/list', {
      body: { p: page, size, source: flags.source } as any,
      headers: { 'Content-Type': 'application/x-www-form-urlencoded' },
    })
    if (error) this.error(applyCliTerms(formatApiError(error)), { exit: EXIT_ERROR })
    const resp = data as any
    const items: unknown[] = Array.isArray(resp?.data) ? resp.data : resp?.data ? [resp.data] : []
    return { data: items, total_count: resp?.total_count }
  })
  ```
- Render table: columns [ID, Name, Default].
  - ID: `player_id`
  - Name: `player_name`
  - Default: `default_p ? 'yes' : 'no'`
- Count line + empty state "No players found."

**player/update.ts (PLY-02):** POST form to `/player/update`.
- Args: `id` (string, required -- player_id).
- Flags: `--name` (string, optional), `--description` (string, optional), `--data` (string, optional -- JSON escape hatch for untyped player properties).
- Body: start with `{ player_id: Number(args.id) }`. Add `name` and `description` only if defined. If `--data` provided, parse JSON and merge into body.
- Success: `chalk.green('Player updated')`.

**player/delete.ts (PLY-03):** POST form to `/player/delete`.
- Args: `id` (string, required -- player_id).
- Confirmation prompt: `Delete player ${args.id} from ${this.activeWorkspace.domain}? This cannot be undone.`
- Body: `{ player_id: Number(args.id) }`.
- Success: `chalk.green('Player deleted')`.
  </action>
  <done>
    - File packages/twentythree-cli/src/commands/player/list.ts exists with `apiClient.POST('/player/list'` (POST not GET -- Pitfall 2)
    - player/list.ts sends pagination in body with `Content-Type: application/x-www-form-urlencoded`
    - player/list.ts contains `renderTable` with columns including 'ID', 'Name'
    - File packages/twentythree-cli/src/commands/player/update.ts exists with `player_id` in body
    - File packages/twentythree-cli/src/commands/player/delete.ts exists with `confirm({` for destructive confirmation
    - All 3 files contain `static enableJsonFlag = true`
  </done>
  <verify>
    <automated>cd /Users/steffenchristensen/23/twentythree-cli && pnpm --filter twentythree-cli exec tsc --noEmit 2>&1 | head -50</automated>
  </verify>
</task>

<task id="06-04-02" title="Player embed, embed-versions, styles commands">
  <name>Player embed, embed-versions, styles commands</name>
  <read_first>
    - packages/twentythree-cli/src/commands/player/list.ts -- just created; verify player pattern
    - packages/twentythree-cli/src/lib/output.ts -- formatJsonOutput, renderTable
    - .planning/phases/06-engagement-actions/06-RESEARCH.md -- Pattern 5 (player/embed returns JSON with data.embed_code), Pitfall 3, D-4, embed/embed-versions/styles API params
    - packages/twentythree-cli/src/api/types.ts -- verify player/embed response shape (search for embed_code)
  </read_first>
  <action>
Create 3 player command files.

**player/embed.ts (PLY-04, D-4):** GET query to `/player/embed`.
- CRITICAL CORRECTION: `/player/embed` returns JSON with `data.embed_code`, NOT raw HTML. Use openapi-fetch (apiClient.GET), NOT native fetch.
- Flags: `--video-id` (string, optional -- maps to `photo_id`), `--webinar-id` (string, optional -- maps to `live_id`), `--category-id` (string, optional -- maps to `album_id`), `--player-id` (string, optional), `--url` (string, optional), `--width` (integer, optional), `--height` (integer, optional), `--responsive` (boolean, optional -- maps to `responsive_p`), `--autoplay` (boolean, optional -- maps to `autoplay_p`), `--iframe` (boolean, optional -- maps to `iframe_p`), `--start` (integer, optional), `--include-unpublished` (boolean, optional -- maps to `include_unpublished_p`), `--token` (string, optional), `--source` (string, optional).
- Call `this.apiClient.GET('/player/embed', { params: { query: { photo_id: flags['video-id'] ? Number(flags['video-id']) : undefined, live_id: flags['webinar-id'] ? Number(flags['webinar-id']) : undefined, album_id: flags['category-id'] ? Number(flags['category-id']) : undefined, player_id: flags['player-id'] ? Number(flags['player-id']) : undefined, url: flags.url, width: flags.width, height: flags.height, responsive_p: flags.responsive ? 1 : undefined, autoplay_p: flags.autoplay ? 1 : undefined, iframe_p: flags.iframe ? 1 : undefined, start: flags.start, include_unpublished_p: flags['include-unpublished'] ? 1 : undefined, token: flags.token, source: flags.source } } })`.
- Extract: `const embedCode = (data as any)?.data?.embed_code ?? ''`.
- JSON mode: `return formatJsonOutput({ ok: true, data: { embed_code: embedCode }, summary: 'Embed code generated', breadcrumbs })`.
- Normal mode: `process.stdout.write(embedCode)` -- pipeable, no trailing newline per D-4.

**player/embed-versions.ts (PLY-05):** GET query to `/player/embed-versions`.
- Flags: `--object-type` (string, required -- "photo", "live", "album", "site"), `--object-id` (string, required), `--source` (string, optional).
- Call `this.apiClient.GET('/player/embed-versions', { params: { query: { object_type: flags['object-type'], object_id: Number(flags['object-id']), source: flags.source } } })`.
- Extract items, render table or detail output.
- Success output with items.

**player/styles.ts (PLY-06):** GET query to `/player/styles`.
- Flags: `--fields` (string, optional).
- Call `this.apiClient.GET('/player/styles', { params: { query: { fields: flags.fields } } })`.
- Extract items, render table: columns [Name, Description] (or appropriate fields from response).
- Count line + empty state "No player styles found."
  </action>
  <done>
    - File packages/twentythree-cli/src/commands/player/embed.ts exists with `apiClient.GET('/player/embed'` (NOT native fetch -- Pitfall 3)
    - player/embed.ts extracts `embed_code` from `(data as any)?.data?.embed_code`
    - player/embed.ts contains `process.stdout.write(embedCode)` for pipeable output (D-4)
    - player/embed.ts has `--video-id` flag mapping to `photo_id` and `--webinar-id` flag mapping to `live_id`
    - player/embed.ts supports `--json` mode with formatJsonOutput wrapping the embed code
    - File packages/twentythree-cli/src/commands/player/embed-versions.ts exists with `--object-type` (required) and `--object-id` (required)
    - File packages/twentythree-cli/src/commands/player/styles.ts exists with `apiClient.GET('/player/styles'`
    - All 3 files contain `static enableJsonFlag = true`
  </done>
  <verify>
    <automated>cd /Users/steffenchristensen/23/twentythree-cli && pnpm --filter twentythree-cli exec tsc --noEmit 2>&1 | head -50</automated>
  </verify>
</task>

</tasks>

<verification>
  <command>cd /Users/steffenchristensen/23/twentythree-cli && pnpm --filter twentythree-cli exec tsc --noEmit && pnpm --filter twentythree-cli test --run</command>
  <must_haves>
    - 6 files exist under packages/twentythree-cli/src/commands/player/
    - player/list.ts uses POST (not GET) per Pitfall 2
    - player/embed.ts uses apiClient.GET (not native fetch) and extracts embed_code from JSON per Pitfall 3
    - player/embed.ts uses process.stdout.write for pipeable output per D-4
    - player/delete.ts has confirmation prompt
    - TypeScript compiles without errors
  </must_haves>
</verification>

<threat_model>
  <threats>
    <threat id="T-06-07" severity="low" mitigation="player/delete.ts requires interactive confirmation; skipped only in --json mode">Unintended player deletion</threat>
    <threat id="T-06-08" severity="low" mitigation="player/embed.ts writes only the embed_code string to stdout; no token or credential data in the output">Embed code output may contain tracking tokens</threat>
  </threats>
  <notes>Player commands are workspace-scoped. The embed command outputs HTML/JS code that may contain site-specific URLs and tokens -- this is expected behavior (the embed code is meant to be embedded in web pages). The player/list POST quirk (Pitfall 2) is an API design choice, not a security concern.</notes>
</threat_model>

<success_criteria>
All 6 player commands compile and follow established patterns. player/list correctly uses POST. player/embed extracts embed_code from JSON response (not raw HTML) and writes to stdout (D-4). player/delete has confirmation.
</success_criteria>

<output>
After completion, create `.planning/phases/06-engagement-actions/06-04-SUMMARY.md`
</output>
