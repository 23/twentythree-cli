---
plan: "06-03"
phase: "06-engagement-actions"
type: execute
wave: 1
depends_on: []
title: "Comment commands including reaction 3-level topic"
goal: "All 8 comment commands work including 3 reaction sub-commands (11 total)"
requirements: [CMT-01, CMT-02, CMT-03, CMT-04, CMT-05, CMT-06, CMT-07, CMT-08]
files_modified:
  - packages/twentythree-cli/src/commands/comment/list.ts
  - packages/twentythree-cli/src/commands/comment/add.ts
  - packages/twentythree-cli/src/commands/comment/update.ts
  - packages/twentythree-cli/src/commands/comment/delete.ts
  - packages/twentythree-cli/src/commands/comment/promote.ts
  - packages/twentythree-cli/src/commands/comment/clone.ts
  - packages/twentythree-cli/src/commands/comment/set-order.ts
  - packages/twentythree-cli/src/commands/comment/reaction/add.ts
  - packages/twentythree-cli/src/commands/comment/reaction/list.ts
  - packages/twentythree-cli/src/commands/comment/reaction/remove.ts
autonomous: true
must_haves:
  truths:
    - "comment list renders comments with optional object-id/object-type filtering"
    - "comment add creates a comment on an object with content"
    - "comment update modifies a comment's status"
    - "comment delete removes a comment after confirmation"
    - "comment promote toggles promoted status"
    - "comment clone duplicates a comment"
    - "comment set-order reorders comments on an object"
    - "comment reaction add/list/remove work as 3-level oclif topic"
  artifacts:
    - path: "packages/twentythree-cli/src/commands/comment/list.ts"
      provides: "comment list with object_id/object_type filtering"
    - path: "packages/twentythree-cli/src/commands/comment/add.ts"
      provides: "comment add (D-2: standalone topic, --object-id, --object-type)"
    - path: "packages/twentythree-cli/src/commands/comment/delete.ts"
      provides: "comment delete with confirmation"
    - path: "packages/twentythree-cli/src/commands/comment/reaction/add.ts"
      provides: "3-level topic (D-3)"
    - path: "packages/twentythree-cli/src/commands/comment/reaction/list.ts"
      provides: "3-level topic (D-3)"
    - path: "packages/twentythree-cli/src/commands/comment/reaction/remove.ts"
      provides: "3-level topic (D-3)"
  key_links:
    - from: "comment/reaction/add.ts"
      to: "/comment/reaction/add"
      via: "3-level oclif topic via directory structure (D-3)"
      pattern: "apiClient\\.GET.*comment/reaction/add"
    - from: "comment/add.ts"
      to: "/comment/add"
      via: "D-2: --object-id + --object-type, values pass through as-is"
      pattern: "object_type.*object_id"
---

<objective>
Implement all 8 comment commands plus 3 comment reaction sub-commands for the TwentyThree CLI.

Purpose: Enables users to manage comments on videos/webinars/categories and handle reactions on comments. The comment reaction commands introduce the first 3-level oclif topic in the CLI.
Output: 10 command files across src/commands/comment/ and src/commands/comment/reaction/
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-engagement-actions/06-CONTEXT.md
@.planning/phases/06-engagement-actions/06-RESEARCH.md

@packages/twentythree-cli/src/commands/video/list.ts
@packages/twentythree-cli/src/commands/video/get.ts
@packages/twentythree-cli/src/commands/video/update.ts
@packages/twentythree-cli/src/lib/base-command.ts
@packages/twentythree-cli/src/lib/output.ts
@packages/twentythree-cli/src/lib/term-map.ts
@packages/twentythree-cli/src/lib/pagination.ts

<interfaces>
From src/lib/output.ts:
- renderTable(headers: string[], rows: string[][]): Table
- formatJsonOutput(opts: { ok: boolean, data: any, summary: string, breadcrumbs: any[] }): object
- formatApiError(error: unknown): string
- EXIT_ERROR = 1
- EXIT_CANCELLED = 2

From src/lib/pagination.ts:
- fetchAllPages<T>(fetcher: (page, size) => Promise<{data: T[], total_count: number}>): Promise<T[]>
</interfaces>
</context>

<tasks>

<task id="06-03-01" title="Comment list, add, update, delete commands">
  <name>Comment list, add, update, delete commands</name>
  <read_first>
    - packages/twentythree-cli/src/commands/video/list.ts -- list pattern with fetchAllPages
    - packages/twentythree-cli/src/commands/video/update.ts -- POST form action pattern
    - packages/twentythree-cli/src/lib/output.ts -- renderTable, formatJsonOutput, EXIT_CANCELLED
    - .planning/phases/06-engagement-actions/06-RESEARCH.md -- comment API params, D-2 (standalone topic, --object-id, --object-type, values as-is)
  </read_first>
  <action>
Create 4 core comment command files. Per D-2: comment is a standalone topic with `--object-id` and `--object-type` flags. Object type values pass through as-is (API native: "photo", "album", "live"). No term-mapping on --object-type values.

**comment/list.ts (CMT-01):** GET query to `/comment/list` with pagination.
- Flags: `--object-id` (string, optional), `--object-type` (string, optional -- values: "photo", "album"; note "live" not in list enum per Pitfall 4), `--comment-type` (string, optional), `--search` (string, optional), `--order` (string, optional), `--include-reactions` (boolean, optional -- maps to `include_reactions_p`), `--include-replies` (boolean, optional -- maps to `include_reply_to_comments_p`), `--promoted` (boolean, optional -- maps to `promoted_p`).
- Use `fetchAllPages` with p/size pagination.
- Render table: columns [ID, Author, Content, Type, Date].
- Count line + empty state "No comments found."

**comment/add.ts (CMT-02):** POST form to `/comment/add`.
- Flags: `--object-id` (string, required), `--object-type` (string, required -- "photo" or "live"), `--content` (string, optional), `--name` (string, optional), `--email` (string, optional), `--url` (string, optional), `--comment-type` (string, optional -- "comment", "question", "chat"), `--reply-to` (string, optional -- maps to `reply_to_comment_id`), `--comment-time` (string, optional), `--object-token` (string, optional).
- Body: `{ object_id: Number(flags['object-id']), object_type: flags['object-type'], content, name, email, url, comment_type, reply_to_comment_id, comment_time, object_token }`. Only include defined flags.
- Success: `chalk.green('Comment added')` + log comment_id if returned.

**comment/update.ts (CMT-03):** POST form to `/comment/update`.
- Args: `id` (string, required -- comment_id).
- Flags: `--object-id` (string, required), `--status` (string, optional -- maps to `comment_status`, options: "answered", "dismissed", "").
- Body: `{ object_id: Number(flags['object-id']), comment_id: Number(args.id), comment_status: flags.status }`.
- Success: `chalk.green('Comment updated')`.

**comment/delete.ts (CMT-04):** POST form to `/comment/delete`.
- Args: `id` (string, required -- comment_id).
- Confirmation prompt: `Delete comment ${args.id} from ${this.activeWorkspace.domain}? This cannot be undone.`
- Body: `{ comment_id: Number(args.id) }`.
- Success: `chalk.green('Comment deleted')`.
  </action>
  <done>
    - File packages/twentythree-cli/src/commands/comment/list.ts exists with `fetchAllPages` usage and `apiClient.GET('/comment/list'`
    - comment/list.ts has `renderTable` with columns including 'ID', 'Author', 'Content'
    - File packages/twentythree-cli/src/commands/comment/add.ts exists with `--object-id` and `--object-type` flags (both required)
    - comment/add.ts sends `object_type` value as-is (no applyCliTerms on the value itself, per D-2)
    - File packages/twentythree-cli/src/commands/comment/update.ts exists with `comment_id` and `object_id` in body
    - File packages/twentythree-cli/src/commands/comment/delete.ts exists with `confirm({` for destructive confirmation
    - All 4 files contain `static enableJsonFlag = true`
  </done>
  <verify>
    <automated>cd /Users/steffenchristensen/23/twentythree-cli && pnpm --filter twentythree-cli exec tsc --noEmit 2>&1 | head -50</automated>
  </verify>
</task>

<task id="06-03-02" title="Comment promote, clone, set-order commands">
  <name>Comment promote, clone, set-order commands</name>
  <read_first>
    - packages/twentythree-cli/src/commands/comment/add.ts -- just created; verify pattern consistency
    - .planning/phases/06-engagement-actions/06-RESEARCH.md -- promote, clone, set-order API params
  </read_first>
  <action>
Create 3 remaining top-level comment command files.

**comment/promote.ts (CMT-05):** POST form to `/comment/promote`.
- Args: `id` (string, required -- comment_id).
- Flags: `--promoted` (boolean, optional -- maps to `promoted_p`; omit to toggle).
- Body: `{ comment_id: Number(args.id), promoted_p: flags.promoted !== undefined ? (flags.promoted ? 1 : 0) : undefined }`. Only include promoted_p if the flag was explicitly set.
- Success: `chalk.green('Comment promoted')` or `chalk.green('Comment promotion toggled')`.

**comment/clone.ts (CMT-06):** GET query to `/comment/clone`.
- Args: `id` (string, optional -- comment_id).
- Flags: `--clone-type` (string, optional -- maps to `clone_comment_type`, options: "chat", "question", "comment").
- Call `this.apiClient.GET('/comment/clone', { params: { query: { comment_id: args.id ? Number(args.id) : undefined, clone_comment_type: flags['clone-type'] } } })`.
- Success: `chalk.green('Comment cloned')` + log new comment_id if returned.

**comment/set-order.ts (CMT-07):** POST form to `/comment/set-order`.
- Flags: `--object-id` (string, required), `--order` (string, required -- comma-separated comment IDs), `--comment-type` (string, optional -- default "question").
- Body: `{ object_id: Number(flags['object-id']), order: flags.order, comment_type: flags['comment-type'] ?? 'question' }`.
- Success: `chalk.green('Comment order updated')`.
  </action>
  <done>
    - File packages/twentythree-cli/src/commands/comment/promote.ts exists with `promoted_p` in body
    - File packages/twentythree-cli/src/commands/comment/clone.ts exists with `apiClient.GET('/comment/clone'` (GET not POST)
    - File packages/twentythree-cli/src/commands/comment/set-order.ts exists with `--object-id` (required) and `--order` (required) flags
    - comment/set-order.ts body includes `order` field (comma-separated comment IDs)
    - All 3 files contain `static enableJsonFlag = true`
  </done>
  <verify>
    <automated>cd /Users/steffenchristensen/23/twentythree-cli && pnpm --filter twentythree-cli exec tsc --noEmit 2>&1 | head -50</automated>
  </verify>
</task>

<task id="06-03-03" title="Comment reaction add, list, remove (3-level topic)">
  <name>Comment reaction add, list, remove (3-level topic)</name>
  <read_first>
    - packages/twentythree-cli/src/commands/comment/add.ts -- verify existing comment pattern
    - .planning/phases/06-engagement-actions/06-RESEARCH.md -- D-3 (3-level topic), reaction API params, Pattern 6 (3-level oclif topic), Pitfall 5 (class naming)
  </read_first>
  <action>
Create 3 comment reaction command files in the `comment/reaction/` subdirectory. Per D-3: these are a 3-level oclif topic discovered by directory structure. Class names follow Topic1+Topic2+Verb convention.

All three reaction endpoints are GET queries (not POST) despite performing mutations.

**comment/reaction/add.ts (CMT-08):** GET query to `/comment/reaction/add`.
- Class name: `CommentReactionAdd`.
- Args: `id` (string, required -- comment_id).
- Flags: `--reaction` (string, required -- the emoji, maps to `reaction_emoji`), `--object-id` (string, required), `--object-token` (string, required), `--object-type` (string, optional -- "live", "photo", "album"), `--uuid` (string, optional).
- Call `this.apiClient.GET('/comment/reaction/add', { params: { query: { comment_id: Number(args.id), reaction_emoji: flags.reaction, object_id: Number(flags['object-id']), object_token: flags['object-token'], object_type: flags['object-type'], uuid: flags.uuid } } })`.
- Success: `chalk.green('Reaction added')`.

**comment/reaction/list.ts (CMT-08):** GET query to `/comment/reaction/list`.
- Class name: `CommentReactionList`.
- Flags: `--object-id` (string, required), `--object-token` (string, required), `--object-type` (string, optional), `--uuid` (string, optional).
- Extract items from response, render table: columns [Comment ID, Emoji, Count] (or appropriate fields from response).
- Count line + empty state "No reactions found."

**comment/reaction/remove.ts (CMT-08):** GET query to `/comment/reaction/remove`.
- Class name: `CommentReactionRemove`.
- Args: `id` (string, required -- comment_id).
- Flags: `--reaction` (string, required -- maps to `reaction_emoji`), `--object-id` (string, required), `--object-token` (string, required), `--object-type` (string, optional), `--uuid` (string, optional).
- Same GET pattern as add.
- Success: `chalk.green('Reaction removed')`.

CRITICAL: All 3 use `this.apiClient.GET` not POST. The `--object-token` is required (no auto-lookup per research recommendation).
  </action>
  <done>
    - Directory packages/twentythree-cli/src/commands/comment/reaction/ exists with 3 files
    - File comment/reaction/add.ts exports class named `CommentReactionAdd`
    - File comment/reaction/list.ts exports class named `CommentReactionList`
    - File comment/reaction/remove.ts exports class named `CommentReactionRemove`
    - All 3 files use `apiClient.GET` (not POST)
    - All 3 files have `--object-token` as a required flag
    - reaction/add.ts and reaction/remove.ts have `--reaction` flag mapping to `reaction_emoji`
    - All 3 files extend AuthenticatedCommand
  </done>
  <verify>
    <automated>cd /Users/steffenchristensen/23/twentythree-cli && pnpm --filter twentythree-cli exec tsc --noEmit 2>&1 | head -50</automated>
  </verify>
</task>

</tasks>

<verification>
  <command>cd /Users/steffenchristensen/23/twentythree-cli && pnpm --filter twentythree-cli exec tsc --noEmit && pnpm --filter twentythree-cli test --run</command>
  <must_haves>
    - 7 files exist under packages/twentythree-cli/src/commands/comment/ (top-level)
    - 3 files exist under packages/twentythree-cli/src/commands/comment/reaction/
    - comment/add.ts uses --object-id and --object-type (standalone, per D-2)
    - comment/reaction/ files use GET queries (not POST)
    - comment/reaction/ class names follow CommentReaction{Verb} convention
    - comment/delete.ts has confirmation prompt
    - TypeScript compiles without errors
  </must_haves>
</verification>

<threat_model>
  <threats>
    <threat id="T-06-05" severity="low" mitigation="comment/delete.ts requires interactive confirmation; skipped only in --json mode">Unintended comment deletion</threat>
    <threat id="T-06-06" severity="low" mitigation="object_token is a required flag on all reaction commands -- user must provide it explicitly; no auto-lookup that could leak tokens">Object token exposure in comment reaction commands</threat>
  </threats>
  <notes>Comment commands operate on workspace-scoped resources. The object_type values pass through as-is per D-2 (no term mapping), which is intentional -- users provide API-native values. The 3-level topic is discovered automatically by oclif's filesystem convention.</notes>
</threat_model>

<success_criteria>
All 10 comment command files compile. The 3-level `comment reaction` topic works via directory structure (D-3). Comment add uses standalone --object-id/--object-type pattern (D-2). Comment delete has confirmation. All reaction commands require --object-token.
</success_criteria>

<output>
After completion, create `.planning/phases/06-engagement-actions/06-03-SUMMARY.md`
</output>
