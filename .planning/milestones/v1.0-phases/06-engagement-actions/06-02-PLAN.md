---
plan: "06-02"
phase: "06-engagement-actions"
type: execute
wave: 1
depends_on: []
title: "Collector and Tag commands"
goal: "All 3 collector commands and 2 tag commands work correctly"
requirements: [COL-01, COL-02, COL-03, TAG-01, TAG-02]
files_modified:
  - packages/twentythree-cli/src/commands/collector/list.ts
  - packages/twentythree-cli/src/commands/collector/include.ts
  - packages/twentythree-cli/src/commands/collector/exclude.ts
  - packages/twentythree-cli/src/commands/tag/list.ts
  - packages/twentythree-cli/src/commands/tag/related.ts
autonomous: true
must_haves:
  truths:
    - "collector list renders a table of workspace collectors with optional analytics"
    - "collector include attaches a collector to an object using action_id parameter"
    - "collector exclude blocks a collector from an object using action_id parameter"
    - "tag list renders a paginated table of workspace tags"
    - "tag related returns related tags for a given tag"
  artifacts:
    - path: "packages/twentythree-cli/src/commands/collector/list.ts"
      provides: "collector list with optional --include-analytics flag"
    - path: "packages/twentythree-cli/src/commands/collector/include.ts"
      provides: "collector include (GET endpoint, action_id param)"
    - path: "packages/twentythree-cli/src/commands/collector/exclude.ts"
      provides: "collector exclude (GET endpoint, action_id param)"
    - path: "packages/twentythree-cli/src/commands/tag/list.ts"
      provides: "paginated tag list"
    - path: "packages/twentythree-cli/src/commands/tag/related.ts"
      provides: "related tags lookup"
  key_links:
    - from: "collector/include.ts"
      to: "/collector/include"
      via: "GET query with action_id (not collector_id)"
      pattern: "apiClient\\.GET.*collector/include.*action_id"
    - from: "collector/exclude.ts"
      to: "/collector/exclude"
      via: "GET query with action_id"
      pattern: "apiClient\\.GET.*collector/exclude.*action_id"
---

<objective>
Implement 3 collector commands and 2 tag commands for the TwentyThree CLI.

Purpose: Enables users to manage data collectors (lead capture forms) on videos/webinars and browse/search tags across the workspace.
Output: 5 command files across src/commands/collector/ and src/commands/tag/
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-engagement-actions/06-CONTEXT.md
@.planning/phases/06-engagement-actions/06-RESEARCH.md

@packages/twentythree-cli/src/commands/video/list.ts
@packages/twentythree-cli/src/commands/video/get.ts
@packages/twentythree-cli/src/lib/base-command.ts
@packages/twentythree-cli/src/lib/output.ts
@packages/twentythree-cli/src/lib/term-map.ts
@packages/twentythree-cli/src/lib/pagination.ts

<interfaces>
From src/lib/output.ts:
- renderTable(headers: string[], rows: string[][]): Table
- formatJsonOutput(opts: { ok: boolean, data: any, summary: string, breadcrumbs: any[] }): object
- formatApiError(error: unknown): string
- EXIT_ERROR = 1

From src/lib/pagination.ts:
- fetchAllPages<T>(fetcher: (page, size) => Promise<{data: T[], total_count: number}>): Promise<T[]>
</interfaces>
</context>

<tasks>

<task id="06-02-01" title="Collector list, include, exclude commands">
  <name>Collector list, include, exclude commands</name>
  <read_first>
    - packages/twentythree-cli/src/commands/video/list.ts -- list command pattern with fetchAllPages + renderTable
    - packages/twentythree-cli/src/lib/output.ts -- renderTable, formatJsonOutput, EXIT_ERROR
    - packages/twentythree-cli/src/lib/base-command.ts -- AuthenticatedCommand
    - .planning/phases/06-engagement-actions/06-RESEARCH.md -- collector API params; CRITICAL: action_id not collector_id (Pitfall 1)
  </read_first>
  <action>
Create 3 collector command files.

**collector/list.ts (COL-01):** GET query to `/collector/list`.
- Flags: `--object-id` (string, optional), `--include-analytics` (boolean, optional -- maps to `include_analytics_p`).
- Call `this.apiClient.GET('/collector/list', { params: { query: { object_id: flags['object-id'] ? Number(flags['object-id']) : undefined, include_analytics_p: flags['include-analytics'] ? 1 : undefined } } })`.
- Extract items: `(data as any)?.data` with Array.isArray guard.
- Render table: columns [ID, Name, Type]. The ID column shows `action_id` (collectors share action ID space).
- Count line + empty state "No collectors found."

**collector/include.ts (COL-02):** GET query (NOT POST) to `/collector/include`.
- Args: `id` (string, required -- this is the collector's action_id).
- Flags: `--object-id` (string, required).
- CRITICAL: Use `this.apiClient.GET` not POST. Pass as query params: `{ action_id: Number(args.id), object_id: Number(flags['object-id']) }`.
- Success: `chalk.green('Collector included')`.

**collector/exclude.ts (COL-03):** GET query (NOT POST) to `/collector/exclude`.
- Args: `id` (string, required -- this is the collector's action_id).
- Flags: `--object-id` (string, required).
- CRITICAL: Use `this.apiClient.GET` not POST. Pass as query params: `{ action_id: Number(args.id), object_id: Number(flags['object-id']) }`.
- Success: `chalk.green('Collector excluded')`.

PITFALL: The API uses `action_id` not `collector_id` for collector include/exclude. Collectors are a subtype of actions. The CLI positional arg is `<id>` but internally maps to `action_id`.
  </action>
  <done>
    - File packages/twentythree-cli/src/commands/collector/list.ts exists with `apiClient.GET('/collector/list'` call
    - File packages/twentythree-cli/src/commands/collector/include.ts exists with `apiClient.GET('/collector/include'` (GET not POST)
    - File packages/twentythree-cli/src/commands/collector/exclude.ts exists with `apiClient.GET('/collector/exclude'` (GET not POST)
    - collector/include.ts and collector/exclude.ts pass `action_id` in query params (NOT `collector_id`)
    - collector/list.ts contains `renderTable` call
    - All 3 files contain `static enableJsonFlag = true`
  </done>
  <verify>
    <automated>cd /Users/steffenchristensen/23/twentythree-cli && pnpm --filter twentythree-cli exec tsc --noEmit 2>&1 | head -50</automated>
  </verify>
</task>

<task id="06-02-02" title="Tag list and tag related commands">
  <name>Tag list and tag related commands</name>
  <read_first>
    - packages/twentythree-cli/src/commands/video/list.ts -- list command pattern with fetchAllPages
    - packages/twentythree-cli/src/commands/video/get.ts -- simple GET query pattern
    - .planning/phases/06-engagement-actions/06-RESEARCH.md -- tag API params (tag/list has pagination, tag/related needs required tag arg)
  </read_first>
  <action>
Create 2 tag command files.

**tag/list.ts (TAG-01):** GET query to `/tag/list` with pagination.
- Flags: `--search` (string, optional), `--exclude-machine-tags` (boolean, optional -- maps to `exclude_machine_tags_p`), `--only-machine-tags` (boolean, optional -- maps to `only_machine_tags_p`), `--only-published` (boolean, optional -- maps to `only_published_p`), `--orderby` (string, optional -- options: "tag", "count"), `--order` (string, optional -- options: "asc", "desc").
- Use `fetchAllPages` for auto-pagination. The `/tag/list` response includes `total_count`.
- Render table: columns [Tag, Count].
- Count line + empty state "No tags found."

**tag/related.ts (TAG-02):** GET query to `/tag/related`.
- Args: `tag` (string, required).
- Call `this.apiClient.GET('/tag/related', { params: { query: { tag: args.tag } } })`.
- Extract items from `(data as any)?.data` with Array.isArray guard.
- Render table: columns [Tag, Count] (or just list tags).
- Count line + empty state "No related tags found."
  </action>
  <done>
    - File packages/twentythree-cli/src/commands/tag/list.ts exists with `fetchAllPages` import and usage
    - tag/list.ts calls `apiClient.GET('/tag/list'`
    - tag/list.ts contains `renderTable` with column headers including 'Tag'
    - File packages/twentythree-cli/src/commands/tag/related.ts exists with required `tag` arg
    - tag/related.ts calls `apiClient.GET('/tag/related'`
    - Both files contain `static enableJsonFlag = true`
  </done>
  <verify>
    <automated>cd /Users/steffenchristensen/23/twentythree-cli && pnpm --filter twentythree-cli exec tsc --noEmit 2>&1 | head -50</automated>
  </verify>
</task>

</tasks>

<verification>
  <command>cd /Users/steffenchristensen/23/twentythree-cli && pnpm --filter twentythree-cli exec tsc --noEmit && pnpm --filter twentythree-cli test --run</command>
  <must_haves>
    - 3 files exist under packages/twentythree-cli/src/commands/collector/
    - 2 files exist under packages/twentythree-cli/src/commands/tag/
    - collector include/exclude use GET (not POST)
    - collector include/exclude use action_id parameter (not collector_id)
    - tag list uses fetchAllPages for pagination
    - TypeScript compiles without errors
  </must_haves>
</verification>

<threat_model>
  <threats>
    <threat id="T-06-04" severity="low" mitigation="All endpoints are GET queries behind authenticated API calls; no user input interpolated into URLs">Input injection via collector/tag query params</threat>
  </threats>
  <notes>Collector and tag commands are read-heavy (list/query operations). The collector include/exclude are GET-based mutations (API design choice) -- no additional security concerns beyond standard auth. All commands require workspace authentication via AuthenticatedCommand.</notes>
</threat_model>

<success_criteria>
All 5 commands (3 collector + 2 tag) compile and follow established patterns. Collector include/exclude correctly use GET with action_id. Tag list auto-paginates.
</success_criteria>

<output>
After completion, create `.planning/phases/06-engagement-actions/06-02-SUMMARY.md`
</output>
