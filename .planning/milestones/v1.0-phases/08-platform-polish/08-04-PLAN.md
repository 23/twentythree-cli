---
phase: 08-platform-polish
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/twentythree-cli/src/commands/user/list.ts
  - packages/twentythree-cli/src/commands/user/get.ts
  - packages/twentythree-cli/src/commands/user/create.ts
  - packages/twentythree-cli/src/commands/user/update.ts
  - packages/twentythree-cli/src/commands/user/send-invitation.ts
  - packages/twentythree-cli/src/commands/user/get-login-token.ts
  - packages/twentythree-cli/src/commands/user/redeem-login-token.ts
  - packages/twentythree-cli/src/commands/user/tokens.ts
autonomous: true
requirements: [USR-01, USR-02, USR-03, USR-04, USR-05, USR-06, USR-07, USR-08]

must_haves:
  truths:
    - "twentythree user list shows users in a table with ID, Username, Display Name, URL columns"
    - "twentythree user get returns user details as key-value"
    - "twentythree user create creates a user given an email"
    - "twentythree user update supports profile_image upload via multipart FormData"
    - "twentythree user get-login-token returns a login token without leaking it in --json summary"
    - "twentythree user tokens attempts GET to /user/tokens (best-effort, endpoint not in spec)"
  artifacts:
    - path: "packages/twentythree-cli/src/commands/user/list.ts"
      provides: "User list command"
      exports: ["default"]
    - path: "packages/twentythree-cli/src/commands/user/update.ts"
      provides: "User update with multipart support"
      exports: ["default"]
  key_links:
    - from: "user/update.ts"
      to: "/user/update"
      via: "apiClient.POST with bodySerializer FormData"
      pattern: "bodySerializer"
    - from: "user/tokens.ts"
      to: "/user/tokens"
      via: "apiClient.GET (best-effort)"
      pattern: "apiClient\\.GET"
---

<objective>
Implement all 8 user commands: list, get, create, update, send-invitation, get-login-token, redeem-login-token, tokens.

Purpose: User management is a core admin feature. Note: user/update uses multipart/form-data (not form-urlencoded) because it supports profile_image file upload (Pitfall 7). USR-08 (user tokens) references an endpoint not in the swagger spec — implement as best-effort GET to /user/tokens.

Output: 8 new command files in `src/commands/user/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-platform-polish/08-PATTERNS.md

<interfaces>
From src/lib/base-command.ts:
```typescript
export abstract class AuthenticatedCommand<T extends typeof Command> extends BaseCommand<T>
```

From src/lib/output.ts:
```typescript
export function renderTable(headers: string[], rows: string[][]): Table
export function formatJsonOutput(params: JsonOutputParams): JsonOutput
export function formatApiError(error: unknown): string
export function parseBoolParam(primary: boolean | undefined, alt: string | undefined): boolean | undefined
export const EXIT_ERROR = 1
```

From src/commands/video/subtitle/upload.ts (multipart FormData pattern):
```typescript
bodySerializer(body) {
  const fd = new FormData()
  for (const [k, v] of Object.entries(body as Record<string, unknown>)) {
    if (v !== undefined) {
      if (v instanceof Blob) { fd.append(k, v, fileName) }
      else { fd.append(k, String(v)) }
    }
  }
  return fd
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement user list, get, create, and update commands</name>
  <files>
    packages/twentythree-cli/src/commands/user/list.ts
    packages/twentythree-cli/src/commands/user/get.ts
    packages/twentythree-cli/src/commands/user/create.ts
    packages/twentythree-cli/src/commands/user/update.ts
  </files>
  <read_first>
    packages/twentythree-cli/src/commands/audience/field/list.ts
    packages/twentythree-cli/src/commands/video/get.ts
    packages/twentythree-cli/src/commands/category/create.ts
    packages/twentythree-cli/src/commands/video/subtitle/upload.ts
    packages/twentythree-cli/src/commands/video/update.ts
    packages/twentythree-cli/src/lib/base-command.ts
    packages/twentythree-cli/src/lib/output.ts
  </read_first>
  <action>
Create 4 user command files.

**user/list.ts** (USR-01) — Pattern A (GET list):
- Class `UserList` extends `AuthenticatedCommand`
- Flags: `page` (integer), `size` (integer), `search` (string), `'user-id'` (string)
- GET `/user/list` with `params: { query: { search: flags.search, user_id: flags['user-id'] ? Number(flags['user-id']) : undefined, p: flags.page, size: flags.size } }`
- Table columns: `['ID', 'Username', 'Display Name', 'URL']` from `[user_id, username, display_name, url]`
- Use `resolveUrl(r.url, this.activeWorkspace.api_base_url)` for URL column (CLI-07)
- Import `resolveUrl` from output.ts
- Empty state: `'No users found.'`
- agentMetadata: `{ api_endpoint: 'GET /user/list', auth_scope: 'admin', output_shape: { type: 'table', columns: ['ID', 'Username', 'Display Name', 'URL'] }, side_effects: 'none' }`

**user/get.ts** (USR-02) — Pattern E (GET single):
- Class `UserGet` extends `AuthenticatedCommand`
- Args: `id` (string, required, description: 'User ID')
- Flags: `'include-invitation'` (boolean), `'include-invitation-p'` (string, hidden)
- GET `/user/get` with `params: { query: { user_id: Number(args.id), include_invitation_p: inclVal !== undefined ? (inclVal ? 1 : 0) : undefined } }`
- Use `parseBoolParam` for include-invitation
- Key-value output: iterate `Object.entries(obj)` and log each
- agentMetadata: `{ api_endpoint: 'GET /user/get', auth_scope: 'admin', output_shape: { type: 'key-value' }, side_effects: 'none' }`

**user/create.ts** (USR-03) — Pattern B (POST create):
- Class `UserCreate` extends `AuthenticatedCommand`
- Flags: `email` (string, required: true), `username` (string), `'full-name'` (string), `'site-admin'` (boolean, `allowNo: true`), `'site-admin-p'` (string, hidden), `'user-type'` (string)
- Build body conditionally: always include `email`, include other flags only if !== undefined
- Use `parseBoolParam` for site-admin
- POST `/user/create` with form-urlencoded
- Success: `chalk.green('User created')` + `ID: ${userId}`
- agentMetadata: `{ api_endpoint: 'POST /user/create', auth_scope: 'admin', output_shape: { type: 'key-value' }, side_effects: 'creates' }`

**user/update.ts** (USR-04) — Pattern F (POST multipart, Pitfall 7):
- Class `UserUpdate` extends `AuthenticatedCommand`
- Args: `id` (string, required, description: 'User ID')
- Flags: `email` (string), `'full-name'` (string), `password` (string), `'profile-image'` (string, description: 'Path to profile image file')
- CRITICAL: user/update uses multipart/form-data (NOT form-urlencoded) because profile_image is a file field
- Import `* as fs from 'node:fs'` and `* as path from 'node:path'`
- Build body: `{ user_id: Number(args.id) }` + conditional fields
- If `flags['profile-image']` is provided:
  - Validate file exists: `if (!fs.existsSync(imgPath)) this.error(\`File not found: ${imgPath}\`, { exit: EXIT_ERROR })`
  - Read file: `const fileBuffer = fs.readFileSync(imgPath); const fileBlob = new Blob([fileBuffer])`
  - Add to body: `body.profile_image = fileBlob`
- Use `bodySerializer` FormData pattern (same as subtitle/upload.ts) — iterate entries, append Blobs with filename, others as String
- If NO profile-image provided, use standard form-urlencoded: `headers: { 'Content-Type': 'application/x-www-form-urlencoded' }`
- POST `/user/update`
- Success: `chalk.green(\`User ${args.id} updated\`)`
- agentMetadata: `{ api_endpoint: 'POST /user/update', auth_scope: 'admin', output_shape: { type: 'key-value' }, side_effects: 'updates' }`
  </action>
  <verify>
    <automated>cd /Users/steffenchristensen/23/twentythree-cli && ls packages/twentythree-cli/src/commands/user/{list,get,create,update}.ts | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - 4 files exist in packages/twentythree-cli/src/commands/user/
    - user/list.ts contains `apiClient.GET('/user/list'` and `resolveUrl`
    - user/get.ts contains `apiClient.GET('/user/get'` and `parseBoolParam`
    - user/create.ts contains `apiClient.POST('/user/create'` and `'Content-Type': 'application/x-www-form-urlencoded'`
    - user/update.ts contains `bodySerializer` (multipart support for profile_image)
    - All 4 files contain `static agentMetadata` with `auth_scope: 'admin'`
  </acceptance_criteria>
  <done>4 user CRUD commands created with multipart support on update per Pitfall 7</done>
</task>

<task type="auto">
  <name>Task 2: Implement user send-invitation, get-login-token, redeem-login-token, and tokens</name>
  <files>
    packages/twentythree-cli/src/commands/user/send-invitation.ts
    packages/twentythree-cli/src/commands/user/get-login-token.ts
    packages/twentythree-cli/src/commands/user/redeem-login-token.ts
    packages/twentythree-cli/src/commands/user/tokens.ts
  </files>
  <read_first>
    packages/twentythree-cli/src/commands/user/list.ts
    packages/twentythree-cli/src/commands/video/get.ts
    packages/twentythree-cli/src/commands/video/delete.ts
    packages/twentythree-cli/src/lib/base-command.ts
    packages/twentythree-cli/src/lib/output.ts
  </read_first>
  <action>
Create 4 remaining user command files.

**user/send-invitation.ts** (USR-05) — Pattern K (POST action):
- Class `UserSendInvitation` extends `AuthenticatedCommand`
- Args: `id` (string, required, description: 'User ID')
- Flags: `'invitation-message'` (string, description: 'Custom invitation message')
- POST `/user/send-invitation` with body: `{ user_id: Number(args.id), invitation_message: flags['invitation-message'] } as any`
- Headers: `{ 'Content-Type': 'application/x-www-form-urlencoded' }`
- Success: `chalk.green(\`Invitation sent to user ${args.id}\`)`
- agentMetadata: `{ api_endpoint: 'POST /user/send-invitation', auth_scope: 'admin', output_shape: { type: 'key-value' }, side_effects: 'updates' }`

**user/get-login-token.ts** (USR-06) — Pattern E (GET, returns sensitive token):
- Class `UserGetLoginToken` extends `AuthenticatedCommand`
- Args: `id` (string, required, description: 'User ID')
- Flags: `'return-url'` (string, description: 'URL to redirect to after login')
- GET `/user/get-login-token` with `params: { query: { user_id: Number(args.id), return_url: flags['return-url'] } }`
- Non-JSON output: `this.log(\`Login token: ${token}\`)` — print token clearly
- SECURITY: In JSON mode, include token in data but NOT in the summary string: `formatJsonOutput({ ok: true, data: obj, summary: \`Login token generated for user ${args.id}\`, breadcrumbs: [...] })` — summary must NOT contain the actual token value
- agentMetadata: `{ api_endpoint: 'GET /user/get-login-token', auth_scope: 'admin', output_shape: { type: 'key-value' }, side_effects: 'none' }`

**user/redeem-login-token.ts** (USR-07) — Pattern E variant (GET action):
- Class `UserRedeemLoginToken` extends `AuthenticatedCommand`
- Flags: `'login-token'` (string, required: true, description: 'Login token to redeem')
- GET `/user/redeem-login-token` with `params: { query: { login_token: flags['login-token'] } }`
- Key-value output for the response
- agentMetadata: `{ api_endpoint: 'GET /user/redeem-login-token', auth_scope: 'read', output_shape: { type: 'key-value' }, side_effects: 'none' }`

**user/tokens.ts** (USR-08) — Best-effort implementation:
- Class `UserTokens` extends `AuthenticatedCommand`
- Description: `'Retrieve cross-site tokens for the authenticated user'`
- NOTE in code comment: `// USR-08: /user/tokens is not in the OpenAPI swagger spec but is referenced in AUTH-02. Implemented as best-effort GET with any-typed response.`
- Flags: `'cross-sites'` (boolean, default: true, description: 'Include cross-site tokens')
- API call: use raw fetch instead of apiClient since the endpoint is not in the type system:
  ```
  const url = new URL('user/tokens', this.apiBaseUrl)
  if (flags['cross-sites']) url.searchParams.set('cross_sites_p', '1')
  const response = await fetch(url.toString(), {
    headers: { 'Authorization': `Bearer ${this.activeWorkspace.bearer_token}` },
  })
  if (!response.ok) this.error(`API returned ${response.status}: ${response.statusText}`, { exit: EXIT_ERROR })
  const json = await response.json() as any
  ```
- JSON output wraps in standard envelope
- Non-JSON: key-value or table depending on response shape — try table if `json.data` is an array, key-value otherwise
- agentMetadata: `{ api_endpoint: 'GET /user/tokens', auth_scope: 'read', output_shape: { type: 'table', columns: ['Domain', 'Token'] }, side_effects: 'none' }`

All commands: error handling with `applyCliTerms`, `static enableJsonFlag = true`, `static examples`.
  </action>
  <verify>
    <automated>cd /Users/steffenchristensen/23/twentythree-cli && ls packages/twentythree-cli/src/commands/user/{send-invitation,get-login-token,redeem-login-token,tokens}.ts | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - 4 files exist: send-invitation.ts, get-login-token.ts, redeem-login-token.ts, tokens.ts
    - user/send-invitation.ts contains `apiClient.POST('/user/send-invitation'`
    - user/get-login-token.ts does NOT contain the actual token value in the summary string of formatJsonOutput
    - user/redeem-login-token.ts contains `apiClient.GET('/user/redeem-login-token'` or `login_token`
    - user/tokens.ts contains comment about USR-08 not being in spec and uses raw `fetch`
    - All 4 files contain `static agentMetadata`
    - Total of 8 files exist in src/commands/user/
  </acceptance_criteria>
  <done>All 8 user commands created including best-effort USR-08 and secure token handling in USR-06</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI -> TwentyThree API | Admin user operations; login token generation crosses auth boundary |
| CLI stdout | Login tokens printed to terminal are sensitive |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-10 | Information Disclosure | user/get-login-token.ts | mitigate | Token NOT included in --json summary string; data field carries it for programmatic access only |
| T-08-11 | Elevation of Privilege | all user commands | mitigate | auth_scope: 'admin' — AuthenticatedCommand enforces token presence |
| T-08-12 | Tampering | user/update.ts profile_image | mitigate | fs.existsSync validates file path before reading; API validates file type server-side |
| T-08-13 | Information Disclosure | user/tokens.ts | accept | Tokens printed to stdout intentionally — this is the command's purpose; user controls their terminal |
</threat_model>

<verification>
```bash
ls packages/twentythree-cli/src/commands/user/{list,get,create,update,send-invitation,get-login-token,redeem-login-token,tokens}.ts
pnpm --filter twentythree-cli exec tsc --noEmit 2>&1 | grep "user/" || echo "Clean"
pnpm --filter twentythree-cli test --run
```
</verification>

<success_criteria>
- All 8 user command files exist and compile
- user/update uses multipart FormData for profile_image (Pitfall 7)
- user/get-login-token does not leak token in JSON summary
- user/tokens handles missing spec endpoint gracefully
- All commands have --json and agentMetadata
- Existing test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/08-platform-polish/08-04-SUMMARY.md`
</output>
