---
phase: 08-platform-polish
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/twentythree-cli/src/commands/thumbnail/index.ts
  - packages/twentythree-cli/src/commands/thumbnail/list.ts
  - packages/twentythree-cli/src/commands/thumbnail/add.ts
  - packages/twentythree-cli/src/commands/thumbnail/update.ts
  - packages/twentythree-cli/src/commands/thumbnail/delete.ts
  - packages/twentythree-cli/src/commands/thumbnail/duplicate.ts
  - packages/twentythree-cli/src/commands/thumbnail/data.ts
  - packages/twentythree-cli/src/commands/thumbnail/file/list.ts
  - packages/twentythree-cli/src/commands/thumbnail/file/upload.ts
  - packages/twentythree-cli/src/commands/thumbnail/file/delete.ts
autonomous: true
requirements: [THB-01, THB-02, THB-03, THB-04, THB-05, THB-06, THB-07]

must_haves:
  truths:
    - "twentythree thumbnail list shows templates in a table"
    - "twentythree thumbnail add creates a template given name and liquid_template"
    - "twentythree thumbnail delete confirms before deleting"
    - "twentythree thumbnail file upload uploads an image via direct multipart (NOT chunked engine)"
    - "twentythree thumbnail file list shows files for a template"
    - "twentythree thumbnail data outputs formatted JSON for complex nested data"
  artifacts:
    - path: "packages/twentythree-cli/src/commands/thumbnail/index.ts"
      provides: "Topic root for thumbnail"
      exports: ["default"]
    - path: "packages/twentythree-cli/src/commands/thumbnail/file/upload.ts"
      provides: "Direct multipart file upload for template images"
      exports: ["default"]
  key_links:
    - from: "thumbnail/file/upload.ts"
      to: "/thumbnail/template/upload-file"
      via: "apiClient.POST with bodySerializer FormData"
      pattern: "bodySerializer"
---

<objective>
Implement all 7 thumbnail commands plus the 3-level file subtopic (THB-01 through THB-07).

Purpose: Thumbnail templates control video thumbnail generation. This includes CRUD on templates, data retrieval, duplication, and file management via a 3-level oclif topic (thumbnail file list/upload/delete). Per D-3, thumbnail file upload uses direct multipart POST (NOT the chunked engine).

Output: 10 new command files in `src/commands/thumbnail/` including `file/` subdirectory.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-platform-polish/08-PATTERNS.md

<interfaces>
From src/lib/base-command.ts:
```typescript
export abstract class AuthenticatedCommand<T extends typeof Command> extends BaseCommand<T>
```

From src/lib/output.ts:
```typescript
export function renderTable(headers: string[], rows: string[][]): Table
export function formatJsonOutput(params: JsonOutputParams): JsonOutput
export function formatApiError(error: unknown): string
export const EXIT_ERROR = 1, EXIT_CANCELLED = 2
```

From src/commands/video/subtitle/upload.ts (multipart pattern):
```typescript
bodySerializer(body) {
  const fd = new FormData()
  for (const [k, v] of Object.entries(body as Record<string, unknown>)) {
    if (v !== undefined) {
      if (v instanceof Blob) { fd.append(k, v, fileName) }
      else { fd.append(k, String(v)) }
    }
  }
  return fd
}
```

From src/commands/category/index.ts (topic root pattern):
```typescript
import { Command } from '@oclif/core'
export default class CategoryIndex extends Command {
  static description = '...'
  async run(): Promise<void> { this.log('Run "twentythree category --help"...') }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement thumbnail topic root, CRUD commands, duplicate, and data</name>
  <files>
    packages/twentythree-cli/src/commands/thumbnail/index.ts
    packages/twentythree-cli/src/commands/thumbnail/list.ts
    packages/twentythree-cli/src/commands/thumbnail/add.ts
    packages/twentythree-cli/src/commands/thumbnail/update.ts
    packages/twentythree-cli/src/commands/thumbnail/delete.ts
    packages/twentythree-cli/src/commands/thumbnail/duplicate.ts
    packages/twentythree-cli/src/commands/thumbnail/data.ts
  </files>
  <read_first>
    packages/twentythree-cli/src/commands/category/index.ts
    packages/twentythree-cli/src/commands/audience/field/list.ts
    packages/twentythree-cli/src/commands/category/create.ts
    packages/twentythree-cli/src/commands/video/delete.ts
    packages/twentythree-cli/src/commands/video/subtitle/duplicate.ts
    packages/twentythree-cli/src/commands/video/get.ts
    packages/twentythree-cli/src/lib/base-command.ts
    packages/twentythree-cli/src/lib/output.ts
  </read_first>
  <action>
Create 7 thumbnail command files.

**thumbnail/index.ts** — Pattern H (topic root):
- Class `ThumbnailIndex` extends `Command` (from `@oclif/core`, NOT AuthenticatedCommand)
- `static description = 'Manage thumbnail templates — list, add, update, delete, duplicate, data, and manage files'`
- run() logs: `'Run "twentythree thumbnail --help" for available commands'`

**thumbnail/list.ts** (THB-01) — Pattern A (GET list):
- GET `/thumbnail/template/list` with `params: { query: { search: flags.search, object_type: flags['object-type'] } }`
- Flags: `search` (string), `'object-type'` (string)
- Table columns: `['ID', 'Name', 'Type', 'Width', 'Height']` from `[thumbnail_template_id, name, object_type, width, height]`
- Empty state: `'No thumbnail templates found.'`
- agentMetadata: `{ api_endpoint: 'GET /thumbnail/template/list', auth_scope: 'read', output_shape: { type: 'table', columns: ['ID', 'Name', 'Type', 'Width', 'Height'] }, side_effects: 'none' }`

**thumbnail/add.ts** (THB-02) — Pattern B (POST create):
- Flags: `name` (string, required: true), `'liquid-template'` (string, required: true)
- POST `/thumbnail/template/add` with body: `{ name: flags.name, liquid_template: flags['liquid-template'] } as any`
- Extract `thumbnail_template_id` from response
- agentMetadata: `{ api_endpoint: 'POST /thumbnail/template/add', auth_scope: 'write', output_shape: { type: 'key-value' }, side_effects: 'creates' }`

**thumbnail/update.ts** (THB-03) — Pattern D (POST update):
- Args: `id` (string, required, description: 'Thumbnail template ID')
- Flags: `name` (string), `'liquid-template'` (string), `'object-type'` (string), `width` (integer), `height` (integer)
- Build body conditionally starting with `{ thumbnail_template_id: Number(args.id) }`
- POST `/thumbnail/template/update`
- agentMetadata: `{ api_endpoint: 'POST /thumbnail/template/update', auth_scope: 'write', output_shape: { type: 'key-value' }, side_effects: 'updates' }`

**thumbnail/delete.ts** (THB-04) — Pattern C (POST destructive):
- Args: `id` (string, required)
- Confirmation: `Delete thumbnail template ${args.id} from ${this.activeWorkspace.domain}? This cannot be undone.`
- POST `/thumbnail/template/delete` with `body: { thumbnail_template_id: Number(args.id) } as any`
- agentMetadata: `{ api_endpoint: 'POST /thumbnail/template/delete', auth_scope: 'write', output_shape: { type: 'key-value' }, side_effects: 'destructive' }`

**thumbnail/duplicate.ts** (THB-05) — Pattern K (POST action):
- Args: `id` (string, required)
- Flags: `name` (string, description: 'Name for the duplicate')
- POST `/thumbnail/template/duplicate` with body: `{ thumbnail_template_id: Number(args.id), name: flags.name } as any`
- Success: `chalk.green('Thumbnail template duplicated')`
- agentMetadata: `{ api_endpoint: 'POST /thumbnail/template/duplicate', auth_scope: 'write', output_shape: { type: 'key-value' }, side_effects: 'creates' }`

**thumbnail/data.ts** (THB-06) — Pattern E variant (GET, raw JSON output):
- Args: `id` (string, required, description: 'Thumbnail template ID')
- Flags: `'object-id'` (string, required: true, description: 'Object ID to get template data for')
- GET `/thumbnail/template/data` with `params: { query: { thumbnail_template_id: Number(args.id), object_id: Number(flags['object-id']) } }`
- Non-JSON output: `this.log(JSON.stringify((resp?.data ?? resp), null, 2))` — nested object not suitable for key-value
- JSON output: standard envelope
- agentMetadata: `{ api_endpoint: 'GET /thumbnail/template/data', auth_scope: 'read', output_shape: { type: 'key-value' }, side_effects: 'none' }`

All commands: `applyCliTerms(formatApiError(error))`, `static enableJsonFlag = true`, `static examples`, `headers: { 'Content-Type': 'application/x-www-form-urlencoded' }` on POSTs.
  </action>
  <verify>
    <automated>cd /Users/steffenchristensen/23/twentythree-cli && ls packages/twentythree-cli/src/commands/thumbnail/{index,list,add,update,delete,duplicate,data}.ts | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - 7 files exist in packages/twentythree-cli/src/commands/thumbnail/
    - thumbnail/index.ts extends `Command` (not AuthenticatedCommand)
    - thumbnail/list.ts contains `apiClient.GET('/thumbnail/template/list'`
    - thumbnail/add.ts contains `apiClient.POST('/thumbnail/template/add'`
    - thumbnail/delete.ts contains `confirm(` and `EXIT_CANCELLED`
    - thumbnail/data.ts contains `JSON.stringify`
    - All 6 authenticated commands contain `static agentMetadata`
  </acceptance_criteria>
  <done>7 thumbnail commands created with topic root and correct API endpoints</done>
</task>

<task type="auto">
  <name>Task 2: Implement thumbnail file subtopic (3-level: list, upload, delete) per D-3</name>
  <files>
    packages/twentythree-cli/src/commands/thumbnail/file/list.ts
    packages/twentythree-cli/src/commands/thumbnail/file/upload.ts
    packages/twentythree-cli/src/commands/thumbnail/file/delete.ts
  </files>
  <read_first>
    packages/twentythree-cli/src/commands/video/subtitle/upload.ts
    packages/twentythree-cli/src/commands/video/subtitle/list.ts
    packages/twentythree-cli/src/commands/video/delete.ts
    packages/twentythree-cli/src/lib/output.ts
  </read_first>
  <action>
Create 3 thumbnail file subcommand files in the `file/` subdirectory. Per D-3, file upload uses direct multipart POST (NOT the chunked engine). The directory structure `thumbnail/file/*.ts` auto-registers as a 3-level oclif topic.

**thumbnail/file/list.ts** (THB-07 partial) — Pattern A (GET list):
- Class `ThumbnailFileList` extends `AuthenticatedCommand`
- Args: `id` (string, required, description: 'Thumbnail template ID')
- GET `/thumbnail/template/list-files` with `params: { query: { thumbnail_template_id: Number(args.id) } }`
- Table columns: `['Filename', 'Size', 'URL']` from `[filename, size_fmt ?? size, url]`
- Empty state: `'No files found.'`
- agentMetadata: `{ api_endpoint: 'GET /thumbnail/template/list-files', auth_scope: 'read', output_shape: { type: 'table', columns: ['Filename', 'Size', 'URL'] }, side_effects: 'none' }`

**thumbnail/file/upload.ts** (THB-07 partial) — Pattern F (direct multipart, per D-3):
- Class `ThumbnailFileUpload` extends `AuthenticatedCommand`
- Args: `file` (string, required, description: 'Path to the image file to upload')
- Flags: `'template-id'` (string, required: true, description: 'Thumbnail template ID')
- Validate file exists: `if (!fs.existsSync(filePath)) this.error(\`File not found: ${filePath}\`, { exit: EXIT_ERROR })`
- Read file: `const fileBuffer = fs.readFileSync(filePath); const fileName = path.basename(filePath); const fileBlob = new Blob([fileBuffer])`
- POST `/thumbnail/template/upload-file` using `bodySerializer` FormData pattern (same as subtitle/upload.ts):
  ```
  body: { thumbnail_template_id: Number(flags['template-id']), file: fileBlob as unknown as Record<string, never> }
  bodySerializer(body) { ... FormData loop ... }
  ```
- NO progress bar (D-3: simple spinner not needed either per decision — just post and report result)
- Success: `chalk.green('File uploaded')`
- agentMetadata: `{ api_endpoint: 'POST /thumbnail/template/upload-file', auth_scope: 'write', output_shape: { type: 'key-value' }, side_effects: 'creates' }`

**thumbnail/file/delete.ts** (THB-07 partial) — Pattern C (POST destructive):
- Class `ThumbnailFileDelete` extends `AuthenticatedCommand`
- Flags: `'template-id'` (string, required: true), `filename` (string, required: true, description: 'Filename to delete')
- Confirmation: `Delete file "${flags.filename}" from template ${flags['template-id']} on ${this.activeWorkspace.domain}?`
- POST `/thumbnail/template/delete-file` with body: `{ thumbnail_template_id: Number(flags['template-id']), filename: flags.filename } as any`
- Headers: `{ 'Content-Type': 'application/x-www-form-urlencoded' }`
- agentMetadata: `{ api_endpoint: 'POST /thumbnail/template/delete-file', auth_scope: 'write', output_shape: { type: 'key-value' }, side_effects: 'destructive' }`

Import paths from file/ subdirectory: `'../../../lib/base-command.js'`, `'../../../lib/output.js'`, `'../../../lib/term-map.js'`

All commands: `applyCliTerms(formatApiError(error))`, `static enableJsonFlag = true`, `static examples`.
  </action>
  <verify>
    <automated>cd /Users/steffenchristensen/23/twentythree-cli && ls packages/twentythree-cli/src/commands/thumbnail/file/{list,upload,delete}.ts | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - 3 files exist in packages/twentythree-cli/src/commands/thumbnail/file/
    - thumbnail/file/upload.ts contains `bodySerializer` and does NOT contain `uploadChunked` or `chunk-pool` (D-3 compliance)
    - thumbnail/file/upload.ts contains `fs.existsSync` or `fs.readFileSync`
    - thumbnail/file/delete.ts contains `confirm(` and `EXIT_CANCELLED`
    - thumbnail/file/list.ts contains `apiClient.GET('/thumbnail/template/list-files'`
    - All 3 files contain `static agentMetadata`
    - Import paths use `'../../../lib/'` (3-level depth)
  </acceptance_criteria>
  <done>3-level thumbnail file subtopic complete with direct multipart upload per D-3</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI -> TwentyThree API | Template mutations and file uploads cross via authenticated HTTPS |
| Local filesystem -> CLI | thumbnail/file/upload reads arbitrary file path from user |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-07 | Repudiation | thumbnail/delete.ts, thumbnail/file/delete.ts | mitigate | confirm() prompt with workspace domain |
| T-08-08 | Tampering | thumbnail/file/upload.ts | mitigate | Validate file exists via fs.existsSync before reading; API enforces allowed file types server-side |
| T-08-09 | Elevation of Privilege | all commands | mitigate | All extend AuthenticatedCommand |
</threat_model>

<verification>
```bash
ls packages/twentythree-cli/src/commands/thumbnail/{index,list,add,update,delete,duplicate,data}.ts
ls packages/twentythree-cli/src/commands/thumbnail/file/{list,upload,delete}.ts
pnpm --filter twentythree-cli exec tsc --noEmit 2>&1 | grep "thumbnail/" || echo "Clean"
pnpm --filter twentythree-cli test --run
```
</verification>

<success_criteria>
- 10 total thumbnail command files exist
- 3-level topic auto-registers via directory structure
- File upload uses direct multipart (NOT chunked engine) per D-3
- All destructive commands have confirmation prompts
- All commands have --json and agentMetadata
- Existing test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/08-platform-polish/08-03-SUMMARY.md`
</output>
