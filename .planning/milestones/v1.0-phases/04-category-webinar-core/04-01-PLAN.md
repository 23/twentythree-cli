---
phase: 04-category-webinar-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/twentythree-cli/src/commands/category/index.ts
  - packages/twentythree-cli/src/commands/category/list.ts
  - packages/twentythree-cli/src/commands/category/create.ts
  - packages/twentythree-cli/src/commands/category/update.ts
  - packages/twentythree-cli/src/commands/category/delete.ts
  - packages/twentythree-cli/src/commands/category/__tests__/list.test.ts
  - packages/twentythree-cli/src/commands/category/__tests__/create.test.ts
  - packages/twentythree-cli/src/commands/category/__tests__/update.test.ts
  - packages/twentythree-cli/src/commands/category/__tests__/delete.test.ts
autonomous: true
requirements:
  - CAT-01
  - CAT-02
  - CAT-03
  - CAT-04

must_haves:
  truths:
    - "twentythree category list displays a table of categories with ID, Title, Hidden, Created columns"
    - "twentythree category create --title 'X' sends POST /album/create with title field and prints the new album_id"
    - "twentythree category update <id> --title 'Y' sends POST /album/update with album_id and title; interactive fallback when no flags"
    - "twentythree category delete <id> prompts for confirmation including domain name, then sends POST /album/delete"
    - "All four commands support --json returning {ok, data, summary, breadcrumbs}"
    - "No user-visible output contains the term 'album' — applyCliTerms applied everywhere"
  artifacts:
    - path: "packages/twentythree-cli/src/commands/category/index.ts"
      provides: "oclif topic registration for category"
    - path: "packages/twentythree-cli/src/commands/category/list.ts"
      provides: "category list command"
      exports: ["default"]
    - path: "packages/twentythree-cli/src/commands/category/create.ts"
      provides: "category create command"
      exports: ["default"]
    - path: "packages/twentythree-cli/src/commands/category/update.ts"
      provides: "category update command with flag + interactive modes"
      exports: ["default"]
    - path: "packages/twentythree-cli/src/commands/category/delete.ts"
      provides: "category delete command with confirmation"
      exports: ["default"]
  key_links:
    - from: "category/list.ts"
      to: "/album/list"
      via: "this.apiClient.GET"
      pattern: "apiClient\\.GET\\('/album/list'"
    - from: "category/create.ts"
      to: "/album/create"
      via: "this.apiClient.POST"
      pattern: "apiClient\\.POST\\('/album/create'"
    - from: "category/update.ts"
      to: "/album/update"
      via: "this.apiClient.POST"
      pattern: "apiClient\\.POST\\('/album/update'"
    - from: "category/delete.ts"
      to: "/album/delete"
      via: "this.apiClient.POST"
      pattern: "apiClient\\.POST\\('/album/delete'"
---

<objective>
Implement all four category commands (list, create, update, delete) following the exact patterns established in Phase 3 video commands.

Purpose: Categories are a simple CRUD resource with no token lookups, no uploads, and no specialized output. They serve as the direct analogue to video commands but targeting the `/album/*` API endpoints.

Output: Five files in `src/commands/category/` (index + 4 commands) and four test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-category-webinar-core/04-CONTEXT.md
@.planning/phases/04-category-webinar-core/04-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/lib/base-command.ts:
```typescript
export abstract class AuthenticatedCommand<T extends typeof Command> extends BaseCommand<T> {
  protected flags!: BaseFlags<T>
  protected activeWorkspace!: WorkspaceEntry
  protected apiBaseUrl!: string
  protected apiClient!: ReturnType<typeof createApiClient>
  protected printWorkspaceHeader(): void
}
```

From src/lib/output.ts:
```typescript
export const EXIT_SUCCESS = 0
export const EXIT_ERROR = 1
export const EXIT_CANCELLED = 2
export function formatJsonOutput(params: JsonOutputParams): JsonOutput
export function renderTable(headers: string[], rows: string[][]): InstanceType<typeof Table>
export function formatApiError(error: unknown): string
export function parseBoolParam(primary: boolean | undefined, alt: string | undefined): boolean | undefined
```

From src/lib/pagination.ts:
```typescript
export async function fetchAllPages<T>(
  fetchPage: (page: number, size: number) => Promise<{ data?: T[]; total_count?: number }>
): Promise<T[]>
```

From src/lib/term-map.ts:
```typescript
export function applyCliTerms(text: string): string
```

From src/commands/video/index.ts (topic index pattern):
```typescript
import { Command } from '@oclif/core'
export default class VideoIndex extends Command {
  static description = 'Manage videos — upload, list, update, delete, and more'
  async run(): Promise<void> {
    this.log('Run "twentythree video --help" for available commands')
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Category topic index + list + create commands with tests</name>
  <files>
    packages/twentythree-cli/src/commands/category/index.ts
    packages/twentythree-cli/src/commands/category/list.ts
    packages/twentythree-cli/src/commands/category/create.ts
    packages/twentythree-cli/src/commands/category/__tests__/list.test.ts
    packages/twentythree-cli/src/commands/category/__tests__/create.test.ts
  </files>
  <read_first>
    packages/twentythree-cli/src/commands/video/index.ts
    packages/twentythree-cli/src/commands/video/list.ts
    packages/twentythree-cli/src/commands/video/upload.ts
    packages/twentythree-cli/src/lib/base-command.ts
    packages/twentythree-cli/src/lib/output.ts
    packages/twentythree-cli/src/lib/pagination.ts
    packages/twentythree-cli/src/lib/term-map.ts
    packages/twentythree-cli/src/commands/auth/__tests__/status.test.ts
  </read_first>
  <action>
Create three command files and two test files:

**1. `category/index.ts`** — oclif topic index. Follow video/index.ts pattern exactly:
- `import { Command } from '@oclif/core'`
- `static description = 'Manage categories — list, create, update, and delete'`
- `run()` prints `'Run "twentythree category --help" for available commands'`

**2. `category/list.ts`** — CAT-01. Clone the structure of `video/list.ts` exactly, with these differences:
- Class name: `CategoryList`
- `static description = 'List categories in the active workspace'`
- API endpoint: `this.apiClient.GET('/album/list', { params: { query: { p: page, size, include_hidden_p: parseBoolParam(flags['include-hidden'], flags['include-hidden-p']) ? 1 : undefined } } })`
- Flags: `--include-hidden` (boolean, allowNo: true), hidden `--include-hidden-p` (string). No `--limit` flag.
- Table headers: `['ID', 'Title', 'Hidden', 'Created']`
- Row mapping: `[String(c.album_id ?? ''), applyCliTerms(String(c.title ?? '')), c.hide_p ? 'yes' : 'no', String(c.creation_date_ansi ?? '')]`
- Summary: `${categories.length} categor${categories.length === 1 ? 'y' : 'ies'}`
- Breadcrumbs resource: `'category'`
- Empty state: `'No categories found.'`
- Cast response `data as any` same as video/list.ts pattern

**3. `category/create.ts`** — CAT-02. Modeled after video upload success output pattern but simpler (no file upload):
- Class name: `CategoryCreate`
- `static description = 'Create a new category'`
- Extends `AuthenticatedCommand`
- Flags: `--title` (string, required: true), `--description` (string, optional), `--hidden` (boolean, allowNo: true, optional), hidden `--hide-p` (string, optional)
- Body construction: `{ title: flags.title }`. If `flags.description !== undefined` add `description`. Use `parseBoolParam(flags.hidden, flags['hide-p'])` for `hide_p` (send as 1/0).
- POST to `/album/create` with `headers: { 'Content-Type': 'application/x-www-form-urlencoded' }`
- On success: extract `album_id` from `(createData as any)?.data?.album_id`
- Print: `chalk.green('Category created')` then `ID:    ${albumId}` (no admin URL for categories per CONTEXT.md)
- JSON output: `{ ok: true, data: createData, summary: 'Category created', breadcrumbs: [{ domain }, { resource: 'category', id: String(albumId) }] }`

**4. Test files** — Create `__tests__/list.test.ts` and `__tests__/create.test.ts` using vitest. Each test file should:
- Import `{ describe, it, expect }` from vitest
- Have a `describe('CategoryList'/'CategoryCreate')` block
- Include at least 2 test stubs with `it.todo()` for key behaviors:
  - list: `'renders table with ID, Title, Hidden, Created columns'`, `'returns JSON with ok, data, summary, breadcrumbs'`
  - create: `'sends POST /album/create with title in body'`, `'returns JSON with new album_id'`
  </action>
  <verify>
    <automated>cd /Users/steffenchristensen/23/twentythree-cli && pnpm --filter twentythree-cli exec vitest run src/commands/category/__tests__/ --reporter=dot 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - packages/twentythree-cli/src/commands/category/index.ts exists and contains `static description = 'Manage categories`
    - packages/twentythree-cli/src/commands/category/list.ts contains `apiClient.GET('/album/list'` and `renderTable`
    - packages/twentythree-cli/src/commands/category/list.ts contains `applyCliTerms` import and usage
    - packages/twentythree-cli/src/commands/category/list.ts contains `fetchAllPages` import and usage
    - packages/twentythree-cli/src/commands/category/list.ts table headers are `['ID', 'Title', 'Hidden', 'Created']`
    - packages/twentythree-cli/src/commands/category/create.ts contains `apiClient.POST('/album/create'`
    - packages/twentythree-cli/src/commands/category/create.ts contains `Content-Type': 'application/x-www-form-urlencoded`
    - packages/twentythree-cli/src/commands/category/create.ts contains `parseBoolParam` for hide_p handling
    - Test files exist and vitest finds them without errors
  </acceptance_criteria>
  <done>Topic index, list command with pagination, and create command all exist. Tests run (stubs pass). Category list fetches from /album/list with auto-pagination. Category create sends POST to /album/create with title as required field.</done>
</task>

<task type="auto">
  <name>Task 2: Category update + delete commands with tests</name>
  <files>
    packages/twentythree-cli/src/commands/category/update.ts
    packages/twentythree-cli/src/commands/category/delete.ts
    packages/twentythree-cli/src/commands/category/__tests__/update.test.ts
    packages/twentythree-cli/src/commands/category/__tests__/delete.test.ts
  </files>
  <read_first>
    packages/twentythree-cli/src/commands/video/update.ts
    packages/twentythree-cli/src/commands/video/delete.ts
    packages/twentythree-cli/src/commands/category/list.ts
    packages/twentythree-cli/src/commands/category/create.ts
    packages/twentythree-cli/src/lib/base-command.ts
    packages/twentythree-cli/src/lib/output.ts
  </read_first>
  <action>
Create two command files and two test files:

**1. `category/update.ts`** — CAT-03. Clone the structure of `video/update.ts` exactly with these differences:
- Class name: `CategoryUpdate`
- `static description = 'Update metadata for a category'`
- Args: `id` (string, required, description: 'Category ID')
- Flags: `--title` (string, optional), `--description` (string, optional), `--hidden` (boolean, allowNo: true, optional), hidden `--hide-p` (string, optional)
- Body always starts with `{ album_id: categoryId }` where `categoryId = Number(args.id)` (validate finite and > 0)
- Flag mode metadata detection: check `[flags.title, flags.description, flags.hidden, flags['hide-p']].some(v => v !== undefined)`
- Flag mode body construction:
  - `if (flags.title !== undefined) body.title = flags.title`
  - `if (flags.description !== undefined) body.description = flags.description`
  - `const hiddenVal = parseBoolParam(flags.hidden, flags['hide-p']); if (hiddenVal !== undefined) body.hide_p = hiddenVal ? 1 : 0`
- Interactive mode (no metadata flags and not --json): fetch current category via `this.apiClient.GET('/album/list', { params: { query: { album_id: categoryId } } })`. Extract current with `as any` cast + `Array.isArray` check. Prompt for title (text, initialValue: current.title), description (text, initialValue: current.description ?? ''), hidden (select yes/no, initialValue from current.hide_p). Set body.title, body.description, body.hide_p from results.
- POST to `/album/update` with `headers: { 'Content-Type': 'application/x-www-form-urlencoded' }`, body cast as any
- Success: `chalk.green('Category ${args.id} updated')`
- JSON: `{ ok: true, data: updateData, summary: 'Category ${args.id} updated', breadcrumbs: [{ domain }, { resource: 'category', id: args.id }] }`
- Use `isCancel` from `@clack/prompts` and `process.exit(EXIT_CANCELLED)` on cancel

**2. `category/delete.ts`** — CAT-04. Clone `video/delete.ts` exactly with these differences:
- Class name: `CategoryDelete`
- `static description = 'Delete a category from the active workspace'`
- Args: `id` (string, required, description: 'Category ID')
- Confirmation message: `Delete category ${args.id} from ${this.activeWorkspace.domain}? This cannot be undone.`
- POST to `/album/delete` with body `{ album_id: Number(args.id) }` as any, Content-Type x-www-form-urlencoded
- Success: `chalk.green('Category ${args.id} deleted')`
- JSON: `{ ok: true, data: deleteData, summary: 'Category ${args.id} deleted', breadcrumbs: [{ domain }, { resource: 'category', id: args.id }] }`

**3. Test files** — `__tests__/update.test.ts` and `__tests__/delete.test.ts`:
- update: stubs `'sends POST /album/update with only provided flags'`, `'enters interactive mode when no metadata flags provided'`, `'validates album_id is numeric'`
- delete: stubs `'prompts for confirmation including domain name'`, `'sends POST /album/delete with album_id'`, `'skips confirmation in --json mode'`
  </action>
  <verify>
    <automated>cd /Users/steffenchristensen/23/twentythree-cli && pnpm --filter twentythree-cli exec vitest run src/commands/category/__tests__/ --reporter=dot 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - packages/twentythree-cli/src/commands/category/update.ts contains `apiClient.POST('/album/update'`
    - packages/twentythree-cli/src/commands/category/update.ts contains `body.album_id = categoryId` or equivalent `album_id: categoryId`
    - packages/twentythree-cli/src/commands/category/update.ts contains `parseBoolParam(flags.hidden, flags['hide-p'])`
    - packages/twentythree-cli/src/commands/category/update.ts contains `isCancel` import from `@clack/prompts`
    - packages/twentythree-cli/src/commands/category/update.ts contains interactive fetch via `apiClient.GET('/album/list'`
    - packages/twentythree-cli/src/commands/category/delete.ts contains `apiClient.POST('/album/delete'`
    - packages/twentythree-cli/src/commands/category/delete.ts contains `Delete category ${args.id} from ${this.activeWorkspace.domain}`
    - packages/twentythree-cli/src/commands/category/delete.ts contains `confirm` import from `@clack/prompts`
    - Test files exist under __tests__/ and vitest runs them without error
  </acceptance_criteria>
  <done>Category update command implements both flag mode (only provided flags sent) and interactive mode (prompts with current values). Category delete prompts for confirmation with domain name. Both support --json. All four category test files run green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI flags to API body | User input from flags flows into POST body fields |
| API response to terminal | API response data rendered in terminal output |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | category/update | mitigate | Validate album_id is numeric (Number.isFinite, > 0) before sending to API — same as Phase 3 T-03-07 pattern |
| T-04-02 | Repudiation | category/delete | mitigate | Confirmation prompt includes workspace domain so user knows which workspace they are deleting from |
| T-04-03 | Information Disclosure | category/list | accept | Category list is read-only; no sensitive data beyond titles |
| T-04-04 | Spoofing | all category commands | mitigate | All extend AuthenticatedCommand — anonymous mode rejected with clear error |
</threat_model>

<verification>
1. `pnpm --filter twentythree-cli exec vitest run src/commands/category/__tests__/ --reporter=dot` — all test files discovered, stubs pass
2. `grep -r "album" packages/twentythree-cli/src/commands/category/*.ts` shows only API endpoint paths and field names, never user-visible strings
3. `grep -r "applyCliTerms" packages/twentythree-cli/src/commands/category/*.ts` shows usage in list and error handling
</verification>

<success_criteria>
All four category commands (list, create, update, delete) exist, extend AuthenticatedCommand, use correct API endpoints (/album/*), support --json output, and have test files. Delete includes domain-scoped confirmation. Update supports flag and interactive modes. No user-visible output contains "album".
</success_criteria>

<output>
After completion, create `.planning/phases/04-category-webinar-core/04-01-SUMMARY.md`
</output>
