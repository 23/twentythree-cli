---
phase: 6.1
slug: download-and-store-swagger-file-prescribe-api-change-workflo
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-04-16
---

# Phase 6.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | bash + pnpm scripts (no unit test framework needed for shell/docs) |
| **Quick run command** | `bash packages/twentythree-cli/update-api-spec.sh` |
| **Full suite command** | `pnpm --filter twentythree-cli exec tsc --noEmit` |
| **Estimated runtime** | ~10 seconds |

---

## Sampling Rate

- **After shell script created:** Run `bash packages/twentythree-cli/update-api-spec.sh` end-to-end
- **After generate-types update:** Run `pnpm --filter twentythree-cli exec tsc --noEmit` to confirm types regenerate cleanly
- **Before `/gsd-verify-work`:** Full tsc check must pass (pre-existing errors excluded)

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 06.1-01-01 | 01 | 1 | — | — | N/A | manual | `bash packages/twentythree-cli/update-api-spec.sh` | ❌ W0 | ⬜ pending |
| 06.1-01-02 | 01 | 1 | — | — | N/A | automated | `pnpm --filter twentythree-cli exec tsc --noEmit` | ✅ | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `packages/twentythree-cli/specs/` directory created
- [ ] `packages/twentythree-cli/update-api-spec.sh` created and executable

*Existing test infrastructure (vitest, tsc) covers validation.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| First-run (untracked file) shows correct diff output | D-3 | Requires clean git state to test untracked path | Delete specs file, run script, verify "New spec file — no previous version to diff" output |
| Updated spec correctly regenerates types | D-1 | Requires live API or modified local spec | Edit specs file slightly, run script, verify types.ts is updated |
| CLAUDE.md workflow section is actionable | D-4 | Human judgment required | Read the section and follow it cold |

---

## Validation Sign-Off

- [ ] Shell script runs end-to-end without errors
- [ ] `generate-types` uses local file path (not live URL) after update
- [ ] CLAUDE.md section appended correctly
- [ ] Root pnpm alias works from monorepo root
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
