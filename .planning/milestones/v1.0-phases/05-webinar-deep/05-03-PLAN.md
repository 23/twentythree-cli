---
phase: 05-webinar-deep
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/twentythree-cli/src/commands/webinar/recording/start.ts
  - packages/twentythree-cli/src/commands/webinar/recording/stop.ts
  - packages/twentythree-cli/src/commands/webinar/recording/status.ts
  - packages/twentythree-cli/src/commands/webinar/transcription/list.ts
  - packages/twentythree-cli/src/commands/webinar/transcription/connect.ts
  - packages/twentythree-cli/src/commands/webinar/transcription/locales.ts
  - packages/twentythree-cli/src/commands/webinar/transcription/transcriptionlist.ts
  - packages/twentythree-cli/src/commands/webinar/room/info.ts
  - packages/twentythree-cli/src/commands/webinar/room/themes.ts
  - packages/twentythree-cli/src/commands/webinar/room/send-recording.ts
  - packages/twentythree-cli/src/commands/webinar/room/connect.ts
  - packages/twentythree-cli/src/commands/webinar/queued-video/add.ts
  - packages/twentythree-cli/src/commands/webinar/queued-video/remove.ts
autonomous: true
requirements:
  - WEB-16
  - WEB-17
  - WEB-18
  - WEB-20

must_haves:
  truths:
    - "User can start and stop webinar recording and check status"
    - "Recording start/stop do NOT display upload_token from response"
    - "User can list transcriptions, connect a transcription, list locales, and list all transcriptions"
    - "Transcriptionlist uses path /live/transcriptionlist (NOT /live/transcription/list)"
    - "User can get room info, list themes, send recording, and connect to room"
    - "User can add and remove queued videos using --video-id flag"
  artifacts:
    - path: "packages/twentythree-cli/src/commands/webinar/recording/start.ts"
      provides: "Recording start action command"
      contains: "chalk.green"
    - path: "packages/twentythree-cli/src/commands/webinar/transcription/transcriptionlist.ts"
      provides: "Workspace transcription list"
      contains: "/live/transcriptionlist"
    - path: "packages/twentythree-cli/src/commands/webinar/queued-video/add.ts"
      provides: "Add queued video"
      contains: "photo_id"
  key_links:
    - from: "transcription/list.ts"
      to: "fetchWebinarToken"
      via: "token auto-lookup for /live/transcription/list"
      pattern: "fetchWebinarToken"
    - from: "queued-video/add.ts"
      to: "API"
      via: "photo_id mapped from --video-id flag"
      pattern: "video-id.*photo_id"
---

<objective>
Create recording (3), transcription (4), room (4), and queued-video (2) subcommands — 13 commands total.

Purpose: Deliver WEB-16 (recording control), WEB-17 (transcription management), WEB-18 (room management), and WEB-20 (queued video management).
Output: 13 commands covering recording lifecycle, transcription, room info, and video queue.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-webinar-deep/05-RESEARCH.md

<interfaces>
From packages/twentythree-cli/src/lib/base-command.ts:
```typescript
export abstract class AuthenticatedCommand<T extends typeof Command> extends BaseCommand<T> {
  protected async fetchWebinarToken(webinarId: string | number): Promise<string>
  protected apiClient: ReturnType<typeof createApiClient>
  protected apiBaseUrl: string
  protected activeWorkspace: WorkspaceEntry
  protected printWorkspaceHeader(): void
}
```

From packages/twentythree-cli/src/lib/output.ts:
```typescript
export function formatJsonOutput(params: JsonOutputParams): JsonOutput
export function renderTable(headers: string[], rows: string[][]): Table
export function formatApiError(error: unknown): string
export const EXIT_ERROR = 1
export const EXIT_CANCELLED = 2
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create recording start, stop, status and queued-video add, remove commands</name>
  <files>
    packages/twentythree-cli/src/commands/webinar/recording/start.ts
    packages/twentythree-cli/src/commands/webinar/recording/stop.ts
    packages/twentythree-cli/src/commands/webinar/recording/status.ts
    packages/twentythree-cli/src/commands/webinar/queued-video/add.ts
    packages/twentythree-cli/src/commands/webinar/queued-video/remove.ts
  </files>
  <read_first>
    packages/twentythree-cli/src/commands/webinar/delete.ts
    packages/twentythree-cli/src/commands/webinar/create.ts
    packages/twentythree-cli/src/lib/base-command.ts
    packages/twentythree-cli/src/lib/output.ts
  </read_first>
  <action>
**recording/start.ts:**
Create `WebinarRecordingStart` extending `AuthenticatedCommand`.

- `static description = 'Start recording a webinar'`
- `static enableJsonFlag = true`
- Args: `id` (string, required, 'Webinar ID')
- Flags: `...AuthenticatedCommand.baseFlags`
- Action command (Decision D-1): green success line only
- POST to `/live/recording/start` with body `{ live_id: Number(args.id) }` as any, headers `{ 'Content-Type': 'application/x-www-form-urlencoded' }`
- Handle error: `this.error(applyCliTerms(formatApiError(error)), { exit: EXIT_ERROR })`
- SECURITY (Pitfall 6): Do NOT log `upload_token` from response data — only show success message
- Success: `this.log(chalk.green('Recording started'))`
- JSON: `return formatJsonOutput({ ok: true, data, summary: 'Recording started', breadcrumbs: [{ domain: this.activeWorkspace.domain }, { resource: 'webinar', id: args.id }] })`

**recording/stop.ts:**
Create `WebinarRecordingStop`. Same pattern as start.

- POST to `/live/recording/stop` with body `{ live_id: Number(args.id) }`
- SECURITY: Do NOT log `upload_token` from response
- Success: `chalk.green('Recording stopped')`

**recording/status.ts:**
Create `WebinarRecordingStatus`.

- `static description = 'Get recording status for a webinar'`
- Args: `id` (string, required, 'Webinar ID')
- `this.apiClient.GET('/live/recording/status', { params: { query: { live_id: Number(args.id) } } })`
- Display `status` field from response. SECURITY: Do NOT display `upload_token` field
- Table: `renderTable(['Field', 'Value'], [['Status', String((data as any)?.status ?? (data as any)?.data?.status ?? 'unknown')]])`
- JSON: standard shape

**queued-video/add.ts:**
Create `WebinarQueuedVideoAdd`.

- `static description = 'Add a queued video to a webinar'`
- Action command (Decision D-1)
- Args: `id` (string, required, 'Webinar ID')
- Flags: `...AuthenticatedCommand.baseFlags`, `video-id` (string, required: false, description: 'Video ID to queue')
- Interactive fallback: if `!flags['video-id'] && !this.jsonEnabled()`, prompt for video ID
- CRITICAL terminology: CLI flag is `--video-id` but API body field is `photo_id`
- POST to `/live/queuedvideos/add` with body `{ live_id: Number(args.id), photo_id: Number(flags['video-id']) }` as any
- Success: `chalk.green('Video queued')`

**queued-video/remove.ts:**
Create `WebinarQueuedVideoRemove`.

- Action command (Decision D-1)
- Args: `id` (string, required, 'Webinar ID')
- Flags: `...AuthenticatedCommand.baseFlags`, `video-id` (string, required: false)
- Interactive fallback for `--video-id`
- POST to `/live/queuedvideos/remove` with body `{ live_id: Number(args.id), photo_id: Number(flags['video-id']) }`
- Success: `chalk.green('Video removed from queue')`

All imports from `'../../../lib/base-command.js'`, `'../../../lib/output.js'`, `'../../../lib/term-map.js'`
  </action>
  <verify>
    <automated>cd /Users/steffenchristensen/23/twentythree-cli && pnpm --filter twentythree-cli exec tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `recording/start.ts` POSTs to `/live/recording/start` with `live_id`
    - `recording/start.ts` does NOT contain `upload_token` in any `this.log()` call
    - `recording/stop.ts` follows same pattern, no token logging
    - `recording/status.ts` displays `status` field only, not `upload_token`
    - `queued-video/add.ts` uses `photo_id` in body but `--video-id` as flag name
    - `queued-video/remove.ts` uses same terminology mapping
  </acceptance_criteria>
  <done>3 recording commands and 2 queued-video commands compile, tokens never logged, video-id maps to photo_id</done>
</task>

<task type="auto">
  <name>Task 2: Create transcription list, connect, locales, transcriptionlist commands</name>
  <files>
    packages/twentythree-cli/src/commands/webinar/transcription/list.ts
    packages/twentythree-cli/src/commands/webinar/transcription/connect.ts
    packages/twentythree-cli/src/commands/webinar/transcription/locales.ts
    packages/twentythree-cli/src/commands/webinar/transcription/transcriptionlist.ts
  </files>
  <read_first>
    packages/twentythree-cli/src/commands/video/section/list.ts
    packages/twentythree-cli/src/commands/webinar/list.ts
    packages/twentythree-cli/src/lib/base-command.ts
  </read_first>
  <action>
**transcription/list.ts:**
Create `WebinarTranscriptionList`.

- `static description = 'List transcriptions for a webinar'`
- Args: `id` (string, required, 'Webinar ID')
- Flags: `...AuthenticatedCommand.baseFlags`, `token` (string, optional)
- Token auto-lookup: `const token = flags.token ?? await this.fetchWebinarToken(Number(args.id))`
- `this.apiClient.GET('/live/transcription/list', { params: { query: { live_id: Number(args.id), token } } })`
- Extract: `const items: any[] = Array.isArray((data as any)?.data) ? (data as any).data : (data as any)?.data ? [(data as any).data] : []`
- Table with columns: ID, Language, Status (adjust based on actual response shape)

**transcription/connect.ts:**
Create `WebinarTranscriptionConnect`.

- `static description = 'Connect a transcription to a webinar'`
- Args: `id` (string, required, 'Webinar ID')
- Flags: `...AuthenticatedCommand.baseFlags`, `presenter-token` (string, optional, 'Presenter token')
- POST to `/live/transcription/connect` with body `{ live_id: Number(args.id), presenter_token: flags['presenter-token'] }` (only include presenter_token if defined)
- Action command: `chalk.green('Transcription connected')`

**transcription/locales.ts:**
Create `WebinarTranscriptionLocales`.

- `static description = 'List available transcription locales for a webinar'`
- Args: `id` (string, required, 'Webinar ID')
- Flags: `...AuthenticatedCommand.baseFlags`, `token` (string, optional)
- Token auto-lookup: `const token = flags.token ?? await this.fetchWebinarToken(Number(args.id))`
- `this.apiClient.GET('/live/transcription/locales', { params: { query: { live_id: Number(args.id), token } } })`
- Table with columns based on response

**transcription/transcriptionlist.ts:**
Create `WebinarTranscriptionTranscriptionlist`.

- `static description = 'List all transcriptions in the workspace'`
- CRITICAL (Pitfall 4): endpoint path is `/live/transcriptionlist` — NOT `/live/transcription/list`
- This is a workspace-scoped command — no webinar ID arg needed
- `this.apiClient.GET('/live/transcriptionlist' as any, { params: { query: {} } })`
- If the path is not in openapi-fetch types, cast: `(this.apiClient as any).GET('/live/transcriptionlist', ...)`
- Table: columns based on response shape
  </action>
  <verify>
    <automated>cd /Users/steffenchristensen/23/twentythree-cli && pnpm --filter twentythree-cli exec tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `transcription/list.ts` uses `fetchWebinarToken` for auto-lookup
    - `transcription/locales.ts` uses `fetchWebinarToken` for auto-lookup
    - `transcription/transcriptionlist.ts` calls `/live/transcriptionlist` (NOT `/live/transcription/list`)
    - `transcription/connect.ts` POSTs to `/live/transcription/connect`
    - All 4 files have `enableJsonFlag = true`
  </acceptance_criteria>
  <done>4 transcription commands compile, transcriptionlist uses correct distinct path, list/locales use token auto-lookup</done>
</task>

<task type="auto">
  <name>Task 3: Create room info, themes, send-recording, connect commands</name>
  <files>
    packages/twentythree-cli/src/commands/webinar/room/info.ts
    packages/twentythree-cli/src/commands/webinar/room/themes.ts
    packages/twentythree-cli/src/commands/webinar/room/send-recording.ts
    packages/twentythree-cli/src/commands/webinar/room/connect.ts
  </files>
  <read_first>
    packages/twentythree-cli/src/commands/webinar/list.ts
    packages/twentythree-cli/src/commands/webinar/recording/start.ts
  </read_first>
  <action>
**room/info.ts:**
Create `WebinarRoomInfo`.

- `static description = 'Get room information for a webinar'`
- Args: `id` (string, required, 'Webinar ID')
- NOTE: room endpoints use path `/live/webinar/info` not `/live/room/info`
- `this.apiClient.GET('/live/webinar/info' as any, { params: { query: { live_id: Number(args.id) } } })`
- Display key fields in a vertical key-value table: `renderTable(['Field', 'Value'], [['Room URL', ...], ['Status', ...], ...])`
- JSON: standard shape

**room/themes.ts:**
Create `WebinarRoomThemes`.

- `static description = 'List available room themes'`
- No args needed (workspace-scoped, no params)
- `this.apiClient.GET('/live/webinar/room-themes' as any, { params: { query: {} } })`
- Table with theme list columns

**room/send-recording.ts:**
Create `WebinarRoomSendRecording`.

- `static description = 'Send a recording from the webinar room'`
- Args: `id` (string, required, 'Webinar ID')
- Flags: check API types for required fields; likely `live_id` and possibly recipient info
- POST to `/live/webinar/send-recording` with body including `live_id`
- Action command: `chalk.green('Recording sent')`

**room/connect.ts:**
Create `WebinarRoomConnect`.

- `static description = 'Get connection info for a webinar room'`
- Args: `id` (string, required, 'Webinar ID')
- `this.apiClient.GET('/live/webinar/connect' as any, { params: { query: { live_id: Number(args.id) } } })`
- Display connection details in key-value table
- JSON: standard shape

NOTE: If these paths are not in the generated types, use `as any` casts for the path parameter. The API types file is authoritative but may not include all live/webinar/* sub-paths.
  </action>
  <verify>
    <automated>cd /Users/steffenchristensen/23/twentythree-cli && pnpm --filter twentythree-cli exec tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `room/info.ts` calls `/live/webinar/info` (NOT `/live/room/info`)
    - `room/themes.ts` calls `/live/webinar/room-themes`
    - `room/send-recording.ts` is an action command with green success
    - `room/connect.ts` calls `/live/webinar/connect`
    - All 4 files extend `AuthenticatedCommand`
  </acceptance_criteria>
  <done>4 room commands compile, use correct /live/webinar/* paths, send-recording follows action pattern</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI -> API | Webinar IDs, video IDs sent to TwentyThree API |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-10 | Information Disclosure | recording/start.ts, recording/stop.ts | mitigate | Never display upload_token from recording response — only green success message |
| T-05-11 | Information Disclosure | recording/status.ts | mitigate | Display only status field, filter out upload_token from table |
| T-05-12 | Information Disclosure | all commands | mitigate | applyCliTerms() on all error messages |
| T-05-13 | Tampering | queued-video/add.ts | mitigate | Validate video-id is numeric before sending as photo_id |
</threat_model>

<verification>
All 13 commands compile: `pnpm --filter twentythree-cli exec tsc --noEmit`
Recording commands never log upload_token
Transcriptionlist uses correct path /live/transcriptionlist
Queued-video commands map --video-id to photo_id
Room commands use /live/webinar/* paths
</verification>

<success_criteria>
- 3 recording + 4 transcription + 4 room + 2 queued-video commands exist and compile
- Recording start/stop never log upload_token
- Transcriptionlist endpoint path is /live/transcriptionlist
- Queued-video uses --video-id flag mapped to photo_id in body
- All commands support --json
</success_criteria>

<output>
After completion, create `.planning/phases/05-webinar-deep/05-03-SUMMARY.md`
</output>
