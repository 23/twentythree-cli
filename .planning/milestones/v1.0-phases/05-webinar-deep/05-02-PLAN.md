---
phase: 05-webinar-deep
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/twentythree-cli/src/commands/webinar/speaker/list.ts
  - packages/twentythree-cli/src/commands/webinar/speaker/add.ts
  - packages/twentythree-cli/src/commands/webinar/speaker/add-from-user.ts
  - packages/twentythree-cli/src/commands/webinar/speaker/add-from-speaker.ts
  - packages/twentythree-cli/src/commands/webinar/speaker/update.ts
  - packages/twentythree-cli/src/commands/webinar/speaker/remove.ts
  - packages/twentythree-cli/src/commands/webinar/speaker/set-avatar.ts
  - packages/twentythree-cli/src/commands/webinar/speaker/remove-avatar.ts
  - packages/twentythree-cli/src/commands/webinar/speaker/set-order.ts
  - packages/twentythree-cli/src/commands/webinar/speaker/send-invitation.ts
  - packages/twentythree-cli/src/commands/webinar/speaker/request-guest.ts
  - packages/twentythree-cli/src/commands/webinar/speaker/cancel-guest-request.ts
  - packages/twentythree-cli/src/commands/webinar/speaker/connection-types.ts
  - packages/twentythree-cli/src/commands/webinar/speaker/library.ts
  - packages/twentythree-cli/src/commands/webinar/mail/list.ts
  - packages/twentythree-cli/src/commands/webinar/mail/add.ts
  - packages/twentythree-cli/src/commands/webinar/mail/update.ts
  - packages/twentythree-cli/src/commands/webinar/mail/remove.ts
  - packages/twentythree-cli/src/commands/webinar/mail/preview.ts
  - packages/twentythree-cli/src/commands/webinar/mail/send.ts
  - packages/twentythree-cli/src/commands/webinar/mail/test.ts
autonomous: true
requirements:
  - WEB-14
  - WEB-15

must_haves:
  truths:
    - "User can list, add, update, and remove speakers on a webinar"
    - "User can set and remove speaker avatars via chunked upload"
    - "User can send speaker invitations, request guests, and cancel guest requests"
    - "User can browse the speaker library and connection types"
    - "User can list, add, update, and remove webinar mail"
    - "User can preview mail HTML piped to stdout"
    - "User can send and test webinar mail"
  artifacts:
    - path: "packages/twentythree-cli/src/commands/webinar/speaker/add.ts"
      provides: "Speaker add with name+email interactive fallback"
      contains: "live_id.*name.*email"
    - path: "packages/twentythree-cli/src/commands/webinar/speaker/set-avatar.ts"
      provides: "Chunked upload for speaker avatar"
      contains: "live_speaker_id"
    - path: "packages/twentythree-cli/src/commands/webinar/mail/preview.ts"
      provides: "Raw HTML output to stdout"
      contains: "process.stdout.write"
    - path: "packages/twentythree-cli/src/commands/webinar/mail/send.ts"
      provides: "Action command with green success"
      contains: "chalk.green"
  key_links:
    - from: "speaker/set-avatar.ts"
      to: "uploadChunked"
      via: "chunked engine with tokenFieldName: 'live_speaker_id'"
      pattern: "tokenFieldName.*live_speaker_id"
    - from: "mail/preview.ts"
      to: "process.stdout"
      via: "raw HTML piped to stdout"
      pattern: "process.stdout.write"
---

<objective>
Create all 14 webinar speaker subcommands and all 7 webinar mail subcommands (21 commands total).

Purpose: Deliver WEB-14 (full speaker management including avatar upload, invitations, guest requests, library) and WEB-15 (mail CRUD plus preview, send, test).
Output: 14 speaker commands + 7 mail commands, all extending AuthenticatedCommand.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-webinar-deep/05-RESEARCH.md

<interfaces>
From packages/twentythree-cli/src/lib/base-command.ts:
```typescript
export abstract class AuthenticatedCommand<T extends typeof Command> extends BaseCommand<T> {
  protected async fetchWebinarToken(webinarId: string | number): Promise<string>
  protected apiClient: ReturnType<typeof createApiClient>
  protected apiBaseUrl: string
  protected activeWorkspace: WorkspaceEntry
  protected printWorkspaceHeader(): void
}
```

From packages/twentythree-cli/src/lib/output.ts:
```typescript
export function formatJsonOutput(params: JsonOutputParams): JsonOutput
export function renderTable(headers: string[], rows: string[][]): Table
export function formatApiError(error: unknown): string
export function parseBoolParam(primary: boolean | undefined, alt: string | undefined): boolean | undefined
export const EXIT_ERROR = 1
export const EXIT_CANCELLED = 2
```

From packages/twentythree-cli/src/upload/chunked-upload.ts:
```typescript
export function uploadChunked(params: ChunkedUploadParams): Promise<ChunkedUploadResult>
```

From packages/twentythree-cli/src/upload/types.ts:
```typescript
export const DEFAULT_CHUNK_SIZE: number
export const DEFAULT_CONCURRENCY: number
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create speaker list, add, add-from-user, add-from-speaker, library, connection-types commands</name>
  <files>
    packages/twentythree-cli/src/commands/webinar/speaker/list.ts
    packages/twentythree-cli/src/commands/webinar/speaker/add.ts
    packages/twentythree-cli/src/commands/webinar/speaker/add-from-user.ts
    packages/twentythree-cli/src/commands/webinar/speaker/add-from-speaker.ts
    packages/twentythree-cli/src/commands/webinar/speaker/library.ts
    packages/twentythree-cli/src/commands/webinar/speaker/connection-types.ts
  </files>
  <read_first>
    packages/twentythree-cli/src/commands/webinar/list.ts
    packages/twentythree-cli/src/commands/webinar/create.ts
    packages/twentythree-cli/src/lib/base-command.ts
    packages/twentythree-cli/src/lib/output.ts
  </read_first>
  <action>
**speaker/list.ts:**
Create `WebinarSpeakerList` extending `AuthenticatedCommand`.

- `static description = 'List speakers for a webinar'`
- Args: `id` (string, required, 'Webinar ID')
- Flags: `...AuthenticatedCommand.baseFlags`
- NOTE: speaker/list does NOT require a token param — only `live_id`
- `this.apiClient.GET('/live/speaker/list', { params: { query: { live_id: Number(args.id) } } })`
- Extract speakers: `const speakers: any[] = Array.isArray((data as any)?.data) ? (data as any).data : (data as any)?.data ? [(data as any).data] : []`
- Table: `renderTable(['ID', 'Name', 'Email', 'Role', 'Order'], speakers.map(s => [String(s.live_speaker_id ?? ''), String(s.name ?? ''), String(s.email ?? ''), String(s.role ?? ''), String(s.speaker_order ?? '')]))`

**speaker/add.ts:**
Create `WebinarSpeakerAdd` extending `AuthenticatedCommand`.

- `static description = 'Add a speaker to a webinar'`
- Args: `id` (string, required, 'Webinar ID')
- Flags: `...AuthenticatedCommand.baseFlags`, `name` (string, optional), `email` (string, optional), `title` (string, optional), `description` (string, optional)
- Interactive fallback (Decision D-2): if `!flags.name || !flags.email` and `!this.jsonEnabled()`, prompt for name and email via `text()` from `@clack/prompts`
- Flag mode: if `!flags.name` error `'--name is required in non-interactive mode'`; same for `--email`
- POST to `/live/speaker/add` with body `{ live_id: Number(args.id), name, email, title?, description? }`
- Extract `live_speaker_id` from response
- Success: `chalk.green('Speaker added')`, print speaker ID and admin URL

**speaker/add-from-user.ts:**
Create `WebinarSpeakerAddFromUser`.

- Args: `id` (string, required, 'Webinar ID')
- Flags: `...AuthenticatedCommand.baseFlags`, `user-id` (string, required: false)
- Interactive fallback for `--user-id` when not provided
- POST to `/live/speaker/add-from-user` with body `{ live_id: Number(args.id), user_id: Number(flags['user-id']) }`
- Success: `chalk.green('Speaker added from user')`

**speaker/add-from-speaker.ts:**
Create `WebinarSpeakerAddFromSpeaker`.

- Args: `id` (string, required, 'Webinar ID')
- Flags: `...AuthenticatedCommand.baseFlags`, `speaker-id` (string, required: false)
- Interactive fallback for `--speaker-id`
- POST to `/live/speaker/add-from-speaker` with body `{ live_id: Number(args.id), live_speaker_id: Number(flags['speaker-id']) }`
- Success: `chalk.green('Speaker added from library')`

**speaker/library.ts:**
Create `WebinarSpeakerLibrary`.

- `static description = 'List speakers in the workspace library'`
- No args needed (workspace-scoped)
- `this.apiClient.GET('/live/speaker/library/list', { params: { query: {} } })`
- Table with columns: ID, Name, Email

**speaker/connection-types.ts:**
Create `WebinarSpeakerConnectionTypes`.

- `static description = 'List available speaker connection types'`
- No args needed (workspace-scoped)
- `this.apiClient.GET('/live/speaker/connection-types', { params: { query: {} } })`
- Table: appropriate columns from response data

All commands import from `'../../../lib/base-command.js'`, `'../../../lib/output.js'`, `'../../../lib/term-map.js'`; use `applyCliTerms(formatApiError(error))` on all error paths.
  </action>
  <verify>
    <automated>cd /Users/steffenchristensen/23/twentythree-cli && pnpm --filter twentythree-cli exec tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `speaker/list.ts` calls `/live/speaker/list` with `live_id` only (no token)
    - `speaker/add.ts` has interactive fallback for `name` and `email`
    - `speaker/add.ts` POSTs to `/live/speaker/add`
    - `speaker/add-from-user.ts` uses `user_id` in body
    - `speaker/add-from-speaker.ts` uses `live_speaker_id` in body
    - `speaker/library.ts` calls `/live/speaker/library/list`
    - All 6 files have `enableJsonFlag = true`
  </acceptance_criteria>
  <done>6 speaker commands (list, add, add-from-user, add-from-speaker, library, connection-types) compile and follow established patterns</done>
</task>

<task type="auto">
  <name>Task 2: Create speaker update, remove, set-avatar, remove-avatar, set-order commands</name>
  <files>
    packages/twentythree-cli/src/commands/webinar/speaker/update.ts
    packages/twentythree-cli/src/commands/webinar/speaker/remove.ts
    packages/twentythree-cli/src/commands/webinar/speaker/set-avatar.ts
    packages/twentythree-cli/src/commands/webinar/speaker/remove-avatar.ts
    packages/twentythree-cli/src/commands/webinar/speaker/set-order.ts
  </files>
  <read_first>
    packages/twentythree-cli/src/commands/webinar/upload-image.ts
    packages/twentythree-cli/src/commands/webinar/delete.ts
    packages/twentythree-cli/src/commands/webinar/speaker/add.ts
  </read_first>
  <action>
**speaker/update.ts:**
Create `WebinarSpeakerUpdate`.

- Args: `id` (string, required, 'Speaker ID')
- Flags: `...AuthenticatedCommand.baseFlags`, `name` (string, optional), `email` (string, optional), `title` (string, optional), `description` (string, optional)
- CRITICAL: uses `live_speaker_id` (the speaker ID), NOT `live_id`
- Build body: only include fields where `flags.X !== undefined` (prevent clearing unset fields)
- POST to `/live/speaker/update` with body `{ live_speaker_id: Number(args.id), ...definedFields }`
- Success: `chalk.green('Speaker updated')`

**speaker/remove.ts:**
Create `WebinarSpeakerRemove`.

- Args: `id` (string, required, 'Speaker ID')
- Confirmation: `confirm({ message: \`Remove speaker \${args.id} from \${this.activeWorkspace.domain}? This cannot be undone.\` })`
- POST to `/live/speaker/remove` with body `{ live_speaker_id: Number(args.id) }`
- Success: `chalk.green(\`Speaker \${args.id} removed\`)`

**speaker/set-avatar.ts:**
Create `WebinarSpeakerSetAvatar`.

- `static description = 'Upload an avatar image for a speaker'`
- Args: `id` (string, required, 'Speaker ID'), `file` (string, required, 'Path to image file')
- Flags: `...AuthenticatedCommand.baseFlags`, `chunk-size` (integer, default: DEFAULT_CHUNK_SIZE), `concurrency` (integer, default: DEFAULT_CONCURRENCY)
- CRITICAL: `tokenFieldName: 'live_speaker_id'` — NOT 'live_id'. The upload token value is the speaker ID.
- In `run()`:
  1. Validate file: `await stat(args.file)`
  2. Create inline `ProgressBar` (same pattern as upload-image.ts)
  3. `uploadChunked({ filePath: args.file, uploadToken: String(args.id), tokenFieldName: 'live_speaker_id', uploadUrl: \`\${this.apiBaseUrl}live/speaker/set-avatar\`, bearerToken: this.activeWorkspace.bearer_token || undefined, chunkSize: flags['chunk-size'], concurrency: flags.concurrency, onProgress(...) { ... } })`
  4. Success: `chalk.green('Speaker avatar uploaded')`, print speaker ID

**speaker/remove-avatar.ts:**
Create `WebinarSpeakerRemoveAvatar`.

- Args: `id` (string, required, 'Speaker ID')
- POST to `/live/speaker/remove-avatar` with body `{ live_speaker_id: Number(args.id) }`
- Action command: `chalk.green('Speaker avatar removed')`

**speaker/set-order.ts:**
Create `WebinarSpeakerSetOrder`.

- Args: `id` (string, required, 'Webinar ID')
- Flags: `...AuthenticatedCommand.baseFlags`, `speaker-id` (string, required: false), `order` (integer, required: false)
- Interactive fallback for both flags
- POST to `/live/speaker/set-order` with body `{ live_id: Number(args.id), live_speaker_id: Number(flags['speaker-id']), order: Number(flags.order) }`
- Action command: `chalk.green('Speaker order updated')`
  </action>
  <verify>
    <automated>cd /Users/steffenchristensen/23/twentythree-cli && pnpm --filter twentythree-cli exec tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `speaker/update.ts` uses `live_speaker_id` in body
    - `speaker/remove.ts` has confirmation prompt
    - `speaker/set-avatar.ts` uses `tokenFieldName: 'live_speaker_id'` (NOT 'live_id')
    - `speaker/set-avatar.ts` uses `uploadChunked`
    - `speaker/remove-avatar.ts` outputs green success line only
    - `speaker/set-order.ts` sends `live_id`, `live_speaker_id`, and `order`
  </acceptance_criteria>
  <done>5 speaker management commands compile, set-avatar uses correct tokenFieldName 'live_speaker_id', remove has confirmation</done>
</task>

<task type="auto">
  <name>Task 3: Create speaker action commands (send-invitation, request-guest, cancel-guest-request)</name>
  <files>
    packages/twentythree-cli/src/commands/webinar/speaker/send-invitation.ts
    packages/twentythree-cli/src/commands/webinar/speaker/request-guest.ts
    packages/twentythree-cli/src/commands/webinar/speaker/cancel-guest-request.ts
  </files>
  <read_first>
    packages/twentythree-cli/src/commands/webinar/speaker/remove.ts
  </read_first>
  <action>
All three are action commands (Decision D-1): green success line only, no table output.

**speaker/send-invitation.ts:**
Create `WebinarSpeakerSendInvitation`.

- `static description = 'Send an invitation to a speaker'`
- Args: `id` (string, required, 'Speaker ID')
- POST to `/live/speaker/send-invitation` with body `{ live_speaker_id: Number(args.id) }` and `'Content-Type': 'application/x-www-form-urlencoded'`
- Handle error: `this.error(applyCliTerms(formatApiError(error)), { exit: EXIT_ERROR })`
- Success: `this.log(chalk.green('Speaker invitation sent'))`
- JSON: `formatJsonOutput({ ok: true, data, summary: 'Speaker invitation sent', breadcrumbs: [{ domain: this.activeWorkspace.domain }, { resource: 'speaker', id: args.id }] })`

**speaker/request-guest.ts:**
Create `WebinarSpeakerRequestGuest`.

- `static description = 'Request a speaker as a guest'`
- Args: `id` (string, required, 'Speaker ID')
- POST to `/live/speaker/request-guest` with body `{ live_speaker_id: Number(args.id) }`
- Success: `chalk.green('Guest request sent')`

**speaker/cancel-guest-request.ts:**
Create `WebinarSpeakerCancelGuestRequest`.

- `static description = 'Cancel a guest request for a speaker'`
- Args: `id` (string, required, 'Speaker ID')
- POST to `/live/speaker/cancel-guest-request` with body `{ live_speaker_id: Number(args.id) }`
- Success: `chalk.green('Guest request cancelled')`

All three follow identical structure: parse args, printWorkspaceHeader, POST, handle error, green success, JSON return.
  </action>
  <verify>
    <automated>cd /Users/steffenchristensen/23/twentythree-cli && pnpm --filter twentythree-cli exec tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - All 3 files output green success line only (action command pattern)
    - All 3 use `live_speaker_id` in body
    - All 3 have `enableJsonFlag = true`
    - No table rendering in any of these files
  </acceptance_criteria>
  <done>3 speaker action commands compile, all follow action command pattern with green success line</done>
</task>

<task type="auto">
  <name>Task 4: Create mail list, add, update, remove commands</name>
  <files>
    packages/twentythree-cli/src/commands/webinar/mail/list.ts
    packages/twentythree-cli/src/commands/webinar/mail/add.ts
    packages/twentythree-cli/src/commands/webinar/mail/update.ts
    packages/twentythree-cli/src/commands/webinar/mail/remove.ts
  </files>
  <read_first>
    packages/twentythree-cli/src/commands/webinar/list.ts
    packages/twentythree-cli/src/commands/webinar/create.ts
    packages/twentythree-cli/src/commands/webinar/delete.ts
  </read_first>
  <action>
**mail/list.ts:**
Create `WebinarMailList` extending `AuthenticatedCommand`.

- `static description = 'List emails for a webinar'`
- Args: `id` (string, required, 'Webinar ID')
- Flags: `...AuthenticatedCommand.baseFlags`
- `this.apiClient.GET('/live/mail/list', { params: { query: { live_id: Number(args.id) } } })`
- Extract mails: `const mails: any[] = Array.isArray((data as any)?.data) ? (data as any).data : (data as any)?.data ? [(data as any).data] : []`
- Table: `renderTable(['ID', 'Subject', 'Status', 'Send Date'], mails.map(m => [String(m.live_mail_id ?? m.id ?? ''), applyCliTerms(String(m.subject ?? '')), String(m.status ?? ''), String(m.send_date ?? '')]))`

**mail/add.ts:**
Create `WebinarMailAdd`.

- Args: `id` (string, required, 'Webinar ID')
- Flags: `...AuthenticatedCommand.baseFlags`, `subject` (string, optional), `message` (string, optional)
- Interactive fallback: if `!flags.subject || !flags.message` and `!this.jsonEnabled()`, prompt via `text()`
- Flag mode: error if `--subject` or `--message` missing
- POST to `/live/mail/add` with body `{ live_id: Number(args.id), subject, message }`
- Extract `live_mail_id` from response
- Success: `chalk.green('Mail created')`, print ID

**mail/update.ts:**
Create `WebinarMailUpdate`.

- Args: `id` (string, required, 'Mail ID')
- Flags: `...AuthenticatedCommand.baseFlags`, `subject` (string, optional), `message` (string, optional)
- POST to `/live/mail/update` with body `{ live_mail_id: Number(args.id), ...definedFields }`
- Success: `chalk.green('Mail updated')`

**mail/remove.ts:**
Create `WebinarMailRemove`.

- Args: `id` (string, required, 'Mail ID')
- Confirmation prompt mentioning domain
- POST to `/live/mail/remove` with body `{ live_mail_id: Number(args.id) }`
- Success: `chalk.green(\`Mail \${args.id} removed\`)`
  </action>
  <verify>
    <automated>cd /Users/steffenchristensen/23/twentythree-cli && pnpm --filter twentythree-cli exec tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `mail/list.ts` calls `/live/mail/list` with `live_id`
    - `mail/add.ts` has interactive fallback for `subject` and `message`
    - `mail/update.ts` uses `live_mail_id` (NOT `live_id`)
    - `mail/remove.ts` has confirmation prompt
    - All 4 files have `enableJsonFlag = true`
  </acceptance_criteria>
  <done>4 mail CRUD commands compile, add has interactive fallback, update/remove use mail ID correctly</done>
</task>

<task type="auto">
  <name>Task 5: Create mail preview, send, and test commands</name>
  <files>
    packages/twentythree-cli/src/commands/webinar/mail/preview.ts
    packages/twentythree-cli/src/commands/webinar/mail/send.ts
    packages/twentythree-cli/src/commands/webinar/mail/test.ts
  </files>
  <read_first>
    packages/twentythree-cli/src/commands/webinar/mail/list.ts
    packages/twentythree-cli/src/commands/webinar/speaker/send-invitation.ts
  </read_first>
  <action>
**mail/preview.ts:**
Create `WebinarMailPreview`.

- `static description = 'Preview a webinar email as raw HTML'`
- Args: `id` (string, required, 'Mail ID')
- CRITICAL (Decision D-3): output raw HTML to stdout via `process.stdout.write()`, NOT `this.log()` — `this.log()` adds trailing newline that corrupts HTML piping
- `this.apiClient.GET('/live/mail/preview', { params: { query: { live_mail_id: Number(args.id) } } })` — cast path as needed if openapi-fetch types don't include it
- If types don't match, use: `(this.apiClient as any).GET('/live/mail/preview', ...)`
- Handle error: `this.error(applyCliTerms(formatApiError(error)), { exit: EXIT_ERROR })`
- Non-JSON: `process.stdout.write(String(data))` — write raw HTML content directly
- JSON mode: `formatJsonOutput({ ok: true, data, summary: 'Mail preview', breadcrumbs: [{ domain }, { resource: 'mail', id: args.id }] })`
- NOTE: Do not enableJsonFlag for preview — it outputs raw HTML by default. If `--json` is needed, add `enableJsonFlag = true` but check `this.jsonEnabled()` first to decide between HTML or JSON output.

**mail/send.ts:**
Create `WebinarMailSend`.

- Action command (Decision D-1): green success line only
- Args: `id` (string, required, 'Mail ID')
- POST to `/live/mail/send` with body `{ live_mail_id: Number(args.id) }`
- Success: `chalk.green('Mail sent')`

**mail/test.ts:**
Create `WebinarMailTest`.

- Action command (Decision D-1)
- `static description = 'Send a test email'`
- Args: `id` (string, required, 'Mail ID')
- Flags: `...AuthenticatedCommand.baseFlags`, `email` (string, required: false, 'Recipient email for test')
- Interactive fallback: if `!flags.email && !this.jsonEnabled()`, prompt for email
- POST to `/live/mail/test` with body `{ live_mail_id: Number(args.id), email }`
- Success: `chalk.green('Test email sent')`
  </action>
  <verify>
    <automated>cd /Users/steffenchristensen/23/twentythree-cli && pnpm --filter twentythree-cli exec tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `mail/preview.ts` uses `process.stdout.write` (NOT `this.log`) for HTML output
    - `mail/send.ts` outputs green success line only (action pattern)
    - `mail/test.ts` has interactive fallback for `email`
    - `mail/send.ts` and `mail/test.ts` POST with `live_mail_id`
  </acceptance_criteria>
  <done>Mail preview pipes raw HTML to stdout, send and test are action commands with green success</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI -> API | Speaker IDs, email addresses, mail content sent to API |
| File system -> upload | Avatar image files uploaded via chunked engine |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | Information Disclosure | speaker/set-avatar.ts | mitigate | Progress bar shows only byte counts, never speaker token |
| T-05-06 | Tampering | speaker/set-avatar.ts | mitigate | tokenFieldName explicitly set to 'live_speaker_id' |
| T-05-07 | Information Disclosure | all commands | mitigate | applyCliTerms() on all error messages |
| T-05-08 | Repudiation | speaker/remove.ts, mail/remove.ts | mitigate | Confirmation prompt includes domain before delete |
| T-05-09 | Information Disclosure | mail/preview.ts | accept | Raw HTML output is intentional per Decision D-3; user controls where it's piped |
</threat_model>

<verification>
All 21 commands compile: `pnpm --filter twentythree-cli exec tsc --noEmit`
Speaker set-avatar uses `tokenFieldName: 'live_speaker_id'` (not 'live_id')
Mail preview uses `process.stdout.write` for raw HTML
Action commands (send-invitation, request-guest, cancel-guest-request, mail send, mail test) output green success only
</verification>

<success_criteria>
- 14 speaker commands and 7 mail commands exist and compile
- Speaker add has interactive fallback for name+email
- Speaker set-avatar uses chunked upload with 'live_speaker_id'
- Mail preview pipes raw HTML to stdout
- All action commands output green success line only
- All commands support --json
</success_criteria>

<output>
After completion, create `.planning/phases/05-webinar-deep/05-02-SUMMARY.md`
</output>
