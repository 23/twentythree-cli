---
phase: 03-video-core
plan: 04
type: execute
wave: 3
depends_on: [03-01, 03-02]
files_modified:
  - packages/twentythree-cli/src/commands/video/replace.ts
  - packages/twentythree-cli/src/commands/video/transcoding-progress.ts
  - packages/twentythree-cli/src/commands/video/frame.ts
  - packages/twentythree-cli/src/commands/video/section/list.ts
  - packages/twentythree-cli/src/commands/video/section/create.ts
  - packages/twentythree-cli/src/commands/video/section/update.ts
  - packages/twentythree-cli/src/commands/video/section/delete.ts
  - packages/twentythree-cli/src/commands/video/section/set-thumbnail.ts
autonomous: true
requirements:
  - VID-06
  - VID-07
  - VID-08
  - VID-09

must_haves:
  truths:
    - "User can replace a video file using get-replace-token flow with chunked upload"
    - "User can check transcoding progress for a video"
    - "User can extract a frame from a video"
    - "User can list, create, update, delete sections and set section thumbnails"
    - "All commands output JSON with correct shape when --json is passed"
    - "Section delete requires confirmation with workspace domain"
  artifacts:
    - path: "packages/twentythree-cli/src/commands/video/replace.ts"
      provides: "video replace command using get-replace-token + chunked upload"
      min_lines: 50
    - path: "packages/twentythree-cli/src/commands/video/transcoding-progress.ts"
      provides: "video transcoding-progress command"
      min_lines: 20
    - path: "packages/twentythree-cli/src/commands/video/frame.ts"
      provides: "video frame command"
      min_lines: 20
    - path: "packages/twentythree-cli/src/commands/video/section/list.ts"
      provides: "video section list command"
      min_lines: 25
    - path: "packages/twentythree-cli/src/commands/video/section/create.ts"
      provides: "video section create command"
    - path: "packages/twentythree-cli/src/commands/video/section/update.ts"
      provides: "video section update command"
    - path: "packages/twentythree-cli/src/commands/video/section/delete.ts"
      provides: "video section delete command with confirmation"
    - path: "packages/twentythree-cli/src/commands/video/section/set-thumbnail.ts"
      provides: "video section set-thumbnail command"
  key_links:
    - from: "packages/twentythree-cli/src/commands/video/replace.ts"
      to: "packages/twentythree-cli/src/upload/chunked-upload.ts"
      via: "import uploadChunked"
      pattern: "uploadChunked"
---

<objective>
Implement video replace, transcoding-progress, frame, and all five section subcommands. Video replace uses the replace-token flow (distinct from upload-token). Section commands provide chapter management.

Purpose: Completes the video-level operations (replace, transcoding, frame) and the first subcommand tree (sections).
Output: 8 command files
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-video-core/03-CONTEXT.md
@.planning/phases/03-video-core/03-RESEARCH.md
@.planning/phases/03-video-core/03-01-SUMMARY.md
@.planning/phases/03-video-core/03-02-SUMMARY.md

@packages/twentythree-cli/src/lib/base-command.ts
@packages/twentythree-cli/src/api/client.ts
@packages/twentythree-cli/src/lib/term-map.ts
@packages/twentythree-cli/src/lib/output.ts
@packages/twentythree-cli/src/upload/types.ts
@packages/twentythree-cli/src/upload/chunked-upload.ts
@packages/twentythree-cli/src/commands/video/upload.ts
@packages/twentythree-cli/src/commands/video/delete.ts

<interfaces>
From RESEARCH.md — Replace Flow:
```
Step 1: GET /photo/get-replace-token?photo_id=<id>
  Response: { data: { replace_token: string, ... } }
Step 2: POST /photo/replace (multipart)
  Fields: replace_token, file (chunk), resumable* params
```
IMPORTANT: replace uses `replace_token` field name (NOT `upload_token`) and posts to `/photo/replace` (NOT `/photo/redeem-upload-token`).

From RESEARCH.md — Section Fields:
| Field | Required | Notes |
|-------|----------|-------|
| photo_id | Yes | Video ID |
| section_id | Update/Delete only | Section to modify |
| start_time | Create/Update | Seconds (integer) |
| title | Create/Update | Section title |
| description | Create/Update | Description text |

API operations: videoSectionList, videoSectionCreate, videoSectionUpdate, videoSectionDelete, videoSectionSetThumbnail
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement video replace, transcoding-progress, and frame commands</name>
  <files>
    packages/twentythree-cli/src/commands/video/replace.ts,
    packages/twentythree-cli/src/commands/video/transcoding-progress.ts,
    packages/twentythree-cli/src/commands/video/frame.ts
  </files>
  <read_first>
    packages/twentythree-cli/src/commands/video/upload.ts,
    packages/twentythree-cli/src/upload/chunked-upload.ts,
    packages/twentythree-cli/src/upload/types.ts,
    packages/twentythree-cli/src/lib/base-command.ts,
    packages/twentythree-cli/src/lib/output.ts,
    .planning/phases/03-video-core/03-RESEARCH.md
  </read_first>
  <action>
    **video replace** (`src/commands/video/replace.ts`):

    1. Extend `AuthenticatedCommand`. Args: `id` (required, video ID), `file` (required, path to replacement video). Flags: `--chunk-size`, `--concurrency` (same as upload).
    2. Call `this.printWorkspaceHeader()`.
    3. Step 1 — Get replace token (NOT upload token — per RESEARCH.md Pitfall 1):
       ```typescript
       const { data: tokenData, error } = await this.apiClient.GET('/photo/get-replace-token', {
         params: { query: { photo_id: args.id } },
       })
       ```
    4. Step 2 — Upload chunks to `/photo/replace` using chunked engine. CRITICAL: the `uploadToken` param passed to `uploadChunked` must be the `replace_token` value, and the `uploadUrl` must be `${this.activeWorkspace.api_base_url}photo/replace`. The chunked engine sends this as `upload_token` in FormData — but the replace endpoint accepts it under the same field name. Verify this in types.ts; if the endpoint expects `replace_token` as the field name, a small adaptation in the chunked engine call or a wrapper is needed.
    5. Wire cli-progress bar identical to video upload.
    6. Success → log and return formatJsonOutput.

    **video transcoding-progress** (`src/commands/video/transcoding-progress.ts`):

    1. Extend `AuthenticatedCommand`. Args: `id` (required).
    2. Call `this.printWorkspaceHeader()`.
    3. Call `this.apiClient.GET('/photo/get-transcoding-progress', { params: { query: { photo_id: args.id } } })`.
    4. Display progress percentage and status. If `--json`, return formatJsonOutput with the transcoding data.
    5. Apply `applyCliTerms` to any error messages.

    **video frame** (`src/commands/video/frame.ts`):

    1. Extend `AuthenticatedCommand`. Args: `id` (required). Flags: `--time` (number, seconds into video, optional).
    2. Call `this.printWorkspaceHeader()`.
    3. Call `this.apiClient.POST('/photo/frame', { body: { photo_id: Number(args.id), ...(flags.time !== undefined && { time: flags.time }) }, headers: { 'Content-Type': 'application/x-www-form-urlencoded' } })`.
    4. Success → log frame URL or confirmation. If `--json`, return formatJsonOutput.
  </action>
  <verify>
    <automated>cd /Users/steffenchristensen/23/twentythree-cli && pnpm --filter twentythree-cli build && pnpm --filter twentythree-cli test --run</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "get-replace-token" packages/twentythree-cli/src/commands/video/replace.ts` succeeds
    - `grep -q "replace_token\|uploadToken" packages/twentythree-cli/src/commands/video/replace.ts` succeeds
    - `grep -q "uploadChunked" packages/twentythree-cli/src/commands/video/replace.ts` succeeds
    - `grep -q "photo/replace" packages/twentythree-cli/src/commands/video/replace.ts` succeeds (correct endpoint)
    - `grep -q "get-transcoding-progress" packages/twentythree-cli/src/commands/video/transcoding-progress.ts` succeeds
    - `grep -q "photo/frame" packages/twentythree-cli/src/commands/video/frame.ts` succeeds
    - All three files contain `AuthenticatedCommand` and `printWorkspaceHeader`
    - Build succeeds
  </acceptance_criteria>
  <done>video replace uses get-replace-token + chunked upload to /photo/replace; video transcoding-progress shows progress; video frame extracts a frame; all support --json and apply term-map</done>
</task>

<task type="auto">
  <name>Task 2: Implement all five video section subcommands</name>
  <files>
    packages/twentythree-cli/src/commands/video/section/list.ts,
    packages/twentythree-cli/src/commands/video/section/create.ts,
    packages/twentythree-cli/src/commands/video/section/update.ts,
    packages/twentythree-cli/src/commands/video/section/delete.ts,
    packages/twentythree-cli/src/commands/video/section/set-thumbnail.ts
  </files>
  <read_first>
    packages/twentythree-cli/src/lib/base-command.ts,
    packages/twentythree-cli/src/lib/output.ts,
    packages/twentythree-cli/src/lib/term-map.ts,
    packages/twentythree-cli/src/commands/video/delete.ts,
    packages/twentythree-cli/src/commands/video/section/index.ts,
    .planning/phases/03-video-core/03-RESEARCH.md
  </read_first>
  <action>
    All section commands extend `AuthenticatedCommand`, call `this.printWorkspaceHeader()`, support `--json`, and apply `applyCliTerms`.

    **section list** (`src/commands/video/section/list.ts`):
    1. Args: `id` (required, video ID).
    2. `GET /photo/section/list` with `params: { query: { photo_id: args.id } }`.
    3. Render table: ID, Title, Start Time, Description.
    4. If `--json`, return formatJsonOutput with `{ resource: 'video', id: args.id }` and `{ resource: 'section' }` in breadcrumbs.

    **section create** (`src/commands/video/section/create.ts`):
    1. Args: `id` (required, video ID). Flags: `--title` (required), `--start-time` (required, integer seconds), `--description` (optional).
    2. `POST /photo/section/create` with form-encoded body: `{ photo_id, title, start_time, description }`.
    3. Log success, return formatJsonOutput.

    **section update** (`src/commands/video/section/update.ts`):
    1. Args: `id` (required, video ID). Flags: `--section-id` (required), `--title`, `--start-time`, `--description` (all optional — only send provided, per Pitfall 3 pattern).
    2. `POST /photo/section/update` with form-encoded body including only provided fields.

    **section delete** (`src/commands/video/section/delete.ts`):
    1. Args: `id` (required, video ID). Flags: `--section-id` (required).
    2. Confirmation prompt: `Delete section ${flags['section-id']} from video ${args.id} on ${this.activeWorkspace.domain}?`
    3. If cancelled, `process.exit(EXIT_CANCELLED)`.
    4. If `--json`, skip confirmation.
    5. `POST /photo/section/delete` with `{ photo_id, section_id }`.

    **section set-thumbnail** (`src/commands/video/section/set-thumbnail.ts`):
    1. Args: `id` (required, video ID). Flags: `--section-id` (required), `--time` (optional, seconds).
    2. `POST /photo/section/set-thumbnail` with `{ photo_id, section_id, time }`.
    3. Log success, return formatJsonOutput.
  </action>
  <verify>
    <automated>cd /Users/steffenchristensen/23/twentythree-cli && pnpm --filter twentythree-cli build && pnpm --filter twentythree-cli test --run</automated>
  </verify>
  <acceptance_criteria>
    - `ls packages/twentythree-cli/src/commands/video/section/{list,create,update,delete,set-thumbnail}.ts` — all 5 files exist
    - `grep -q "AuthenticatedCommand" packages/twentythree-cli/src/commands/video/section/list.ts` succeeds
    - `grep -q "photo/section/list" packages/twentythree-cli/src/commands/video/section/list.ts` succeeds
    - `grep -q "photo/section/create" packages/twentythree-cli/src/commands/video/section/create.ts` succeeds
    - `grep -q "photo/section/update" packages/twentythree-cli/src/commands/video/section/update.ts` succeeds
    - `grep -q "photo/section/delete" packages/twentythree-cli/src/commands/video/section/delete.ts` succeeds
    - `grep -q "confirm" packages/twentythree-cli/src/commands/video/section/delete.ts` succeeds
    - `grep -q "photo/section/set-thumbnail" packages/twentythree-cli/src/commands/video/section/set-thumbnail.ts` succeeds
    - All files contain `printWorkspaceHeader` and `formatJsonOutput`
    - Build succeeds
  </acceptance_criteria>
  <done>All 5 section subcommands work: list (table), create (required flags), update (conditional fields), delete (confirmation), set-thumbnail; all support --json and apply term-map</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI → API | Replace tokens, section mutations cross network boundary |
| User input → API body | Section title, description, start_time sent as API body fields |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-10 | I (Information Disclosure) | replace.ts | mitigate | Never log replace_token; progress bar shows only byte counts |
| T-03-11 | R (Repudiation) | section/delete.ts | mitigate | Confirmation prompt includes workspace domain and section ID |
| T-03-12 | T (Tampering) | section/update.ts | mitigate | Only send fields user explicitly provided; prevent clearing fields with undefined |
</threat_model>

<verification>
1. `pnpm --filter twentythree-cli build` — build succeeds with all 8 new commands
2. `pnpm --filter twentythree-cli test --run` — all tests pass
3. `ls packages/twentythree-cli/src/commands/video/{replace,transcoding-progress,frame}.ts` — all exist
4. `ls packages/twentythree-cli/src/commands/video/section/{list,create,update,delete,set-thumbnail}.ts` — all exist
</verification>

<success_criteria>
- video replace uses get-replace-token flow (not upload-token) with chunked upload to /photo/replace
- video transcoding-progress and video frame work correctly
- All 5 section subcommands work with proper API endpoints
- Section delete has confirmation with workspace domain
- All commands extend AuthenticatedCommand, call printWorkspaceHeader, support --json
- No legacy terms in user-visible output
</success_criteria>

<output>
After completion, create `.planning/phases/03-video-core/03-04-SUMMARY.md`
</output>
