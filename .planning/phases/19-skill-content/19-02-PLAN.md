---
phase: 19
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/twentythree-skills/skills/reference/app.md
  - packages/twentythree-skills/skills/reference/openupload.md
  - packages/twentythree-skills/skills/reference/player.md
  - packages/twentythree-skills/skills/reference/presentation.md
  - packages/twentythree-skills/skills/reference/protection.md
  - packages/twentythree-skills/skills/reference/session.md
  - packages/twentythree-skills/skills/reference/setting.md
  - packages/twentythree-skills/skills/reference/site.md
autonomous: true
requirements: [SKILL-02]
tags: [skills, documentation, markdown, reference, platform]

must_haves:
  truths:
    - "An agent can read app.md, player.md, openupload.md and discover every command with flags"
    - "An agent can read platform-mgmt refs (presentation, protection, session, setting, site) and know auth scope for each"
    - "Every file has frontmatter name matching filename stem"
    - "validate-skills.mjs does not fail because of Plan 02 files"
  artifacts:
    - path: "packages/twentythree-skills/skills/reference/app.md"
      provides: "All 6 app commands (incl. set-thumbnail, remove-thumbnail)"
      contains: "name: app"
    - path: "packages/twentythree-skills/skills/reference/openupload.md"
      provides: "All 3 openupload commands (list, upload-file, update-file)"
      contains: "name: openupload"
    - path: "packages/twentythree-skills/skills/reference/player.md"
      provides: "All 6 player commands (incl. embed as anonymous)"
      contains: "name: player"
    - path: "packages/twentythree-skills/skills/reference/presentation.md"
      provides: "presentation page + setting subtopic commands"
      contains: "name: presentation"
    - path: "packages/twentythree-skills/skills/reference/protection.md"
      provides: "All 3 protection commands (protect, unprotect, verify)"
      contains: "name: protection"
    - path: "packages/twentythree-skills/skills/reference/session.md"
      provides: "Both session commands (get-token, redeem-token)"
      contains: "name: session"
    - path: "packages/twentythree-skills/skills/reference/setting.md"
      provides: "Single setting update command"
      contains: "name: setting"
    - path: "packages/twentythree-skills/skills/reference/site.md"
      provides: "Both site commands (get, search)"
      contains: "name: site"
  key_links:
    - from: "skills/SKILL.md resource index"
      to: "skills/reference/{app,openupload,player,presentation,protection,session,setting,site}.md"
      via: "agent navigates from root skill index"
      pattern: "filename stem matches RESOURCE_GROUPS entry"
---

<objective>
Write 8 reference files for platform-management and media-integration resource groups: app, openupload, player, presentation, protection, session, setting, site. Each file follows the house style in `skills/SKILL.md` and documents every command with flag tables + 2+ examples using `--json`. Per D-03 consistency: all files at equal comprehensive depth.

Purpose: SKILL-02 subset. These 8 topics cover platform configuration (setting, site, presentation), access control (protection, session), viewer integrations (player, app), and third-party upload tokens (openupload).

Output: 8 markdown files in `packages/twentythree-skills/skills/reference/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@packages/twentythree-skills/skills/SKILL.md
@packages/twentythree-skills/scripts/validate-skills.mjs
@.planning/phases/19-skill-content/19-CONTEXT.md
@.planning/phases/19-skill-content/19-RESEARCH.md
@.planning/phases/19-skill-content/19-PATTERNS.md

<interfaces>
<!-- Document structure contract (same as Plan 01) - see 19-PATTERNS.md for full spec -->

Every file must:
- Start with `---\nname: <resource>\ndescription: <one-line>\n---` frontmatter
- Have `# TwentyThree <Resource> Commands` H1
- Have opening blockquote with 2-line summary + `--json` reminder
- Have `## Prerequisites` with auth scope + `auth credentials` reminder
- Have `## Commands` with one `### <resource> <verb>` subsection per command
- Each command subsection: auth/side-effects/output line, flag table, 2 bash examples with --json
- Have `## Common Patterns` with multi-step workflows
- Skip `## Terminology Notes` (none of these topics use legacy naming divergent from CLI)

Filenames (must match RESOURCE_GROUPS exactly):
app, openupload, player, presentation, protection, session, setting, site
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Write app.md, openupload.md, player.md (media/integration refs)</name>
  <files>
    packages/twentythree-skills/skills/reference/app.md,
    packages/twentythree-skills/skills/reference/openupload.md,
    packages/twentythree-skills/skills/reference/player.md
  </files>
  <read_first>
    packages/twentythree-skills/skills/SKILL.md,
    .planning/phases/19-skill-content/19-PATTERNS.md,
    .planning/phases/19-skill-content/19-RESEARCH.md
  </read_first>
  <action>
Create three reference files. Run these CLI calls BEFORE writing:
- `twentythree app --help` then `twentythree app <verb> --agent` for each of: list, add, update, delete, set-thumbnail, remove-thumbnail
- `twentythree openupload --help` then `--agent` for: list, upload-file, update-file
- `twentythree player --help` then `--agent` for: list, update, delete, embed, embed-versions, styles

**app.md**:
- Frontmatter: `name: app`, `description: Manage TwentyThree apps (player design integrations) including thumbnails.`
- `# TwentyThree App Commands`
- Opening blockquote with `--json` reminder
- `## Prerequisites` — auth scope: read (list), write (add, update, delete, set-thumbnail, remove-thumbnail). Include `--agent` reminder.
- `## Commands` — 6 subsections, one per command (`### app list`, `### app add`, `### app update`, `### app delete`, `### app set-thumbnail`, `### app remove-thumbnail`). Each with auth/side-effects/output line, flag table, 2 examples with `--json`.
- `## Common Patterns` — example: uploading a custom thumbnail for an app: `app set-thumbnail <app-id> ./thumb.png --json`, and the remove counterpart.
- NO Terminology Notes section.

**openupload.md**:
- Frontmatter: `name: openupload`, `description: Third-party upload tokens — allow anonymous users to upload files using a pre-issued token.`
- `# TwentyThree Open Upload Commands`
- Opening blockquote reminding `--json` + brief note that openupload is the standard chunked-upload engine used for token-authenticated third-party uploads.
- `## Prerequisites` — auth scope: read (list), write (upload-file, update-file). Clarify that `upload-file` and `update-file` also require the `--token-upload-id` and `--token` values shared by the workspace admin. Include `--agent` reminder.
- `## Commands` — 3 subsections: `### openupload list`, `### openupload upload-file`, `### openupload update-file`. Each with auth/side-effects/output, flag table (reference research doc §21 for flag data), 2 examples.
- For `### openupload upload-file`, include an inline memory rule: "Always use chunked upload for file uploads. This command handles chunking automatically — never construct multipart requests directly."
- `## Common Patterns` — the full workflow: admin creates token (out-of-band), shares `--token-upload-id` and `--token` with third party, third party runs `openupload upload-file --file-path ./video.mp4 --token-upload-id <id> --token <tok> --json` and captures the returned `upload-key`, then optionally runs `openupload update-file --token-upload-id <id> --token <tok> --upload-key <key> --title "..." --json` to set metadata.
- NO Terminology Notes section.

**player.md**:
- Frontmatter: `name: player`, `description: List and configure TwentyThree embed players, generate embed HTML, and discover available styles.`
- `# TwentyThree Player Commands`
- Opening blockquote with `--json` reminder
- `## Prerequisites` — auth scope: anonymous (embed), read (list, embed-versions, styles), write (update, delete). Include `--agent` reminder.
- `## Commands` — 6 subsections: `### player list`, `### player update`, `### player delete`, `### player embed`, `### player embed-versions`, `### player styles`. Each with auth/side-effects/output, flag table, 2 examples.
- For `### player embed`, show a redirect-to-file example: `twentythree player embed --video-id 123 --responsive > embed.html` (this one is NOT a --json example — document this as an explicit exception because embed outputs HTML).
- `## Common Patterns` — example: generate responsive embed code for a video: `player embed --video-id <id> --responsive --autoplay --json`; discover style options: `player styles --json`.
- NO Terminology Notes section.

Target length: app.md ~85-100 lines, openupload.md ~80-100 lines, player.md ~100-120 lines.
  </action>
  <verify>
    <automated>test -f packages/twentythree-skills/skills/reference/app.md && test -f packages/twentythree-skills/skills/reference/openupload.md && test -f packages/twentythree-skills/skills/reference/player.md && grep -q "^name: app$" packages/twentythree-skills/skills/reference/app.md && grep -q "^name: openupload$" packages/twentythree-skills/skills/reference/openupload.md && grep -q "^name: player$" packages/twentythree-skills/skills/reference/player.md</automated>
  </verify>
  <acceptance_criteria>
    - All 3 files exist at the exact paths listed in `<files>`
    - Each file has `^name: <stem>$` frontmatter line matching filename
    - Each file has `^description: ` frontmatter line
    - Each file has `## Prerequisites`, `## Commands`, `## Common Patterns` sections
    - app.md contains all 6 command headings: `### app list`, `### app add`, `### app update`, `### app delete`, `### app set-thumbnail`, `### app remove-thumbnail`
    - openupload.md contains 3 command headings: `### openupload list`, `### openupload upload-file`, `### openupload update-file`
    - openupload.md contains a note about chunked upload being standard (grep: `chunked upload`)
    - player.md contains all 6 command headings: `### player list`, `### player update`, `### player delete`, `### player embed`, `### player embed-versions`, `### player styles`
    - player.md contains an example redirecting embed output to a file (grep: `> embed.html` or similar)
    - app.md line count >= 80
    - openupload.md line count >= 75
    - player.md line count >= 95
    - No file contains `npx`
  </acceptance_criteria>
  <done>app.md, openupload.md, player.md exist with all commands documented and style notes. openupload contains chunked-upload memory rule. player shows explicit non-JSON embed redirect example.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Write presentation.md, protection.md, session.md (access/config refs)</name>
  <files>
    packages/twentythree-skills/skills/reference/presentation.md,
    packages/twentythree-skills/skills/reference/protection.md,
    packages/twentythree-skills/skills/reference/session.md
  </files>
  <read_first>
    packages/twentythree-skills/skills/SKILL.md,
    packages/twentythree-skills/skills/reference/app.md,
    .planning/phases/19-skill-content/19-PATTERNS.md,
    .planning/phases/19-skill-content/19-RESEARCH.md
  </read_first>
  <action>
Run these CLI calls BEFORE writing:
- `twentythree presentation --help`, `twentythree presentation page --help`, `twentythree presentation page link-locations --agent`, `twentythree presentation setting --help`, `twentythree presentation setting list --agent`, `twentythree presentation setting update --agent`
- `twentythree protection --help` then `--agent` for: protect, unprotect, verify
- `twentythree session --help` then `--agent` for: get-token, redeem-token

**presentation.md**:
- Frontmatter: `name: presentation`, `description: Workspace-level presentation and embed settings (link locations and setting CRUD).`
- `# TwentyThree Presentation Commands`
- Opening blockquote with `--json` reminder. Clarify: "Presentation commands manage workspace-level configuration. There is no CRUD for presentation objects."
- `## Prerequisites` — auth scope: read (page link-locations, setting list), write (setting update). Include `--agent` reminder.
- `## Commands` section organised as two subtopics:
  - `### presentation page link-locations` (read, no flags)
  - `### presentation setting list` (read, no flags)
  - `### presentation setting update` (write; `--set key=value` repeatable flag)
- Each with auth/side-effects/output, flag table, 2 examples with `--json`.
- `## Common Patterns` — example: discover available link-locations before authoring content: `presentation page link-locations --json`; update multiple settings at once: `presentation setting update --set keyA=valA --set keyB=valB --json`.
- NO Terminology Notes section.

**protection.md**:
- Frontmatter: `name: protection`, `description: Apply and verify access protection (password/SSO/token) on videos and webinars.`
- `# TwentyThree Protection Commands`
- Opening blockquote with `--json` reminder
- `## Prerequisites` — auth scope: write (protect, unprotect), read (verify). Include `--agent` reminder. Note valid `--protection-method` values: `password`, `sso`, `token`.
- `## Commands` — 3 subsections: `### protection protect`, `### protection unprotect`, `### protection verify`. Each with auth/side-effects/output, flag table, 2 examples with `--json`.
- `## Common Patterns` — example: protecting a webinar with a password for 30 min grace period: `protection protect --protection-method password --object-id <webinar-id> --grace-minutes 30 --json`; verifying a viewer's access before serving an embed: `protection verify --protection-method token --video-id <id> --verification-data <payload> --json`.
- NO Terminology Notes section.

**session.md**:
- Frontmatter: `name: session`, `description: Generate and redeem short-lived viewer session tokens for SSO-style access.`
- `# TwentyThree Session Commands`
- Opening blockquote with `--json` reminder + note: "Session tokens are for viewer SSO. They are distinct from CLI auth (`twentythree auth credentials`)."
- `## Prerequisites` — auth scope: read (both). Include `--agent` reminder.
- `## Commands` — 2 subsections: `### session get-token`, `### session redeem-token`. Each with auth/side-effects/output, flag table, 2 examples with `--json`.
- `## Common Patterns` — describe the full SSO viewer flow: server-side `session get-token --email viewer@example.com --full-name "Jane Doe" --return-url https://... --json` → pass returned token to viewer → viewer `session redeem-token --session-token <tok> --json`.
- NO Terminology Notes section.

Target length: presentation.md ~70-90 lines, protection.md ~80-100 lines, session.md ~70-90 lines.
  </action>
  <verify>
    <automated>test -f packages/twentythree-skills/skills/reference/presentation.md && test -f packages/twentythree-skills/skills/reference/protection.md && test -f packages/twentythree-skills/skills/reference/session.md && grep -q "^name: presentation$" packages/twentythree-skills/skills/reference/presentation.md && grep -q "^name: protection$" packages/twentythree-skills/skills/reference/protection.md && grep -q "^name: session$" packages/twentythree-skills/skills/reference/session.md</automated>
  </verify>
  <acceptance_criteria>
    - All 3 files exist at the exact paths listed in `<files>`
    - Each file has `^name: <stem>$` frontmatter line matching filename
    - Each file has `## Prerequisites`, `## Commands`, `## Common Patterns` sections
    - presentation.md contains 3 command headings: `### presentation page link-locations`, `### presentation setting list`, `### presentation setting update`
    - presentation.md explicitly notes NO CRUD for presentation objects (grep: "no CRUD" or "workspace-level")
    - protection.md contains 3 command headings: `### protection protect`, `### protection unprotect`, `### protection verify`
    - protection.md mentions all 3 protection methods: `password`, `sso`, `token` (grep each)
    - session.md contains 2 command headings: `### session get-token`, `### session redeem-token`
    - session.md distinguishes session tokens from CLI auth (grep: `auth credentials`)
    - presentation.md line count >= 65
    - protection.md line count >= 75
    - session.md line count >= 65
    - No file contains `npx`
  </acceptance_criteria>
  <done>presentation.md, protection.md, session.md exist. Access-control topics clearly scoped. Session flow documented end-to-end.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Write setting.md, site.md (config refs — completes Plan 02 set)</name>
  <files>
    packages/twentythree-skills/skills/reference/setting.md,
    packages/twentythree-skills/skills/reference/site.md
  </files>
  <read_first>
    packages/twentythree-skills/skills/SKILL.md,
    packages/twentythree-skills/skills/reference/presentation.md,
    .planning/phases/19-skill-content/19-PATTERNS.md,
    .planning/phases/19-skill-content/19-RESEARCH.md
  </read_first>
  <action>
Run these CLI calls BEFORE writing:
- `twentythree setting --help` then `twentythree setting update --agent`
- `twentythree site --help` then `--agent` for: get, search

**setting.md** (only 1 command — but full D-03 depth still applies):
- Frontmatter: `name: setting`, `description: Update workspace settings via key=value pairs with optional dry-run validation.`
- `# TwentyThree Setting Commands`
- Opening blockquote with `--json` reminder. Add: "Setting keys are workspace-specific. Check the workspace admin UI for the list of valid keys."
- `## Prerequisites` — auth scope: write. Include `--agent` reminder.
- `## Commands` — 1 subsection: `### setting update`. Document the `--set` repeatable flag and the `--validate-only` dry-run flag. Include a flag table, 3+ examples (basic single --set, multi --set, --validate-only).
- `## Common Patterns` — include examples of:
  - Dry-run a setting change: `setting update --set branding.color=#ff0000 --validate-only --json`
  - Apply multiple settings atomically: `setting update --set key1=val1 --set key2=val2 --json`
  - Discuss the pattern of running `--validate-only` first, checking output, then re-running without the flag.
- Because this topic has only 1 command, the Common Patterns section must carry more weight — aim for 5+ realistic examples.
- NO Terminology Notes section.

**site.md**:
- Frontmatter: `name: site`, `description: Query workspace-level information (domain, quotas) and run cross-content search.`
- `# TwentyThree Site Commands`
- Opening blockquote with `--json` reminder
- `## Prerequisites` — auth scope: read (both). Include `--agent` reminder.
- `## Commands` — 2 subsections: `### site get`, `### site search`. Each with auth/side-effects/output, flag table, 2 examples with `--json`.
- For `### site get`, show basic: `site get --json` and richer: `site get --include-presentation --include-quota --json`.
- For `### site search`, show basic text search: `site search --search "product demo" --json` and scoped: `site search --search "q2" --search-in title --search-in tags --size 20 --json`.
- `## Common Patterns` — workspace health check: `site get --include-quota --json`; cross-content search that returns videos + webinars + categories in one call: `site search --search <term> --json`.
- NO Terminology Notes section.

Target length: setting.md ~80-95 lines (thanks to expanded Common Patterns), site.md ~80-100 lines.

At end of Task 3, run `pnpm --filter twentythree-skills test` to confirm the 8 files written by Plan 02 no longer appear in validator's "Missing reference file" errors.
  </action>
  <verify>
    <automated>test -f packages/twentythree-skills/skills/reference/setting.md && test -f packages/twentythree-skills/skills/reference/site.md && grep -q "^name: setting$" packages/twentythree-skills/skills/reference/setting.md && grep -q "^name: site$" packages/twentythree-skills/skills/reference/site.md && grep -q "### setting update" packages/twentythree-skills/skills/reference/setting.md && grep -q "### site get" packages/twentythree-skills/skills/reference/site.md && grep -q "### site search" packages/twentythree-skills/skills/reference/site.md</automated>
  </verify>
  <acceptance_criteria>
    - `packages/twentythree-skills/skills/reference/setting.md` exists with `^name: setting$` frontmatter
    - `packages/twentythree-skills/skills/reference/site.md` exists with `^name: site$` frontmatter
    - setting.md contains `### setting update` heading
    - setting.md documents both `--set` flag AND `--validate-only` flag (grep each)
    - setting.md has at least 5 fenced bash blocks (count: `grep -c '```bash' packages/twentythree-skills/skills/reference/setting.md` >= 5) to compensate for only 1 command
    - site.md contains 2 command headings: `### site get`, `### site search`
    - site.md documents `--include-presentation`, `--include-quota` for site get (grep each)
    - setting.md line count >= 75
    - site.md line count >= 75
    - No file contains `npx`
    - `pnpm --filter twentythree-skills test` does not error about missing app.md, openupload.md, player.md, presentation.md, protection.md, session.md, setting.md, or site.md (will still error about the 14 unwritten files — that's expected)
  </acceptance_criteria>
  <done>setting.md and site.md exist. Plan 02 complete: 8 reference files for platform/integration/config topics written.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Markdown content phase — no runtime, no trust boundaries crossed.

| Boundary | Description |
|----------|-------------|
| (none) | Static markdown; no execution path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-02-01 | Information Disclosure | Examples referencing workspace-specific settings or tokens | mitigate | All examples use placeholders (`<webinar-id>`, `<tok>`, `<id>`); NO real tokens, domains, or secrets allowed. Grep for obvious leaks before commit: `grep -E "(twentythree\.com\|Bearer [A-Za-z0-9])" packages/twentythree-skills/skills/reference/*.md` must return no matches. |
| T-19-02-02 | Information Disclosure | openupload.md describing token sharing | mitigate | File mentions that the workspace admin shares `--token-upload-id` and `--token` out-of-band. Explicitly does NOT include an example with a real token value. |
</threat_model>

<verification>
After all 3 tasks:

```bash
pnpm --filter twentythree-skills test
```

Expected: errors listing 14 remaining missing files; NONE of the 8 Plan 02 files in the error list.

```bash
wc -l packages/twentythree-skills/skills/reference/{app,openupload,player,presentation,protection,session,setting,site}.md
grep -l "npx" packages/twentythree-skills/skills/reference/*.md  # empty
```
</verification>

<success_criteria>
- All 8 reference files created at exact filenames: app.md, openupload.md, player.md, presentation.md, protection.md, session.md, setting.md, site.md
- Every file has `name:` frontmatter matching filename stem + `description:` line
- Every file has ## Prerequisites, ## Commands, ## Common Patterns
- Every command in each topic documented as `### <resource> <verb>` with auth/side-effects line, flag table, 2+ examples (player embed being the documented exception for HTML output)
- openupload.md contains chunked-upload-is-standard memory rule
- setting.md compensates for its 1-command topic with 5+ examples in Common Patterns
- No file contains `npx`
- `pnpm --filter twentythree-skills test` does not report missing-file errors for any Plan 02 file
</success_criteria>

<output>
After completion, create `.planning/phases/19-skill-content/19-02-SUMMARY.md` following the summary template.
</output>
