---
phase: 19
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/twentythree-skills/skills/reference/video.md
autonomous: true
requirements: [SKILL-02]
tags: [skills, documentation, markdown, reference, video, subtitles]

must_haves:
  truths:
    - "An agent reading video.md can upload a file, list videos, update metadata, replace, delete, and run any video section or video subtitle command"
    - "An agent understands CLI `video` maps to API `photo` (terminology note)"
    - "An agent understands chunked upload is automatic and never constructs multipart requests directly"
    - "validate-skills.mjs does not fail because of video.md"
  artifacts:
    - path: "packages/twentythree-skills/skills/reference/video.md"
      provides: "8 top-level video commands + video section (6) + video subtitle (11) subtopics. Terminology Notes required."
      contains: "name: video"
      min_lines: 200
  key_links:
    - from: "workflows/upload-and-publish.md (Plan 06)"
      to: "reference/video.md"
      via: "workflow references video upload, video update, video transcoding-progress, video frame"
      pattern: "upload-and-publish copies command invocations verbatim from video.md"
    - from: "reference/video.md"
      to: "chunked-upload memory rule"
      via: "inline blockquote"
      pattern: "`--chunk-size` and `--concurrency` are chunked-upload tunables; never use direct multipart"
---

<objective>
Write video.md — the single most important reference file. Covers 8 top-level commands (upload, list, get, update, delete, replace, frame, transcoding-progress) plus `video section` subtopic (6 commands) plus `video subtitle` subtopic (11 commands) for a total of 25 commands. Includes the mandatory Terminology Notes section (CLI video to API photo) and the chunked-upload memory rule.

Purpose: SKILL-02 requires 22 reference files; video.md is the highest-traffic one. Per D-03 consistency, it gets the same quality depth as any other file — which given its surface area means ~200+ lines.

Output: 1 markdown file: `packages/twentythree-skills/skills/reference/video.md`.

Special handling:
- A1 gap: `video section update` and `video section set-thumbnail` flags NOT in research doc — run `--agent` before writing
- A2 gap: `video subtitle create/upload/update/delete/list/etc` full flags NOT in research doc — run `--agent` for EVERY subtitle command
- REQUIRED: Terminology Notes section (CLI `video` to API `photo`)
- REQUIRED: chunked-upload memory rule inline
- REQUIRED: admin-URL output note (user's memory: "Print ID + admin URL after upload/replace")
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@packages/twentythree-skills/skills/SKILL.md
@packages/twentythree-skills/scripts/validate-skills.mjs
@.planning/phases/19-skill-content/19-CONTEXT.md
@.planning/phases/19-skill-content/19-RESEARCH.md
@.planning/phases/19-skill-content/19-PATTERNS.md

<interfaces>
Standard document structure (see 19-PATTERNS.md).

Terminology Notes pattern (REQUIRED for video.md):
```
## Terminology Notes

CLI `video` = API `photo`. The `api_endpoint` field in `--agent` output uses the API name (e.g. `POST /photo/redeem-upload-token`, `GET /photo/list`).
When reading the raw OpenAPI spec or debugging API responses, `photo` refers to a video asset.
```

Chunked-upload rule (REQUIRED inline under `### video upload`):
```
> **Chunked upload is automatic.** `twentythree video upload <file>` handles chunking internally.
> `--chunk-size` (default 5MB) and `--concurrency` (default 5) are tunables — never construct multipart requests directly.
```

Admin-URL output note (REQUIRED inline under `### video upload` and `### video replace`):
After upload/replace, the CLI prints the resource ID and its admin URL. Capture `data.id` and `data.admin_url` from the `--json` response.

Gap notices (from 19-RESEARCH.md Assumptions Log):
- A1: `video section update` and `video section set-thumbnail` — run `--agent` for both before writing
- A2: `video subtitle <cmd>` for create/upload/update/delete and all others except list — run `--agent` for every subtitle command (11 total)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Gather full flag data and write video.md top-level (8 commands) + Terminology Notes</name>
  <files>
    packages/twentythree-skills/skills/reference/video.md
  </files>
  <read_first>
    packages/twentythree-skills/skills/SKILL.md,
    .planning/phases/19-skill-content/19-PATTERNS.md,
    .planning/phases/19-skill-content/19-RESEARCH.md
  </read_first>
  <action>
Run these CLI calls BEFORE writing:
- `twentythree video --help`
- `twentythree video <verb> --agent` for each of: upload, list, get, update, delete, replace, frame, transcoding-progress
- `twentythree video section --help` and `--agent` for each of: list, create, update, delete, generate, set-thumbnail (A1 gap applies to update and set-thumbnail)
- `twentythree video subtitle --help` then `--agent` for every command it lists (A2 gap: 11 commands — run `--agent` for each)

Create `packages/twentythree-skills/skills/reference/video.md` in ONE pass (this task writes the full file — do not split across tasks). Full structure:

**Frontmatter**:
```
---
name: video
description: Manage video assets — upload, list, metadata updates, thumbnails, transcoding status, sections (chapters), and subtitles.
---
```

**Opening**:
- H1: `# TwentyThree Video Commands`
- Opening blockquote:
  > Video is the primary asset type in TwentyThree. Every example uses `--json` for machine-readable output.
  > CLI `video` maps to API `photo` — see Terminology Notes at the bottom of this file.

**Prerequisites**:
- Auth scope varies: read (list, get, transcoding-progress), write (upload, update, delete, replace, frame, all section writes, all subtitle writes). Include `--agent` reminder.

**Commands** (`## Commands`) — 8 top-level subsections. For each: auth/side-effects/output line, flag table, 2 examples with `--json`.
1. `### video upload <file>`
   - auth: write; side effects: creates; output: key-value (id + admin_url)
   - Flag table: `--title`, `--description`, `--tags`, `--category-id`, `--publish` (bool), `--chunk-size` (default 5MB), `--concurrency` (default 5)
   - Inline BEFORE first example: chunked-upload blockquote
   - Inline AFTER flag table: admin-URL output note (print id and admin_url)
   - Examples: basic `twentythree video upload ./video.mp4 --title "Demo" --json`; realistic with category `twentythree video upload ./video.mp4 --title "Q2 Keynote" --category-id <cat-id> --tags "product q2" --publish --json`
2. `### video list` — auth: read; flags `--limit`, `--include-unpublished`; 2 examples with --json
3. `### video get <id>` — auth: read; no flags; 2 examples
4. `### video update <id>` — auth: write; flags `--title`, `--description`, `--tags`, `--category-id`, `--publish` (bool), `--promote` (bool), `--publish-date` (ISO 8601), `--360` (bool); 2 examples
5. `### video delete <id>` — auth: write (destructive); no flags; 2 examples with explicit warning blockquote about destructive action
6. `### video replace <id> <file>` — auth: write; flags `--chunk-size`, `--concurrency`; 2 examples; include admin-URL output note
7. `### video frame <id>` — auth: write; flag `--time` (seconds); 2 examples
8. `### video transcoding-progress <id>` — auth: read; no flags; 2 examples

After all 8 top-level commands, include `## Subtopic: video section` placeholder heading — Task 2 will fill it in.

DO NOT add Terminology Notes or Common Patterns yet — those come after subtopics (Task 3).

Target line count for this task's write: ~140-180 lines (file will grow in Tasks 2-3).
  </action>
  <verify>
    <automated>test -f packages/twentythree-skills/skills/reference/video.md && grep -q "^name: video$" packages/twentythree-skills/skills/reference/video.md && grep -q "### video upload" packages/twentythree-skills/skills/reference/video.md && grep -q "### video replace" packages/twentythree-skills/skills/reference/video.md && grep -q "### video transcoding-progress" packages/twentythree-skills/skills/reference/video.md && grep -q "chunked upload" packages/twentythree-skills/skills/reference/video.md</automated>
  </verify>
  <acceptance_criteria>
    - `packages/twentythree-skills/skills/reference/video.md` exists with `^name: video$` frontmatter
    - Contains `## Prerequisites` and `## Commands` sections
    - Contains all 8 top-level command headings: `### video upload`, `### video list`, `### video get`, `### video update`, `### video delete`, `### video replace`, `### video frame`, `### video transcoding-progress`
    - Contains the chunked-upload blockquote mentioning both "Chunked upload is automatic" and "`--chunk-size`" (grep both strings present)
    - `### video upload` subsection mentions printing `admin_url` (grep: `admin_url`)
    - `### video replace` subsection mentions printing `admin_url`
    - `### video delete` subsection contains a destructive-action warning (grep: `destructive` OR `cannot be undone` OR similar clear wording)
    - `### video update` flag table documents at least: `--title`, `--description`, `--tags`, `--category-id`, `--publish`, `--publish-date`
    - Every `### video <cmd>` subsection contains at least one fenced bash block with `--json`
    - File line count at end of Task 1 >= 130
    - File does not contain `npx`
  </acceptance_criteria>
  <done>video.md exists with all 8 top-level command subsections documented, chunked-upload memory rule inline, admin-URL output note on upload/replace, destructive warning on delete. Ready for subtopics in Task 2.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Append `video section` and `video subtitle` subtopics (close A1 + A2 gaps)</name>
  <files>
    packages/twentythree-skills/skills/reference/video.md
  </files>
  <read_first>
    packages/twentythree-skills/skills/reference/video.md,
    packages/twentythree-skills/skills/SKILL.md,
    .planning/phases/19-skill-content/19-PATTERNS.md,
    .planning/phases/19-skill-content/19-RESEARCH.md
  </read_first>
  <action>
Append two full subtopic sections to `packages/twentythree-skills/skills/reference/video.md`. A1 and A2 gaps MUST be closed by running `--agent` for every command before writing.

Required CLI calls BEFORE writing:
- `twentythree video section --agent` and `twentythree video section <cmd> --agent` for each of: list, create, update (A1), delete, generate, set-thumbnail (A1) — 6 commands total
- `twentythree video subtitle --help` to enumerate the full subtitle command list (research doc counts 11 commands), then `twentythree video subtitle <cmd> --agent` for each of those 11 commands (A2 — close the gap for every one)

Replace the `## Subtopic: video section` placeholder written in Task 1 with the full subtopic content:

**`## Subtopic: video section`** (6 commands: list, create, update, delete, generate, set-thumbnail)
- Opening paragraph: "Sections are chapter markers on a video. Start-time is expressed in seconds from the beginning of the video."
- Subsection per command, each as `### video section <cmd>` with auth/side-effects/output line, flag table, 2 examples with `--json`:
  1. `### video section list <id>` — read; no flags
  2. `### video section create <id>` — write; flags `--title` (required), `--start-time` (required, seconds), `--description`
  3. `### video section update <id> <section-id>` — write; flags from A1 `--agent`
  4. `### video section delete <id> <section-id>` — write; no flags
  5. `### video section generate <id>` — write; no flags (AI-generated from transcript). Include note explaining this.
  6. `### video section set-thumbnail <id> <section-id>` — write; flags from A1 `--agent`

**`## Subtopic: video subtitle`** (11 commands — full A2 closure)
- Opening paragraph: "Subtitle commands manage caption tracks on a video (SRT/WebVTT formats). Use `--agent` to inspect available locales and types for any command."
- One `### video subtitle <cmd>` subsection PER command in the `--help` output. For each command: auth/side-effects/output line, flag table populated from `--agent`, 2 examples with `--json`.
- Start with `### video subtitle list <id>` (read; flag `--include-drafts`), then proceed through the remaining 10 commands in the order they appear in `--help`.
- For upload commands that accept SRT/WebVTT files, show an example upload with a file path argument.
- For locale-bound commands, document the locale flag and show at least one example using a specific locale (e.g. `en-US`).

After both subtopics are appended, DO NOT YET add Common Patterns or Terminology Notes — Task 3 adds those.

Target line count for this task's append: +120-180 lines (so running total ~260-360 lines after Task 2).
  </action>
  <verify>
    <automated>test -f packages/twentythree-skills/skills/reference/video.md && grep -q "## Subtopic: video section" packages/twentythree-skills/skills/reference/video.md && grep -q "## Subtopic: video subtitle" packages/twentythree-skills/skills/reference/video.md && grep -q "### video section list" packages/twentythree-skills/skills/reference/video.md && grep -q "### video section create" packages/twentythree-skills/skills/reference/video.md && grep -q "### video section update" packages/twentythree-skills/skills/reference/video.md && grep -q "### video section set-thumbnail" packages/twentythree-skills/skills/reference/video.md && grep -q "### video section generate" packages/twentythree-skills/skills/reference/video.md && grep -q "### video subtitle list" packages/twentythree-skills/skills/reference/video.md</automated>
  </verify>
  <acceptance_criteria>
    - File contains `## Subtopic: video section` heading
    - File contains all 6 `### video section <cmd>` headings: list, create, update, delete, generate, set-thumbnail
    - A1 gap closed: `### video section update` subsection AND `### video section set-thumbnail` subsection each contain a flag table OR explicit "(no flags)" line populated from live `--agent` (the subsection must NOT say "see --agent" — the flags themselves must be in the file)
    - File contains `## Subtopic: video subtitle` heading
    - File contains at least 11 `### video subtitle <cmd>` headings — count: `grep -c "^### video subtitle" packages/twentythree-skills/skills/reference/video.md` >= 11
    - A2 gap closed: every `### video subtitle <cmd>` subsection has a flag table (or explicit "(no flags)" line) populated from live `--agent` — no subsection delegates to "run --agent for flags"
    - video.md line count >= 260 (running total at end of Task 2)
    - File does not contain `npx`
  </acceptance_criteria>
  <done>Both video section (6 cmds) and video subtitle (11 cmds) subtopics written in full, with all flags sourced from live --agent (A1 and A2 gaps closed). Ready for Task 3.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Add Common Patterns + Terminology Notes; finalise video.md</name>
  <files>
    packages/twentythree-skills/skills/reference/video.md
  </files>
  <read_first>
    packages/twentythree-skills/skills/reference/video.md,
    packages/twentythree-skills/skills/SKILL.md,
    .planning/phases/19-skill-content/19-PATTERNS.md
  </read_first>
  <action>
Append the final two sections to video.md:

**`## Common Patterns`** — include at least 5 realistic multi-step examples with `--json`:
1. Upload + metadata + publish (mini-workflow): `twentythree video upload ./demo.mp4 --title "Q2 Demo" --json` capture id then `twentythree video update <id> --tags "demo q2" --category-id <cat-id> --publish --json`
2. Poll for transcoding completion: loop description plus `twentythree video transcoding-progress <id> --json`
3. Extract a thumbnail frame: `twentythree video frame <id> --time 15 --json`
4. Create a chapter marker: `twentythree video section create <id> --title "Intro" --start-time 0 --json` then `twentythree video section create <id> --title "Demo" --start-time 30 --json`
5. Generate AI chapters from transcript: `twentythree video section generate <id> --json` (explain prerequisite: transcript must exist)
6. Upload an SRT subtitle track: `twentythree video subtitle upload <id> <file.srt> --locale en-US --json` (or whatever flag names `--agent` returned)

**`## Terminology Notes`** (REQUIRED):
```
## Terminology Notes

CLI `video` = API `photo`. The `api_endpoint` field in `--agent` output uses the API name:

- `twentythree video upload` -> `POST /photo/redeem-upload-token`
- `twentythree video list` -> `GET /photo/list`
- `twentythree video update` -> `PATCH /photo/<id>`

When reading the raw OpenAPI spec or debugging API responses, `photo` refers to a video asset.
When commenting on a video via the `comment` topic, pass `--object-type photo`.
```

After writing, run `pnpm --filter twentythree-skills test` and confirm video.md is no longer in the validator's missing-file errors.

Final line count target: >=200 lines (research doc notes "Webinar.md may need 150+ lines to be genuinely comprehensive"; video.md with 8 top-level + 6 section + 11 subtitle = 25 commands will naturally reach 250-350 lines).
  </action>
  <verify>
    <automated>test -f packages/twentythree-skills/skills/reference/video.md && grep -q "## Common Patterns" packages/twentythree-skills/skills/reference/video.md && grep -q "## Terminology Notes" packages/twentythree-skills/skills/reference/video.md && grep -q "photo" packages/twentythree-skills/skills/reference/video.md && test "$(wc -l < packages/twentythree-skills/skills/reference/video.md)" -ge 200</automated>
  </verify>
  <acceptance_criteria>
    - File contains `## Common Patterns` section with at least 5 fenced bash blocks (grep count: `grep -c '```bash' packages/twentythree-skills/skills/reference/video.md` increased by at least 5 in Common Patterns)
    - File contains `## Terminology Notes` section
    - Terminology Notes section mentions `photo` (the API legacy name) — grep: `photo`
    - Terminology Notes section mentions at least one example API endpoint path `/photo/` — grep: `/photo/`
    - video.md final line count >= 200
    - File does not contain `npx`
    - `pnpm --filter twentythree-skills test` does not list video.md in "Missing reference file" errors
    - File structure (in order): frontmatter -> H1 -> opening blockquote -> Prerequisites -> Commands -> Subtopic: video section -> Subtopic: video subtitle -> Common Patterns -> Terminology Notes
  </acceptance_criteria>
  <done>video.md is complete: 25 commands documented, 5+ multi-step common patterns, Terminology Notes linking video to photo. Plan 04 finished.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Markdown content phase — no runtime trust boundaries.

| Boundary | Description |
|----------|-------------|
| (none) | Static markdown |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-04-01 | Tampering | Examples instructing agents to overwrite video metadata | accept | Examples use placeholders (`<id>`, `<cat-id>`); the API enforces auth scope. Doc cannot grant privileges it does not have at runtime. |
| T-19-04-02 | Information Disclosure | Admin URL example output shown in doc | accept | Example shows placeholder `admin_url`, not a real URL. Admin URLs require a valid auth token to access anyway. |
| T-19-04-03 | Denial of Service | Agent misuses `video delete` because doc does not flag destructiveness | mitigate | `### video delete` subsection contains explicit destructive-action warning (verified in Task 1 acceptance criteria). |
</threat_model>

<verification>
After all 3 tasks:

```bash
pnpm --filter twentythree-skills test
```

Expected at end of Plan 04: validator errors only for files owned by Plans 05 (webinar.md) and any earlier plans still in progress. video.md MUST NOT appear in errors.

Spot-check:
```bash
wc -l packages/twentythree-skills/skills/reference/video.md   # >=200
grep -c "^### video" packages/twentythree-skills/skills/reference/video.md  # >=8 top-level (section/subtitle use ### video section <cmd>)
grep -c "^### video section" packages/twentythree-skills/skills/reference/video.md  # 6
grep -c "^### video subtitle" packages/twentythree-skills/skills/reference/video.md # >=11
grep -q "chunked upload" packages/twentythree-skills/skills/reference/video.md  # memory rule present
grep -q "admin_url" packages/twentythree-skills/skills/reference/video.md       # admin-URL output note present
grep -l "npx" packages/twentythree-skills/skills/reference/video.md  # empty
```
</verification>

<success_criteria>
- video.md created at exact path
- Frontmatter has `name: video` matching filename stem
- 8 top-level commands + 6 video section commands + 11 video subtitle commands = 25 `### video <...>` subsections documented
- A1 gap closed: `video section update` and `video section set-thumbnail` flags sourced from live `--agent`
- A2 gap closed: every `video subtitle <cmd>` flag table sourced from live `--agent` (not "see --agent for flags")
- Terminology Notes section explains `video` -> `photo` API mapping with concrete endpoint examples
- Chunked-upload memory rule present
- Admin-URL output note present on upload and replace
- Destructive warning on delete
- Final file line count >= 200
- No occurrence of `npx`
</success_criteria>

<output>
After completion, create `.planning/phases/19-skill-content/19-04-SUMMARY.md` following the summary template.
</output>
