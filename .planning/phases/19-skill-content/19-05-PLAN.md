---
phase: 19
plan: 05
type: execute
wave: 2
depends_on: [19-04]
files_modified:
  - packages/twentythree-skills/skills/reference/webinar.md
  - packages/twentythree-skills/skills/reference/webhook.md
autonomous: true
requirements: [SKILL-02]
tags: [skills, documentation, markdown, reference, webinar, webhook]

must_haves:
  truths:
    - "An agent reading webinar.md can create a webinar, configure it, manage speakers, run recordings, attach mail, upload attachments, section chapters, and use queued video / transcription / room features"
    - "An agent understands CLI `webinar` maps to API `live` (terminology note) — all api_endpoint fields in --agent output use /live/*"
    - "An agent reading webinar.md is warned that `webinar get` does NOT exist and retrieves details via `webinar list`"
    - "An agent reading webhook.md can list, subscribe, unsubscribe, and sample webhook events"
    - "An agent knows the canonical webhook subscribe flow: `webhook events` → pick event → `webhook sample <event>` → `webhook subscribe --event <event> --target-url <url>`"
    - "After Plan 05 completes, all 22 reference files exist and validate-skills.mjs Gate 2 exits 0"
  artifacts:
    - path: "packages/twentythree-skills/skills/reference/webinar.md"
      provides: "11 top-level webinar commands + 9 subtopics (speaker, series, mail, recording, room, section, attachment, queued-video, transcription) totalling 40+ commands. Terminology Notes required."
      contains: "name: webinar"
      min_lines: 250
    - path: "packages/twentythree-skills/skills/reference/webhook.md"
      provides: "5 webhook commands (list, subscribe, unsubscribe, events, sample) with the discover-then-subscribe flow"
      contains: "name: webhook"
      min_lines: 90
  key_links:
    - from: "workflows/webinar-lifecycle.md (Plan 06)"
      to: "reference/webinar.md"
      via: "workflow copies webinar create / section / speaker / upload-image / update --publish / room connect / recording start-stop invocations verbatim from webinar.md"
      pattern: "webinar-lifecycle references webinar.md command signatures"
    - from: "reference/webinar.md"
      to: "API `/live/*` endpoints"
      via: "Terminology Notes section maps CLI webinar → API live"
      pattern: "CLI `webinar` = API `live`"
    - from: "reference/webhook.md"
      to: "webhook event registry"
      via: "Common Patterns shows `webhook events` → `webhook sample <event>` → `webhook subscribe --event <event>`"
      pattern: "discover-then-subscribe"
---

<objective>
Write 2 reference files:
1. **webinar.md** — the largest and most complex reference file in Phase 19. 11 top-level commands plus 9 subtopics (speaker, series, mail, recording, room, section, attachment, queued-video, transcription) — 40+ commands total. A5 gap applies: the implementing agent must run `twentythree webinar <subtopic> <cmd> --agent` for every command in every subtopic before writing. Research explicitly flags this file will reach 250+ lines; D-01 depth target is a floor, not a cap.
2. **webhook.md** — 5 commands (list, subscribe, unsubscribe, events, sample) with emphasis on the discover-then-subscribe flow and event-type handling.

Purpose: SKILL-02 requires 22 hand-authored reference files. Plans 01-04 covered 20 of them; this plan finishes the reference set (adds webinar + webhook). After this plan, `pnpm --filter twentythree-skills test` must exit 0 (Gate 2 satisfied).

Output: 2 markdown files in `packages/twentythree-skills/skills/reference/`.

Special handling:
- A5 gap: `webinar speaker`, `webinar mail`, `webinar attachment`, `webinar series`, `webinar section`, `webinar queued-video`, `webinar transcription`, `webinar room` — flags NOT documented in research doc; MUST be sourced from live `--agent` output for every command in every subtopic
- REQUIRED: Terminology Notes section (CLI `webinar` = API `live`) — matches video.md pattern established in Plan 04
- REQUIRED: inline warning that `webinar get` does NOT exist (Pitfall 5 in research doc) — use `webinar list` with filters instead
- REQUIRED: admin-URL output note on `webinar create` and `webinar repeat` (both return `{ id, admin_url }` per research doc §3)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@packages/twentythree-skills/skills/SKILL.md
@packages/twentythree-skills/scripts/validate-skills.mjs
@packages/twentythree-skills/skills/reference/video.md
@.planning/phases/19-skill-content/19-CONTEXT.md
@.planning/phases/19-skill-content/19-RESEARCH.md
@.planning/phases/19-skill-content/19-PATTERNS.md

<interfaces>
<!-- Standard document structure (from 19-PATTERNS.md) — same as Plans 01-04. -->

<!-- Terminology Notes pattern REQUIRED for webinar.md (matches video.md pattern established in Plan 04): -->
```
## Terminology Notes

CLI `webinar` = API `live`. The `api_endpoint` field in `--agent` output uses the API name:

- `twentythree webinar create` → `POST /live/create` (or similar)
- `twentythree webinar list` → `GET /live/list`
- `twentythree webinar recording start` → `POST /live/<id>/recording/start`

When reading the raw OpenAPI spec, `live` refers to a webinar. The `live session` endpoint family (`/live/webinar/*`) backs the `webinar session` concept in research docs.
When commenting on a webinar via the `comment` topic, pass `--object-type live`.
```

<!-- "No webinar get" callout REQUIRED (matches Pitfall 5 in research doc): -->
```
> **There is no `webinar get` command.** To retrieve details for a specific webinar,
> use `twentythree webinar list --search "<title>" --json` or filter by status/ID client-side.
> The API does not expose a single-record GET; list + filter is the canonical pattern.
```

<!-- Admin-URL output note REQUIRED on `webinar create` and `webinar repeat`:
After create/repeat, the CLI prints the resource id and its admin URL. Capture `data.id` and `data.admin_url` from the --json response. -->

<!-- A5 gap closure: each subtopic MUST have every command documented with a flag table
populated from live `--agent` output. Subsections must NOT delegate to "run --agent for flags".
The 9 subtopics and their expected commands per research §3:
- speaker (14 cmds)
- series (11 cmds)
- mail (7 cmds)
- recording (4 cmds: start, stop, status, split)
- room (4 cmds, incl. connect)
- section (4 cmds)
- attachment (4 cmds)
- queued-video (2 cmds)
- transcription (4 cmds)
The exact command list per subtopic comes from `twentythree webinar <subtopic> --help` at write time.
-->

<!-- webhook.md canonical flow (REQUIRED in Common Patterns): -->
```
1. Discover available events
   twentythree webhook events --json

2. Inspect a sample payload for one event
   twentythree webhook sample <event> --json

3. Subscribe
   twentythree webhook subscribe --event <event> --target-url https://example.com/hook --json
```

<!-- Validator RESOURCE_GROUPS (authoritative filenames): webinar, webhook must exist at
packages/twentythree-skills/skills/reference/{webinar,webhook}.md exactly. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Gather A5 flag data + write webinar.md top-level (11 commands) and first 4 subtopics (speaker, series, mail, recording)</name>
  <files>
    packages/twentythree-skills/skills/reference/webinar.md
  </files>
  <read_first>
    packages/twentythree-skills/skills/SKILL.md,
    packages/twentythree-skills/skills/reference/video.md,
    .planning/phases/19-skill-content/19-PATTERNS.md,
    .planning/phases/19-skill-content/19-RESEARCH.md
  </read_first>
  <action>
**A5 gap closure — mandatory CLI discovery BEFORE writing any section.** The research doc lists commands at subtopic level but NOT their flags. You MUST run `--agent` for every command before authoring that command's subsection. A subsection is INVALID if it delegates to "see --agent for flags" — the flags themselves must be in the file.

Required CLI runs in order:
1. `twentythree webinar --help` — confirm the 11 top-level commands match research §3 (create, list, update, delete, repeat, metrics, clips, highlights, log, list-formats, upload-image)
2. `twentythree webinar <verb> --agent` for EACH of the 11 top-level commands
3. `twentythree webinar speaker --help` — enumerate speaker subcommands (research says 14; verify against `--help` output at write time)
4. `twentythree webinar speaker <cmd> --agent` for EACH speaker subcommand
5. `twentythree webinar series --help` — enumerate series subcommands (research says 11)
6. `twentythree webinar series <cmd> --agent` for EACH series subcommand
7. `twentythree webinar mail --help` — enumerate mail subcommands (research says 7)
8. `twentythree webinar mail <cmd> --agent` for EACH mail subcommand
9. `twentythree webinar recording --help` — enumerate recording subcommands (research says 4: start, stop, status, split)
10. `twentythree webinar recording <cmd> --agent` for each of: start, stop, status, split

Then create `packages/twentythree-skills/skills/reference/webinar.md` in this task (top-level + 4 subtopics in one write; remaining 5 subtopics + finale land in Task 2).

**Frontmatter**:
```
---
name: webinar
description: Create and manage live webinars (linked to API /live/*) — configure sessions, speakers, agenda, attachments, recordings, transcriptions, mail, and series.
---
```

**Opening**:
- H1: `# TwentyThree Webinar Commands`
- Opening blockquote:
  > Webinars are live broadcast events. Every example uses `--json` for machine-readable output.
  > CLI `webinar` maps to API `live` — see Terminology Notes at the bottom of this file.
- Immediately below opening blockquote, include the "no webinar get" callout block from the Interfaces contract (word-for-word).

**Prerequisites**:
- Auth scope: read (list, metrics, clips, highlights, log, list-formats, recording status, most subtopic list cmds), write (create, update, delete, repeat, upload-image, recording start/stop/split, speaker/series/mail/section/attachment/queued-video writes). Include the `--agent` discovery reminder line.

**Commands** (`## Commands`) — 11 top-level subsections. For each: auth/side-effects/output line, flag table (from `--agent`), 2 examples with `--json`:
1. `### webinar create` — auth: write; side effects: creates; output: key-value (id + admin_url). Flags: `--title` (required), `--description`, `--status` (upcoming/live/previous), `--live-date` (ISO 8601), `--draft` (bool), `--publish` (bool). Include admin-URL output note.
2. `### webinar list` — auth: read; flags `--limit` (default 20), `--all` (bool), `--include-private` (bool), `--status`, `--search`. Second example: filter by status and search.
3. `### webinar update <id>` — auth: write; flags: `--title`, `--description`, `--status`, `--live-date`, `--draft`, `--publish`. Second example: publish a draft (`--publish`).
4. `### webinar delete <id>` — auth: write; destructive; no flags. Include destructive warning blockquote (matches video delete pattern).
5. `### webinar repeat <id>` — auth: write; creates a repeat occurrence; flag: `--date` (required, ISO 8601). Include admin-URL output note.
6. `### webinar metrics <id>` — auth: read; no flags; 2 examples.
7. `### webinar clips <id>` — auth: read; no flags; 2 examples. Note: clips become available after recording stops.
8. `### webinar highlights <id>` — auth: read; flag `--video-id` to scope to a specific recording; 2 examples.
9. `### webinar log <id>` — auth: read; no flags; 2 examples. Note: log returns the event log (chat/questions/registrations timeline).
10. `### webinar list-formats` — auth: read; no flags; 2 examples.
11. `### webinar upload-image <id> <file>` — auth: write; creates; flags: `--type` (thumbnail/preview/before_webinar, default thumbnail), `--chunk-size`, `--concurrency`. Include chunked-upload reminder inline.

Then write 4 of the 9 subtopics IN THIS TASK (the remaining 5 go in Task 2):

**`## Subtopic: webinar speaker`** (research says 14 commands; verify via `--help`)
- Opening paragraph: "Speakers are presenters attached to a webinar. Commands cover list, add, update, remove, invite, and speaker-specific settings."
- One `### webinar speaker <cmd>` subsection per command from the `--help` output. Each with auth/side-effects/output line, flag table (from `--agent`), 2 examples with `--json`.

**`## Subtopic: webinar series`** (research says 11 commands; verify via `--help`)
- Opening paragraph: "A series groups multiple webinars together (e.g. a recurring show). Commands cover list, create, update, delete, and attaching webinars to a series."
- One `### webinar series <cmd>` subsection per command from `--help`. Each with auth/side-effects/output, flag table, 2 examples with `--json`.

**`## Subtopic: webinar mail`** (research says 7 commands; verify via `--help`)
- Opening paragraph: "Mail templates and scheduled emails associated with a webinar (invitations, reminders, post-event follow-ups)."
- One `### webinar mail <cmd>` subsection per command. Each with auth/side-effects/output, flag table, 2 examples.

**`## Subtopic: webinar recording`** (4 commands: start, stop, status, split — confirmed in research §3)
- Opening paragraph: "Control server-side recording for a live webinar. Recording must be started while the webinar is live, stopped before clips become available, and can be split mid-session to create a chaptered archive."
- `### webinar recording start <id>` — write; no flags; 2 examples.
- `### webinar recording stop <id>` — write; no flags; 2 examples.
- `### webinar recording status <id>` — read; no flags; 2 examples.
- `### webinar recording split <id>` — write; no flags (or whatever `--agent` returns); 2 examples.

DO NOT yet write the remaining 5 subtopics (room, section, attachment, queued-video, transcription) or Common Patterns or Terminology Notes — Task 2 adds those.

Target file size at end of Task 1: ~180-260 lines.
  </action>
  <verify>
    <automated>test -f packages/twentythree-skills/skills/reference/webinar.md && grep -q "^name: webinar$" packages/twentythree-skills/skills/reference/webinar.md && grep -q "### webinar create" packages/twentythree-skills/skills/reference/webinar.md && grep -q "### webinar upload-image" packages/twentythree-skills/skills/reference/webinar.md && grep -q "## Subtopic: webinar speaker" packages/twentythree-skills/skills/reference/webinar.md && grep -q "## Subtopic: webinar series" packages/twentythree-skills/skills/reference/webinar.md && grep -q "## Subtopic: webinar mail" packages/twentythree-skills/skills/reference/webinar.md && grep -q "## Subtopic: webinar recording" packages/twentythree-skills/skills/reference/webinar.md && grep -q "### webinar recording start" packages/twentythree-skills/skills/reference/webinar.md && grep -q "webinar get" packages/twentythree-skills/skills/reference/webinar.md</automated>
  </verify>
  <acceptance_criteria>
    - `packages/twentythree-skills/skills/reference/webinar.md` exists with `^name: webinar$` frontmatter (anchored) and `^description: ` line
    - File contains `## Prerequisites` and `## Commands` sections
    - All 11 top-level command headings present: `### webinar create`, `### webinar list`, `### webinar update`, `### webinar delete`, `### webinar repeat`, `### webinar metrics`, `### webinar clips`, `### webinar highlights`, `### webinar log`, `### webinar list-formats`, `### webinar upload-image`
    - "No `webinar get`" callout present — grep: `webinar get` must appear at least once AND be framed as a warning/note (not a command subsection)
    - `### webinar create` subsection mentions printing `admin_url` (grep: `admin_url`)
    - `### webinar repeat` subsection mentions printing `admin_url`
    - `### webinar delete` subsection contains a destructive-action warning (grep: `destructive` OR `cannot be undone` OR similar)
    - `### webinar upload-image` mentions chunked upload (grep: `chunk`)
    - File contains all 4 Task-1 subtopics: `## Subtopic: webinar speaker`, `## Subtopic: webinar series`, `## Subtopic: webinar mail`, `## Subtopic: webinar recording`
    - File contains all 4 recording headings: `### webinar recording start`, `### webinar recording stop`, `### webinar recording status`, `### webinar recording split`
    - Count of `^### webinar speaker ` headings >= 10 (research says 14 — accept >=10 to allow for --help variance, but it MUST be >10 since 1 would indicate delegation rather than documentation)
    - Count of `^### webinar series ` headings >= 8 (research says 11)
    - Count of `^### webinar mail ` headings >= 5 (research says 7)
    - No speaker/series/mail/recording subsection contains the string "run --agent for flags" or "see --agent" as a substitute for a flag table (grep-check: if such phrases appear, A5 gap NOT closed)
    - webinar.md line count >= 180 at end of Task 1
    - File does not contain `npx`
  </acceptance_criteria>
  <done>webinar.md has frontmatter, all 11 top-level commands documented, "no webinar get" callout inline, and 4 of 9 subtopics (speaker, series, mail, recording) fully documented with flag tables sourced from live `--agent`. Ready for Task 2 to append remaining 5 subtopics and finalise.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Append remaining 5 webinar subtopics (room, section, attachment, queued-video, transcription) + Common Patterns + Terminology Notes</name>
  <files>
    packages/twentythree-skills/skills/reference/webinar.md
  </files>
  <read_first>
    packages/twentythree-skills/skills/reference/webinar.md,
    packages/twentythree-skills/skills/reference/video.md,
    packages/twentythree-skills/skills/SKILL.md,
    .planning/phases/19-skill-content/19-PATTERNS.md,
    .planning/phases/19-skill-content/19-RESEARCH.md
  </read_first>
  <action>
**A5 gap closure continues.** Run the CLI for the remaining 5 subtopics before writing.

Required CLI runs in order:
1. `twentythree webinar room --help` — enumerate room subcommands (research says 4, including `connect`)
2. `twentythree webinar room <cmd> --agent` for each room subcommand
3. `twentythree webinar section --help` — enumerate section subcommands (research says 4)
4. `twentythree webinar section <cmd> --agent` for each section subcommand
5. `twentythree webinar attachment --help` — enumerate attachment subcommands (research says 4)
6. `twentythree webinar attachment <cmd> --agent` for each attachment subcommand
7. `twentythree webinar queued-video --help` — enumerate queued-video subcommands (research says 2)
8. `twentythree webinar queued-video <cmd> --agent` for each queued-video subcommand
9. `twentythree webinar transcription --help` — enumerate transcription subcommands (research says 4)
10. `twentythree webinar transcription <cmd> --agent` for each transcription subcommand

Append the following sections to the existing `packages/twentythree-skills/skills/reference/webinar.md` (do NOT rewrite Task 1's content — append only):

**`## Subtopic: webinar room`** (4 commands — research says incl. connect)
- Opening paragraph: "Room commands control the live broadcast room (stream key, room URL, connection info). `webinar room connect` is the command agents use to retrieve the stream key and room URL before going live."
- One `### webinar room <cmd>` subsection per command. Each with auth/side-effects/output, flag table (from `--agent`), 2 examples with `--json`.
- On `### webinar room connect <id>` include a note: "Returns stream key and room URL. These credentials enable the broadcaster to start streaming into the webinar."

**`## Subtopic: webinar section`** (4 commands — section add/update/delete/list or similar; verify via `--help`)
- Opening paragraph: "Sections are agenda items (time-stamped chapter markers) displayed to viewers. Unlike `video section`, webinar sections can be scheduled relative to the webinar start time."
- One `### webinar section <cmd>` subsection per command, flag table from `--agent`, 2 examples with `--json`.

**`## Subtopic: webinar attachment`** (4 commands — research)
- Opening paragraph: "Attachments are files (PDFs, slides, links) made available to webinar viewers."
- One `### webinar attachment <cmd>` subsection per command. Include file-upload examples where relevant (e.g. `webinar attachment upload <id> ./slides.pdf --json`).

**`## Subtopic: webinar queued-video`** (2 commands — research)
- Opening paragraph: "Queued videos are pre-recorded clips played during a live webinar (simulated-live / interstitial content)."
- Two `### webinar queued-video <cmd>` subsections, flag tables, 2 examples each.

**`## Subtopic: webinar transcription`** (4 commands — research)
- Opening paragraph: "Transcription commands manage speech-to-text transcripts generated from webinar recordings. Transcripts feed into closed captions and AI-generated summaries."
- One `### webinar transcription <cmd>` subsection per command, flag tables, 2 examples.

**`## Common Patterns`** — at least 6 realistic multi-step examples combining commands. Include:
1. Create + publish in one flow: `webinar create --title "Q2 Town Hall" --live-date "2026-05-15T16:00:00Z" --json` → capture `id` → `webinar update <id> --publish --json`
2. Go-live sequence: `webinar room connect <id> --json` → start stream externally → `webinar recording start <id> --json`
3. Session archive sequence: `webinar recording stop <id> --json` → `webinar clips <id> --json` → `webinar update <id> --status previous --json`
4. Add a speaker and an agenda section: `webinar speaker add <id> --json` (use actual flags from `--agent`) + `webinar section add <id> --json`
5. Attach slides as a downloadable: `webinar attachment upload <id> ./slides.pdf --json` (use actual command/flag names from `--agent`)
6. Schedule a repeat: `webinar repeat <id> --date "2026-05-22T16:00:00Z" --json`
7. Filter list by status: `webinar list --status live --json` and `webinar list --status previous --include-private --json`

**`## Terminology Notes`** (REQUIRED — copy from Interfaces contract, adjust for real endpoint names if `--agent` output reveals different exact paths):
```
## Terminology Notes

CLI `webinar` = API `live`. The `api_endpoint` field in `--agent` output uses the API name:

- `twentythree webinar create` → `POST /live/create` (confirm exact path via `webinar create --agent`)
- `twentythree webinar list` → `GET /live/list`
- `twentythree webinar recording start` → `POST /live/<id>/recording/start`

When reading the raw OpenAPI spec, `live` refers to a webinar. The `live session` endpoint family (`/live/webinar/*`) backs the session concept referenced in research docs.
When commenting on a webinar via the `comment` topic, pass `--object-type live`.
```

After writing, run `pnpm --filter twentythree-skills test` — at end of Task 2 the only missing file in the validator output should be `webhook.md` (which Task 3 produces). webinar.md MUST NOT appear as a missing file error.

Target final line count for webinar.md: >=250 (research doc says "150+ lines"; with 11 top-level + 9 subtopics × ~2-14 commands each = 40+ commands, 250+ is the natural minimum).
  </action>
  <verify>
    <automated>test -f packages/twentythree-skills/skills/reference/webinar.md && grep -q "## Subtopic: webinar room" packages/twentythree-skills/skills/reference/webinar.md && grep -q "## Subtopic: webinar section" packages/twentythree-skills/skills/reference/webinar.md && grep -q "## Subtopic: webinar attachment" packages/twentythree-skills/skills/reference/webinar.md && grep -q "## Subtopic: webinar queued-video" packages/twentythree-skills/skills/reference/webinar.md && grep -q "## Subtopic: webinar transcription" packages/twentythree-skills/skills/reference/webinar.md && grep -q "### webinar room connect" packages/twentythree-skills/skills/reference/webinar.md && grep -q "## Common Patterns" packages/twentythree-skills/skills/reference/webinar.md && grep -q "## Terminology Notes" packages/twentythree-skills/skills/reference/webinar.md && grep -q "live" packages/twentythree-skills/skills/reference/webinar.md && test "$(wc -l < packages/twentythree-skills/skills/reference/webinar.md)" -ge 250</automated>
  </verify>
  <acceptance_criteria>
    - File contains all 5 additional subtopic headings: `## Subtopic: webinar room`, `## Subtopic: webinar section`, `## Subtopic: webinar attachment`, `## Subtopic: webinar queued-video`, `## Subtopic: webinar transcription`
    - File contains `### webinar room connect` heading (mentioned explicitly in research)
    - Count of `^### webinar room ` headings >= 3 (research says 4)
    - Count of `^### webinar section ` headings >= 3 (research says 4)
    - Count of `^### webinar attachment ` headings >= 3 (research says 4)
    - Count of `^### webinar queued-video ` headings >= 2 (research says 2)
    - Count of `^### webinar transcription ` headings >= 3 (research says 4)
    - Total count of `^### webinar ` headings across whole file >= 35 (11 top-level + expected 24 subtopic-cmds lower bound = 35)
    - No section delegates to "run --agent for flags" — grep MUST NOT find `see --agent` or `run --agent for flags` as a replacement for documented flags in any subtopic subsection
    - File contains `## Common Patterns` section
    - Common Patterns section contains at least 6 fenced bash blocks (count the triple-backtick-bash markers under `## Common Patterns` — each example counts)
    - File contains `## Terminology Notes` section
    - Terminology Notes section mentions `live` (the API legacy name) AND `/live/` (endpoint path prefix)
    - Terminology Notes section includes note about `comment --object-type live`
    - File final line count >= 250
    - File does not contain `npx`
    - File structure order: frontmatter → H1 → opening blockquote → "no webinar get" callout → Prerequisites → Commands → Subtopic: webinar speaker → series → mail → recording → room → section → attachment → queued-video → transcription → Common Patterns → Terminology Notes
    - `pnpm --filter twentythree-skills test` does NOT list webinar.md in "Missing reference file" errors
  </acceptance_criteria>
  <done>webinar.md is complete: 11 top-level commands + 9 subtopics (~40+ commands total) documented with flag tables sourced from live `--agent`, 6+ Common Patterns, Terminology Notes, "no webinar get" callout. A5 gap fully closed.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Write webhook.md (5 commands + discover-then-subscribe flow) and verify validator exits 0</name>
  <files>
    packages/twentythree-skills/skills/reference/webhook.md
  </files>
  <read_first>
    packages/twentythree-skills/skills/SKILL.md,
    packages/twentythree-skills/skills/reference/action.md,
    .planning/phases/19-skill-content/19-PATTERNS.md,
    .planning/phases/19-skill-content/19-RESEARCH.md
  </read_first>
  <action>
Required CLI runs BEFORE writing:
- `twentythree webhook --help` — confirm 5 commands: list, subscribe, unsubscribe, events, sample
- `twentythree webhook <verb> --agent` for each of: list, subscribe, unsubscribe, events, sample

Create `packages/twentythree-skills/skills/reference/webhook.md`:

**Frontmatter**:
```
---
name: webhook
description: Subscribe to and manage TwentyThree platform event webhooks (video uploads, webinar events, recording completions, etc.)
---
```

**Opening**:
- H1: `# TwentyThree Webhook Commands`
- Opening blockquote:
  > Webhooks deliver platform events (video upload complete, webinar started, recording finished, etc.) to a URL you control.
  > Every example uses `--json` for machine-readable output.

**Prerequisites**:
- Auth scope: read (list, events, sample), write (subscribe, unsubscribe). Include `--agent` discovery reminder.

**Commands** (`## Commands`) — one subsection per command. Each with auth/side-effects/output line, flag table, 2 examples with `--json`:

1. `### webhook list` — auth: read; no flags; output: table. Second example pipes to `jq` or filters by event type (if `--json` output includes event metadata).
2. `### webhook subscribe` — auth: write; creates; output: key-value (webhook_id). Required flags: `--target-url`, `--event`. Second example demonstrates subscribing to a specific event type discovered via `webhook events`.
3. `### webhook unsubscribe` — auth: write; destructive; flags: `--webhook-id` OR `--target-url` (at least one required). Include destructive warning blockquote. Second example uses `--target-url` form.
4. `### webhook events` — auth: read; flag: `--test-authentication` (bool). Second example uses `--test-authentication --json` to validate auth before subscribing.
5. `### webhook sample <event>` — auth: read; no flags. Explain: returns a sample JSON payload for the given event type. Useful for consumer-side schema validation before subscribing. Second example pipes sample to a file for schema inference.

**`## Common Patterns`** — include at least 3 multi-step flows:
1. **Discover-then-subscribe (canonical flow)** — word-for-word match to the Interfaces contract:
   ```bash
   # 1. Discover available events
   twentythree webhook events --json

   # 2. Inspect a sample payload for one event
   twentythree webhook sample <event> --json

   # 3. Subscribe
   twentythree webhook subscribe --event <event> --target-url https://example.com/hook --json
   ```
2. **Audit + clean up** — `webhook list --json` to enumerate existing subscriptions, then `webhook unsubscribe --webhook-id <id> --json` for each stale one.
3. **Authentication test before subscribing** — `webhook events --test-authentication --json` to confirm the workspace credentials are valid for webhook subscription, then subscribe.

- NO Terminology Notes section — webhook uses the same name in CLI and API (per 19-PATTERNS.md "Does NOT apply to" list).

**Final verification step (inside this task)**:
After webhook.md is written, run:
```bash
pnpm --filter twentythree-skills test
```
Expected output: validator exits 0 (all 22 reference files now exist). Capture the exit code; if non-zero, inspect which file is missing and confirm it's not webhook.md. The only valid reason for a non-zero exit at this point is a bug in an earlier plan's output, not this plan.

Also run a final sanity grep across all 22 reference files:
```bash
grep -l "npx" packages/twentythree-skills/skills/reference/*.md
```
Expected: empty (no file should contain `npx`).

Target line count: >=90.
  </action>
  <verify>
    <automated>test -f packages/twentythree-skills/skills/reference/webhook.md && grep -q "^name: webhook$" packages/twentythree-skills/skills/reference/webhook.md && grep -q "### webhook list" packages/twentythree-skills/skills/reference/webhook.md && grep -q "### webhook subscribe" packages/twentythree-skills/skills/reference/webhook.md && grep -q "### webhook unsubscribe" packages/twentythree-skills/skills/reference/webhook.md && grep -q "### webhook events" packages/twentythree-skills/skills/reference/webhook.md && grep -q "### webhook sample" packages/twentythree-skills/skills/reference/webhook.md && grep -q "webhook events" packages/twentythree-skills/skills/reference/webhook.md && test "$(wc -l < packages/twentythree-skills/skills/reference/webhook.md)" -ge 90 && cd packages/twentythree-skills && node scripts/validate-skills.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `packages/twentythree-skills/skills/reference/webhook.md` exists with `^name: webhook$` frontmatter and `^description: ` line
    - File contains `## Prerequisites`, `## Commands`, `## Common Patterns` sections
    - File contains all 5 command headings: `### webhook list`, `### webhook subscribe`, `### webhook unsubscribe`, `### webhook events`, `### webhook sample`
    - `### webhook subscribe` flag table includes `--target-url` and `--event`
    - `### webhook unsubscribe` flag table shows both `--webhook-id` and `--target-url` with "at least one required" annotation
    - `### webhook unsubscribe` contains destructive-action warning
    - `### webhook events` flag table includes `--test-authentication`
    - Common Patterns section contains the discover-then-subscribe 3-step flow (grep all 3: `webhook events`, `webhook sample`, `webhook subscribe --event`)
    - No Terminology Notes section (webhook uses same name in CLI and API)
    - File does not contain `npx`
    - File line count >= 90
    - `pnpm --filter twentythree-skills test` exits 0 — validator passes Gate 2 with all 22 reference files present
    - Sanity grep: `grep -l "npx" packages/twentythree-skills/skills/reference/*.md` returns empty
  </acceptance_criteria>
  <done>webhook.md is complete with 5 commands documented, discover-then-subscribe flow in Common Patterns. Validator exits 0 — Phase 19 Gate 2 (SKILL-02) satisfied for the reference file subset. Plans 01-05 now cover all 22 required reference files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Markdown content phase — no runtime trust boundaries.

| Boundary | Description |
|----------|-------------|
| (none) | Static markdown |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-05-01 | Tampering | Agent instructions copied from webinar.md/webhook.md could be modified to exfiltrate tokens | accept | Markdown ships read-only inside the npm package; immutable once published. Agent runtime enforces auth — doc cannot grant scope it does not have. |
| T-19-05-02 | Information Disclosure | Examples show webhook `--target-url` values | accept | Examples use placeholder `https://example.com/hook` — no real URLs, no real tokens. Reviewed at commit time. |
| T-19-05-03 | Denial of Service | Agent misuses `webinar delete` or `webhook unsubscribe` because doc does not flag destructiveness | mitigate | Both subsections contain explicit destructive-action warnings (verified in acceptance criteria of Tasks 1 and 3). |
| T-19-05-04 | Spoofing | Doc suggests a user subscribe a webhook to an arbitrary URL | accept | User is the one running the CLI; they control `--target-url`. API requires auth to subscribe. No spoofing pathway the doc introduces. |
</threat_model>

<verification>
After all 3 tasks complete, run:

```bash
pnpm --filter twentythree-skills test
```

Expected: exit code 0. Validator says all 22 reference files present. This is the definitive Gate 2 signal for SKILL-02.

Spot-check content:
```bash
wc -l packages/twentythree-skills/skills/reference/webinar.md   # >=250
wc -l packages/twentythree-skills/skills/reference/webhook.md   # >=90
grep -c "^### webinar" packages/twentythree-skills/skills/reference/webinar.md  # >=35
grep -c "## Subtopic:" packages/twentythree-skills/skills/reference/webinar.md  # == 9
grep -q "Terminology Notes" packages/twentythree-skills/skills/reference/webinar.md  # present
grep -q "webinar get" packages/twentythree-skills/skills/reference/webinar.md  # callout present
grep -l "npx" packages/twentythree-skills/skills/reference/*.md  # empty
ls packages/twentythree-skills/skills/reference/ | wc -l  # >=22
```
</verification>

<success_criteria>
- `webinar.md` created with frontmatter `name: webinar`
- 11 top-level webinar commands documented
- All 9 webinar subtopics (speaker, series, mail, recording, room, section, attachment, queued-video, transcription) documented
- Every subtopic's commands have flag tables sourced from live `--agent` (A5 gap closed — no subsection delegates to "run --agent for flags")
- "No `webinar get`" callout present
- Terminology Notes section explains `webinar` → `live` API mapping
- Admin-URL output note on `webinar create` and `webinar repeat`
- Destructive warning on `webinar delete`
- webinar.md line count >= 250
- `webhook.md` created with frontmatter `name: webhook`
- 5 webhook commands documented with flag tables
- Discover-then-subscribe flow present in Common Patterns
- Destructive warning on `webhook unsubscribe`
- webhook.md line count >= 90
- No file contains `npx`
- `pnpm --filter twentythree-skills test` exits 0 — Gate 2 / SKILL-02 satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/19-skill-content/19-05-SUMMARY.md` following the summary template.
</output>
