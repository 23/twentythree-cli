---
phase: 19
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/twentythree-skills/skills/reference/action.md
  - packages/twentythree-skills/skills/reference/audience.md
  - packages/twentythree-skills/skills/reference/collector.md
  - packages/twentythree-skills/skills/reference/comment.md
  - packages/twentythree-skills/skills/reference/poll.md
  - packages/twentythree-skills/skills/reference/spot.md
  - packages/twentythree-skills/skills/reference/tag.md
autonomous: true
requirements: [SKILL-02]
tags: [skills, documentation, markdown, reference]

must_haves:
  truths:
    - "An agent can read action.md and discover every action command with flags and examples"
    - "An agent can read audience.md and discover every audience command (including field subtopic) with flags"
    - "An agent can read collector.md, comment.md, poll.md, spot.md, tag.md and copy working bash examples"
    - "Every file has frontmatter with name matching the filename stem"
    - "validate-skills.mjs does not fail because of Plan 01 files (no missing files in its set)"
  artifacts:
    - path: "packages/twentythree-skills/skills/reference/action.md"
      provides: "Documentation for all 9 action commands"
      contains: "name: action"
    - path: "packages/twentythree-skills/skills/reference/audience.md"
      provides: "Documentation for 11 audience commands + audience field subtopic (4 cmds)"
      contains: "name: audience"
    - path: "packages/twentythree-skills/skills/reference/collector.md"
      provides: "Documentation for all 3 collector commands"
      contains: "name: collector"
    - path: "packages/twentythree-skills/skills/reference/comment.md"
      provides: "Documentation for all 7 comment commands + comment reaction subtopic"
      contains: "name: comment"
    - path: "packages/twentythree-skills/skills/reference/poll.md"
      provides: "Documentation for all 6 poll commands"
      contains: "name: poll"
    - path: "packages/twentythree-skills/skills/reference/spot.md"
      provides: "Documentation for all 7 spot commands"
      contains: "name: spot"
    - path: "packages/twentythree-skills/skills/reference/tag.md"
      provides: "Documentation for both tag commands (read-only topic)"
      contains: "name: tag"
  key_links:
    - from: "skills/SKILL.md resource index"
      to: "skills/reference/{action,audience,collector,comment,poll,spot,tag}.md"
      via: "agent navigates from root skill index into topic reference"
      pattern: "resource-group name matches filename stem"
---

<objective>
Write 7 reference files for simple CRUD-shaped resource groups: action, audience, collector, comment, poll, spot, tag. Each file must comprehensively document every command in its topic with flag tables, 2+ examples per command (all including --json), and auth-scope annotations — per D-01 and D-03. Ready for validate-skills.mjs Gate 2.

Purpose: SKILL-02 requires 22 hand-authored reference files. This plan delivers 7 of them (the lower-complexity CRUD topics). Per D-03, these files get full comprehensive depth — no tiered treatment.

Output: 7 markdown files in `packages/twentythree-skills/skills/reference/`, each ~80-100+ lines, conforming to the house style established in `skills/SKILL.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@packages/twentythree-skills/skills/SKILL.md
@packages/twentythree-skills/scripts/validate-skills.mjs
@.planning/phases/19-skill-content/19-CONTEXT.md
@.planning/phases/19-skill-content/19-RESEARCH.md
@.planning/phases/19-skill-content/19-PATTERNS.md

<interfaces>
<!-- Frontmatter contract required for every reference file (from 19-PATTERNS.md frontmatter pattern) -->

```yaml
---
name: <resource>        # MUST match filename stem, lowercase, no hyphens
description: <one-line description>
---
```

<!-- Document structure contract (from 19-PATTERNS.md document structure pattern) -->

```markdown
---
name: <resource>
description: <one-line>
---

# TwentyThree <Resource> Commands

> <two-line summary>
> Always use `--json` in agentic contexts for structured output.

## Prerequisites

Auth scope required: <scope(s)>. Run `twentythree auth credentials` if not already configured.
Verify: `twentythree auth status --json`

> For any flag not listed here, run `twentythree <resource> <cmd> --agent` to get the complete flag list, types, and defaults.

## Commands

### <resource> <verb>

**Auth scope:** <scope>  **Side effects:** <none|creates|updates|destructive>  **Output:** <table|key-value|none>

Flags:

| Flag | Required | Description |
|------|----------|-------------|
| `--<flag>` | yes/no | description |

```bash
# Basic usage
twentythree <resource> <verb> --json

# Realistic agent usage
twentythree <resource> <verb> --<flag> <value> --json
```

[...one ### section per command...]

## Common Patterns

[2–4 realistic multi-flag examples combining commands]

## Terminology Notes  (include only if CLI name diverges from API name)
```

<!-- Validator RESOURCE_GROUPS (authoritative filenames; must match exactly):
action, analytics, app, audience, category, collector, comment, openupload,
player, poll, presentation, protection, session, setting, site, spot, tag,
thumbnail, user, video, webhook, webinar -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Write action.md, collector.md, tag.md (3 smallest CRUD refs)</name>
  <files>
    packages/twentythree-skills/skills/reference/action.md,
    packages/twentythree-skills/skills/reference/collector.md,
    packages/twentythree-skills/skills/reference/tag.md
  </files>
  <read_first>
    packages/twentythree-skills/skills/SKILL.md,
    .planning/phases/19-skill-content/19-PATTERNS.md,
    .planning/phases/19-skill-content/19-RESEARCH.md
  </read_first>
  <action>
Create three reference files. All three follow the document-structure pattern in 19-PATTERNS.md exactly. Every example MUST end with `--json`. Do NOT use or mention `npx`.

For each file, run the CLI to confirm flag accuracy before authoring:
- `twentythree action --help` then `twentythree action <verb> --agent` for each of: list, add, get, update, delete, types, exclude, include, upload
- `twentythree collector --help` then `twentythree collector <verb> --agent` for each of: list, include, exclude
- `twentythree tag --help` then `twentythree tag <verb> --agent` for list and related

**action.md** (full content from 19-RESEARCH.md §6 plus live `--agent` verification):
- Frontmatter: `name: action`, `description: Manage calls-to-action (CTAs) and interactive overlays on videos and webinars.`
- `# TwentyThree Action Commands`
- Opening blockquote reminding to use `--json`
- `## Prerequisites` — auth scope: read (list/get/types), write (add/update/delete/exclude/include/upload). Include the `--agent` discovery reminder line.
- `## Commands` — one `### action <verb>` subsection for each of the 9 commands. Each subsection has: auth/side-effects/output line, flag table, 2 bash examples (basic + realistic, both with `--json`).
- `## Common Patterns` — include a multi-step example: `action types --json` -> pick a type -> `action add --type <type> --object-id <video-id> --fields key=value --json`
- NO Terminology Notes section (action uses same name in CLI and API).

**collector.md**:
- Frontmatter: `name: collector`, `description: Attach and remove lead-capture collectors (forms) on videos and webinars.`
- `# TwentyThree Collector Commands`
- Opening blockquote reminding to use `--json`
- `## Prerequisites` — auth scope: read (list), write (include/exclude).
- `## Commands` — one `### collector <verb>` subsection for each of: list, include, exclude. Each with auth/side-effects/output line, flag table, 2 examples with `--json`.
- `## Common Patterns` — include a workflow: `collector list --json` -> find collector_id -> `collector include <collector-id> --object-id <video-id> --json`.
- NO Terminology Notes section.

**tag.md**:
- Frontmatter: `name: tag`, `description: List and discover content tags. Tags are created implicitly via video update --tags.`
- `# TwentyThree Tag Commands`
- Opening blockquote reminding to use `--json`
- `## Prerequisites` — auth scope: anonymous for both commands (no auth required). Include the `--agent` discovery reminder line.
- `## Commands` — one `### tag <verb>` subsection for each of: list, related. Each with auth/side-effects/output line, flag table, 2 examples with `--json`.
- `## Common Patterns` — show how to find related tags for content discovery: `tag list --search <term> --json` then `tag related <tag> --json`.
- Include an inline note: "To add or remove tags from content, use `video update --tags` or `webinar update --tags`. The `tag` topic itself is read-only."
- NO Terminology Notes section.

Target length per file: action.md ~90-120 lines (9 commands), collector.md ~70-85 lines (3 commands), tag.md ~60-80 lines (2 commands, so Common Patterns carries more weight).
  </action>
  <verify>
    <automated>test -f packages/twentythree-skills/skills/reference/action.md && test -f packages/twentythree-skills/skills/reference/collector.md && test -f packages/twentythree-skills/skills/reference/tag.md && grep -q "^name: action$" packages/twentythree-skills/skills/reference/action.md && grep -q "^name: collector$" packages/twentythree-skills/skills/reference/collector.md && grep -q "^name: tag$" packages/twentythree-skills/skills/reference/tag.md</automated>
  </verify>
  <acceptance_criteria>
    - `packages/twentythree-skills/skills/reference/action.md` exists
    - `packages/twentythree-skills/skills/reference/collector.md` exists
    - `packages/twentythree-skills/skills/reference/tag.md` exists
    - action.md contains `^name: action$` (anchored line) and `^description: ` in frontmatter
    - collector.md contains `^name: collector$` and `^description: `
    - tag.md contains `^name: tag$` and `^description: `
    - action.md contains `## Prerequisites`, `## Commands`, and `## Common Patterns`
    - collector.md contains `## Prerequisites`, `## Commands`, and `## Common Patterns`
    - tag.md contains `## Prerequisites`, `## Commands`, and `## Common Patterns`
    - action.md contains all 9 command headings: `### action list`, `### action add`, `### action get`, `### action update`, `### action delete`, `### action types`, `### action exclude`, `### action include`, `### action upload` (verify with grep `-c` count == 9)
    - collector.md contains 3 command headings: `### collector list`, `### collector include`, `### collector exclude`
    - tag.md contains 2 command headings: `### tag list`, `### tag related`
    - No file contains the string `npx` (run: `grep -l "npx" packages/twentythree-skills/skills/reference/action.md packages/twentythree-skills/skills/reference/collector.md packages/twentythree-skills/skills/reference/tag.md` MUST return empty)
    - Every fenced bash block in these files contains `--json` (spot-check with `grep -A 3 '```bash' <file>`)
    - action.md line count >= 90 (check: `wc -l`)
    - collector.md line count >= 60
    - tag.md line count >= 50
  </acceptance_criteria>
  <done>All 3 files exist with correct frontmatter, all commands documented, every example uses --json, prerequisites section includes auth scope.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Write audience.md, comment.md (richer CRUD refs with subtopics)</name>
  <files>
    packages/twentythree-skills/skills/reference/audience.md,
    packages/twentythree-skills/skills/reference/comment.md
  </files>
  <read_first>
    packages/twentythree-skills/skills/SKILL.md,
    packages/twentythree-skills/skills/reference/action.md,
    .planning/phases/19-skill-content/19-PATTERNS.md,
    .planning/phases/19-skill-content/19-RESEARCH.md
  </read_first>
  <action>
Create two reference files for topics with field/reaction subtopics. Read action.md (produced by Task 1) first to match the exact style.

Verify flag accuracy via CLI before writing:
- `twentythree audience --help` then `twentythree audience <verb> --agent` for: list, search, metrics, timelines, register, unregister, remove, companies, funnel, identity-sources, list-collectors
- `twentythree audience field --help` then `twentythree audience field <verb> --agent` for: list, set, remove, types
- `twentythree comment --help` then `twentythree comment <verb> --agent` for: list, add, update, delete, promote, clone, set-order
- `twentythree comment reaction --help` and `twentythree comment reaction add --agent`

**audience.md**:
- Frontmatter: `name: audience`, `description: Manage viewer audience records, segments, lead-capture registrations, and audience fields.`
- `# TwentyThree Audience Commands`
- Opening blockquote reminding to use `--json`
- `## Prerequisites` — auth scope: read (list, search, metrics, timelines, companies, funnel, identity-sources, list-collectors, field list), write (register, unregister, remove, field set). Include `--agent` reminder.
- `## Commands` section with ALL 11 top-level commands each as its own `### audience <verb>` subsection, following the research flag inventory (19-RESEARCH.md §5). Each needs auth/side-effects/output annotation, flag table, 2 examples with `--json`.
- `## Subtopic: audience field` section with: `### audience field list`, `### audience field set`, `### audience field remove`, `### audience field types`. Each with auth/side-effects/output, flag table, 2 examples.
- `## Common Patterns` — include these multi-step examples:
  1. Registering a new audience member: `audience register --email ... --object-id <video-id> --firstname ... --json`
  2. Searching for a viewer by text and getting their timeline: `audience search --text "..." --json` then `audience timelines --uuid <uuid> --json`
  3. Adding a custom field to all audience records: `audience field set --key company_size --type string --label "Company Size" --json`
- NO Terminology Notes section.

**comment.md**:
- Frontmatter: `name: comment`, `description: Moderate and manage comments, questions, and chat on videos, categories, and webinars.`
- `# TwentyThree Comment Commands`
- Opening blockquote reminding to use `--json`
- `## Prerequisites` — auth scope: read (list), write (add, update, delete, promote, clone, set-order). Include `--agent` reminder.
- `## Commands` — ALL 7 commands: `### comment list`, `### comment add`, `### comment update`, `### comment delete`, `### comment promote`, `### comment clone`, `### comment set-order`. Each with auth/side-effects/output, flag table, 2 examples with `--json`.
- `## Subtopic: comment reaction` section with `### comment reaction add` (1 command; document its flags from `--agent`).
- `## Common Patterns` — include: (a) listing questions only for a webinar: `comment list --object-id <id> --object-type live --comment-type question --json`; (b) promoting a comment: `comment promote <id> --json`.
- `## Terminology Notes` (REQUIRED per 19-PATTERNS.md "Applies to" list):
  > The `--object-type` flag uses the legacy API object names:
  > - pass `photo` for a video
  > - pass `album` for a category
  > - pass `live` for a webinar
  >
  > This is the only place in the `comment` topic where terminology diverges from CLI naming.

Target length: audience.md ~140-180 lines (11 + 4 = 15 commands), comment.md ~100-130 lines (7 + 1 = 8 commands).
  </action>
  <verify>
    <automated>test -f packages/twentythree-skills/skills/reference/audience.md && test -f packages/twentythree-skills/skills/reference/comment.md && grep -q "^name: audience$" packages/twentythree-skills/skills/reference/audience.md && grep -q "^name: comment$" packages/twentythree-skills/skills/reference/comment.md && grep -q "## Terminology Notes" packages/twentythree-skills/skills/reference/comment.md</automated>
  </verify>
  <acceptance_criteria>
    - `packages/twentythree-skills/skills/reference/audience.md` exists
    - `packages/twentythree-skills/skills/reference/comment.md` exists
    - audience.md contains `^name: audience$` and `^description: ` in frontmatter
    - comment.md contains `^name: comment$` and `^description: ` in frontmatter
    - audience.md contains all 11 top-level command headings: `### audience list`, `### audience search`, `### audience metrics`, `### audience timelines`, `### audience register`, `### audience unregister`, `### audience remove`, `### audience companies`, `### audience funnel`, `### audience identity-sources`, `### audience list-collectors`
    - audience.md contains all 4 subtopic headings: `### audience field list`, `### audience field set`, `### audience field remove`, `### audience field types`
    - comment.md contains all 7 top-level command headings: `### comment list`, `### comment add`, `### comment update`, `### comment delete`, `### comment promote`, `### comment clone`, `### comment set-order`
    - comment.md contains `### comment reaction add`
    - comment.md contains `## Terminology Notes` section (REQUIRED per PATTERNS.md)
    - comment.md `## Terminology Notes` section mentions `photo`, `album`, AND `live` as legacy object-type values
    - audience.md line count >= 130
    - comment.md line count >= 90
    - No file contains `npx`
  </acceptance_criteria>
  <done>audience.md and comment.md exist, fully document their topic + subtopic, comment.md has the Terminology Notes section for --object-type legacy names.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Write poll.md and spot.md (complete Plan 01 set)</name>
  <files>
    packages/twentythree-skills/skills/reference/poll.md,
    packages/twentythree-skills/skills/reference/spot.md
  </files>
  <read_first>
    packages/twentythree-skills/skills/SKILL.md,
    packages/twentythree-skills/skills/reference/action.md,
    .planning/phases/19-skill-content/19-PATTERNS.md,
    .planning/phases/19-skill-content/19-RESEARCH.md
  </read_first>
  <action>
Create the final 2 reference files in Plan 01. Assumption A3 applies to poll — you MUST run `--agent` for the un-documented commands:

Required CLI runs BEFORE writing:
- `twentythree poll --help` then `--agent` for each of: list, add, update, remove, answer, set-options
- **A3 gap**: `twentythree poll answer --agent` and `twentythree poll set-options --agent` — the research doc does not list these flags; they MUST come from the live `--agent` output.
- `twentythree spot --help` then `--agent` for each of: list, create, check, update, delete, set-videos, reset-version

**poll.md**:
- Frontmatter: `name: poll`, `description: Create and manage in-webinar polls (questions with answer options shown to live viewers).`
- `# TwentyThree Poll Commands`
- Opening blockquote reminding to use `--json`
- `## Prerequisites` — auth scope: anonymous (list), write (add/update/remove/answer/set-options). Include `--agent` reminder. Note: polls are attached to webinars (live objects) via `--object-id`.
- `## Commands` — one subsection for each of 6 commands: `### poll list`, `### poll add`, `### poll update`, `### poll remove`, `### poll answer`, `### poll set-options`. Each has auth/side-effects/output, flag table (for answer and set-options use the flags returned by --agent in the step above), 2 examples with `--json`.
- `## Common Patterns` — include: (a) creating a poll with options: `poll add --object-id <webinar-id> --question "..." --json` -> capture poll_id -> `poll set-options <poll-id> ... --json`; (b) opening/closing a poll for voting: `poll update <id> --open --json` and `poll update <id> --open=false --display-results --json`.
- NO Terminology Notes section (poll topic uses consistent naming, even though --object-id points at the legacy `live` object the flag name itself is `--object-id`).

**spot.md**:
- Frontmatter: `name: spot`, `description: Create and manage embeddable video widgets (spots) that reference one or more videos.`
- `# TwentyThree Spot Commands`
- Opening blockquote reminding to use `--json`
- `## Prerequisites` — auth scope: read (list, check), write (create, update, delete, set-videos, reset-version). Include `--agent` reminder. Call out: "Use `spot check <id>` rather than `spot get` — there is no `spot get` command."
- `## Commands` — one subsection per command: `### spot list`, `### spot create`, `### spot check`, `### spot update`, `### spot delete`, `### spot set-videos`, `### spot reset-version`. Each with auth/side-effects/output, flag table, 2 examples with `--json`.
- `## Common Patterns` — include: (a) creating a spot and attaching videos in 2 steps: `spot create --spot-name "Homepage" --json` -> capture spot_id -> `spot set-videos <spot-id> --videos <id1>,<id2>,<id3> --json`; (b) finding active spots: `spot list --active --json`.
- NO Terminology Notes section.

Target length: poll.md ~85-105 lines, spot.md ~95-115 lines.
  </action>
  <verify>
    <automated>test -f packages/twentythree-skills/skills/reference/poll.md && test -f packages/twentythree-skills/skills/reference/spot.md && grep -q "^name: poll$" packages/twentythree-skills/skills/reference/poll.md && grep -q "^name: spot$" packages/twentythree-skills/skills/reference/spot.md && grep -q "### poll answer" packages/twentythree-skills/skills/reference/poll.md && grep -q "### poll set-options" packages/twentythree-skills/skills/reference/poll.md && grep -q "### spot check" packages/twentythree-skills/skills/reference/spot.md</automated>
  </verify>
  <acceptance_criteria>
    - `packages/twentythree-skills/skills/reference/poll.md` exists with `^name: poll$` frontmatter
    - `packages/twentythree-skills/skills/reference/spot.md` exists with `^name: spot$` frontmatter
    - poll.md contains all 6 command headings: `### poll list`, `### poll add`, `### poll update`, `### poll remove`, `### poll answer`, `### poll set-options`
    - poll.md `### poll answer` subsection contains a flag table row (indicates A3 `--agent` was actually run and flags were captured)
    - poll.md `### poll set-options` subsection contains a flag table row (indicates A3 `--agent` was actually run)
    - spot.md contains all 7 command headings: `### spot list`, `### spot create`, `### spot check`, `### spot update`, `### spot delete`, `### spot set-videos`, `### spot reset-version`
    - spot.md mentions "no `spot get` command" OR "use `spot check`" to guard against Pitfall-style command invention
    - poll.md line count >= 80
    - spot.md line count >= 90
    - Neither file contains `npx`
    - Run `pnpm --filter twentythree-skills test` at end of Plan 01 — validator must NOT error about missing action.md, audience.md, collector.md, comment.md, poll.md, spot.md, or tag.md (it WILL still error on the other 15 files until later plans ship, which is expected)
  </acceptance_criteria>
  <done>poll.md and spot.md exist with all commands documented. A3 gap closed (poll answer/set-options flags sourced from live --agent). Plan 01 complete: 7 reference files written.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This phase produces static markdown content only. There are no trust boundaries crossed: no code executes, no credentials are read, no network calls are made, and no user input is processed by anything these files author.

| Boundary | Description |
|----------|-------------|
| (none) | Markdown content-only; no runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01-01 | Information Disclosure | Command examples in reference files | accept | Examples use placeholder values (`<video-id>`, `user@example.com`); no real tokens, domains, or PII. Reviewed at commit time. |
| T-19-01-02 | Tampering | Markdown consumed by AI agents downstream | accept | Static markdown served read-only from npm package; immutable once published. Agent trust boundary belongs to agent runtime, not this package. |
</threat_model>

<verification>
After all 3 tasks:

```bash
pnpm --filter twentythree-skills test
```

Expected at end of Plan 01 (with 7 of 22 files present): validator errors listing the other 15 missing files (action.md etc. MUST NOT appear in error list).

Spot-check quality:
```bash
wc -l packages/twentythree-skills/skills/reference/{action,audience,collector,comment,poll,spot,tag}.md
grep -c "^### " packages/twentythree-skills/skills/reference/action.md   # should be 9
grep -c "^### " packages/twentythree-skills/skills/reference/audience.md # should be 15 (11 + 4 field subtopic)
grep -c "^### " packages/twentythree-skills/skills/reference/comment.md  # should be 8 (7 + 1 reaction)
grep -l "npx" packages/twentythree-skills/skills/reference/*.md          # MUST be empty
```
</verification>

<success_criteria>
- All 7 reference files created at the exact filenames: action.md, audience.md, collector.md, comment.md, poll.md, spot.md, tag.md
- Every file has `name:` frontmatter matching its filename stem and a `description:` line
- Every file has ## Prerequisites, ## Commands, ## Common Patterns sections
- Every command in each topic has a `### <resource> <verb>` subsection with auth/side-effects annotation, flag table, and 2+ bash examples ending in `--json`
- comment.md includes the Terminology Notes section (photo/album/live for --object-type)
- A3 gap closed: poll answer and poll set-options flags sourced from live `--agent` CLI output (not research doc)
- No occurrence of `npx` anywhere in any file
- `pnpm --filter twentythree-skills test` does NOT produce "Missing reference file" errors for any of the 7 files in this plan
</success_criteria>

<output>
After completion, create `.planning/phases/19-skill-content/19-01-SUMMARY.md` following the summary template.
</output>
