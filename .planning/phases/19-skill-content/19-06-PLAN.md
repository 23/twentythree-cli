---
phase: 19
plan: 06
type: execute
wave: 3
depends_on: [19-04, 19-05]
files_modified:
  - packages/twentythree-skills/skills/workflows/upload-and-publish.md
  - packages/twentythree-skills/skills/workflows/webinar-lifecycle.md
autonomous: true
requirements: [SKILL-03]
tags: [skills, documentation, markdown, workflow, video, webinar]

must_haves:
  truths:
    - "An agent reading upload-and-publish.md can execute the complete upload→metadata→publish sequence with exact commands and flags (no inference required)"
    - "An agent reading webinar-lifecycle.md can execute the complete create→configure→publish→go-live→record→archive sequence"
    - "Every step in both workflows specifies: exact command + flags, expected --json output shape, the field to capture for the next step, and the error to check on failure"
    - "Each workflow file contains a Prerequisites section stating required auth scope"
    - "Each workflow file contains an Error Handling table mapping step failures to diagnostic commands"
    - "Both files use admin_url output note pattern — capture `data.id` and `data.admin_url` from create/upload responses"
    - "SKILL-03 satisfied: 2 workflow files exist in skills/workflows/"
  artifacts:
    - path: "packages/twentythree-skills/skills/workflows/upload-and-publish.md"
      provides: "6-step workflow: list categories (optional), upload, metadata, frame, transcoding poll, publish"
      contains: "name: upload-and-publish"
      min_lines: 120
    - path: "packages/twentythree-skills/skills/workflows/webinar-lifecycle.md"
      provides: "10-step workflow: create, sections, speakers, upload-image, publish, room connect, recording start/stop, status check, archive"
      contains: "name: webinar-lifecycle"
      min_lines: 150
  key_links:
    - from: "workflows/upload-and-publish.md"
      to: "reference/video.md, reference/category.md"
      via: "workflow cross-references reference files for full flag detail"
      pattern: "workflow shows minimum viable command; reference files show exhaustive flag options"
    - from: "workflows/webinar-lifecycle.md"
      to: "reference/webinar.md"
      via: "workflow cross-references webinar.md for full flag detail"
      pattern: "workflow copies command signatures verbatim from webinar.md"
    - from: "workflow Step 2 (upload)"
      to: "Step 3 (metadata)"
      via: "capture data.id from --json response as video_id"
      pattern: "each step captures a specific field from --json output for subsequent steps"
---

<objective>
Write the 2 workflow files required by SKILL-03:
1. **upload-and-publish.md** — 6-step sequence for uploading a video file, setting metadata, and publishing it. Cross-references `reference/video.md` and `reference/category.md` (produced in Plans 03 and 04).
2. **webinar-lifecycle.md** — 10-step sequence for the full webinar lifecycle: create → configure (sections/speakers/thumbnail) → publish → go-live (room connect, recording start/stop) → archive. Cross-references `reference/webinar.md` (produced in Plan 05).

Purpose: SKILL-03 requires 2-3 workflow files. CONTEXT.md D-02 locks the scope to exactly these 2 workflows (additional workflow patterns deferred to roadmap). Each file must be immediately copy-paste usable by an agent with minimal parameter substitution — not vague guidance. Every step must have: exact command + flags, expected --json output shape, field to capture, error to check.

Output: 2 markdown files in `packages/twentythree-skills/skills/workflows/` (this directory does NOT exist yet; must be created).

Special handling:
- Must create `packages/twentythree-skills/skills/workflows/` directory (Phase 18 only created `skills/` and `skills/reference/`)
- Each workflow step template: command + expected output + field to capture + what to check on failure
- Cross-references to reference files must use valid relative paths (e.g. `../reference/video.md`)
- Error Handling section at the bottom of each file
- depends_on: 19-04 (video.md) and 19-05 (webinar.md) — workflows reference commands documented in those files
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@packages/twentythree-skills/skills/SKILL.md
@packages/twentythree-skills/scripts/validate-skills.mjs
@packages/twentythree-skills/skills/reference/video.md
@packages/twentythree-skills/skills/reference/webinar.md
@packages/twentythree-skills/skills/reference/category.md
@.planning/phases/19-skill-content/19-CONTEXT.md
@.planning/phases/19-skill-content/19-RESEARCH.md
@.planning/phases/19-skill-content/19-PATTERNS.md

<interfaces>
<!-- Workflow file structure (from 19-PATTERNS.md §Workflow file structure pattern) -->

```markdown
---
name: <workflow-name>
description: <one-line description of the end-to-end goal>
---

# Workflow: <Human-Readable Title>

> <two-line summary of what this workflow accomplishes>
> All commands use `--json` — capture IDs from the `data.id` field of each response.

## Prerequisites

- Auth scope required: **<read|write|admin>**
- Run `twentythree auth credentials` if not already configured.
- Confirm auth: `twentythree auth status --json`

## Steps

### 1. <Step name>

```bash
twentythree <cmd> --flag <val> --json
```

Expected output shape: `{ data: { id, <other fields> } }`
Capture: `data.<field>` as `<variable_name>`
On failure: <what to check>

### 2. <Next step>
...

## Error Handling

| Step | Failure | What to check |
|------|---------|---------------|
| Step N | <failure mode> | <diagnostic command> |
```

<!-- Workflow step data (from 19-RESEARCH.md §Workflow File Specifications — VERIFIED via CLI) -->

<!-- upload-and-publish sequence: -->
<!--
1. (Optional) twentythree category list --json  → pick category_id
2. twentythree video upload <file> --title "…" --category-id <id> --json → capture data.id as video_id + data.admin_url
3. (Optional) twentythree video update <video_id> --description "…" --tags "…" --json
4. (Optional) twentythree video frame <video_id> --time <seconds> --json
5. (Optional) twentythree video transcoding-progress <video_id> --json  → poll until complete
6. twentythree video update <video_id> --publish --json
-->

<!-- webinar-lifecycle sequence: -->
<!--
1. twentythree webinar create --title "…" --live-date "ISO 8601" --json → capture data.id as webinar_id + data.admin_url
2. (Optional) twentythree webinar section add <webinar_id> --title "Intro" --start-time 0 --json  (exact cmd name from webinar.md §section subtopic)
3. (Optional) twentythree webinar speaker add <webinar_id> … --json  (exact cmd name from webinar.md §speaker subtopic)
4. (Optional) twentythree webinar upload-image <webinar_id> ./thumb.jpg --type thumbnail --json
5. twentythree webinar update <webinar_id> --publish --json
6. twentythree webinar room connect <webinar_id> --json → capture stream key + room URL
7. (Optional, once live) twentythree webinar recording start <webinar_id> --json
8. (After session ends) twentythree webinar recording stop <webinar_id> --json
9. twentythree webinar recording status <webinar_id> --json  AND  twentythree webinar clips <webinar_id> --json
10. (Optional) twentythree webinar update <webinar_id> --status previous --json
-->

<!-- Error handling hints (from RESEARCH.md):
upload-and-publish:
  - Upload fails → `twentythree auth status` and `twentythree doctor`
  - Transcoding stuck → retry after 30s; contact support if >10 min
  - Publish permission error → confirm write scope (not read)

webinar-lifecycle:
  - room connect fails → confirm status is live/upcoming (not previous)
  - recording start fails → confirm write scope; check `recording status` first
  - clips empty → processing takes time after `recording stop`; poll `webinar clips`
-->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create workflows directory and write upload-and-publish.md (6-step video workflow)</name>
  <files>
    packages/twentythree-skills/skills/workflows/upload-and-publish.md
  </files>
  <read_first>
    packages/twentythree-skills/skills/SKILL.md,
    packages/twentythree-skills/skills/reference/video.md,
    packages/twentythree-skills/skills/reference/category.md,
    .planning/phases/19-skill-content/19-PATTERNS.md,
    .planning/phases/19-skill-content/19-RESEARCH.md
  </read_first>
  <action>
Before writing, confirm the `packages/twentythree-skills/skills/workflows/` directory exists; create it if missing:

```bash
mkdir -p packages/twentythree-skills/skills/workflows
```

Then also re-verify the upload command flags are still current by running `twentythree video upload --agent` and `twentythree video update --agent` — if anything has drifted from reference/video.md, prefer the live `--agent` output.

Create `packages/twentythree-skills/skills/workflows/upload-and-publish.md`. Structure:

**Frontmatter**:
```
---
name: upload-and-publish
description: Upload a video file, set metadata, and publish it on TwentyThree.
---
```

**Opening**:
- H1: `# Workflow: Upload and Publish a Video`
- Opening blockquote:
  > Complete sequence for uploading a video file, setting metadata, and making it publicly visible.
  > All commands use `--json` — capture IDs from the `data.id` field of each response.
- Cross-reference note (immediately after blockquote):
  > See [`reference/video.md`](../reference/video.md) for exhaustive flag details and [`reference/category.md`](../reference/category.md) for category selection.

**Prerequisites**:
```
## Prerequisites

- Auth scope required: **write**
- Run `twentythree auth credentials` if not already configured.
- Confirm auth: `twentythree auth status --json`
- Chunked upload is automatic — `twentythree video upload` handles chunking internally (see `reference/video.md` §video upload).
```

**Steps** — each step MUST follow this template precisely. Include ALL 6 steps from the Interfaces sequence:

**Step 1: (Optional) List categories to find a target category ID**

```bash
twentythree category list --json
```

Expected output shape: `{ data: [ { id, title, description, hidden }, ... ] }`
Capture: `data[n].id` of the desired row as `category_id`
On failure:
  - `401 Unauthorized` → run `twentythree auth status --json` to confirm credentials
  - Empty list → confirm workspace has categories; create one with `twentythree category create --title "…" --json`

**Step 2: Upload the video**

```bash
twentythree video upload ./video.mp4 --title "Product Demo" --category-id <category_id> --json
```

Expected output shape: `{ data: { id, admin_url } }`
Capture:
  - `data.id` as `video_id`
  - `data.admin_url` as `admin_url` (surface this to the user; the memory rule says: always print ID + admin URL after upload)
On failure:
  - Network error or `401` → run `twentythree auth status` and `twentythree doctor`
  - Upload stalls → retry; chunked upload resumes automatically (see `reference/video.md` for `--chunk-size` / `--concurrency` tuning)
  - `413 Payload Too Large` → the file exceeds workspace quota; check `twentythree site get --include-quota --json`

**Step 3: (Optional) Set additional metadata**

```bash
twentythree video update <video_id> --description "Launch video for Q2 release" --tags "product q2 launch" --json
```

Expected output shape: `{ data: { id, updated_at, ... } }`
Capture: none (fire-and-forget update)
On failure:
  - `403 Forbidden` → confirm auth scope is `write`, not `read`
  - `404 Not Found` → confirm `video_id` is correct and video exists (`twentythree video get <video_id> --json`)

**Step 4: (Optional) Set a custom thumbnail frame**

```bash
twentythree video frame <video_id> --time 10 --json
```

Expected output shape: `{ data: { id, thumbnail_url } }` (or similar — confirm via `--agent`)
Capture: none
On failure:
  - `400 Invalid Time` → ensure `--time` is less than video duration (check via `twentythree video get <video_id> --json`)

**Step 5: (Optional) Wait for transcoding to complete**

```bash
twentythree video transcoding-progress <video_id> --json
```

Expected output shape: `{ data: { status, progress } }`
Capture: `data.status` — must be `complete` before publishing
Polling pattern:
```bash
while true; do
  STATUS=$(twentythree video transcoding-progress <video_id> --json | jq -r '.data.status')
  if [ "$STATUS" = "complete" ]; then break; fi
  sleep 30
done
```
On failure:
  - Status stuck at `processing` for >10 minutes → contact support; do NOT publish
  - Status `failed` → check the video file; re-upload if corrupt

**Step 6: Publish**

```bash
twentythree video update <video_id> --publish --json
```

Expected output shape: `{ data: { id, published: true } }`
Capture: confirm `data.published` is `true`
On failure:
  - `403 Forbidden` → confirm auth scope is `write`
  - Publish fails silently → run `twentythree video get <video_id> --json` and check `published` field

**Error Handling** (summary table at the bottom — reinforces per-step notes):

```markdown
## Error Handling

| Step | Failure | What to check |
|------|---------|---------------|
| 1 | Empty category list | `twentythree category create --title "…" --json` to create one |
| 2 | Upload auth/network error | `twentythree auth status --json`, `twentythree doctor` |
| 2 | Quota exceeded | `twentythree site get --include-quota --json` |
| 3 | 403 Forbidden | Confirm auth scope is `write` |
| 4 | Invalid time | `--time` must be less than duration from `video get` |
| 5 | Transcoding stuck | Poll `transcoding-progress` after 30s; support after 10 min |
| 6 | Publish fails silently | `video get <id> --json` to check `published` field |
```

**Final note section**:
```
## Notes

- All 6 steps can be scripted in sequence; only Steps 2 and 6 are strictly required (1, 3, 4, 5 are optional).
- For a minimal end-to-end sequence: Step 2 (upload with `--publish` directly) replaces Steps 2+6:
  `twentythree video upload ./video.mp4 --title "Demo" --publish --json`
  This is valid but skips the optional metadata, frame, and transcoding-check steps.
```

Target line count: >=120.
  </action>
  <verify>
    <automated>test -d packages/twentythree-skills/skills/workflows && test -f packages/twentythree-skills/skills/workflows/upload-and-publish.md && grep -q "^name: upload-and-publish$" packages/twentythree-skills/skills/workflows/upload-and-publish.md && grep -q "video upload" packages/twentythree-skills/skills/workflows/upload-and-publish.md && grep -q "transcoding-progress" packages/twentythree-skills/skills/workflows/upload-and-publish.md && grep -q "video update" packages/twentythree-skills/skills/workflows/upload-and-publish.md && grep -q "publish" packages/twentythree-skills/skills/workflows/upload-and-publish.md && grep -q "Error Handling" packages/twentythree-skills/skills/workflows/upload-and-publish.md && test "$(wc -l < packages/twentythree-skills/skills/workflows/upload-and-publish.md)" -ge 120</automated>
  </verify>
  <acceptance_criteria>
    - `packages/twentythree-skills/skills/workflows/` directory exists
    - `packages/twentythree-skills/skills/workflows/upload-and-publish.md` exists with `^name: upload-and-publish$` frontmatter (anchored line) and `^description: ` line
    - Cross-reference to `reference/video.md` present (grep: `reference/video.md`)
    - Cross-reference to `reference/category.md` present (grep: `reference/category.md`)
    - File contains `## Prerequisites`, `## Steps`, `## Error Handling` sections (grep each)
    - Prerequisites section states `write` auth scope (grep: `**write**` or `write` in auth context)
    - File contains all 6 step headings: `### 1.`, `### 2.`, `### 3.`, `### 4.`, `### 5.`, `### 6.` (or `### Step 1`, etc. — just `grep -c "^### "` must be >= 6)
    - Every step includes the words `Expected output shape` (grep count: `grep -c "Expected output shape" ...` must be >= 4, i.e. present on majority of steps)
    - Every step that creates/captures data includes `Capture:` line (grep count: `grep -c "^Capture:" ...` must be >= 2 — Steps 1 and 2 minimum)
    - Every step includes `On failure:` guidance (grep count: `grep -c "^On failure:" ...` must be >= 4)
    - File contains `admin_url` (grep match) — Step 2 must surface admin URL capture
    - File contains `transcoding-progress` command (grep match) — Step 5
    - Error Handling table is a markdown table (contains `|---|` separator)
    - File does not contain `npx`
    - File line count >= 120
    - All fenced bash blocks use `--json` (except any demo of `auth status`, which also uses `--json`) — spot-check with `grep -B 0 -A 2 '```bash' packages/twentythree-skills/skills/workflows/upload-and-publish.md | grep -c "json"` must be close to the number of bash blocks
  </acceptance_criteria>
  <done>upload-and-publish.md exists with 6-step video workflow, every step specifies exact command, expected output, capture field, and error guidance. Error Handling table at bottom. Cross-references to reference files. SKILL-03 half-satisfied (1 of 2 workflows).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Write webinar-lifecycle.md (10-step webinar workflow) and final validator run</name>
  <files>
    packages/twentythree-skills/skills/workflows/webinar-lifecycle.md
  </files>
  <read_first>
    packages/twentythree-skills/skills/SKILL.md,
    packages/twentythree-skills/skills/workflows/upload-and-publish.md,
    packages/twentythree-skills/skills/reference/webinar.md,
    .planning/phases/19-skill-content/19-PATTERNS.md,
    .planning/phases/19-skill-content/19-RESEARCH.md
  </read_first>
  <action>
Read `packages/twentythree-skills/skills/workflows/upload-and-publish.md` (from Task 1) first to match exact style.

Read `packages/twentythree-skills/skills/reference/webinar.md` (from Plan 05) to copy exact command signatures — every webinar command name + flag used in this workflow must already appear in webinar.md. If webinar.md uses a different exact command name (e.g. `webinar section add` vs `webinar section create`), use what webinar.md uses.

Before writing, re-verify key commands via CLI if unclear:
- `twentythree webinar create --agent`
- `twentythree webinar section --help` (confirm add-or-create verb)
- `twentythree webinar speaker --help` (confirm add verb)
- `twentythree webinar room connect --agent`
- `twentythree webinar recording start --agent`, `stop --agent`, `status --agent`

Create `packages/twentythree-skills/skills/workflows/webinar-lifecycle.md`. Structure:

**Frontmatter**:
```
---
name: webinar-lifecycle
description: Create, configure, run, record, and archive a live webinar end-to-end.
---
```

**Opening**:
- H1: `# Workflow: Webinar Lifecycle`
- Opening blockquote:
  > Complete sequence from creating a webinar through archiving it after the session ends.
  > All commands use `--json` — capture IDs from the `data.id` field of each response.
  > CLI `webinar` maps to API `live` — see `reference/webinar.md` §Terminology Notes.
- Cross-reference note:
  > See [`reference/webinar.md`](../reference/webinar.md) for exhaustive flag details on every command referenced below.

**Prerequisites**:
```
## Prerequisites

- Auth scope required: **write** (create, update, upload-image, recording, publishing)
  - Some read-only steps (room connect, clips) accept `read` scope
- Run `twentythree auth credentials` if not already configured.
- Confirm auth: `twentythree auth status --json`
- NOTE: There is no `webinar get` command — to retrieve webinar details, use `webinar list --search "<title>" --json` (see `reference/webinar.md`).
```

**Steps** — 10 steps. Use the same template as upload-and-publish.md (command block → Expected output shape → Capture → On failure):

**Step 1: Create the webinar**

```bash
twentythree webinar create --title "Q2 Town Hall" --live-date "2026-05-15T16:00:00Z" --description "Quarterly update" --json
```

Expected output shape: `{ data: { id, admin_url } }`
Capture:
  - `data.id` as `webinar_id`
  - `data.admin_url` as `admin_url` (surface to the user)
On failure:
  - `400 Invalid date` → `--live-date` must be ISO 8601 (e.g. `2026-05-15T16:00:00Z`)
  - `401 Unauthorized` → run `twentythree auth status --json`

**Step 2: (Optional) Add agenda sections**

```bash
# Use the exact command name from webinar.md §Subtopic: webinar section
# Example uses "add" — if webinar.md shows "create" instead, substitute accordingly:
twentythree webinar section add <webinar_id> --title "Intro" --start-time 0 --json
twentythree webinar section add <webinar_id> --title "Q&A" --start-time 1800 --json
```

Expected output shape: `{ data: { id, title, start_time } }`
Capture: `data.id` as `section_id` if you intend to update/delete later; otherwise none
On failure:
  - `404 Not Found` → confirm `webinar_id` exists
  - Validation error → confirm `--start-time` is in seconds, not mm:ss

**Step 3: (Optional) Add speakers**

```bash
# Exact flags: see webinar.md §Subtopic: webinar speaker — consult before running
twentythree webinar speaker add <webinar_id> --name "Jane Doe" --role "Host" --json
```

Expected output shape: `{ data: { id, name, role } }`
Capture: `data.id` as `speaker_id` if you need to update later
On failure:
  - Missing required flag → run `twentythree webinar speaker add --agent` to see complete flag list

**Step 4: (Optional) Upload a thumbnail image**

```bash
twentythree webinar upload-image <webinar_id> ./thumb.jpg --type thumbnail --json
```

Expected output shape: `{ data: { id, image_url } }`
Capture: none (image is attached to the webinar)
On failure:
  - `413 Payload Too Large` → resize image; chunked upload is automatic but size limits still apply

**Step 5: Publish the webinar (make visible to registrants)**

```bash
twentythree webinar update <webinar_id> --publish --json
```

Expected output shape: `{ data: { id, published: true } }`
Capture: confirm `data.published` is `true`
On failure:
  - `403 Forbidden` → auth scope `write` required

**Step 6: Get room connection info (when going live)**

```bash
twentythree webinar room connect <webinar_id> --json
```

Expected output shape: `{ data: { stream_key, room_url } }` (confirm exact field names via `webinar room connect --agent`)
Capture:
  - `data.stream_key` as `stream_key` — pass to your broadcaster
  - `data.room_url` as `room_url` — pass to moderators
On failure:
  - Webinar status is `previous` → room connection unavailable; use `webinar update --status upcoming` to re-open
  - `404` → confirm `webinar_id` is correct

**Step 7: Start recording (optional, once the live session is active)**

```bash
twentythree webinar recording start <webinar_id> --json
```

Expected output shape: `{ data: { recording_id, status: "recording" } }`
Capture: `data.recording_id` if the API returns one; otherwise none (recording is attached to the webinar)
On failure:
  - `409 Conflict` → recording already in progress; run `webinar recording status <webinar_id> --json` to check
  - `403 Forbidden` → auth scope `write` required

**Step 8: Stop recording (after session ends)**

```bash
twentythree webinar recording stop <webinar_id> --json
```

Expected output shape: `{ data: { recording_id, status: "stopped" } }`
Capture: none
On failure:
  - `409` → no recording in progress; confirm with `recording status`

**Step 9: Check recording status and clip availability**

```bash
twentythree webinar recording status <webinar_id> --json
twentythree webinar clips <webinar_id> --json
```

Expected output shape: `{ data: { status, ... } }` and `{ data: [ { clip_id, url, ... } ] }`
Capture:
  - `recording status` `data.status` — should be `processing` immediately after stop, `complete` once clips are ready
  - `webinar clips` array — each element is a clip. If empty, recording is still processing
On failure:
  - Clips array empty > 10 minutes after `stop` → contact support
  - Polling pattern:
    ```bash
    while true; do
      CLIPS=$(twentythree webinar clips <webinar_id> --json | jq '.data | length')
      if [ "$CLIPS" -gt 0 ]; then break; fi
      sleep 60
    done
    ```

**Step 10: (Optional) Archive — mark as previous**

```bash
twentythree webinar update <webinar_id> --status previous --json
```

Expected output shape: `{ data: { id, status: "previous" } }`
Capture: none
On failure:
  - `400` → check current status; some transitions may be blocked (e.g. `live` → `previous` may require `recording stop` first)

**Error Handling summary table**:

```markdown
## Error Handling

| Step | Failure | What to check |
|------|---------|---------------|
| 1 | Invalid date | `--live-date` must be ISO 8601 (e.g. `2026-05-15T16:00:00Z`) |
| 2-3 | Missing required flags | Run `twentythree webinar <subtopic> <cmd> --agent` to enumerate |
| 5 | Publish 403 | Confirm auth scope is `write` |
| 6 | Room connection blocked | Webinar status must be `upcoming` or `live`, not `previous` |
| 7 | Recording 409 | Already recording; `webinar recording status` first |
| 8 | Stop 409 | No recording in progress |
| 9 | Clips empty | Processing can take 10+ minutes; poll every 60s |
| 10 | Status transition blocked | `recording stop` must be called before archiving |
```

**Final note section**:
```
## Notes

- Steps 2-4 (sections, speakers, thumbnail) are optional but improve viewer experience.
- Step 6 (`room connect`) returns credentials for a third-party broadcaster; agents should NOT try to stream content themselves — pass credentials to the user.
- Steps 7-8 (recording start/stop) are optional. If you skip them, no clips will be generated.
- Step 9's polling loop can be replaced by subscribing to the `recording.completed` webhook event (see `reference/webhook.md`).
```

**Final verification step (inside this task)**:
After writing, run:
```bash
pnpm --filter twentythree-skills test
```
Confirm exit 0 (Gate 2 should already pass from Plan 05; this task doesn't change reference files so validator state should be unchanged).

Also confirm the workflows directory is populated:
```bash
ls packages/twentythree-skills/skills/workflows/
# Must list exactly: upload-and-publish.md  webinar-lifecycle.md
```

Target line count: >=150.
  </action>
  <verify>
    <automated>test -f packages/twentythree-skills/skills/workflows/webinar-lifecycle.md && grep -q "^name: webinar-lifecycle$" packages/twentythree-skills/skills/workflows/webinar-lifecycle.md && grep -q "webinar create" packages/twentythree-skills/skills/workflows/webinar-lifecycle.md && grep -q "webinar room connect" packages/twentythree-skills/skills/workflows/webinar-lifecycle.md && grep -q "webinar recording start" packages/twentythree-skills/skills/workflows/webinar-lifecycle.md && grep -q "webinar recording stop" packages/twentythree-skills/skills/workflows/webinar-lifecycle.md && grep -q "webinar clips" packages/twentythree-skills/skills/workflows/webinar-lifecycle.md && grep -q "## Error Handling" packages/twentythree-skills/skills/workflows/webinar-lifecycle.md && test "$(wc -l < packages/twentythree-skills/skills/workflows/webinar-lifecycle.md)" -ge 150 && cd packages/twentythree-skills && node scripts/validate-skills.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `packages/twentythree-skills/skills/workflows/webinar-lifecycle.md` exists with `^name: webinar-lifecycle$` frontmatter (anchored) and `^description: ` line
    - Cross-reference to `reference/webinar.md` present (grep: `reference/webinar.md`)
    - Mention of "no `webinar get`" somewhere in the Prerequisites or early notes (grep: `webinar get`)
    - File contains `## Prerequisites`, `## Steps`, `## Error Handling` sections
    - Prerequisites states `write` auth scope required
    - File contains all 10 step headings — `grep -c "^### " packages/twentythree-skills/skills/workflows/webinar-lifecycle.md` must be >= 10
    - Step 1 (`webinar create`) present AND captures `admin_url` (grep: `admin_url`)
    - Step 6 (`webinar room connect`) present AND captures `stream_key` and `room_url` (grep both)
    - Step 7 (`webinar recording start`) and Step 8 (`webinar recording stop`) both present
    - Step 9 includes polling loop pattern (grep: `while true` or `CLIPS=` or similar polling idiom)
    - Step 10 (`webinar update --status previous`) present
    - Every step includes `Expected output shape` — grep count `grep -c "Expected output shape" ...` >= 8 (allow some steps to vary slightly)
    - Every step includes `On failure:` — grep count `grep -c "^On failure:" ...` >= 8
    - Error Handling table is a markdown table (contains `|---|` separator) with rows covering at least Steps 1, 6, 7, 8, 9, 10
    - File does not contain `npx`
    - File line count >= 150
    - `ls packages/twentythree-skills/skills/workflows/` returns exactly two files: `upload-and-publish.md`, `webinar-lifecycle.md`
    - `pnpm --filter twentythree-skills test` exits 0 (validator state unchanged from Plan 05; this plan only adds workflow files which the validator does not check)
  </acceptance_criteria>
  <done>webinar-lifecycle.md exists with 10-step webinar workflow, every step specifies exact command, expected output, capture field, and error guidance. Error Handling table at bottom. Polling pattern for clip availability included. SKILL-03 fully satisfied (2 of 2 workflows). Phase 19 content complete: 22 reference files + 2 workflows.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Markdown content phase — no runtime trust boundaries.

| Boundary | Description |
|----------|-------------|
| (none) | Static markdown |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-06-01 | Information Disclosure | Workflow files show webinar stream keys and admin URLs as capture targets | accept | Examples use placeholder IDs (`<webinar_id>`) and describe capture targets — no real keys or URLs appear in the markdown. Runtime enforces auth. |
| T-19-06-02 | Tampering | Workflow instructs agent to publish/archive webinars based on copied commands | accept | Doc cannot grant scope it does not have at runtime; API enforces `write` auth. Placeholder `webinar_id` forces the agent to produce one from a prior step, not fabricate. |
| T-19-06-03 | Elevation of Privilege | Agent follows workflow with elevated auth token | accept | Out of scope for doc — token scope is controlled at the `twentythree auth credentials` step outside these files. |
| T-19-06-04 | Denial of Service | Agent runs Step 10 (archive) prematurely, ending live session | mitigate | Step 10 is flagged `(Optional)`; Error Handling row for Step 10 explicitly notes `recording stop` must precede archiving. |
</threat_model>

<verification>
After all 2 tasks complete:

```bash
# Directory exists with both files
ls packages/twentythree-skills/skills/workflows/
# Expected: upload-and-publish.md  webinar-lifecycle.md

# Content sanity
wc -l packages/twentythree-skills/skills/workflows/*.md
grep -c "^### " packages/twentythree-skills/skills/workflows/upload-and-publish.md  # >= 6
grep -c "^### " packages/twentythree-skills/skills/workflows/webinar-lifecycle.md   # >= 10
grep -l "npx" packages/twentythree-skills/skills/workflows/*.md  # empty

# Validator still exits 0 (no regression from Plan 05)
pnpm --filter twentythree-skills test

# End-of-phase count: 22 reference + 2 workflow = 24 content files
ls packages/twentythree-skills/skills/reference/ | wc -l   # 22
ls packages/twentythree-skills/skills/workflows/ | wc -l   # 2
```
</verification>

<success_criteria>
- `packages/twentythree-skills/skills/workflows/` directory exists
- `upload-and-publish.md` created with frontmatter `name: upload-and-publish`
- 6 workflow steps documented, every step has: command + Expected output shape + Capture field + On failure guidance
- upload-and-publish.md captures `admin_url` from video upload response
- upload-and-publish.md Error Handling section present
- upload-and-publish.md line count >= 120
- `webinar-lifecycle.md` created with frontmatter `name: webinar-lifecycle`
- 10 workflow steps documented, same template as upload-and-publish
- webinar-lifecycle.md captures `admin_url` from webinar create response
- webinar-lifecycle.md captures `stream_key` and `room_url` from `webinar room connect`
- webinar-lifecycle.md Step 9 includes polling pattern for clip availability
- webinar-lifecycle.md Error Handling section present
- webinar-lifecycle.md line count >= 150
- No file contains `npx`
- `pnpm --filter twentythree-skills test` exits 0 (Gate 2 unchanged — still passes from Plan 05)
- SKILL-03 satisfied: exactly 2 workflow files present in `skills/workflows/`
- Phase 19 content complete: 22 reference files + 2 workflow files = 24 files
</success_criteria>

<output>
After completion, create `.planning/phases/19-skill-content/19-06-SUMMARY.md` following the summary template.
</output>
