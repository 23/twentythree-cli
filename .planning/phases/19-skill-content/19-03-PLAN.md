---
phase: 19
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/twentythree-skills/skills/reference/analytics.md
  - packages/twentythree-skills/skills/reference/category.md
  - packages/twentythree-skills/skills/reference/thumbnail.md
  - packages/twentythree-skills/skills/reference/user.md
autonomous: true
requirements: [SKILL-02]
tags: [skills, documentation, markdown, reference, analytics, admin]

must_haves:
  truths:
    - "An agent reading analytics.md can run any of the 4 analytics subtopics (video, live, conversions, usage) with correct flags"
    - "An agent reading category.md knows category has no `get` command and that list is anonymous"
    - "An agent reading thumbnail.md understands the Liquid template workflow and can use thumbnail data to preview render variables"
    - "An agent reading user.md sees the security warning about --password visibility before attempting it"
    - "validate-skills.mjs does not fail because of Plan 03 files"
  artifacts:
    - path: "packages/twentythree-skills/skills/reference/analytics.md"
      provides: "All 4 analytics subtopics (video, live, conversions, usage) with common flag pattern"
      contains: "name: analytics"
    - path: "packages/twentythree-skills/skills/reference/category.md"
      provides: "All 4 category commands (no get) with terminology note"
      contains: "name: category"
    - path: "packages/twentythree-skills/skills/reference/thumbnail.md"
      provides: "All 6 thumbnail commands + thumbnail file subtopic"
      contains: "name: thumbnail"
    - path: "packages/twentythree-skills/skills/reference/user.md"
      provides: "All 9 user commands with admin-scope warning + password security warning"
      contains: "name: user"
  key_links:
    - from: "workflows/upload-and-publish.md (Plan 06)"
      to: "reference/category.md"
      via: "Step 1 points readers to category reference for --category-id lookup details"
      pattern: "cross-reference category list from upload workflow"
    - from: "reference/user.md --password warning"
      to: "user update command flags"
      via: "inline warning block"
      pattern: "password visible in process lists and shell history"
---

<objective>
Write 4 reference files for analytics (4 subtopics), category, thumbnail (+ file subtopic), and user (admin-only). This plan handles the higher-complexity refs that are NOT video/webinar. Per D-03 consistency: full comprehensive depth for all.

Purpose: SKILL-02 subset. These cover reporting (analytics), content organisation (category), video presentation (thumbnail), and workspace administration (user — the only admin-scope topic in the CLI).

Output: 4 markdown files in `packages/twentythree-skills/skills/reference/`.

Special handling:
- analytics.md — multi-subtopic structure with a shared common-flag pattern block
- category.md — REQUIRES Terminology Notes section (CLI category to API album)
- user.md — REQUIRES inline security warning for --password (ASVS-adjacent content concern)
- thumbnail.md — gap A6 applies: run `twentythree thumbnail file --help` and `--agent` for sub-commands before writing
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@packages/twentythree-skills/skills/SKILL.md
@packages/twentythree-skills/scripts/validate-skills.mjs
@.planning/phases/19-skill-content/19-CONTEXT.md
@.planning/phases/19-skill-content/19-RESEARCH.md
@.planning/phases/19-skill-content/19-PATTERNS.md

<interfaces>
Standard document structure (same as Plans 01-02) — see 19-PATTERNS.md.

Additional: Terminology Notes pattern (for category.md) — from 19-PATTERNS.md.
Example:
```
## Terminology Notes

CLI `category` = API `album`. The `api_endpoint` field in `--agent` output uses the API name (e.g. `GET /album/list`).
```

Additional: Security warning pattern (for user.md) — from 19-PATTERNS.md.
Inline after `### user update` flag table:
```
> **Security:** `--password` is visible in process lists and shell history.
> Prefer the admin UI for password changes in sensitive environments.
```

Gap notices (from 19-RESEARCH.md Assumptions Log):
- A4: `user delete` and `user redeem-login-token` flags NOT in research doc — MUST run `twentythree user delete --agent` and `twentythree user redeem-login-token --agent` before writing those sections
- A6: `thumbnail file delete` flags NOT in research doc — MUST run `twentythree thumbnail file --help` and `twentythree thumbnail file delete --agent`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Write analytics.md (4 subtopics, shared common-flag pattern)</name>
  <files>
    packages/twentythree-skills/skills/reference/analytics.md
  </files>
  <read_first>
    packages/twentythree-skills/skills/SKILL.md,
    .planning/phases/19-skill-content/19-PATTERNS.md,
    .planning/phases/19-skill-content/19-RESEARCH.md
  </read_first>
  <action>
Run these CLI calls BEFORE writing (sample the shared flag surface):
- `twentythree analytics --help`
- `twentythree analytics video --help` then `--agent` for at least: totals, timeseries, performance, published, weekday
- `twentythree analytics live --help` then `--agent` for at least: totals, timeseries, event, event-timeseries, event-totals, weekday
- `twentythree analytics conversions --help` then `--agent` for: totals, timeseries
- `twentythree analytics usage --help` then `--agent` for at least: devices, domains, locations, sources, sourceids, spots, storage, traffic

Because analytics subtopics share a common flag pattern, the reference file uses a shared-flags block rather than repeating the same flag table for every command. This is a deliberate deviation from the per-command flag table pattern in other refs — document the reason explicitly.

Structure for **analytics.md**:
- Frontmatter: `name: analytics`, `description: Pull reporting data from TwentyThree — video, live, conversions, and usage metrics.`
- `# TwentyThree Analytics Commands`
- Opening blockquote: `--json` reminder + note that all analytics commands are read-only.
- `## Prerequisites` — auth scope: read (all analytics commands). Include `--agent` reminder.
- `## Shared Flag Pattern` section documenting the common flags applicable to all analytics commands. Include a single table with at least these rows:
  - `--date-start <YYYY-MM-DD>` — first date
  - `--date-end <YYYY-MM-DD>` — last date
  - `--date-expression <value>` — predefined range (e.g. `thisweek`, `lastyear`, `thismonth`, `lastmonth`)
  - `--groupby <dimension>` — group results
  - `--orderby <field>` — sort field
  - `--order <asc|desc>` — sort direction
  - `--page <n>` / `--size <n>` — pagination (where applicable)
  - `--selection <value>` — scope to specific objects/types
  Include a note: "For the exact flag set per subcommand (some add command-specific flags), run `twentythree analytics <subtopic> <cmd> --agent`."
- `## Commands` with 4 subsections, one per subtopic:
  - `### analytics video` — with 5 commands as nested `#### analytics video <cmd>` headings, each with 1 example using `--json`. Commands: totals, timeseries, performance, published, weekday.
  - `### analytics live` — with 6 commands: totals, timeseries, event, event-timeseries, event-totals, weekday. Each with 1 example.
  - `### analytics conversions` — with 2 commands: totals, timeseries. Each with 1 example.
  - `### analytics usage` — with 8 commands: devices, domains, locations, sources, sourceids, spots, storage, traffic. Each with 1 example.
- `## Common Patterns` — include examples:
  1. This month's video totals: `analytics video totals --date-expression thismonth --json`
  2. Last-period traffic by device: `analytics usage devices --date-expression lastmonth --orderby plays --order desc --json`
  3. Time-series for a specific live event: `analytics live event-timeseries --date-start 2026-04-01 --date-end 2026-04-30 --selection <live-id> --json`
  4. Storage usage report: `analytics usage storage --json`
- NO Terminology Notes section.

Target length: analytics.md ~140-180 lines (covers 21 commands across 4 subtopics — the shared-flag pattern keeps it readable).
  </action>
  <verify>
    <automated>test -f packages/twentythree-skills/skills/reference/analytics.md && grep -q "^name: analytics$" packages/twentythree-skills/skills/reference/analytics.md && grep -q "### analytics video" packages/twentythree-skills/skills/reference/analytics.md && grep -q "### analytics live" packages/twentythree-skills/skills/reference/analytics.md && grep -q "### analytics conversions" packages/twentythree-skills/skills/reference/analytics.md && grep -q "### analytics usage" packages/twentythree-skills/skills/reference/analytics.md</automated>
  </verify>
  <acceptance_criteria>
    - `packages/twentythree-skills/skills/reference/analytics.md` exists
    - File contains `^name: analytics$` and `^description: ` frontmatter
    - Contains `## Prerequisites`, `## Shared Flag Pattern`, `## Commands`, `## Common Patterns` sections
    - Contains all 4 subtopic headings: `### analytics video`, `### analytics live`, `### analytics conversions`, `### analytics usage`
    - Shared Flag Pattern table documents at least these flags: `--date-start`, `--date-end`, `--date-expression`, `--groupby`, `--orderby`, `--order`
    - analytics video section references at least 5 commands by name (totals, timeseries, performance, published, weekday)
    - analytics usage section references at least 8 commands by name (devices, domains, locations, sources, sourceids, spots, storage, traffic)
    - File contains the literal string `thismonth` AND `lastmonth` as example `--date-expression` values
    - analytics.md line count >= 130 (`wc -l`)
    - File does not contain `npx`
  </acceptance_criteria>
  <done>analytics.md covers all 4 subtopics with shared-flag pattern, correctly reflecting 21+ commands without per-command flag-table bloat.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Write category.md, thumbnail.md (CRUD + template; includes A6 gap closure)</name>
  <files>
    packages/twentythree-skills/skills/reference/category.md,
    packages/twentythree-skills/skills/reference/thumbnail.md
  </files>
  <read_first>
    packages/twentythree-skills/skills/SKILL.md,
    packages/twentythree-skills/skills/reference/analytics.md,
    .planning/phases/19-skill-content/19-PATTERNS.md,
    .planning/phases/19-skill-content/19-RESEARCH.md
  </read_first>
  <action>
Run these CLI calls BEFORE writing:
- `twentythree category --help` then `--agent` for: list, create, update, delete (NOTE: there is NO `category get` command — Pitfall 6)
- `twentythree thumbnail --help` then `--agent` for: list, add, update, delete, duplicate, data
- **A6 gap**: `twentythree thumbnail file --help` and `twentythree thumbnail file delete --agent` — flags NOT in research doc, must come from live CLI

**category.md**:
- Frontmatter: `name: category`, `description: Organise videos and webinars into collections (categories). API name: album.`
- `# TwentyThree Category Commands`
- Opening blockquote with `--json` reminder + note: "Category is the CLI name; the API uses `album`. No `category get` command exists — use `category list` with a search term or ID filter to retrieve details."
- `## Prerequisites` — auth scope: anonymous (list), write (create, update, delete). Explicitly call out: "Category `list` does not require auth — it works before running `twentythree auth credentials`. All other category commands require a `write` token." Include `--agent` reminder.
- `## Commands` — 4 subsections (NO `### category get`): `### category list`, `### category create`, `### category update`, `### category delete`. Each with auth/side-effects/output, flag table, 2 examples with `--json`.
- Include an inline warning under `### category create`: "The API returns `output_shape: none` for category create — no ID is returned in the response. To find the newly created category, re-run `category list --json` and filter by title."
- `## Common Patterns` — example: creating a category then looking it up: `category create --title "Q2 Demos" --description "..." --json` followed by `category list --search "Q2 Demos" --json`; listing hidden categories: `category list --include-hidden --json`.
- `## Terminology Notes` (REQUIRED per 19-PATTERNS.md "Applies to" list):
  Paragraphs stating: CLI `category` = API `album`. The `api_endpoint` field in `--agent` output uses the API name (e.g. `GET /album/list`, `POST /album/create`). When debugging API responses or reading the raw OpenAPI spec, look for `/album/*` paths.

**thumbnail.md**:
- Frontmatter: `name: thumbnail`, `description: Manage thumbnail templates (Liquid-based) used to render video thumbnails with metadata variables.`
- `# TwentyThree Thumbnail Commands`
- Opening blockquote with `--json` reminder
- `## Prerequisites` — auth scope: read (list, data), write (add, update, delete, duplicate). Include `--agent` reminder. Note: "Thumbnail templates use Liquid templating with video metadata variables. Use `thumbnail data` to preview the render variables before authoring a template."
- `## Commands` — 6 top-level subsections: `### thumbnail list`, `### thumbnail add`, `### thumbnail update`, `### thumbnail delete`, `### thumbnail duplicate`, `### thumbnail data`. Each with auth/side-effects/output, flag table, 2 examples with `--json`.
- `## Subtopic: thumbnail file` — include at least one command (`### thumbnail file delete`) documented from the live `--agent` call (A6 gap closure). If additional `thumbnail file` subcommands exist per `--help`, document each with the same structure (auth/side-effects/output, flag table, 2 examples).
- `## Common Patterns` — include examples:
  1. Authoring a template: `thumbnail data <template-id> --object-id <video-id> --json` to preview render data, then `thumbnail add --name "Brand" --liquid-template "<html>...{{ photo.title }}..." --json`
  2. Duplicating an existing template as a starting point: `thumbnail duplicate <id> --name "Brand v2" --json`
  3. Listing templates scoped to object type: `thumbnail list --object-type photo --json`
- NO Terminology Notes section.

Target length: category.md ~80-100 lines, thumbnail.md ~100-130 lines.
  </action>
  <verify>
    <automated>test -f packages/twentythree-skills/skills/reference/category.md && test -f packages/twentythree-skills/skills/reference/thumbnail.md && grep -q "^name: category$" packages/twentythree-skills/skills/reference/category.md && grep -q "^name: thumbnail$" packages/twentythree-skills/skills/reference/thumbnail.md && grep -q "## Terminology Notes" packages/twentythree-skills/skills/reference/category.md && grep -q "### thumbnail file delete" packages/twentythree-skills/skills/reference/thumbnail.md</automated>
  </verify>
  <acceptance_criteria>
    - `packages/twentythree-skills/skills/reference/category.md` exists with `^name: category$` frontmatter
    - `packages/twentythree-skills/skills/reference/thumbnail.md` exists with `^name: thumbnail$` frontmatter
    - category.md contains 4 command headings: `### category list`, `### category create`, `### category update`, `### category delete`
    - category.md does NOT contain `### category get` (Pitfall 6 — this command does not exist); grep `### category get` MUST return no matches
    - category.md contains `## Terminology Notes` section mentioning `album`
    - category.md warns that `category create` returns no ID (grep: `output_shape: none` OR `no ID is returned`)
    - thumbnail.md contains all 6 top-level command headings: `### thumbnail list`, `### thumbnail add`, `### thumbnail update`, `### thumbnail delete`, `### thumbnail duplicate`, `### thumbnail data`
    - thumbnail.md contains `### thumbnail file delete` (A6 gap closed — flags must be sourced from live `--agent`)
    - thumbnail.md mentions Liquid templating (grep: `Liquid`)
    - category.md line count >= 80
    - thumbnail.md line count >= 95
    - No file contains `npx`
  </acceptance_criteria>
  <done>category.md has correct 4-command coverage + terminology notes. thumbnail.md closes A6 gap with live --agent data for thumbnail file delete. No pitfall 6 (no fake `category get`).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Write user.md (admin-only; includes A4 gap closure + security warning)</name>
  <files>
    packages/twentythree-skills/skills/reference/user.md
  </files>
  <read_first>
    packages/twentythree-skills/skills/SKILL.md,
    packages/twentythree-skills/skills/reference/category.md,
    .planning/phases/19-skill-content/19-PATTERNS.md,
    .planning/phases/19-skill-content/19-RESEARCH.md
  </read_first>
  <action>
Run these CLI calls BEFORE writing:
- `twentythree user --help`
- `twentythree user <cmd> --agent` for each of: list, create, get, update, send-invitation, get-login-token, tokens
- **A4 gap**: `twentythree user delete --agent` and `twentythree user redeem-login-token --agent` — flags NOT in research doc; pull them from live CLI

**user.md**:
- Frontmatter: `name: user`, `description: Manage workspace users (admin-scope only). Invite, update, delete, and issue login tokens.`
- `# TwentyThree User Commands`
- Opening blockquote with `--json` reminder + ALL-CAPS callout: "**ALL user commands require admin scope.** If your bearer token lacks admin, every command in this topic will fail with 401/403."
- `## Prerequisites` — auth scope: admin (all commands). Include `--agent` reminder. Call out: "Run `twentythree auth status --json` to confirm the current workspace's bearer token has admin scope before using these commands."
- `## Commands` — 9 subsections, one per command: `### user list`, `### user create`, `### user get`, `### user update`, `### user delete`, `### user get-login-token`, `### user send-invitation`, `### user tokens`, `### user redeem-login-token`. Each with auth/side-effects/output, flag table, 2 examples with `--json`. For `### user delete` and `### user redeem-login-token`, the flag tables MUST be populated from the live `--agent` output (A4 gap closure).

**CRITICAL — security warning** (required by 19-PATTERNS.md "Security warning pattern"):
Immediately after the `### user update` command's flag table (before the bash examples), place this blockquote verbatim:

> **Security:** `--password` is visible in process lists and shell history.
> Prefer the admin UI for password changes in sensitive environments.

Under `### user update`, include 2 examples. The password-changing example MUST appear AFTER this blockquote.

- `## Common Patterns` — include:
  1. Invite a new user: `user create --email colleague@example.com --full-name "New User" --user-type editor --json` then capture user_id then `user send-invitation <user_id> --invitation-message "Welcome" --json`
  2. Issue a time-limited login token: `user get-login-token <user_id> --return-url https://video.twentythree.com/ --json`
  3. Audit active tokens: `user tokens --json`
- NO Terminology Notes section.

Target length: user.md ~130-160 lines (9 commands with full flag tables + security warning).

At the end of Task 3, run `pnpm --filter twentythree-skills test` and confirm the 4 files from Plan 03 no longer appear in "Missing reference file" errors.
  </action>
  <verify>
    <automated>test -f packages/twentythree-skills/skills/reference/user.md && grep -q "^name: user$" packages/twentythree-skills/skills/reference/user.md && grep -q "admin scope" packages/twentythree-skills/skills/reference/user.md && grep -q "process lists" packages/twentythree-skills/skills/reference/user.md && grep -q "### user delete" packages/twentythree-skills/skills/reference/user.md && grep -q "### user redeem-login-token" packages/twentythree-skills/skills/reference/user.md</automated>
  </verify>
  <acceptance_criteria>
    - `packages/twentythree-skills/skills/reference/user.md` exists with `^name: user$` frontmatter
    - File contains opening ALL-CAPS admin-scope callout (grep: `ALL user commands require admin scope` OR `admin scope`)
    - Contains all 9 command headings: `### user list`, `### user create`, `### user get`, `### user update`, `### user delete`, `### user get-login-token`, `### user send-invitation`, `### user tokens`, `### user redeem-login-token`
    - `### user delete` subsection contains a flag table row (indicates A4 `--agent` was run; if the command truly has no flags, the subsection must explicitly state "(no flags)" instead)
    - `### user redeem-login-token` subsection contains a flag table row (indicates A4 `--agent` was run)
    - File contains the security warning blockquote containing both "process lists" AND "shell history" (both strings)
    - The line number of the security warning is BEFORE the first bash example that uses `--password` (verify by reading: warning must come before the example)
    - user.md line count >= 120
    - File does not contain `npx`
    - Run: `pnpm --filter twentythree-skills test` at end of Plan 03 — validator must NOT error about analytics.md, category.md, thumbnail.md, or user.md (will still error about the 10 remaining missing files written by Plans 04-05)
  </acceptance_criteria>
  <done>user.md exists with all 9 commands documented, admin scope callout at top, A4 gap closed via live --agent for user delete and user redeem-login-token, security warning blockquote placed before --password example. Plan 03 complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Markdown content phase — no runtime, no trust boundaries crossed. However, user.md documents an operation (`--password`) where shell-history disclosure is a real concern, so the file itself is the mitigation.

| Boundary | Description |
|----------|-------------|
| (none at runtime) | Static markdown |
| (documentation advisory) | user.md advises agents to prefer admin UI for password ops |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-03-01 | Information Disclosure | user.md examples touching passwords | mitigate | The file MUST include the blockquote warning that `--password` is visible in process lists and shell history before showing any example. Verify by grep: `process lists` AND `shell history` both present. |
| T-19-03-02 | Information Disclosure | Examples referencing emails/tokens/URLs in user.md and analytics.md | mitigate | All examples use `<user_id>`, `colleague@example.com`, and generic domain placeholders. No real tokens or workspace-specific data. Spot-check at commit time. |
| T-19-03-03 | Elevation of Privilege | Docs undersell admin-scope requirement of user commands | mitigate | user.md opens with ALL-CAPS callout that ALL user commands require admin scope; Prerequisites section repeats this; each command subsection tags auth_scope=admin. |
</threat_model>

<verification>
After all 3 tasks:

```bash
pnpm --filter twentythree-skills test
```

Expected at end of Plan 03: validator errors for 10 missing files (openupload if Plan 02 hasn't shipped; video, webinar, and any files Plans 04-05 own). The 4 Plan 03 files MUST NOT appear in the error list.

Spot-check:
```bash
wc -l packages/twentythree-skills/skills/reference/{analytics,category,thumbnail,user}.md
grep -c "^### " packages/twentythree-skills/skills/reference/analytics.md   # should be >=4 (subtopics)
grep -c "^### " packages/twentythree-skills/skills/reference/user.md        # should be >=9 (9 commands)
grep -l "category get" packages/twentythree-skills/skills/reference/category.md  # MUST be empty (Pitfall 6)
grep -l "npx" packages/twentythree-skills/skills/reference/*.md             # MUST be empty
grep -A 1 "Security" packages/twentythree-skills/skills/reference/user.md   # must show the warning blockquote
```
</verification>

<success_criteria>
- All 4 reference files created at exact filenames: analytics.md, category.md, thumbnail.md, user.md
- Every file has `name:` frontmatter matching filename stem + `description:` line
- analytics.md uses shared-flag pattern for its 4 subtopics (21+ commands covered)
- category.md contains Terminology Notes section linking CLI `category` to API `album`, and does NOT contain a fake `### category get` heading
- thumbnail.md covers the `thumbnail file` subtopic with live `--agent` data (A6 gap closed)
- user.md documents all 9 user commands with live `--agent` data for user delete and user redeem-login-token (A4 gap closed)
- user.md includes the `--password` security warning blockquote BEFORE any password example
- No file contains `npx`
- `pnpm --filter twentythree-skills test` does not report missing-file errors for any Plan 03 file
</success_criteria>

<output>
After completion, create `.planning/phases/19-skill-content/19-03-SUMMARY.md` following the summary template.
</output>
