---
phase: 15-tab-completion
plan: 02
type: execute
wave: 2
depends_on: [15-01]
files_modified:
  - README.md
  - packages/twentythree-cli/docs/guides/getting-started.md
  - packages/twentythree-cli/package.json
autonomous: false
requirements: [COMPLETE-01, COMPLETE-02, COMPLETE-03]

must_haves:
  truths:
    - "README.md documents tab completion setup"
    - "Getting-started guide includes tab completion as a numbered step"
    - "Package version is 1.0.2"
    - "npm package is published at version 1.0.2"
  artifacts:
    - path: "README.md"
      provides: "Tab Completion section in repo README"
      contains: "## Tab Completion"
    - path: "packages/twentythree-cli/docs/guides/getting-started.md"
      provides: "Tab completion setup step in getting-started guide"
      contains: "tab completion"
    - path: "packages/twentythree-cli/package.json"
      provides: "Version 1.0.2"
      contains: "1.0.2"
  key_links:
    - from: "README.md"
      to: "twentythree autocomplete"
      via: "setup instruction"
      pattern: "twentythree autocomplete"
    - from: "packages/twentythree-cli/docs/guides/getting-started.md"
      to: "twentythree autocomplete"
      via: "step instruction"
      pattern: "twentythree autocomplete"
---

<objective>
Update documentation with tab completion instructions, bump version to 1.0.2, rebuild, and publish to npm.

Purpose: Users can discover tab completion from the README or getting-started guide, and the new feature is available on npm (per D-04, D-05).
Output: Updated docs, version 1.0.2 published to npm.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-tab-completion/15-CONTEXT.md
@.planning/phases/15-tab-completion/15-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update documentation with tab completion section</name>
  <files>README.md, packages/twentythree-cli/docs/guides/getting-started.md</files>
  <read_first>
    - README.md (find the section ordering: Quickstart, Commands, Terminology, Documentation)
    - packages/twentythree-cli/docs/guides/getting-started.md (find step numbering: Step 1, Step 2, Step 3)
    - .planning/phases/15-tab-completion/15-PATTERNS.md (README.md and getting-started.md pattern sections)
  </read_first>
  <action>
**README.md** (per D-04):

Insert a `## Tab Completion` section AFTER the `## Quickstart` section and BEFORE the `## Commands` section. Use this exact content:

```markdown
## Tab Completion

Enable tab completion for bash or zsh -- run once, then use `<TAB>` to discover commands and flags.

```sh
twentythree autocomplete
```

Follow the on-screen instructions to add the eval line to your shell RC file (`~/.zshrc` or `~/.bashrc`), then restart your terminal.
```

**getting-started.md** (per D-04):

Insert a new step AFTER "Step 2: Select a workspace" and BEFORE the current "Step 3: Run your first command". Renumber the existing "Step 3" to "Step 4". Use this exact content for the new step:

```markdown
## Step 3: Enable tab completion (optional)

Run this once to set up `<TAB>` completion for all commands and flags:

```bash
twentythree autocomplete
```

The command detects your shell (bash or zsh), builds the completion cache, and shows the eval line to paste into your RC file. After sourcing your RC file or restarting your terminal, try:

```bash
twentythree video <TAB>
```
```

Then rename the existing "## Step 3: Run your first command" to "## Step 4: Run your first command".

Also update the "## Next steps" section to mention tab completion:
- After the existing "Run `twentythree <topic> --help`..." line, add: `\nUse `<TAB>` after any command to discover subcommands and flags.`
  </action>
  <verify>
    <automated>grep -c "## Tab Completion" README.md && grep -c "tab completion" packages/twentythree-cli/docs/guides/getting-started.md && grep -c "Step 4" packages/twentythree-cli/docs/guides/getting-started.md</automated>
  </verify>
  <acceptance_criteria>
    - README.md contains `## Tab Completion` as a heading
    - README.md contains `twentythree autocomplete` in a code block within that section
    - README.md has `## Tab Completion` appearing AFTER `## Quickstart` and BEFORE `## Commands`
    - packages/twentythree-cli/docs/guides/getting-started.md contains `## Step 3: Enable tab completion (optional)`
    - packages/twentythree-cli/docs/guides/getting-started.md contains `## Step 4: Run your first command` (renumbered from Step 3)
    - packages/twentythree-cli/docs/guides/getting-started.md contains `twentythree autocomplete` in a code block
  </acceptance_criteria>
  <done>Both README.md and getting-started.md document tab completion setup with exact eval line instructions per D-04</done>
</task>

<task type="auto">
  <name>Task 2: Bump version to 1.0.2, rebuild dist, and update CHANGELOG</name>
  <files>packages/twentythree-cli/package.json</files>
  <read_first>
    - packages/twentythree-cli/package.json (current version field)
    - CHANGELOG.md (existing format and entries for 1.0.0 and 1.0.1)
  </read_first>
  <action>
Per D-05, bump the version and rebuild:

1. Edit `packages/twentythree-cli/package.json`: change `"version": "1.0.1"` to `"version": "1.0.2"`
2. Add a `## 1.0.2` entry to `CHANGELOG.md` at the top (below the heading), following the existing format. Entry should note:
   - Added: Tab completion for bash and zsh via `@oclif/plugin-autocomplete`
   - Added: Guided `twentythree autocomplete` setup command with @clack/prompts
3. Run `pnpm --filter twentythree-cli run build` to rebuild dist/ with the new version and updated manifest
4. Run `pnpm --filter twentythree-cli test --run` to confirm all tests still pass
  </action>
  <verify>
    <automated>cd /Users/steffenchristensen/23/twentythree-cli && node -e "const p=require('./packages/twentythree-cli/package.json'); if(p.version!=='1.0.2') process.exit(1); console.log('version OK:', p.version)" && pnpm --filter twentythree-cli test --run</automated>
  </verify>
  <acceptance_criteria>
    - packages/twentythree-cli/package.json `"version"` field is `"1.0.2"`
    - CHANGELOG.md contains a `## 1.0.2` entry
    - CHANGELOG.md 1.0.2 entry mentions "tab completion" or "autocomplete"
    - `pnpm --filter twentythree-cli run build` exits 0
    - `pnpm --filter twentythree-cli test --run` exits 0
  </acceptance_criteria>
  <done>Version is 1.0.2, CHANGELOG updated, dist/ rebuilt with new manifest including autocomplete plugin</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify tab completion works and publish to npm</name>
  <files>packages/twentythree-cli/package.json</files>
  <action>
Human verification checkpoint. No automated action — user verifies the guided flow, tab completion, and publishes manually.
  </action>
  <what-built>
Tab completion via @oclif/plugin-autocomplete with guided setup command. The CLI now has a `twentythree autocomplete` command that detects the user's shell, builds the completion cache, and shows the eval line to paste into their RC file.
  </what-built>
  <how-to-verify>
1. Run `twentythree autocomplete` and confirm the guided flow appears with shell detection, spinner, and eval line display
2. Copy the eval line and paste it into your RC file (e.g. `~/.zshrc`)
3. Run `source ~/.zshrc` (or restart terminal)
4. Type `twentythree video ` then press TAB -- confirm subcommands appear (list, get, upload, etc.)
5. Type `twentythree video list --` then press TAB -- confirm flags appear
6. If all looks good, publish: `cd packages/twentythree-cli && npm publish`
7. Verify: `npm view twentythree-cli version` should return `1.0.2`
  </how-to-verify>
  <verify>Manual verification -- see how-to-verify steps above</verify>
  <done>Tab completion works in interactive shell, package published to npm at 1.0.2</done>
  <resume-signal>Type "approved" if completion works and publish succeeded, or describe any issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docs -> User | Documentation shows shell commands; user executes them manually |
| npm publish -> registry | Package published to public npm registry |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-04 | Tampering | npm publish | accept | Same publish pattern as Phase 14 (1.0.1). Human checkpoint verifies before publish. npm 2FA protects the registry account. |
| T-15-05 | Information Disclosure | README eval line | accept | Eval line contains only the CLI binary name and shell type — no secrets or credentials |
</threat_model>

<verification>
1. README.md contains `## Tab Completion` section
2. getting-started.md contains tab completion step
3. package.json version is 1.0.2
4. `npm view twentythree-cli version` returns 1.0.2 (after human publish)
5. Tab completion works in interactive shell (manual)
</verification>

<success_criteria>
- Docs updated per D-04 (README + getting-started)
- Version bumped to 1.0.2 per D-05
- CHANGELOG updated with 1.0.2 entry
- Tab completion verified working in zsh/bash (manual checkpoint)
- Package published to npm at 1.0.2 (manual checkpoint)
</success_criteria>

<output>
After completion, create `.planning/phases/15-tab-completion/15-02-SUMMARY.md`
</output>
