---
phase: 01-foundation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-workspace.yaml
  - turbo.json
  - tsconfig.base.json
  - .changeset/config.json
  - .gitignore
  - packages/twentythree-cli/package.json
  - packages/twentythree-cli/tsconfig.json
  - packages/twentythree-skills/package.json
  - packages/twentythree-skills/SKILL.md
autonomous: true
requirements:
  - FOUND-01
must_haves:
  truths:
    - "pnpm install succeeds in the monorepo root and resolves both packages"
    - "turbo run build finds both packages (even if build scripts are stubs)"
    - "twentythree-skills contains SKILL.md with valid frontmatter"
  artifacts:
    - path: "package.json"
      provides: "Workspace root with private flag, packageManager, engines, turbo scripts"
      contains: "private"
    - path: "pnpm-workspace.yaml"
      provides: "Package directory declaration"
      contains: "packages"
    - path: "turbo.json"
      provides: "Task pipeline for build, test, lint"
      contains: "tasks"
    - path: "tsconfig.base.json"
      provides: "Shared TypeScript config"
      contains: "strict"
    - path: ".changeset/config.json"
      provides: "Changesets configuration for versioning"
      contains: "baseBranch"
    - path: "packages/twentythree-cli/package.json"
      provides: "CLI package stub with oclif config"
      contains: "twentythree-cli"
    - path: "packages/twentythree-cli/tsconfig.json"
      provides: "CLI package TS config extending base"
      contains: "tsconfig.base.json"
    - path: "packages/twentythree-skills/package.json"
      provides: "Skills package stub"
      contains: "twentythree-skills"
    - path: "packages/twentythree-skills/SKILL.md"
      provides: "Stub skill definition"
      contains: "name: twentythree"
  key_links:
    - from: "pnpm-workspace.yaml"
      to: "packages/*"
      via: "workspace declaration"
      pattern: "packages"
    - from: "packages/twentythree-cli/tsconfig.json"
      to: "tsconfig.base.json"
      via: "extends field"
      pattern: "extends"
---

<objective>
Scaffold the pnpm monorepo with two packages: `twentythree-cli` (the real CLI package) and `twentythree-skills` (a stub with only SKILL.md). Establish root workspace config, turborepo task pipeline, shared TypeScript base config, changesets versioning, and .gitignore.

Purpose: Every downstream plan depends on this structure existing. The monorepo layout, package names, and build pipeline shape everything that follows.
Output: A fully configured monorepo where `pnpm install` succeeds and `turbo run build` discovers both packages.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create root workspace configuration files</name>
  <files>package.json, pnpm-workspace.yaml, turbo.json, tsconfig.base.json, .changeset/config.json, .gitignore</files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md (Patterns 2, 3, 13, 14 for exact file contents)
    - CLAUDE.md (stack versions)
  </read_first>
  <action>
Create 6 files at the repo root:

**package.json** (root workspace):
```json
{
  "private": true,
  "name": "twentythree-cli-monorepo",
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=10.0.0"
  },
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "generate-types": "openapi-typescript https://video.twentythree.com/apidocs/swagger.json -o packages/twentythree-cli/src/api/types.ts"
  },
  "devDependencies": {
    "turbo": "^2.9.6",
    "@changesets/cli": "^2.30.0",
    "typescript": "^5.0.0"
  }
}
```

**pnpm-workspace.yaml**:
```yaml
packages:
  - "packages/*"
```

**turbo.json** (turborepo v2 uses `tasks` NOT `pipeline`):
```json
{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "test": {
      "dependsOn": ["build"],
      "inputs": ["src/**/*.ts", "test/**/*.ts", "vitest.config.*"]
    },
    "lint": {
      "inputs": ["src/**/*.ts", ".eslintrc*"]
    },
    "generate-types": {
      "outputs": ["src/api/types.ts"],
      "cache": false
    }
  }
}
```

**tsconfig.base.json**:
```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "commonjs",
    "moduleResolution": "node",
    "lib": ["ES2022"],
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "declaration": true,
    "declarationMap": false,
    "sourceMap": false,
    "outDir": "dist",
    "rootDir": "src"
  }
}
```

**.changeset/config.json**:
```json
{
  "$schema": "https://unpkg.com/@changesets/config@3.0.0/schema.json",
  "changelog": "@changesets/cli/changelog",
  "commit": false,
  "linked": [["twentythree-cli", "twentythree-skills"]],
  "access": "public",
  "baseBranch": "main",
  "updateInternalDependencies": "patch",
  "ignore": []
}
```

**.gitignore** (add or update existing):
```
node_modules/
dist/
*.tsbuildinfo
.turbo/
oclif.manifest.json
.DS_Store
```
  </action>
  <verify>
    <automated>test -f package.json && test -f pnpm-workspace.yaml && test -f turbo.json && test -f tsconfig.base.json && test -f .changeset/config.json && test -f .gitignore && echo "ALL_ROOT_FILES_EXIST"</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains `"private": true`
    - package.json contains `"packageManager": "pnpm@10.33.0"`
    - package.json contains `"node": ">=22.0.0"`
    - pnpm-workspace.yaml contains `packages/*`
    - turbo.json contains `"tasks"` (NOT `"pipeline"`)
    - tsconfig.base.json contains `"strict": true`
    - .changeset/config.json contains `"baseBranch": "main"`
    - .gitignore contains `node_modules/` and `dist/` and `oclif.manifest.json`
  </acceptance_criteria>
  <done>All 6 root config files exist with correct content; no placeholder values</done>
</task>

<task type="auto">
  <name>Task 2: Create both package stubs and install dependencies</name>
  <files>packages/twentythree-cli/package.json, packages/twentythree-cli/tsconfig.json, packages/twentythree-cli/src/commands/.gitkeep, packages/twentythree-skills/package.json, packages/twentythree-skills/SKILL.md</files>
  <read_first>
    - package.json (root, just created in Task 1)
    - .planning/phases/01-foundation/01-RESEARCH.md (Patterns 4, 11, 12 for exact package.json contents and SKILL.md format)
  </read_first>
  <action>
Create both package directories and their config files, then run `pnpm install`.

**packages/twentythree-cli/package.json**:
```json
{
  "name": "twentythree-cli",
  "version": "0.1.0",
  "description": "Terminal access to every TwentyThree API endpoint",
  "license": "MIT",
  "type": "commonjs",
  "engines": {
    "node": ">=22.0.0"
  },
  "bin": {
    "twentythree": "./bin/run.js"
  },
  "main": "./dist/index.js",
  "files": [
    "/bin",
    "/dist",
    "/oclif.manifest.json"
  ],
  "scripts": {
    "build": "tsdown",
    "postbuild": "oclif manifest",
    "test": "vitest run"
  },
  "oclif": {
    "bin": "twentythree",
    "dirname": "twentythree",
    "commands": "./dist/commands",
    "topicSeparator": " "
  },
  "dependencies": {
    "@oclif/core": "^4.10.5",
    "openapi-fetch": "^0.17.0"
  },
  "devDependencies": {
    "oclif": "^4.23.0",
    "tsdown": "^0.21.8",
    "typescript": "^5.0.0",
    "openapi-typescript": "^7.13.0",
    "vitest": "^4.1.4",
    "@oclif/test": "latest"
  }
}
```

**packages/twentythree-cli/tsconfig.json**:
```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "outDir": "dist",
    "rootDir": "src"
  },
  "include": ["src/**/*.ts"],
  "exclude": ["src/**/*.test.ts", "dist", "node_modules"]
}
```

**packages/twentythree-cli/src/commands/.gitkeep**: Empty file. Creates the commands directory for oclif discovery. No real commands in Phase 1.

**packages/twentythree-skills/package.json**:
```json
{
  "name": "twentythree-skills",
  "version": "0.1.0",
  "description": "AI agent skills for the TwentyThree CLI",
  "license": "MIT",
  "files": [
    "SKILL.md"
  ],
  "keywords": ["ai", "skills", "twentythree", "cli"]
}
```

**packages/twentythree-skills/SKILL.md**: Create this file with YAML frontmatter (triple-dash delimiters) containing the following fields, then a markdown body below.

Frontmatter fields (between the triple-dash delimiters):
- `name: twentythree`
- `description:` (multi-line) "TwentyThree CLI skills for AI agents. Commands for managing videos, categories, webinars, and all TwentyThree resources."
- `triggers:` list of "twentythree", "video platform", "webinar management"
- `invocable: true`
- `argument-hint: "<command> [flags]"`

Body content after the closing frontmatter delimiter:
- H1 heading: "TwentyThree CLI"
- Blockquote: "Skills content is a work in progress. This package will contain AI agent skills covering all TwentyThree CLI commands when complete."
- Link line: "See [twentythree-cli](https://www.npmjs.com/package/twentythree-cli) for the CLI itself."

Reference 01-RESEARCH.md Pattern 12 for the exact file content.

After creating all files, run:
```bash
pnpm install
```

This installs all dependencies for both packages and the root workspace. Verify it succeeds with exit code 0.
  </action>
  <verify>
    <automated>cd /Users/steffenchristensen/23/twentythree-cli && pnpm install --frozen-lockfile 2>/dev/null || pnpm install && echo "INSTALL_SUCCESS"</automated>
  </verify>
  <acceptance_criteria>
    - packages/twentythree-cli/package.json contains `"name": "twentythree-cli"`
    - packages/twentythree-cli/package.json contains `"bin":` with `"twentythree": "./bin/run.js"`
    - packages/twentythree-cli/package.json contains `"oclif":` with `"commands": "./dist/commands"`
    - packages/twentythree-cli/package.json contains `"type": "commonjs"`
    - packages/twentythree-cli/package.json contains `"node": ">=22.0.0"`
    - packages/twentythree-cli/tsconfig.json contains `"extends": "../../tsconfig.base.json"`
    - packages/twentythree-skills/package.json contains `"name": "twentythree-skills"`
    - packages/twentythree-skills/SKILL.md contains `name: twentythree`
    - packages/twentythree-cli/src/commands/.gitkeep exists
    - `pnpm install` exits with code 0
    - node_modules/ directory exists at repo root
    - packages/twentythree-cli/node_modules/ exists (or symlinked from root)
  </acceptance_criteria>
  <done>Both packages exist with correct configuration; pnpm install succeeds; dependency tree is resolved</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries in this plan. This plan creates project scaffolding only -- no user input processing, no network calls, no credential handling.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | N/A | scaffold | accept | No security surface -- project structure files only; no runtime code in this plan |
</threat_model>

<verification>
- `pnpm install` exits 0
- `ls packages/twentythree-cli/package.json packages/twentythree-skills/SKILL.md` shows both files
- `cat turbo.json | grep tasks` confirms v2 key name
- `cat packages/twentythree-cli/package.json | grep twentythree-cli` confirms package name
</verification>

<success_criteria>
- Monorepo structure exists with two packages discoverable by pnpm
- All root config files (package.json, pnpm-workspace.yaml, turbo.json, tsconfig.base.json, .changeset/config.json, .gitignore) are present and correctly configured
- `pnpm install` succeeds
- twentythree-skills has SKILL.md stub with frontmatter
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
