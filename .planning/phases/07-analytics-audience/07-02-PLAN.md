---
phase: 07-analytics-audience
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/twentythree-cli/src/commands/analytics/__tests__/video-index.test.ts
  - packages/twentythree-cli/src/commands/analytics/__tests__/live.test.ts
  - packages/twentythree-cli/src/commands/analytics/__tests__/conversions.test.ts
  - packages/twentythree-cli/src/commands/analytics/__tests__/usage.test.ts
  - packages/twentythree-cli/src/commands/audience/__tests__/list.test.ts
  - packages/twentythree-cli/src/commands/audience/__tests__/search.test.ts
  - packages/twentythree-cli/src/commands/audience/__tests__/register.test.ts
  - packages/twentythree-cli/src/commands/audience/__tests__/mutations.test.ts
  - packages/twentythree-cli/src/commands/audience/field/__tests__/set.test.ts
  - packages/twentythree-cli/src/commands/analytics/video/index.ts
  - packages/twentythree-cli/src/commands/analytics/live/index.ts
  - packages/twentythree-cli/src/commands/analytics/live/timeseries.ts
  - packages/twentythree-cli/src/commands/analytics/live/totals.ts
  - packages/twentythree-cli/src/commands/analytics/live/weekday.ts
  - packages/twentythree-cli/src/commands/analytics/live/event.ts
  - packages/twentythree-cli/src/commands/analytics/live/event-timeseries.ts
  - packages/twentythree-cli/src/commands/analytics/live/event-totals.ts
  - packages/twentythree-cli/src/commands/analytics/conversions/index.ts
  - packages/twentythree-cli/src/commands/analytics/conversions/timeseries.ts
  - packages/twentythree-cli/src/commands/analytics/conversions/totals.ts
autonomous: true
requirements:
  - ANL-01
  - ANL-03
  - ANL-04
  - ANL-05
  - ANL-06
  - ANL-08
must_haves:
  truths:
    - "Root `analytics video` command exists (index.ts) and returns paginated video analytics data from GET /analytics/data/videos"
    - "Root `analytics live` command exists (index.ts) and returns paginated live analytics data from GET /analytics/data/live"
    - "Root `analytics conversions` command exists (index.ts) and returns conversion analytics data from GET /analytics/data/conversions"
    - "All 6 analytics live sub-dimension commands exist and accept date range filters per D-2/D-3"
    - "Both analytics conversions sub-dimension commands exist and return correct data"
    - "All new commands support --json output with { ok, data, summary, breadcrumbs } shape"
    - "Wave 0 vitest .todo() stub files exist for analytics and audience command groups"
  artifacts:
    - path: "packages/twentythree-cli/src/commands/analytics/video/index.ts"
      provides: "Root analytics videos command"
      contains: "apiClient.GET('/analytics/data/videos'"
    - path: "packages/twentythree-cli/src/commands/analytics/live/index.ts"
      provides: "Root analytics live command"
      contains: "apiClient.GET('/analytics/data/live'"
    - path: "packages/twentythree-cli/src/commands/analytics/conversions/index.ts"
      provides: "Root analytics conversions command"
      contains: "apiClient.GET('/analytics/data/conversions'"
    - path: "packages/twentythree-cli/src/commands/analytics/live/timeseries.ts"
      provides: "analytics live timeseries command"
      contains: "apiClient.GET('/analytics/data/live/timeseries'"
    - path: "packages/twentythree-cli/src/commands/analytics/live/event-timeseries.ts"
      provides: "analytics live event-timeseries command"
      contains: "apiClient.GET('/analytics/data/live/event/timeseries'"
    - path: "packages/twentythree-cli/src/commands/analytics/conversions/timeseries.ts"
      provides: "analytics conversions timeseries command"
      contains: "apiClient.GET('/analytics/data/conversions/timeseries'"
  key_links:
    - from: "packages/twentythree-cli/src/commands/analytics/live/timeseries.ts"
      to: "/analytics/data/live/timeseries"
      via: "apiClient.GET"
      pattern: "apiClient\\.GET\\('/analytics/data/live/timeseries'"
    - from: "packages/twentythree-cli/src/lib/analytics-flags.ts"
      to: "all analytics commands"
      via: "spread import"
      pattern: "ANALYTICS_DATE_FLAGS"
---

<objective>
Create Wave 0 vitest stub files for all Phase 7 command groups, the root `analytics video` command (ANL-01 gap), the root `analytics live` command (ANL-03), all 6 `analytics live` sub-dimension commands (ANL-04), the root `analytics conversions` command (ANL-05), and both `analytics conversions` sub-dimension commands (ANL-06).

Purpose: Wave 0 establishes test scaffolding for all plans in Phase 7. Then covers the live and conversions analytics surfaces, reusing the shared analytics flag module from plan 07-01. Also closes the ANL-01 gap by adding the root videos command, and satisfies ANL-03/ANL-05 with root live and conversions commands.

Output: 9 test stub files (Wave 0) + 11 command files -- 1 root video index + 1 root live index + 6 live sub-dimensions + 1 root conversions index + 2 conversions sub-dimensions. All follow the exact pattern established by `analytics/video/timeseries.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-analytics-audience/07-CONTEXT.md
@.planning/phases/07-analytics-audience/07-RESEARCH.md
@.planning/phases/07-analytics-audience/07-01-SUMMARY.md

@packages/twentythree-cli/src/commands/analytics/video/timeseries.ts
@packages/twentythree-cli/src/commands/analytics/video/weekday.ts
@packages/twentythree-cli/src/lib/analytics-flags.ts
@packages/twentythree-cli/src/lib/output.ts
@packages/twentythree-cli/src/lib/base-command.ts
@packages/twentythree-cli/src/commands/webinar/__tests__/metrics.test.ts

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/lib/analytics-flags.ts:
```typescript
export const ANALYTICS_DATE_FLAGS = {
  'date-start': Flags.string({ description: 'First date (YYYY-MM-DD)', required: false }),
  'date-end': Flags.string({ description: 'Last date (YYYY-MM-DD)', required: false }),
  'date-expression': Flags.string({ description: 'Predefined date range (e.g. thisweek, lastyear)', required: false }),
}
export const ANALYTICS_PAGINATION_FLAGS = {
  page: Flags.integer({ description: 'Page number', required: false }),
  size: Flags.integer({ description: 'Page size', required: false }),
}
export const ANALYTICS_FILTER_FLAGS = {
  selection: Flags.string({ description: 'Scope to specific objects/types', required: false }),
  groupby: Flags.string({ description: 'Group results by dimension', required: false }),
  orderby: Flags.string({ description: 'Order results by field', required: false }),
  order: Flags.string({ description: 'Sort direction (asc/desc)', required: false }),
}
```

From src/lib/output.ts:
```typescript
export const EXIT_SUCCESS = 0
export const EXIT_ERROR = 1
export const EXIT_CANCELLED = 2
export function formatJsonOutput(params: JsonOutputParams): JsonOutput
export function renderTable(headers: string[], rows: string[][]): InstanceType<typeof Table>
export function formatApiError(error: unknown): string
```

From src/lib/base-command.ts:
```typescript
export abstract class AuthenticatedCommand<T extends typeof Command> extends BaseCommand<T>
// Provides: this.apiClient, this.activeWorkspace, this.printWorkspaceHeader(), this.jsonEnabled()
```

API path keys from types.ts (verified):
- `/analytics/data/videos` (root, GET, has p/size)
- `/analytics/data/live` (root, GET, has p/size)
- `/analytics/data/live/timeseries` (GET, no p/size)
- `/analytics/data/live/totals` (GET, no p/size)
- `/analytics/data/live/weekday` (GET, has p/size)
- `/analytics/data/live/event` (GET, has p/size)
- `/analytics/data/live/event/timeseries` (GET, no p/size) -- NOTE: path is `/live/event/timeseries` not `/live/event-timeseries`
- `/analytics/data/live/event/totals` (GET, no p/size) -- NOTE: path is `/live/event/totals`
- `/analytics/data/conversions` (root, GET, no p/size)
- `/analytics/data/conversions/timeseries` (GET, no p/size)
- `/analytics/data/conversions/totals` (GET, no p/size)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Create Wave 0 vitest .todo() stub files for all Phase 7 command groups</name>
  <files>
    packages/twentythree-cli/src/commands/analytics/__tests__/video-index.test.ts
    packages/twentythree-cli/src/commands/analytics/__tests__/live.test.ts
    packages/twentythree-cli/src/commands/analytics/__tests__/conversions.test.ts
    packages/twentythree-cli/src/commands/analytics/__tests__/usage.test.ts
    packages/twentythree-cli/src/commands/audience/__tests__/list.test.ts
    packages/twentythree-cli/src/commands/audience/__tests__/search.test.ts
    packages/twentythree-cli/src/commands/audience/__tests__/register.test.ts
    packages/twentythree-cli/src/commands/audience/__tests__/mutations.test.ts
    packages/twentythree-cli/src/commands/audience/field/__tests__/set.test.ts
  </files>
  <read_first>
    packages/twentythree-cli/src/commands/webinar/__tests__/metrics.test.ts
  </read_first>
  <action>
Create vitest `.todo()` stub files following the exact pattern from `webinar/__tests__/metrics.test.ts`. Each file imports `{ describe, it }` from `'vitest'` and contains `.todo()` placeholders for the key behaviors per requirement.

Create the `__tests__/` directories first:
- `packages/twentythree-cli/src/commands/analytics/__tests__/`
- `packages/twentythree-cli/src/commands/audience/__tests__/`
- `packages/twentythree-cli/src/commands/audience/field/__tests__/`

**File 1: `analytics/__tests__/video-index.test.ts`** (ANL-01)
```typescript
import { describe, it } from 'vitest'

describe('AnalyticsVideoIndex', () => {
  it.todo('renders paginated video analytics table from GET /analytics/data/videos')
  it.todo('returns JSON with ok, data, summary, breadcrumbs when --json passed')
  it.todo('passes p and size query params for pagination')
})
```

**File 2: `analytics/__tests__/live.test.ts`** (ANL-03, ANL-04)
```typescript
import { describe, it } from 'vitest'

describe('AnalyticsLive', () => {
  it.todo('root live command renders paginated data from GET /analytics/data/live')
  it.todo('timeseries passes date flags to GET /analytics/data/live/timeseries')
  it.todo('totals returns aggregate data without pagination')
  it.todo('weekday renders with pagination flags')
  it.todo('event renders with pagination flags')
  it.todo('event-timeseries uses /live/event/timeseries path (slash not hyphen)')
  it.todo('event-totals uses /live/event/totals path (slash not hyphen)')
})
```

**File 3: `analytics/__tests__/conversions.test.ts`** (ANL-05, ANL-06)
```typescript
import { describe, it } from 'vitest'

describe('AnalyticsConversions', () => {
  it.todo('root conversions command renders data from GET /analytics/data/conversions')
  it.todo('timeseries passes date flags without pagination')
  it.todo('totals returns aggregate data without pagination')
})
```

**File 4: `analytics/__tests__/usage.test.ts`** (ANL-07)
```typescript
import { describe, it } from 'vitest'

describe('AnalyticsUsage', () => {
  it.todo('devices renders paginated table from GET /analytics/data/usage/devices')
  it.todo('storage renders key-value output (single object, no pagination, no date params)')
  it.todo('all 7 standard usage commands include pagination flags')
  it.todo('storage does NOT include pagination flags')
})
```

**File 5: `audience/__tests__/list.test.ts`** (AUD-01)
```typescript
import { describe, it } from 'vitest'

describe('AudienceList', () => {
  it.todo('renders table with uuid, name, email, company, score, timelines columns')
  it.todo('passes p, size, offset query params for pagination')
  it.todo('returns JSON with ok, data, summary, breadcrumbs when --json passed')
})
```

**File 6: `audience/__tests__/search.test.ts`** (AUD-02)
```typescript
import { describe, it } from 'vitest'

describe('AudienceSearch', () => {
  it.todo('uses GET not POST for /audience/search')
  it.todo('requires --text flag')
  it.todo('passes size and offset query params')
})
```

**File 7: `audience/__tests__/register.test.ts`** (AUD-03)
```typescript
import { describe, it } from 'vitest'

describe('AudienceRegister', () => {
  it.todo('sends POST form to /audience/register with email required')
  it.todo('uses Content-Type application/x-www-form-urlencoded')
  it.todo('only includes defined flags in body')
})
```

**File 8: `audience/__tests__/mutations.test.ts`** (AUD-04, AUD-05)
```typescript
import { describe, it } from 'vitest'

describe('AudienceUnregister', () => {
  it.todo('sends POST form with object-id required')
})

describe('AudienceRemove', () => {
  it.todo('prompts for confirmation before destructive action')
  it.todo('exits with code 2 on cancel')
})
```

**File 9: `audience/field/__tests__/set.test.ts`** (AUD-12)
```typescript
import { describe, it } from 'vitest'

describe('AudienceFieldSet', () => {
  it.todo('sends POST form with key, type, label required')
  it.todo('uses Content-Type application/x-www-form-urlencoded')
})

describe('AudienceFieldList', () => {
  it.todo('uses GET not POST for /audience/field/list')
})

describe('AudienceFieldRemove', () => {
  it.todo('prompts for confirmation before destructive action')
})
```

After creating all 9 files, run `pnpm --filter twentythree-cli test --run` to verify all stubs are discovered and pass (todo tests are not failures).
  </action>
  <verify>
    <automated>cd /Users/steffenchristensen/23/twentythree-cli && pnpm --filter twentythree-cli test --run 2>&1 | tail -20</automated>
  </verify>
  <done>Wave 0 test stubs created for all Phase 7 command groups. vitest discovers and reports all .todo() stubs. Test suite passes (todos are not failures).</done>
</task>

<task type="auto">
  <name>Task 1: Create root analytics video/live commands and all 6 analytics live sub-dimension commands</name>
  <files>
    packages/twentythree-cli/src/commands/analytics/video/index.ts
    packages/twentythree-cli/src/commands/analytics/live/index.ts
    packages/twentythree-cli/src/commands/analytics/live/timeseries.ts
    packages/twentythree-cli/src/commands/analytics/live/totals.ts
    packages/twentythree-cli/src/commands/analytics/live/weekday.ts
    packages/twentythree-cli/src/commands/analytics/live/event.ts
    packages/twentythree-cli/src/commands/analytics/live/event-timeseries.ts
    packages/twentythree-cli/src/commands/analytics/live/event-totals.ts
  </files>
  <read_first>
    packages/twentythree-cli/src/commands/analytics/video/timeseries.ts
    packages/twentythree-cli/src/commands/analytics/video/weekday.ts
    packages/twentythree-cli/src/lib/analytics-flags.ts
    packages/twentythree-cli/src/lib/output.ts
    packages/twentythree-cli/src/lib/base-command.ts
  </read_first>
  <action>
All 8 files follow the EXACT pattern from `analytics/video/timeseries.ts` (for non-paginated) or `analytics/video/weekday.ts` (for paginated). Copy the pattern precisely -- same imports, same `run()` structure, same response handling (`data as any`, `Array.isArray` guard, `renderTable`, `chalk.dim` count, `formatJsonOutput`).

**Imports for all files** (same as timeseries.ts, adjust relative path depth for live/ commands which are at same level as video/):
```typescript
import chalk from 'chalk'
import { AuthenticatedCommand } from '../../../lib/base-command.js'
import { renderTable, formatJsonOutput, formatApiError, EXIT_ERROR } from '../../../lib/output.js'
import { applyCliTerms } from '../../../lib/term-map.js'
import { ANALYTICS_DATE_FLAGS, ANALYTICS_FILTER_FLAGS, ANALYTICS_PAGINATION_FLAGS } from '../../../lib/analytics-flags.js'
```

**File 1: `analytics/video/index.ts`** -- Root analytics videos command (closes ANL-01 gap)
- Class: `AnalyticsVideoIndex extends AuthenticatedCommand<typeof AnalyticsVideoIndex>`
- `static description = 'Get video analytics data'`
- `static enableJsonFlag = true`
- `static flags = { ...AuthenticatedCommand.baseFlags, ...ANALYTICS_DATE_FLAGS, ...ANALYTICS_PAGINATION_FLAGS, ...ANALYTICS_FILTER_FLAGS }` (root has p/size)
- API path: `this.apiClient.GET('/analytics/data/videos', { params: { query: { date_start: flags['date-start'], date_end: flags['date-end'], date_expression: flags['date-expression'], selection: flags.selection, groupby: flags.groupby as any, orderby: flags.orderby as any, order: flags.order as any, p: flags.page, size: flags.size } } })`
- Table headers: `['Plays', 'Engagement', 'Playrate', 'Avg View Time', 'Traffic']`
- Row fields: `plays, engagement, playrate, avg_viewtime, traffic`
- NOTE: oclif treats `index.ts` inside a topic directory as the command for the bare topic. Test this by running `pnpm --filter twentythree-cli build` after creating.

**File 2: `analytics/live/index.ts`** -- Root analytics live command (ANL-03)
- Class: `AnalyticsLiveIndex extends AuthenticatedCommand<typeof AnalyticsLiveIndex>`
- `static description = 'Get live/webinar analytics data'`
- `static enableJsonFlag = true`
- `static flags = { ...AuthenticatedCommand.baseFlags, ...ANALYTICS_DATE_FLAGS, ...ANALYTICS_PAGINATION_FLAGS, ...ANALYTICS_FILTER_FLAGS }` (root has p/size)
- API path: `this.apiClient.GET('/analytics/data/live', { params: { query: { date_start: flags['date-start'], date_end: flags['date-end'], date_expression: flags['date-expression'], selection: flags.selection, groupby: flags.groupby as any, orderby: flags.orderby as any, order: flags.order as any, p: flags.page, size: flags.size } } })`
- Table headers: `['Plays', 'Peak Viewers', 'Engagement', 'Playrate', 'Avg View Time']`
- Row fields: `plays, peakviewers, engagement, playrate, avg_viewtime`

**File 3: `analytics/live/timeseries.ts`**
- Class: `AnalyticsLiveTimeseries`
- `static description = 'Get live/webinar analytics time series data'`
- NO pagination flags (timeseries has no p/size)
- API path: `'/analytics/data/live/timeseries'`
- Table headers: `['Date', 'Plays', 'Peak Viewers', 'Engagement', 'Playrate']`
- Row fields: `date, plays, peakviewers, engagement, playrate`

**File 4: `analytics/live/totals.ts`**
- Class: `AnalyticsLiveTotals`
- `static description = 'Get live/webinar analytics totals'`
- NO pagination flags
- API path: `'/analytics/data/live/totals'`
- Table headers: `['Plays', 'Peak Viewers', 'Engagement', 'Playrate', 'Avg View Time']`
- Row fields: `plays, peakviewers, engagement, playrate, avg_viewtime`

**File 5: `analytics/live/weekday.ts`**
- Class: `AnalyticsLiveWeekday`
- `static description = 'Get live/webinar analytics by day of week'`
- HAS pagination flags (`...ANALYTICS_PAGINATION_FLAGS`) and maps `p: flags.page, size: flags.size`
- API path: `'/analytics/data/live/weekday'`
- Table headers: `['Day', 'Plays', 'Peak Viewers', 'Engagement', 'Playrate']`
- Row fields: `day (or weekday), plays, peakviewers, engagement, playrate`

**File 6: `analytics/live/event.ts`**
- Class: `AnalyticsLiveEvent`
- `static description = 'Get live/webinar event analytics'`
- HAS pagination flags
- API path: `'/analytics/data/live/event'`
- Table headers: `['Event', 'Plays', 'Peak Viewers', 'Engagement', 'Performance']`
- Row fields: `event (or title), plays, peakviewers, engagement, performance`

**File 7: `analytics/live/event-timeseries.ts`**
- Class: `AnalyticsLiveEventTimeseries`
- `static description = 'Get live/webinar event time series analytics'`
- NO pagination flags
- CRITICAL API path: `'/analytics/data/live/event/timeseries'` (path is `/live/event/timeseries` with a slash, NOT `/live/event-timeseries` -- the CLI filename is hyphenated but the API path uses a slash)
- Table headers: `['Date', 'Plays', 'Peak Viewers', 'Engagement', 'Playrate']`
- Row fields: `date, plays, peakviewers, engagement, playrate`

**File 8: `analytics/live/event-totals.ts`**
- Class: `AnalyticsLiveEventTotals`
- `static description = 'Get live/webinar event totals analytics'`
- NO pagination flags
- CRITICAL API path: `'/analytics/data/live/event/totals'` (slash, not hyphen)
- Table headers: `['Plays', 'Peak Viewers', 'Engagement', 'Playrate', 'Avg View Time']`
- Row fields: `plays, peakviewers, engagement, playrate, avg_viewtime`

**For ALL files:**
- Do NOT apply `applyCliTerms()` to data values -- only to error messages (Pitfall 7)
- Empty state: `this.log('No data found.')`
- Count line: `` this.log(chalk.dim(`${rows.length} row(s)`)) ``
- JSON output: `formatJsonOutput({ ok: true, data: rows, summary: '${rows.length} row(s)', breadcrumbs: [{ domain: this.activeWorkspace.domain }, { resource: 'analytics' }] })`
- Error handling: `if (error) { this.error(applyCliTerms(formatApiError(error)), { exit: EXIT_ERROR }) }`
- Response cast: `const resp = data as any; const rows: any[] = Array.isArray(resp?.data) ? resp.data : resp?.data ? [resp.data] : []`
- Each file should include 2-3 `static examples` lines similar to the video commands

After creating all 8 files, run `pnpm --filter twentythree-cli exec tsc --noEmit` to check for TypeScript errors.
  </action>
  <verify>
    <automated>cd /Users/steffenchristensen/23/twentythree-cli && ls packages/twentythree-cli/src/commands/analytics/video/index.ts packages/twentythree-cli/src/commands/analytics/live/index.ts packages/twentythree-cli/src/commands/analytics/live/*.ts | wc -l && pnpm --filter twentythree-cli exec tsc --noEmit 2>&1 | grep -c "analytics/live\|analytics/video/index" || echo "0 new errors"</automated>
  </verify>
  <acceptance_criteria>
    - File `packages/twentythree-cli/src/commands/analytics/video/index.ts` exists and contains `apiClient.GET('/analytics/data/videos'`
    - File `packages/twentythree-cli/src/commands/analytics/live/index.ts` exists and contains `apiClient.GET('/analytics/data/live'` (root, not a sub-dimension path)
    - File `packages/twentythree-cli/src/commands/analytics/live/timeseries.ts` contains `apiClient.GET('/analytics/data/live/timeseries'`
    - File `packages/twentythree-cli/src/commands/analytics/live/totals.ts` contains `apiClient.GET('/analytics/data/live/totals'`
    - File `packages/twentythree-cli/src/commands/analytics/live/weekday.ts` contains `apiClient.GET('/analytics/data/live/weekday'` and `p: flags.page`
    - File `packages/twentythree-cli/src/commands/analytics/live/event.ts` contains `apiClient.GET('/analytics/data/live/event'` and `p: flags.page`
    - File `packages/twentythree-cli/src/commands/analytics/live/event-timeseries.ts` contains `apiClient.GET('/analytics/data/live/event/timeseries'` (slash not hyphen)
    - File `packages/twentythree-cli/src/commands/analytics/live/event-totals.ts` contains `apiClient.GET('/analytics/data/live/event/totals'` (slash not hyphen)
    - All 8 files have `static enableJsonFlag = true`
    - All 8 files import from `../../../lib/analytics-flags.js`
    - No new TypeScript errors from these files
  </acceptance_criteria>
  <done>Root analytics video and live commands created (ANL-01, ANL-03 complete). All 6 analytics live sub-dimension commands implemented (ANL-04). All use shared date filter flags per D-2/D-3 and pagination per D-4 where applicable. --json output works per ANL-08.</done>
</task>

<task type="auto">
  <name>Task 2: Create root analytics conversions command and conversions sub-dimension commands (timeseries, totals)</name>
  <files>
    packages/twentythree-cli/src/commands/analytics/conversions/index.ts
    packages/twentythree-cli/src/commands/analytics/conversions/timeseries.ts
    packages/twentythree-cli/src/commands/analytics/conversions/totals.ts
  </files>
  <read_first>
    packages/twentythree-cli/src/commands/analytics/video/timeseries.ts
    packages/twentythree-cli/src/commands/analytics/live/timeseries.ts
  </read_first>
  <action>
All 3 conversions commands follow the EXACT same pattern as `analytics/video/timeseries.ts` -- non-paginated GET commands with date+filter flags. The root conversions command has no p/size per RESEARCH table.

**File 1: `analytics/conversions/index.ts`** -- Root analytics conversions command (ANL-05)
- Class: `AnalyticsConversionsIndex extends AuthenticatedCommand<typeof AnalyticsConversionsIndex>`
- `static description = 'Get conversion analytics data'`
- `static enableJsonFlag = true`
- `static flags = { ...AuthenticatedCommand.baseFlags, ...ANALYTICS_DATE_FLAGS, ...ANALYTICS_FILTER_FLAGS }` (NO pagination -- root conversions has no p/size per RESEARCH table)
- API path: `this.apiClient.GET('/analytics/data/conversions', { params: { query: { date_start: flags['date-start'], date_end: flags['date-end'], date_expression: flags['date-expression'], selection: flags.selection, groupby: flags.groupby as any, orderby: flags.orderby as any, order: flags.order as any } } })`
- Table headers: `['Conversions', 'Views', 'Visits', 'Engagement']`
- Row fields: `conversions, views, visits, engagement`

**File 2: `analytics/conversions/timeseries.ts`**
- Class: `AnalyticsConversionsTimeseries extends AuthenticatedCommand<typeof AnalyticsConversionsTimeseries>`
- `static description = 'Get conversion analytics time series data'`
- `static enableJsonFlag = true`
- `static flags = { ...AuthenticatedCommand.baseFlags, ...ANALYTICS_DATE_FLAGS, ...ANALYTICS_FILTER_FLAGS }` (NO pagination -- conversions timeseries has no p/size per RESEARCH table)
- API path: `this.apiClient.GET('/analytics/data/conversions/timeseries', { params: { query: { date_start: flags['date-start'], date_end: flags['date-end'], date_expression: flags['date-expression'], selection: flags.selection, groupby: flags.groupby as any, orderby: flags.orderby as any, order: flags.order as any } } })`
- Table headers: `['Date', 'Conversions', 'Views', 'Visits']`
- Row fields: `date, conversions, views, visits`

**File 3: `analytics/conversions/totals.ts`**
- Class: `AnalyticsConversionsTotals`
- `static description = 'Get conversion analytics totals'`
- NO pagination flags
- API path: `'/analytics/data/conversions/totals'`
- Table headers: `['Conversions', 'Views', 'Visits', 'Engagement']`
- Row fields: `conversions, views, visits, engagement`

Same imports, error handling, response cast, JSON output, empty state, and count line as all other analytics commands.

After creating, run `pnpm --filter twentythree-cli exec tsc --noEmit` and `pnpm --filter twentythree-cli test --run` to verify no new errors and all test stubs still pass.
  </action>
  <verify>
    <automated>cd /Users/steffenchristensen/23/twentythree-cli && ls packages/twentythree-cli/src/commands/analytics/conversions/*.ts | wc -l && pnpm --filter twentythree-cli exec tsc --noEmit 2>&1 | grep -c "analytics/conversions" || echo "0 new errors" && pnpm --filter twentythree-cli test --run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File `packages/twentythree-cli/src/commands/analytics/conversions/index.ts` contains `apiClient.GET('/analytics/data/conversions'` (root, not a sub-dimension path)
    - File `packages/twentythree-cli/src/commands/analytics/conversions/timeseries.ts` contains `apiClient.GET('/analytics/data/conversions/timeseries'`
    - File `packages/twentythree-cli/src/commands/analytics/conversions/totals.ts` contains `apiClient.GET('/analytics/data/conversions/totals'`
    - None of the 3 files include ANALYTICS_PAGINATION_FLAGS (no p/size for conversions)
    - All 3 files have `static enableJsonFlag = true`
    - All 3 files import from `../../../lib/analytics-flags.js`
    - No new TypeScript errors from these files
    - vitest suite passes
  </acceptance_criteria>
  <done>Root analytics conversions command (ANL-05) and both sub-dimension commands implemented (ANL-06). No pagination per API spec. Date filter flags per D-2/D-3. --json output per ANL-08.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI -> API | Analytics GET requests with user-provided date/filter/pagination strings passed as query params |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | I (Information Disclosure) | analytics GET endpoints | accept | Analytics data is scoped to authenticated workspace via bearer token. No cross-tenant risk. |
| T-07-02 | T (Tampering) | date filter and pagination params | accept | All params are strings/integers passed as query params to API. API validates and rejects invalid values. No CLI-side validation needed. |
| T-07-03 | S (Spoofing) | live event path `/live/event/timeseries` | accept | Path is hardcoded in source; user cannot alter the API path. |
</threat_model>

<verification>
1. Wave 0 test stubs: 9 test files exist across `analytics/__tests__/`, `audience/__tests__/`, `audience/field/__tests__/`
2. Root `analytics/video/index.ts` exists and maps to GET `/analytics/data/videos`
3. Root `analytics/live/index.ts` exists and maps to GET `/analytics/data/live`
4. Root `analytics/conversions/index.ts` exists and maps to GET `/analytics/data/conversions`
5. All 6 `analytics/live/` sub-dimension command files exist with correct API paths
6. All 3 `analytics/conversions/` command files (including root) exist with correct API paths
7. All 11 commands have `enableJsonFlag = true`
8. All commands import and spread shared analytics flags
9. `tsc --noEmit` produces no new errors from these files
10. `pnpm --filter twentythree-cli test --run` passes (all stubs discovered, no failures)
11. Paginated commands (video index, live index, live weekday, live event) include `p: flags.page, size: flags.size` in query
12. Non-paginated commands (timeseries, totals, event-timeseries, event-totals, conversions root/timeseries/totals) do NOT spread ANALYTICS_PAGINATION_FLAGS
</verification>

<success_criteria>
- `twentythree analytics video --help` shows date, pagination, and filter flags (root command via index.ts)
- `twentythree analytics live --help` shows date, pagination, and filter flags (root command via index.ts)
- `twentythree analytics live timeseries --help` shows date flags but NOT --page/--size
- `twentythree analytics live event-timeseries --help` shows date flags
- `twentythree analytics conversions --help` shows date and filter flags but NOT --page/--size (root command via index.ts)
- `twentythree analytics conversions totals --help` shows date flags but NOT --page/--size
- All 11 commands compile without TypeScript errors
- vitest suite passes with .todo() stubs discovered
</success_criteria>

<output>
After completion, create `.planning/phases/07-analytics-audience/07-02-SUMMARY.md`
</output>
