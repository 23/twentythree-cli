---
phase: 14-bug-audit-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/twentythree-cli/src/commands/video/section/delete.ts
  - packages/twentythree-cli/src/commands/video/subtitle/delete.ts
  - packages/twentythree-cli/src/commands/video/update.ts
  - packages/twentythree-cli/src/commands/webinar/mail/preview.ts
  - packages/twentythree-cli/src/upload/chunked-upload.ts
  - packages/twentythree-cli/src/commands/video/subtitle/data.ts
  - packages/twentythree-cli/src/commands/video/frame.ts
  - packages/twentythree-cli/src/commands/video/section/list.ts
  - packages/twentythree-cli/src/commands/video/replace.ts
  - packages/twentythree-cli/src/commands/video/transcoding-progress.ts
  - packages/twentythree-cli/src/commands/video/get.ts
  - packages/twentythree-cli/src/commands/video/list.ts
  - packages/twentythree-cli/src/commands/auth/credentials.ts
  - packages/twentythree-cli/src/lib/base-command.ts
  - tsconfig.base.json
autonomous: true
requirements:
  - BUG-01
  - BUG-02

must_haves:
  truths:
    - "tsc --noEmit exits 0 with no TypeScript errors"
    - "vitest test suite passes with no new failures"
    - "No command file calls formatApiError without importing it"
    - "No file references HeadersInit (DOM-only type not available in this project)"
  artifacts:
    - path: "packages/twentythree-cli/src/commands/video/section/delete.ts"
      provides: "formatApiError import added"
      contains: "formatApiError"
    - path: "packages/twentythree-cli/src/commands/video/subtitle/delete.ts"
      provides: "formatApiError import added"
      contains: "formatApiError"
    - path: "packages/twentythree-cli/src/commands/video/update.ts"
      provides: "formatApiError import added"
      contains: "formatApiError"
    - path: "packages/twentythree-cli/src/commands/video/subtitle/data.ts"
      provides: "token param added to query"
      contains: "fetchVideoToken"
    - path: "tsconfig.base.json"
      provides: "moduleResolution set to bundler"
      contains: "bundler"
  key_links:
    - from: "video/section/delete.ts"
      to: "lib/output.ts"
      via: "named import"
      pattern: "import.*formatApiError.*from.*output"
    - from: "video/subtitle/data.ts"
      to: "base-command.ts fetchVideoToken"
      via: "this.fetchVideoToken(args.id)"
      pattern: "fetchVideoToken"
---

<objective>
Fix all 15 TypeScript errors across the twentythree-cli codebase.

Purpose: Eliminate runtime crash risks (TS2304 missing imports cause ReferenceErrors) and compile-time type mismatches per D-01 and D-02. This is the prerequisite to rebuilding and publishing the fixed CLI.

Output: All 15 source files corrected; `tsc --noEmit` exits 0; vitest passes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-bug-audit-fix/14-CONTEXT.md
@.planning/phases/14-bug-audit-fix/14-RESEARCH.md
@.planning/phases/14-bug-audit-fix/14-PATTERNS.md

<interfaces>
<!-- Source of truth for shared utilities — all TS2304 fixes import from here -->
From packages/twentythree-cli/src/lib/output.ts:
```typescript
export const EXIT_SUCCESS = 0
export const EXIT_ERROR = 1
export const EXIT_CANCELLED = 2
export function formatJsonOutput(data: unknown, breadcrumb?: Breadcrumb): string
export function renderTable(headers: string[], rows: string[][]): string
export function formatBytes(bytes: number): string
export function formatApiError(error: unknown): string
export function parseBoolParam(...values: (boolean | undefined)[]): boolean | undefined
export function resolveUrl(domain: string, path: string): string
```

From packages/twentythree-cli/src/api/types.ts (relevant param type):
```typescript
// GET /photo/list query params:
include_unpublished_p?: string;  // <-- string, NOT number
```

From packages/twentythree-cli/src/lib/base-command.ts:
```typescript
protected async fetchVideoToken(videoId: string | number): Promise<string>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix runtime-crash TS2304 errors (missing imports and HeadersInit)</name>
  <files>
    packages/twentythree-cli/src/commands/video/section/delete.ts,
    packages/twentythree-cli/src/commands/video/subtitle/delete.ts,
    packages/twentythree-cli/src/commands/video/update.ts,
    packages/twentythree-cli/src/commands/webinar/mail/preview.ts,
    packages/twentythree-cli/src/upload/chunked-upload.ts
  </files>
  <read_first>
    packages/twentythree-cli/src/commands/video/section/delete.ts,
    packages/twentythree-cli/src/commands/video/subtitle/delete.ts,
    packages/twentythree-cli/src/commands/video/update.ts,
    packages/twentythree-cli/src/commands/webinar/mail/preview.ts,
    packages/twentythree-cli/src/upload/chunked-upload.ts,
    packages/twentythree-cli/src/lib/output.ts
  </read_first>
  <action>
Fix 5 TS2304 errors that cause runtime ReferenceErrors. Per D-02 these are highest priority.

**formatApiError missing import (D-03, D-04, D-05):**

1. `src/commands/video/section/delete.ts` — Find the import line from `../../../lib/output.js` and add `formatApiError` to the named imports. The existing import is:
   `import { formatJsonOutput, EXIT_ERROR, EXIT_CANCELLED } from '../../../lib/output.js'`
   Change to:
   `import { formatJsonOutput, formatApiError, EXIT_ERROR, EXIT_CANCELLED } from '../../../lib/output.js'`

2. `src/commands/video/subtitle/delete.ts` — Same fix. Find import from `../../../lib/output.js`, add `formatApiError`:
   `import { formatJsonOutput, formatApiError, EXIT_ERROR, EXIT_CANCELLED } from '../../../lib/output.js'`

3. `src/commands/video/update.ts` — Find import from `../../lib/output.js`, add `formatApiError`:
   `import { formatJsonOutput, parseBoolParam, formatApiError, EXIT_ERROR, EXIT_CANCELLED } from '../../lib/output.js'`

**HeadersInit not in scope (D-13, D-14):**

4. `src/commands/webinar/mail/preview.ts` line 72 — Replace `const headers: HeadersInit = {}` with `const headers: Record<string, string> = {}`. The project has `"lib": ["ES2022"]` with no DOM lib, so `HeadersInit` is undefined. `Record<string, string>` is compatible with Node 22 native fetch headers.

5. `src/upload/chunked-upload.ts` line 125 — Replace `const headers: HeadersInit = {}` with `const headers: Record<string, string> = {}`. Same rationale.
  </action>
  <verify>
    <automated>cd /Users/steffenchristensen/23/twentythree-cli && pnpm --filter twentythree-cli exec tsc --noEmit 2>&1 | grep -c "TS2304" | xargs -I{} test {} -le 1</automated>
  </verify>
  <acceptance_criteria>
    - `video/section/delete.ts` contains `import { formatJsonOutput, formatApiError, EXIT_ERROR, EXIT_CANCELLED } from '../../../lib/output.js'`
    - `video/subtitle/delete.ts` contains `import { formatJsonOutput, formatApiError, EXIT_ERROR, EXIT_CANCELLED } from '../../../lib/output.js'`
    - `video/update.ts` contains `formatApiError` in its import line from `../../lib/output.js`
    - `webinar/mail/preview.ts` contains `Record<string, string>` and does NOT contain `HeadersInit`
    - `upload/chunked-upload.ts` contains `Record<string, string>` and does NOT contain `HeadersInit`
    - `pnpm --filter twentythree-cli exec tsc --noEmit 2>&1 | grep "TS2304"` returns at most the conf module resolution warning (D-17), no formatApiError or HeadersInit errors
  </acceptance_criteria>
  <done>All 5 TS2304 runtime-crash errors eliminated. formatApiError imported in 3 files. HeadersInit replaced in 2 files.</done>
</task>

<task type="auto">
  <name>Task 2: Fix type mismatches, missing property, and tsconfig (TS2322, TS2345, TS2741, TS18048, TS2307)</name>
  <files>
    packages/twentythree-cli/src/commands/video/section/list.ts,
    packages/twentythree-cli/src/commands/video/replace.ts,
    packages/twentythree-cli/src/commands/video/transcoding-progress.ts,
    packages/twentythree-cli/src/commands/video/get.ts,
    packages/twentythree-cli/src/commands/video/list.ts,
    packages/twentythree-cli/src/commands/video/frame.ts,
    packages/twentythree-cli/src/commands/video/subtitle/data.ts,
    packages/twentythree-cli/src/commands/auth/credentials.ts,
    packages/twentythree-cli/src/lib/base-command.ts,
    tsconfig.base.json
  </files>
  <read_first>
    packages/twentythree-cli/src/commands/video/section/list.ts,
    packages/twentythree-cli/src/commands/video/replace.ts,
    packages/twentythree-cli/src/commands/video/transcoding-progress.ts,
    packages/twentythree-cli/src/commands/video/get.ts,
    packages/twentythree-cli/src/commands/video/list.ts,
    packages/twentythree-cli/src/commands/video/frame.ts,
    packages/twentythree-cli/src/commands/video/subtitle/data.ts,
    packages/twentythree-cli/src/commands/auth/credentials.ts,
    packages/twentythree-cli/src/lib/base-command.ts,
    tsconfig.base.json
  </read_first>
  <action>
Fix the remaining 10 TypeScript errors per D-01 (all errors in scope).

**photo_id string-to-number mismatches (D-06, D-09, D-12) — wrap args.id with Number():**

1. `src/commands/video/section/list.ts` line 42 — Change `photo_id: args.id` to `photo_id: Number(args.id)`. The arg is `required: true` so unconditional Number() is safe.

2. `src/commands/video/replace.ts` line 95 — Change `photo_id: args.id` to `photo_id: Number(args.id)`.

3. `src/commands/video/transcoding-progress.ts` line 44 — Change `photo_id: args.id` to `photo_id: Number(args.id)`.

**include_unpublished_p number-to-string mismatches (D-07, D-08, D-16):**

The generated type for `/photo/list` query param `include_unpublished_p` is `string`, not `number`. All three call sites pass the integer `1`.

4. `src/commands/video/get.ts` line 45 — Change `include_unpublished_p: 1` to `include_unpublished_p: '1'`.

5. `src/commands/video/list.ts` line 57 — Change `parseBoolParam(...) ? 1 : undefined` to `parseBoolParam(...) ? '1' : undefined`. The full line becomes:
   `include_unpublished_p: parseBoolParam(flags['include-unpublished'], flags['include-unpublished-p']) ? '1' : undefined,`

6. `src/lib/base-command.ts` line 184 — Change `include_unpublished_p: 1` to `include_unpublished_p: '1'`.

**Path not in PathsWithMethod (D-10):**

7. `src/commands/video/frame.ts` line 48 — Change `this.apiClient.POST('/photo/frame',` to `this.apiClient.POST('/photo/frame' as any,`. This is the established codebase pattern for spec gaps.

**Missing required token property (D-11):**

8. `src/commands/video/subtitle/data.ts` — Add `const token = await this.fetchVideoToken(args.id)` before the GET call (around line 52). Then add `token,` to the query params object and ensure `photo_id` is `Number(args.id)`. The query becomes:
   ```
   params: {
     query: {
       photo_id: Number(args.id),
       token,
       locale: flags['subtitle-id'],
       subtitle_format: flags.format,
       type: flags.type,
     },
   },
   ```
   `fetchVideoToken` is available via `AuthenticatedCommand` — no import needed.

**Possibly undefined (D-15):**

9. `src/commands/auth/credentials.ts` line 30 — Change `v.includes('.')` to `v?.includes('.')`. This handles the `string | symbol` union from `@clack/prompts` strict types.

**conf module resolution (D-17):**

10. `tsconfig.base.json` — Change `"moduleResolution": "node"` to `"moduleResolution": "bundler"`. This resolves the `Cannot find module 'conf'` error and aligns with tsdown/Rolldown's bundler-first approach.
  </action>
  <verify>
    <automated>cd /Users/steffenchristensen/23/twentythree-cli && pnpm --filter twentythree-cli exec tsc --noEmit && pnpm --filter twentythree-cli test --run</automated>
  </verify>
  <acceptance_criteria>
    - `video/section/list.ts` contains `photo_id: Number(args.id)`
    - `video/replace.ts` contains `photo_id: Number(args.id)`
    - `video/transcoding-progress.ts` contains `photo_id: Number(args.id)`
    - `video/get.ts` contains `include_unpublished_p: '1'` (string, not number)
    - `video/list.ts` contains `? '1' : undefined` for include_unpublished_p
    - `lib/base-command.ts` line 184 contains `include_unpublished_p: '1'`
    - `video/frame.ts` contains `'/photo/frame' as any`
    - `video/subtitle/data.ts` contains `fetchVideoToken` and `token,` in query params
    - `auth/credentials.ts` contains `v?.includes('.')`
    - `tsconfig.base.json` contains `"moduleResolution": "bundler"`
    - `pnpm --filter twentythree-cli exec tsc --noEmit` exits 0
    - `pnpm --filter twentythree-cli test --run` passes with 0 failures
  </acceptance_criteria>
  <done>All 15 TypeScript errors fixed. tsc --noEmit exits 0. vitest passes with no new failures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries introduced. This phase edits existing source files to fix type errors and missing imports. No authentication, authorization, or input validation changes.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering | `as any` casts in video/frame.ts, video/get.ts | accept | Cast is scoped to openapi-fetch path argument only; the API server validates all input; existing pattern in codebase; no user-supplied data flows through the cast |
| T-14-02 | Information Disclosure | `fetchVideoToken` added to subtitle/data.ts | accept | Token is already used in identical pattern by section/list.ts; token is ephemeral per-video; transmitted over existing authenticated HTTPS channel |
</threat_model>

<verification>
1. `pnpm --filter twentythree-cli exec tsc --noEmit` exits 0 with zero errors
2. `pnpm --filter twentythree-cli test --run` passes all existing tests with no new failures
3. `grep -r "HeadersInit" packages/twentythree-cli/src/` returns zero results
4. `grep -rn "formatApiError" packages/twentythree-cli/src/commands/video/section/delete.ts packages/twentythree-cli/src/commands/video/subtitle/delete.ts packages/twentythree-cli/src/commands/video/update.ts` shows formatApiError in import lines of all 3 files
</verification>

<success_criteria>
- tsc --noEmit exits 0 (zero TypeScript errors)
- vitest passes with no new test failures
- All 15 errors from the Complete Error Inventory in RESEARCH.md are resolved
</success_criteria>

<output>
After completion, create `.planning/phases/14-bug-audit-fix/14-01-SUMMARY.md`
</output>
