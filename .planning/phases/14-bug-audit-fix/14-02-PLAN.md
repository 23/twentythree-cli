---
phase: 14-bug-audit-fix
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - packages/twentythree-cli/package.json
autonomous: false
requirements:
  - BUG-01
  - BUG-02

must_haves:
  truths:
    - "npm registry has a patched version of twentythree-cli with all TypeScript fixes compiled into dist/"
    - "globally installed twentythree-cli runs video list without ReferenceError"
  artifacts:
    - path: "packages/twentythree-cli/package.json"
      provides: "bumped patch version"
      contains: "1.0.1"
    - path: "packages/twentythree-cli/dist/"
      provides: "rebuilt bundle reflecting all source fixes"
  key_links:
    - from: "package.json version"
      to: "npm registry"
      via: "npm publish"
      pattern: "1.0.1"
---

<objective>
Bump version to 1.0.1, rebuild dist/, and publish to npm so end users get the fixed CLI.

Purpose: Per D-19, fixing source alone does not fix globally installed CLIs. The stale dist/ is the root cause of BUG-01 (`parseBoolParam is not defined`). Rebuild + publish makes the fix available via `npm install -g twentythree-cli`.

Output: Version 1.0.1 published to npm; dist/ reflects all 15 fixes from Plan 01.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-bug-audit-fix/14-CONTEXT.md
@.planning/phases/14-bug-audit-fix/14-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump version to 1.0.1 and rebuild</name>
  <files>packages/twentythree-cli/package.json</files>
  <read_first>
    packages/twentythree-cli/package.json
  </read_first>
  <action>
1. In `packages/twentythree-cli/package.json`, change `"version": "1.0.0"` to `"version": "1.0.1"`.

2. Run the full build to regenerate dist/:
   ```
   cd packages/twentythree-cli && pnpm build
   ```

3. Verify the build succeeded by running both gates per D-18:
   ```
   pnpm --filter twentythree-cli exec tsc --noEmit
   pnpm --filter twentythree-cli test --run
   ```

4. Verify dist/ was freshly built by checking its modification timestamp is within the last minute.
  </action>
  <verify>
    <automated>cd /Users/steffenchristensen/23/twentythree-cli && pnpm --filter twentythree-cli build && pnpm --filter twentythree-cli exec tsc --noEmit && pnpm --filter twentythree-cli test --run</automated>
  </verify>
  <acceptance_criteria>
    - `packages/twentythree-cli/package.json` contains `"version": "1.0.1"`
    - `packages/twentythree-cli/dist/` exists and was modified after the version bump
    - `pnpm --filter twentythree-cli exec tsc --noEmit` exits 0
    - `pnpm --filter twentythree-cli test --run` exits 0
  </acceptance_criteria>
  <done>Version bumped to 1.0.1 and dist/ rebuilt with all fixes compiled in.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Publish 1.0.1 to npm and verify</name>
  <files>packages/twentythree-cli/package.json</files>
  <action>
Human publishes 1.0.1 to npm and verifies the global install works. The executor presents the publish command and verification steps to the user.

Publish command: `cd packages/twentythree-cli && npm publish`

The `prepack` script in package.json runs `pnpm build` automatically, so dist/ will be freshly rebuilt during publish.
  </action>
  <what-built>
Version bumped to 1.0.1, dist/ rebuilt with all 15 TypeScript fixes. Ready to publish to npm per D-19.
  </what-built>
  <how-to-verify>
1. Run `cd packages/twentythree-cli && npm publish` to push 1.0.1 to npm
2. After publish succeeds, verify: `npm view twentythree-cli version` should return `1.0.1`
3. Install globally: `npm install -g twentythree-cli`
4. Run `twentythree --version` and confirm it shows `1.0.1`
5. Run `twentythree video list` (with an authenticated workspace) and confirm NO `ReferenceError: parseBoolParam is not defined` error
  </how-to-verify>
  <verify>
    <automated>npm view twentythree-cli version 2>&1 | grep -q "1.0.1"</automated>
  </verify>
  <acceptance_criteria>
    - `npm view twentythree-cli version` returns `1.0.1`
    - `twentythree --version` shows `1.0.1` after global install
    - `twentythree video list` runs without ReferenceError
  </acceptance_criteria>
  <done>Version 1.0.1 published to npm and verified working via global install.</done>
  <resume-signal>Type "published" after confirming npm publish succeeded, or describe any issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local build -> npm registry | Publishing distributes code to all users |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-03 | Tampering | npm publish | mitigate | Verify `tsc --noEmit` and `vitest` pass before publish; prepack script ensures fresh build; human checkpoint confirms publish |
| T-14-04 | Repudiation | version bump | accept | Git commit tracks the version change; npm registry is immutable per version |
</threat_model>

<verification>
1. `npm view twentythree-cli version` returns `1.0.1`
2. `twentythree --version` shows `1.0.1` after global install
3. `twentythree video list` runs without ReferenceError (BUG-01 resolved for end users)
</verification>

<success_criteria>
- Version 1.0.1 is live on npm
- Global install works and `twentythree --version` shows 1.0.1
- `video list` runs without the parseBoolParam crash
</success_criteria>

<output>
After completion, create `.planning/phases/14-bug-audit-fix/14-02-SUMMARY.md`
</output>
