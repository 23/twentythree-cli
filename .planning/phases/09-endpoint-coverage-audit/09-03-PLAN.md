---
phase: 09-endpoint-coverage-audit
plan: 03
type: execute
wave: 3
depends_on:
  - 09-01
  - 09-02
files_modified:
  - packages/twentythree-cli/src/commands/analytics/usage/devices/timeseries.ts
  - packages/twentythree-cli/src/commands/analytics/usage/devices/totals.ts
  - packages/twentythree-cli/src/commands/analytics/usage/domains/totals.ts
  - packages/twentythree-cli/src/commands/analytics/usage/locations/totals.ts
  - packages/twentythree-cli/src/commands/analytics/usage/sourceids/totals.ts
  - packages/twentythree-cli/src/commands/analytics/usage/sources/totals.ts
  - packages/twentythree-cli/src/commands/analytics/usage/spots/timeseries.ts
  - packages/twentythree-cli/src/commands/analytics/usage/spots/totals.ts
  - packages/twentythree-cli/src/commands/analytics/usage/traffic/timeseries.ts
  - packages/twentythree-cli/src/commands/analytics/usage/traffic/totals.ts
autonomous: true
requirements:
  - AUDIT-02
must_haves:
  truths:
    - "Running `twentythree analytics usage devices timeseries --help` shows command help"
    - "Running `twentythree analytics usage traffic totals --help` shows command help"
    - "Each new command has a unique agentMetadata.api_endpoint matching the OpenAPI spec path"
    - "Audit script exits 0 after all 10 usage commands are added"
  artifacts:
    - path: "packages/twentythree-cli/src/commands/analytics/usage/devices/timeseries.ts"
      provides: "GET /analytics/data/usage/devices/timeseries command"
      contains: "api_endpoint: 'GET /analytics/data/usage/devices/timeseries'"
    - path: "packages/twentythree-cli/src/commands/analytics/usage/devices/totals.ts"
      provides: "GET /analytics/data/usage/devices/totals command"
      contains: "api_endpoint: 'GET /analytics/data/usage/devices/totals'"
    - path: "packages/twentythree-cli/src/commands/analytics/usage/domains/totals.ts"
      provides: "GET /analytics/data/usage/domains/totals command"
      contains: "api_endpoint: 'GET /analytics/data/usage/domains/totals'"
    - path: "packages/twentythree-cli/src/commands/analytics/usage/locations/totals.ts"
      provides: "GET /analytics/data/usage/locations/totals command"
      contains: "api_endpoint: 'GET /analytics/data/usage/locations/totals'"
    - path: "packages/twentythree-cli/src/commands/analytics/usage/sourceids/totals.ts"
      provides: "GET /analytics/data/usage/sourceids/totals command"
      contains: "api_endpoint: 'GET /analytics/data/usage/sourceids/totals'"
    - path: "packages/twentythree-cli/src/commands/analytics/usage/sources/totals.ts"
      provides: "GET /analytics/data/usage/sources/totals command"
      contains: "api_endpoint: 'GET /analytics/data/usage/sources/totals'"
    - path: "packages/twentythree-cli/src/commands/analytics/usage/spots/timeseries.ts"
      provides: "GET /analytics/data/usage/spots/timeseries command"
      contains: "api_endpoint: 'GET /analytics/data/usage/spots/timeseries'"
    - path: "packages/twentythree-cli/src/commands/analytics/usage/spots/totals.ts"
      provides: "GET /analytics/data/usage/spots/totals command"
      contains: "api_endpoint: 'GET /analytics/data/usage/spots/totals'"
    - path: "packages/twentythree-cli/src/commands/analytics/usage/traffic/timeseries.ts"
      provides: "GET /analytics/data/usage/traffic/timeseries command"
      contains: "api_endpoint: 'GET /analytics/data/usage/traffic/timeseries'"
    - path: "packages/twentythree-cli/src/commands/analytics/usage/traffic/totals.ts"
      provides: "GET /analytics/data/usage/traffic/totals command"
      contains: "api_endpoint: 'GET /analytics/data/usage/traffic/totals'"
  key_links:
    - from: "all 10 new commands"
      to: "src/lib/base-command.js"
      via: "extends AuthenticatedCommand"
      pattern: "extends AuthenticatedCommand"
    - from: "audit script"
      to: "all 10 new commands"
      via: "api_endpoint grep"
      pattern: "api_endpoint.*analytics/data/usage"
---

<objective>
Implement the remaining 10 analytics gap-fill commands in the usage analytics group (devices, domains, locations, sourceids, sources, spots, traffic -- timeseries and/or totals for each). After this plan plus Plan 02, all 18 analytics gaps are closed and the audit script exits 0.

Purpose: Close the final 10 analytics endpoint gaps so the audit script passes clean.
Output: 10 new command files in `src/commands/analytics/usage/` subdirectories
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-endpoint-coverage-audit/09-CONTEXT.md
@.planning/phases/09-endpoint-coverage-audit/09-RESEARCH.md
@.planning/phases/09-endpoint-coverage-audit/09-PATTERNS.md

<interfaces>
<!-- Executor must replicate the exact pattern from these analog files -->

From packages/twentythree-cli/src/commands/analytics/video/timeseries.ts (TIMESERIES archetype):
```typescript
import chalk from 'chalk'
import { AuthenticatedCommand } from '../../../lib/base-command.js'
import { renderTable, formatJsonOutput, formatApiError, EXIT_ERROR } from '../../../lib/output.js'
import { applyCliTerms } from '../../../lib/term-map.js'
import { ANALYTICS_DATE_FLAGS, ANALYTICS_FILTER_FLAGS } from '../../../lib/analytics-flags.js'

// Class structure: description, examples, enableJsonFlag, agentMetadata, flags, run()
// run(): parse, printWorkspaceHeader, apiClient.GET, error handling, json output, table render
```

From packages/twentythree-cli/src/commands/analytics/usage/devices.ts (parent command -- reference for usage group):
```typescript
// Parent uses ANALYTICS_PAGINATION_FLAGS -- sub-series do NOT use pagination
// Parent's agentMetadata: api_endpoint: 'GET /analytics/data/usage/devices'
```

Import depth for 4-level commands (usage/devices/, usage/spots/, usage/traffic/):
```typescript
import { AuthenticatedCommand } from '../../../../lib/base-command.js'
import { renderTable, formatJsonOutput, formatApiError, EXIT_ERROR } from '../../../../lib/output.js'
import { applyCliTerms } from '../../../../lib/term-map.js'
import { ANALYTICS_DATE_FLAGS, ANALYTICS_FILTER_FLAGS } from '../../../../lib/analytics-flags.js'
```

Existing parent commands in analytics/usage/ that already exist (NOT to be modified):
- devices.ts, domains.ts, locations.ts, sourceids.ts, sources.ts, spots.ts, traffic.ts, storage.ts
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create usage analytics sub-series commands (devices, domains, locations, sourceids, sources)</name>
  <read_first>
    - packages/twentythree-cli/src/commands/analytics/usage/devices.ts (parent device command -- column/structure reference)
    - packages/twentythree-cli/src/commands/analytics/usage/domains.ts (parent domains command -- column reference)
    - packages/twentythree-cli/src/commands/analytics/usage/locations.ts (parent locations command -- column reference)
    - packages/twentythree-cli/src/commands/analytics/usage/sourceids.ts (parent sourceids command -- column reference)
    - packages/twentythree-cli/src/commands/analytics/usage/sources.ts (parent sources command -- column reference)
    - packages/twentythree-cli/src/commands/analytics/video/timeseries.ts (timeseries run() body archetype)
    - packages/twentythree-cli/src/commands/analytics/video/totals.ts (totals run() body archetype)
  </read_first>
  <files>
    packages/twentythree-cli/src/commands/analytics/usage/devices/timeseries.ts
    packages/twentythree-cli/src/commands/analytics/usage/devices/totals.ts
    packages/twentythree-cli/src/commands/analytics/usage/domains/totals.ts
    packages/twentythree-cli/src/commands/analytics/usage/locations/totals.ts
    packages/twentythree-cli/src/commands/analytics/usage/sourceids/totals.ts
    packages/twentythree-cli/src/commands/analytics/usage/sources/totals.ts
  </files>
  <action>
Create 6 new command files. All are 4 levels deep -- imports use `'../../../../lib/'` prefix. Each follows the exact timeseries.ts or totals.ts archetype pattern. Flags: `ANALYTICS_DATE_FLAGS` + `ANALYTICS_FILTER_FLAGS` (NO pagination).

**Per-command specifics:**

1. `analytics/usage/devices/timeseries.ts`:
   - Class: `AnalyticsUsageDevicesTimeseries`
   - Description: `'Get usage analytics by device type - time series'`
   - api_endpoint: `'GET /analytics/data/usage/devices/timeseries'`
   - API path in GET call: `'/analytics/data/usage/devices/timeseries'`
   - output_shape columns: `['Date', 'Device', 'Plays', 'Engagement']`
   - Table headers: `['Date', 'Device', 'Plays', 'Engagement']`
   - Row mapping: `row.date, row.device, row.plays, row.engagement`

2. `analytics/usage/devices/totals.ts`:
   - Class: `AnalyticsUsageDevicesTotals`
   - Description: `'Get aggregated usage analytics by device type - totals'`
   - api_endpoint: `'GET /analytics/data/usage/devices/totals'`
   - API path in GET call: `'/analytics/data/usage/devices/totals'`
   - output_shape columns: `['Device', 'Plays', 'Engagement', 'Traffic', 'Impressions']`
   - Table headers: `['Device', 'Plays', 'Engagement', 'Traffic', 'Impressions']`
   - Row mapping: `row.device, row.plays, row.engagement, row.traffic, row.impressions`

3. `analytics/usage/domains/totals.ts`:
   - Class: `AnalyticsUsageDomainsTotals`
   - Description: `'Get aggregated usage analytics by embed domain - totals'`
   - api_endpoint: `'GET /analytics/data/usage/domains/totals'`
   - API path in GET call: `'/analytics/data/usage/domains/totals'`
   - output_shape columns: `['Domain', 'Plays', 'Engagement', 'Traffic']`
   - Table headers: `['Domain', 'Plays', 'Engagement', 'Traffic']`
   - Row mapping: `row.domain, row.plays, row.engagement, row.traffic`

4. `analytics/usage/locations/totals.ts`:
   - Class: `AnalyticsUsageLocationsTotals`
   - Description: `'Get aggregated usage analytics by location - totals'`
   - api_endpoint: `'GET /analytics/data/usage/locations/totals'`
   - API path in GET call: `'/analytics/data/usage/locations/totals'`
   - output_shape columns: `['Location', 'Plays', 'Engagement', 'Traffic']`
   - Table headers: `['Location', 'Plays', 'Engagement', 'Traffic']`
   - Row mapping: `row.location, row.plays, row.engagement, row.traffic`

5. `analytics/usage/sourceids/totals.ts`:
   - Class: `AnalyticsUsageSourceidsTotals`
   - Description: `'Get aggregated usage analytics by source ID - totals'`
   - api_endpoint: `'GET /analytics/data/usage/sourceids/totals'`
   - API path in GET call: `'/analytics/data/usage/sourceids/totals'`
   - output_shape columns: `['Source ID', 'Plays', 'Engagement', 'Traffic']`
   - Table headers: `['Source ID', 'Plays', 'Engagement', 'Traffic']`
   - Row mapping: `row.sourceid, row.plays, row.engagement, row.traffic`

6. `analytics/usage/sources/totals.ts`:
   - Class: `AnalyticsUsageSourcesTotals`
   - Description: `'Get aggregated usage analytics by source - totals'`
   - api_endpoint: `'GET /analytics/data/usage/sources/totals'`
   - API path in GET call: `'/analytics/data/usage/sources/totals'`
   - output_shape columns: `['Source', 'Plays', 'Engagement', 'Traffic']`
   - Table headers: `['Source', 'Plays', 'Engagement', 'Traffic']`
   - Row mapping: `row.source, row.plays, row.engagement, row.traffic`

Each `run()` body must include the complete query parameter pass-through from the timeseries/totals archetype (date_start, date_end, date_expression, selection, groupby, orderby, order). Include eslint-disable comments for `as any` casts.

Examples pattern for each: `<%= config.bin %> analytics usage {topic} {type} --date-expression thisweek` (3 examples matching existing style).
  </action>
  <verify>
    <automated>cd /Users/steffenchristensen/23/twentythree-cli && pnpm --filter twentythree-cli exec tsc --noEmit 2>&1 | grep -c "error TS" || echo "0 errors"</automated>
  </verify>
  <acceptance_criteria>
    - File `src/commands/analytics/usage/devices/timeseries.ts` contains `api_endpoint: 'GET /analytics/data/usage/devices/timeseries'`
    - File `src/commands/analytics/usage/devices/totals.ts` contains `api_endpoint: 'GET /analytics/data/usage/devices/totals'`
    - File `src/commands/analytics/usage/domains/totals.ts` contains `api_endpoint: 'GET /analytics/data/usage/domains/totals'`
    - File `src/commands/analytics/usage/locations/totals.ts` contains `api_endpoint: 'GET /analytics/data/usage/locations/totals'`
    - File `src/commands/analytics/usage/sourceids/totals.ts` contains `api_endpoint: 'GET /analytics/data/usage/sourceids/totals'`
    - File `src/commands/analytics/usage/sources/totals.ts` contains `api_endpoint: 'GET /analytics/data/usage/sources/totals'`
    - All 6 files contain `extends AuthenticatedCommand`
    - All 6 files contain `static enableJsonFlag = true`
    - All 6 files import from `'../../../../lib/base-command.js'`
    - `tsc --noEmit` introduces no new type errors
  </acceptance_criteria>
  <done>6 usage analytics sub-series commands created (devices timeseries/totals, domains totals, locations totals, sourceids totals, sources totals).</done>
</task>

<task type="auto">
  <name>Task 2: Create usage spots and traffic sub-series commands, run final audit</name>
  <read_first>
    - packages/twentythree-cli/src/commands/analytics/usage/spots.ts (parent spots command -- column reference)
    - packages/twentythree-cli/src/commands/analytics/usage/traffic.ts (parent traffic command -- column reference)
    - packages/twentythree-cli/src/commands/analytics/video/timeseries.ts (timeseries archetype)
    - packages/twentythree-cli/src/commands/analytics/video/totals.ts (totals archetype)
    - packages/twentythree-cli/scripts/audit-endpoints.mjs (audit script to run at end)
    - packages/twentythree-cli/src/lib/audit.ts (EXCLUDED_OPERATIONS -- verify still correct)
  </read_first>
  <files>
    packages/twentythree-cli/src/commands/analytics/usage/spots/timeseries.ts
    packages/twentythree-cli/src/commands/analytics/usage/spots/totals.ts
    packages/twentythree-cli/src/commands/analytics/usage/traffic/timeseries.ts
    packages/twentythree-cli/src/commands/analytics/usage/traffic/totals.ts
  </files>
  <action>
Create the final 4 usage analytics sub-series commands. All are 4 levels deep -- imports use `'../../../../lib/'`. Same archetype pattern as all other analytics commands.

**Per-command specifics:**

1. `analytics/usage/spots/timeseries.ts`:
   - Class: `AnalyticsUsageSpotsTimeseries`
   - Description: `'Get spot usage analytics - time series'`
   - api_endpoint: `'GET /analytics/data/usage/spots/timeseries'`
   - API path in GET call: `'/analytics/data/usage/spots/timeseries'`
   - output_shape columns: `['Date', 'Spot', 'Plays', 'Engagement']`
   - Table headers: `['Date', 'Spot', 'Plays', 'Engagement']`
   - Row mapping: `row.date, row.spot, row.plays, row.engagement`

2. `analytics/usage/spots/totals.ts`:
   - Class: `AnalyticsUsageSpotsTotals`
   - Description: `'Get aggregated spot usage analytics - totals'`
   - api_endpoint: `'GET /analytics/data/usage/spots/totals'`
   - API path in GET call: `'/analytics/data/usage/spots/totals'`
   - output_shape columns: `['Spot', 'Plays', 'Engagement', 'Traffic']`
   - Table headers: `['Spot', 'Plays', 'Engagement', 'Traffic']`
   - Row mapping: `row.spot, row.plays, row.engagement, row.traffic`

3. `analytics/usage/traffic/timeseries.ts`:
   - Class: `AnalyticsUsageTrafficTimeseries`
   - Description: `'Get traffic usage analytics - time series'`
   - api_endpoint: `'GET /analytics/data/usage/traffic/timeseries'`
   - API path in GET call: `'/analytics/data/usage/traffic/timeseries'`
   - output_shape columns: `['Date', 'Traffic Type', 'Plays', 'Engagement']`
   - Table headers: `['Date', 'Traffic Type', 'Plays', 'Engagement']`
   - Row mapping: `row.date, row.traffic_type ?? row.type, row.plays, row.engagement`

4. `analytics/usage/traffic/totals.ts`:
   - Class: `AnalyticsUsageTrafficTotals`
   - Description: `'Get aggregated traffic usage analytics - totals'`
   - api_endpoint: `'GET /analytics/data/usage/traffic/totals'`
   - API path in GET call: `'/analytics/data/usage/traffic/totals'`
   - output_shape columns: `['Traffic Type', 'Plays', 'Engagement', 'Traffic', 'Impressions']`
   - Table headers: `['Traffic Type', 'Plays', 'Engagement', 'Traffic', 'Impressions']`
   - Row mapping: `row.traffic_type ?? row.type, row.plays, row.engagement, row.traffic, row.impressions`

Each follows the same complete pattern: query parameter pass-through, eslint-disable comments, error handling, JSON output, empty state, table rendering, row count footer.

**After creating all 4 files:** Run the audit script to verify it exits 0. All 18 analytics gaps should now be covered by commands (8 from Plan 02 + 10 from Plan 03). The 8 EXCLUDED_OPERATIONS entries cover the remaining non-analytics gaps and phantoms. Expected output: `Gaps: 0`, `Phantoms: 0`, exit code 0.

Also run `pnpm --filter twentythree-cli test --run` to confirm no test regressions.
  </action>
  <verify>
    <automated>cd /Users/steffenchristensen/23/twentythree-cli && node packages/twentythree-cli/scripts/audit-endpoints.mjs && echo "AUDIT PASSED (exit 0)"</automated>
  </verify>
  <acceptance_criteria>
    - File `src/commands/analytics/usage/spots/timeseries.ts` contains `api_endpoint: 'GET /analytics/data/usage/spots/timeseries'`
    - File `src/commands/analytics/usage/spots/totals.ts` contains `api_endpoint: 'GET /analytics/data/usage/spots/totals'`
    - File `src/commands/analytics/usage/traffic/timeseries.ts` contains `api_endpoint: 'GET /analytics/data/usage/traffic/timeseries'`
    - File `src/commands/analytics/usage/traffic/totals.ts` contains `api_endpoint: 'GET /analytics/data/usage/traffic/totals'`
    - All 4 files contain `extends AuthenticatedCommand` and `static enableJsonFlag = true`
    - `node packages/twentythree-cli/scripts/audit-endpoints.mjs` exits 0
    - Audit output shows `Gaps: 0` and `Phantoms: 0`
    - `pnpm --filter twentythree-cli test --run` passes
    - `pnpm --filter twentythree-cli exec tsc --noEmit` introduces no new errors
  </acceptance_criteria>
  <done>All 18 analytics gaps filled. Audit script exits 0 -- every OpenAPI endpoint is either covered by a command or documented in EXCLUDED_OPERATIONS. Phase 9 goal achieved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| New commands extend AuthenticatedCommand | All API calls require authenticated workspace -- no anonymous access possible |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-07 | Spoofing | New usage analytics commands | mitigate | All 10 commands extend `AuthenticatedCommand` which validates workspace auth before API calls |
| T-09-08 | Information Disclosure | Analytics data response | accept | Analytics data is read-only, scoped to authenticated workspace; no cross-tenant risk |
| T-09-09 | Elevation of Privilege | agentMetadata auth_scope | mitigate | All commands declare `auth_scope: 'read'` matching GET-only analytics endpoints |
</threat_model>

<verification>
1. `node packages/twentythree-cli/scripts/audit-endpoints.mjs` exits 0 (the ultimate phase gate)
2. `pnpm --filter twentythree-cli exec tsc --noEmit` shows no new errors
3. `pnpm --filter twentythree-cli test --run` passes
4. All 10 files exist with correct `api_endpoint` values
</verification>

<success_criteria>
- All 18 analytics gap endpoints have corresponding command files
- Audit script exits 0: 0 gaps, 0 phantoms
- Every new command meets D-03 quality bar: typed flags, --json, table rendering, agentMetadata
- Phase 9 goal achieved: every OpenAPI endpoint covered or documented as intentional omission
</success_criteria>

<output>
After completion, create `.planning/phases/09-endpoint-coverage-audit/09-03-SUMMARY.md`
</output>
