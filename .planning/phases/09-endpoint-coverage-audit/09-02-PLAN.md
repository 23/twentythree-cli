---
phase: 09-endpoint-coverage-audit
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - packages/twentythree-cli/src/commands/analytics/video/weekday/timeseries.ts
  - packages/twentythree-cli/src/commands/analytics/video/weekday/totals.ts
  - packages/twentythree-cli/src/commands/analytics/video/performance/timeseries.ts
  - packages/twentythree-cli/src/commands/analytics/video/performance/totals.ts
  - packages/twentythree-cli/src/commands/analytics/video/published/timeseries.ts
  - packages/twentythree-cli/src/commands/analytics/video/published/totals.ts
  - packages/twentythree-cli/src/commands/analytics/live/weekday/timeseries.ts
  - packages/twentythree-cli/src/commands/analytics/live/weekday/totals.ts
autonomous: true
requirements:
  - AUDIT-02
must_haves:
  truths:
    - "Running `twentythree analytics video weekday timeseries --help` shows command help"
    - "Running `twentythree analytics video weekday totals --help` shows command help"
    - "Running `twentythree analytics video performance timeseries --help` shows command help"
    - "Running `twentythree analytics video performance totals --help` shows command help"
    - "Running `twentythree analytics video published timeseries --help` shows command help"
    - "Running `twentythree analytics video published totals --help` shows command help"
    - "Running `twentythree analytics live weekday timeseries --help` shows command help"
    - "Running `twentythree analytics live weekday totals --help` shows command help"
    - "Each new command has a unique agentMetadata.api_endpoint matching the OpenAPI spec path"
  artifacts:
    - path: "packages/twentythree-cli/src/commands/analytics/video/weekday/timeseries.ts"
      provides: "GET /analytics/data/videos/weekday/timeseries command"
      contains: "api_endpoint: 'GET /analytics/data/videos/weekday/timeseries'"
    - path: "packages/twentythree-cli/src/commands/analytics/video/weekday/totals.ts"
      provides: "GET /analytics/data/videos/weekday/totals command"
      contains: "api_endpoint: 'GET /analytics/data/videos/weekday/totals'"
    - path: "packages/twentythree-cli/src/commands/analytics/video/performance/timeseries.ts"
      provides: "GET /analytics/data/videos/performance/timeseries command"
      contains: "api_endpoint: 'GET /analytics/data/videos/performance/timeseries'"
    - path: "packages/twentythree-cli/src/commands/analytics/video/performance/totals.ts"
      provides: "GET /analytics/data/videos/performance/totals command"
      contains: "api_endpoint: 'GET /analytics/data/videos/performance/totals'"
    - path: "packages/twentythree-cli/src/commands/analytics/video/published/timeseries.ts"
      provides: "GET /analytics/data/videos/published/timeseries command"
      contains: "api_endpoint: 'GET /analytics/data/videos/published/timeseries'"
    - path: "packages/twentythree-cli/src/commands/analytics/video/published/totals.ts"
      provides: "GET /analytics/data/videos/published/totals command"
      contains: "api_endpoint: 'GET /analytics/data/videos/published/totals'"
    - path: "packages/twentythree-cli/src/commands/analytics/live/weekday/timeseries.ts"
      provides: "GET /analytics/data/live/weekday/timeseries command"
      contains: "api_endpoint: 'GET /analytics/data/live/weekday/timeseries'"
    - path: "packages/twentythree-cli/src/commands/analytics/live/weekday/totals.ts"
      provides: "GET /analytics/data/live/weekday/totals command"
      contains: "api_endpoint: 'GET /analytics/data/live/weekday/totals'"
  key_links:
    - from: "all 8 new commands"
      to: "src/lib/base-command.js"
      via: "extends AuthenticatedCommand"
      pattern: "extends AuthenticatedCommand"
    - from: "all 8 new commands"
      to: "src/lib/analytics-flags.js"
      via: "ANALYTICS_DATE_FLAGS, ANALYTICS_FILTER_FLAGS"
      pattern: "ANALYTICS_DATE_FLAGS"
---

<objective>
Implement 8 analytics gap-fill commands: 6 in the video analytics group (weekday, performance, published -- each with timeseries + totals) and 2 in the live/webinar analytics group (weekday timeseries + totals). All follow the exact pattern of existing sibling commands.

Purpose: Close 8 of the 18 analytics endpoint gaps identified by the audit script.
Output: 8 new command files in `src/commands/analytics/video/` and `src/commands/analytics/live/`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-endpoint-coverage-audit/09-CONTEXT.md
@.planning/phases/09-endpoint-coverage-audit/09-RESEARCH.md
@.planning/phases/09-endpoint-coverage-audit/09-PATTERNS.md

<interfaces>
<!-- Executor must replicate the exact pattern from these analog files -->

From packages/twentythree-cli/src/commands/analytics/video/timeseries.ts (TIMESERIES archetype):
```typescript
import chalk from 'chalk'
import { AuthenticatedCommand } from '../../../lib/base-command.js'
import { renderTable, formatJsonOutput, formatApiError, EXIT_ERROR } from '../../../lib/output.js'
import { applyCliTerms } from '../../../lib/term-map.js'
import { ANALYTICS_DATE_FLAGS, ANALYTICS_FILTER_FLAGS } from '../../../lib/analytics-flags.js'

export default class AnalyticsVideoTimeseries extends AuthenticatedCommand<typeof AnalyticsVideoTimeseries> {
  static description = 'Get video analytics time series data'
  static enableJsonFlag = true
  static agentMetadata = {
    api_endpoint: 'GET /analytics/data/videos/timeseries',
    auth_scope: 'read' as const,
    output_shape: { type: 'table' as const, columns: ['Date', 'Plays', 'Engagement', 'Playrate', 'Traffic'] },
    side_effects: 'none' as const,
  }
  static flags = {
    ...AuthenticatedCommand.baseFlags,
    ...ANALYTICS_DATE_FLAGS,
    ...ANALYTICS_FILTER_FLAGS,
  }
  // run() follows standard pattern: parse, printWorkspaceHeader, GET, error handling, JSON output, table render
}
```

From packages/twentythree-cli/src/commands/analytics/video/totals.ts (TOTALS archetype):
```typescript
// Same imports as timeseries
export default class AnalyticsVideoTotals extends AuthenticatedCommand<typeof AnalyticsVideoTotals> {
  static description = 'Get aggregated video analytics totals'
  static enableJsonFlag = true
  static agentMetadata = {
    api_endpoint: 'GET /analytics/data/videos/totals',
    auth_scope: 'read' as const,
    output_shape: { type: 'table' as const, columns: ['Plays', 'Engagement', 'Playrate', 'Avg View Time', 'Traffic'] },
    side_effects: 'none' as const,
  }
  // Same flags, same run() pattern
}
```

Import depth for 4-level commands (video/weekday/, video/performance/, video/published/, live/weekday/):
```typescript
import { AuthenticatedCommand } from '../../../../lib/base-command.js'
import { renderTable, formatJsonOutput, formatApiError, EXIT_ERROR } from '../../../../lib/output.js'
import { applyCliTerms } from '../../../../lib/term-map.js'
import { ANALYTICS_DATE_FLAGS, ANALYTICS_FILTER_FLAGS } from '../../../../lib/analytics-flags.js'
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create video analytics sub-series commands (weekday, performance, published)</name>
  <read_first>
    - packages/twentythree-cli/src/commands/analytics/video/timeseries.ts (timeseries archetype -- copy structure exactly)
    - packages/twentythree-cli/src/commands/analytics/video/totals.ts (totals archetype -- copy structure exactly)
    - packages/twentythree-cli/src/commands/analytics/video/weekday.ts (existing parent weekday command -- for description/column reference)
    - packages/twentythree-cli/src/commands/analytics/video/performance.ts (existing parent performance command -- for description/column reference)
    - packages/twentythree-cli/src/commands/analytics/video/published.ts (existing parent published command -- for description/column reference)
    - packages/twentythree-cli/src/lib/analytics-flags.ts (flags exports)
  </read_first>
  <files>
    packages/twentythree-cli/src/commands/analytics/video/weekday/timeseries.ts
    packages/twentythree-cli/src/commands/analytics/video/weekday/totals.ts
    packages/twentythree-cli/src/commands/analytics/video/performance/timeseries.ts
    packages/twentythree-cli/src/commands/analytics/video/performance/totals.ts
    packages/twentythree-cli/src/commands/analytics/video/published/timeseries.ts
    packages/twentythree-cli/src/commands/analytics/video/published/totals.ts
  </files>
  <action>
Create 6 new command files. Each is a near-exact copy of the parent-level analog (timeseries.ts or totals.ts) with these changes:

**All 6 files share:**
- Import depth is `../../../../lib/` (4 levels deep)
- `static enableJsonFlag = true`
- `static flags` uses `ANALYTICS_DATE_FLAGS` + `ANALYTICS_FILTER_FLAGS` (NO pagination flags -- sub-series do not paginate)
- `run()` body is identical to the analog timeseries.ts or totals.ts (parse, printWorkspaceHeader, apiClient.GET, error handling, json output, table rendering)
- JSDoc comment block above the class matching style: `/** Analytics video {topic} {type} command -- returns {description}. */`
- Examples use `<%= config.bin %> analytics video {topic} {type} --date-expression thisweek` pattern (3 examples: date-expression, date-start+date-end, --json)

**Per-command specifics:**

1. `analytics/video/weekday/timeseries.ts`:
   - Class: `AnalyticsVideoWeekdayTimeseries`
   - Description: `'Get video analytics by weekday - time series'`
   - api_endpoint: `'GET /analytics/data/videos/weekday/timeseries'`
   - API path in GET call: `'/analytics/data/videos/weekday/timeseries'`
   - output_shape columns: `['Date', 'Day', 'Plays', 'Engagement', 'Playrate']`
   - Table headers: `['Date', 'Day', 'Plays', 'Engagement', 'Playrate']`
   - Row mapping: `row.date, row.day ?? row.weekday, row.plays, row.engagement, row.playrate`

2. `analytics/video/weekday/totals.ts`:
   - Class: `AnalyticsVideoWeekdayTotals`
   - Description: `'Get aggregated video analytics by weekday - totals'`
   - api_endpoint: `'GET /analytics/data/videos/weekday/totals'`
   - API path in GET call: `'/analytics/data/videos/weekday/totals'`
   - output_shape columns: `['Day', 'Plays', 'Engagement', 'Playrate']`
   - Table headers: `['Day', 'Plays', 'Engagement', 'Playrate']`
   - Row mapping: `row.day ?? row.weekday, row.plays, row.engagement, row.playrate`

3. `analytics/video/performance/timeseries.ts`:
   - Class: `AnalyticsVideoPerformanceTimeseries`
   - Description: `'Get video playthrough performance - time series'`
   - api_endpoint: `'GET /analytics/data/videos/performance/timeseries'`
   - API path in GET call: `'/analytics/data/videos/performance/timeseries'`
   - output_shape columns: `['Date', 'Plays', 'Engagement', 'Playthrough']`
   - Table headers: `['Date', 'Plays', 'Engagement', 'Playthrough']`
   - Row mapping: `row.date, row.plays, row.engagement, row.playthrough`

4. `analytics/video/performance/totals.ts`:
   - Class: `AnalyticsVideoPerformanceTotals`
   - Description: `'Get aggregated video playthrough performance - totals'`
   - api_endpoint: `'GET /analytics/data/videos/performance/totals'`
   - API path in GET call: `'/analytics/data/videos/performance/totals'`
   - output_shape columns: `['Plays', 'Engagement', 'Playthrough']`
   - Table headers: `['Plays', 'Engagement', 'Playthrough']`
   - Row mapping: `row.plays, row.engagement, row.playthrough`

5. `analytics/video/published/timeseries.ts`:
   - Class: `AnalyticsVideoPublishedTimeseries`
   - Description: `'Get video analytics by publish date - time series'`
   - api_endpoint: `'GET /analytics/data/videos/published/timeseries'`
   - API path in GET call: `'/analytics/data/videos/published/timeseries'`
   - output_shape columns: `['Date', 'Published', 'Plays', 'Engagement']`
   - Table headers: `['Date', 'Published', 'Plays', 'Engagement']`
   - Row mapping: `row.date, row.published, row.plays, row.engagement`

6. `analytics/video/published/totals.ts`:
   - Class: `AnalyticsVideoPublishedTotals`
   - Description: `'Get aggregated video analytics by publish date - totals'`
   - api_endpoint: `'GET /analytics/data/videos/published/totals'`
   - API path in GET call: `'/analytics/data/videos/published/totals'`
   - output_shape columns: `['Published', 'Plays', 'Engagement']`
   - Table headers: `['Published', 'Plays', 'Engagement']`
   - Row mapping: `row.published, row.plays, row.engagement`

Each `run()` body must include the complete query parameter pass-through matching the timeseries.ts analog:
```typescript
params: { query: {
  date_start: flags['date-start'],
  date_end: flags['date-end'],
  date_expression: flags['date-expression'],
  selection: flags.selection,
  groupby: flags.groupby as any,
  orderby: flags.orderby as any,
  order: flags.order as any,
}}
```

Include the same eslint-disable comments for `as any` casts that appear in the analog files.
  </action>
  <verify>
    <automated>cd /Users/steffenchristensen/23/twentythree-cli && pnpm --filter twentythree-cli exec tsc --noEmit 2>&1 | grep -c "error TS" || echo "0 errors"</automated>
  </verify>
  <acceptance_criteria>
    - File `src/commands/analytics/video/weekday/timeseries.ts` contains `api_endpoint: 'GET /analytics/data/videos/weekday/timeseries'`
    - File `src/commands/analytics/video/weekday/totals.ts` contains `api_endpoint: 'GET /analytics/data/videos/weekday/totals'`
    - File `src/commands/analytics/video/performance/timeseries.ts` contains `api_endpoint: 'GET /analytics/data/videos/performance/timeseries'`
    - File `src/commands/analytics/video/performance/totals.ts` contains `api_endpoint: 'GET /analytics/data/videos/performance/totals'`
    - File `src/commands/analytics/video/published/timeseries.ts` contains `api_endpoint: 'GET /analytics/data/videos/published/timeseries'`
    - File `src/commands/analytics/video/published/totals.ts` contains `api_endpoint: 'GET /analytics/data/videos/published/totals'`
    - All 6 files contain `extends AuthenticatedCommand`
    - All 6 files contain `static enableJsonFlag = true`
    - All 6 files import from `'../../../../lib/base-command.js'`
    - All 6 files contain `ANALYTICS_DATE_FLAGS` and `ANALYTICS_FILTER_FLAGS`
    - `tsc --noEmit` introduces no new type errors from these files
  </acceptance_criteria>
  <done>6 video analytics sub-series commands created matching full hand-authored quality bar (per D-03). Each has typed flags, --json support, table rendering, agentMetadata, and follows oclif conventions.</done>
</task>

<task type="auto">
  <name>Task 2: Create live/webinar weekday sub-series commands</name>
  <read_first>
    - packages/twentythree-cli/src/commands/analytics/live/timeseries.ts (live timeseries archetype)
    - packages/twentythree-cli/src/commands/analytics/live/totals.ts (live totals archetype)
    - packages/twentythree-cli/src/commands/analytics/live/weekday.ts (parent weekday command -- column reference)
  </read_first>
  <files>
    packages/twentythree-cli/src/commands/analytics/live/weekday/timeseries.ts
    packages/twentythree-cli/src/commands/analytics/live/weekday/totals.ts
  </files>
  <action>
Create 2 new command files for the live/webinar weekday sub-series. These follow the same pattern as the video sub-series but under `analytics/live/weekday/`.

Import depth is `../../../../lib/` (4 levels deep). Copy the structure from `analytics/live/timeseries.ts` and `analytics/live/totals.ts` respectively, adjusting:

1. `analytics/live/weekday/timeseries.ts`:
   - Class: `AnalyticsLiveWeekdayTimeseries`
   - Description: `'Get live/webinar analytics by weekday - time series'`
   - api_endpoint: `'GET /analytics/data/live/weekday/timeseries'`
   - API path in GET call: `'/analytics/data/live/weekday/timeseries'`
   - output_shape columns: `['Date', 'Day', 'Plays', 'Peak Viewers', 'Engagement']`
   - Table headers: `['Date', 'Day', 'Plays', 'Peak Viewers', 'Engagement']`
   - Row mapping: `row.date, row.day ?? row.weekday, row.plays, row.peakviewers, row.engagement`
   - Examples: `analytics live weekday timeseries --date-expression thisweek`, `--date-start ... --date-end ...`, `--json`
   - Flags: `ANALYTICS_DATE_FLAGS` + `ANALYTICS_FILTER_FLAGS` (NO pagination -- sub-series do not paginate)

2. `analytics/live/weekday/totals.ts`:
   - Class: `AnalyticsLiveWeekdayTotals`
   - Description: `'Get aggregated live/webinar analytics by weekday - totals'`
   - api_endpoint: `'GET /analytics/data/live/weekday/totals'`
   - API path in GET call: `'/analytics/data/live/weekday/totals'`
   - output_shape columns: `['Day', 'Plays', 'Peak Viewers', 'Engagement', 'Playrate']`
   - Table headers: `['Day', 'Plays', 'Peak Viewers', 'Engagement', 'Playrate']`
   - Row mapping: `row.day ?? row.weekday, row.plays, row.peakviewers, row.engagement, row.playrate`
   - Examples: `analytics live weekday totals --date-expression thismonth`, `--date-start ... --date-end ...`, `--json`
   - Flags: `ANALYTICS_DATE_FLAGS` + `ANALYTICS_FILTER_FLAGS` (NO pagination)

Both files use the complete query parameter pass-through and the same error handling, JSON output, empty state, and table rendering patterns as all other analytics commands.
  </action>
  <verify>
    <automated>cd /Users/steffenchristensen/23/twentythree-cli && pnpm --filter twentythree-cli exec tsc --noEmit 2>&1 | grep -c "error TS" || echo "0 errors"</automated>
  </verify>
  <acceptance_criteria>
    - File `src/commands/analytics/live/weekday/timeseries.ts` contains `api_endpoint: 'GET /analytics/data/live/weekday/timeseries'`
    - File `src/commands/analytics/live/weekday/totals.ts` contains `api_endpoint: 'GET /analytics/data/live/weekday/totals'`
    - Both files contain `extends AuthenticatedCommand`
    - Both files contain `static enableJsonFlag = true`
    - Both files import from `'../../../../lib/base-command.js'`
    - Both files contain `ANALYTICS_DATE_FLAGS` and `ANALYTICS_FILTER_FLAGS`
    - Neither file contains `ANALYTICS_PAGINATION_FLAGS` (sub-series do not paginate)
    - `tsc --noEmit` introduces no new type errors
  </acceptance_criteria>
  <done>2 live/webinar weekday sub-series commands created. Combined with Task 1, 8 of 18 analytics gaps are closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| New commands extend AuthenticatedCommand | All API calls require authenticated workspace -- no anonymous access possible |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04 | Spoofing | New analytics commands | mitigate | All 8 commands extend `AuthenticatedCommand` which validates workspace auth before API calls |
| T-09-05 | Information Disclosure | Analytics data response | accept | Analytics data is read-only, scoped to authenticated workspace; no cross-tenant risk |
| T-09-06 | Elevation of Privilege | agentMetadata auth_scope | mitigate | All commands declare `auth_scope: 'read'` matching GET-only analytics endpoints |
</threat_model>

<verification>
1. `pnpm --filter twentythree-cli exec tsc --noEmit` shows no new errors
2. All 8 files exist with correct `api_endpoint` values
3. `grep -r "api_endpoint:" packages/twentythree-cli/src/commands/analytics/video/weekday/ packages/twentythree-cli/src/commands/analytics/video/performance/ packages/twentythree-cli/src/commands/analytics/video/published/ packages/twentythree-cli/src/commands/analytics/live/weekday/` shows 8 unique spec-matching endpoints
</verification>

<success_criteria>
- 8 new analytics commands exist matching full quality bar (D-03)
- Each command has typed flags, --json output, table rendering, agentMetadata
- Audit script gap count reduced by 8 (from 18 to 10)
</success_criteria>

<output>
After completion, create `.planning/phases/09-endpoint-coverage-audit/09-02-SUMMARY.md`
</output>
