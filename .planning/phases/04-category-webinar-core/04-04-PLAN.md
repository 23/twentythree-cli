---
phase: 04-category-webinar-core
plan: 04
type: execute
wave: 2
depends_on:
  - 04-02
files_modified:
  - packages/twentythree-cli/src/commands/webinar/metrics.ts
  - packages/twentythree-cli/src/commands/webinar/clips.ts
  - packages/twentythree-cli/src/commands/webinar/highlights.ts
  - packages/twentythree-cli/src/commands/webinar/list-formats.ts
  - packages/twentythree-cli/src/commands/webinar/log.ts
  - packages/twentythree-cli/src/commands/webinar/repeat.ts
  - packages/twentythree-cli/src/commands/webinar/__tests__/metrics.test.ts
  - packages/twentythree-cli/src/commands/webinar/__tests__/clips.test.ts
  - packages/twentythree-cli/src/commands/webinar/__tests__/highlights.test.ts
  - packages/twentythree-cli/src/commands/webinar/__tests__/list-formats.test.ts
  - packages/twentythree-cli/src/commands/webinar/__tests__/log.test.ts
  - packages/twentythree-cli/src/commands/webinar/__tests__/repeat.test.ts
autonomous: true
requirements:
  - WEB-06
  - WEB-07
  - WEB-08
  - WEB-09
  - WEB-10
  - WEB-11

must_haves:
  truths:
    - "twentythree webinar metrics <id> renders a two-column key-value table using m.formated ?? m.value (note: formated with one t)"
    - "twentythree webinar clips <id> renders a table with Video ID, Title, Duration, Type, Published, Views columns"
    - "twentythree webinar highlights <id> renders a table with Type, Start, End, Absolute Start columns"
    - "twentythree webinar list-formats renders a table with Key, Name columns (no id argument)"
    - "twentythree webinar log <id> renders a table with Event, Start, End columns"
    - "twentythree webinar repeat <id> --date <datetime> sends POST /live/repeat and prints new webinar ID + admin URL"
    - "All six commands support --json returning {ok, data, summary, breadcrumbs}"
  artifacts:
    - path: "packages/twentythree-cli/src/commands/webinar/metrics.ts"
      provides: "webinar metrics command"
      exports: ["default"]
    - path: "packages/twentythree-cli/src/commands/webinar/clips.ts"
      provides: "webinar clips command"
      exports: ["default"]
    - path: "packages/twentythree-cli/src/commands/webinar/highlights.ts"
      provides: "webinar highlights command"
      exports: ["default"]
    - path: "packages/twentythree-cli/src/commands/webinar/list-formats.ts"
      provides: "webinar list-formats command"
      exports: ["default"]
    - path: "packages/twentythree-cli/src/commands/webinar/log.ts"
      provides: "webinar log command"
      exports: ["default"]
    - path: "packages/twentythree-cli/src/commands/webinar/repeat.ts"
      provides: "webinar repeat command"
      exports: ["default"]
  key_links:
    - from: "webinar/metrics.ts"
      to: "/live/metrics"
      via: "this.apiClient.GET"
      pattern: "formated.*value"
    - from: "webinar/repeat.ts"
      to: "/live/repeat"
      via: "this.apiClient.POST"
      pattern: "schedule_start_time"
---

<objective>
Implement the six read-only and action webinar commands: metrics, clips, highlights, list-formats, log, and repeat. All read-only commands render cli-table3 tables. Repeat sends POST /live/repeat and prints the new webinar's ID and admin URL.

Purpose: These commands complete the Phase 4 webinar surface. Each is a straightforward GET-then-table-render or single POST, following established patterns.

Output: Six command files and six test files in `src/commands/webinar/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-category-webinar-core/04-CONTEXT.md
@.planning/phases/04-category-webinar-core/04-RESEARCH.md

<interfaces>
From src/lib/base-command.ts:
```typescript
export abstract class AuthenticatedCommand<T extends typeof Command> extends BaseCommand<T> {
  protected activeWorkspace!: WorkspaceEntry
  protected apiBaseUrl!: string
  protected apiClient!: ReturnType<typeof createApiClient>
  protected printWorkspaceHeader(): void
}
```

From src/lib/output.ts:
```typescript
export function formatJsonOutput(params: JsonOutputParams): JsonOutput
export function renderTable(headers: string[], rows: string[][]): InstanceType<typeof Table>
export function formatApiError(error: unknown): string
export const EXIT_ERROR = 1
```

From 04-RESEARCH.md (endpoint details):
- /live/metrics GET: returns `{ metric, value, formated }[]` — note "formated" typo (one t)
- /live/clips GET: live_id required; returns clips with photo_id, title, duration_fmt, etc.
- /live/highlights GET: live_id required; returns type, relative_start_time, relative_end_time, absolute_start_time
- /live/list-formats GET: no required params; returns `{ key, name }[]`
- /live/log GET: live_id required; returns event, start_time__date, start_time__time, end_time__date, end_time__time
- /live/repeat POST: live_id + schedule_start_time; returns NEW { live_id, token }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Webinar metrics, clips, highlights commands with tests</name>
  <files>
    packages/twentythree-cli/src/commands/webinar/metrics.ts
    packages/twentythree-cli/src/commands/webinar/clips.ts
    packages/twentythree-cli/src/commands/webinar/highlights.ts
    packages/twentythree-cli/src/commands/webinar/__tests__/metrics.test.ts
    packages/twentythree-cli/src/commands/webinar/__tests__/clips.test.ts
    packages/twentythree-cli/src/commands/webinar/__tests__/highlights.test.ts
  </files>
  <read_first>
    packages/twentythree-cli/src/commands/video/list.ts
    packages/twentythree-cli/src/commands/webinar/list.ts
    packages/twentythree-cli/src/lib/output.ts
    packages/twentythree-cli/src/lib/term-map.ts
    packages/twentythree-cli/src/lib/base-command.ts
  </read_first>
  <action>
All three commands follow the same skeleton: GET endpoint with live_id, cast response as any, extract data array, render table, support --json.

**1. `webinar/metrics.ts`** — WEB-06
- Class name: `WebinarMetrics`
- `static description = 'Retrieve metrics for a webinar'`
- Args: `id` (string, required, description: 'Webinar ID')
- No special flags beyond base
- API call: `this.apiClient.GET('/live/metrics', { params: { query: { live_id: Number(args.id) } } })`
- Error handling: `if (error) this.error(applyCliTerms(formatApiError(error)), { exit: EXIT_ERROR })`
- Extract data: `const resp = data as any; const metrics: any[] = Array.isArray(resp?.data) ? resp.data : resp?.data ? [resp.data] : []`
- Table: TWO-COLUMN key-value table (per CONTEXT.md decision 3):
  ```typescript
  const headers = ['Metric', 'Value']
  const rows = metrics.map((m: any) => [
    applyCliTerms(String(m.metric ?? '')),
    String(m.formated ?? m.value ?? ''),  // CRITICAL: 'formated' not 'formatted'
  ])
  ```
- Empty state: `'No metrics available.'`
- Summary: `${metrics.length} metric${metrics.length === 1 ? '' : 's'}`
- JSON breadcrumbs: `[{ domain }, { resource: 'webinar', id: args.id }]`

**2. `webinar/clips.ts`** — WEB-07
- Class name: `WebinarClips`
- `static description = 'List recording clips from a webinar'`
- Args: `id` (string, required, description: 'Webinar ID')
- API call: `this.apiClient.GET('/live/clips', { params: { query: { live_id: Number(args.id) } } })`
- Table headers: `['Video ID', 'Title', 'Duration', 'Type', 'Published', 'Views']`
- Row mapping:
  ```typescript
  const rows = clips.map((c: any) => [
    String(c.photo_id ?? ''),
    applyCliTerms(String(c.title ?? '')),
    String(c.duration_fmt ?? ''),
    String(c.live_highlight_type ?? ''),
    c.published_p ? 'yes' : 'no',
    String(c.view_count_fmt ?? c.view_count ?? ''),
  ])
  ```
- Empty state: `'No clips found.'`
- Summary: `${clips.length} clip${clips.length === 1 ? '' : 's'}`

**3. `webinar/highlights.ts`** — WEB-08
- Class name: `WebinarHighlights`
- `static description = 'List highlights from a webinar'`
- Args: `id` (string, required, description: 'Webinar ID')
- Optional flags: `--video-id` (string, optional, description: 'Scope to specific recording')
- API call: `this.apiClient.GET('/live/highlights', { params: { query: { live_id: Number(args.id), photo_id: flags['video-id'] ? Number(flags['video-id']) : undefined } } })`
- Table headers: `['Type', 'Start', 'End', 'Absolute Start']`
- Row mapping:
  ```typescript
  const rows = highlights.map((h: any) => [
    String(h.type ?? ''),
    String(h.relative_start_time ?? ''),
    String(h.relative_end_time ?? ''),
    String(h.absolute_start_time ?? ''),
  ])
  ```
- Empty state: `'No highlights found.'`
- Summary: `${highlights.length} highlight${highlights.length === 1 ? '' : 's'}`

**4. Test files** — Three test files with stubs:
- metrics: `'uses formated (one t) not formatted for value display'`, `'renders two-column key-value table'`
- clips: `'renders table with Video ID, Title, Duration columns'`, `'returns JSON with clips data'`
- highlights: `'renders table with Type, Start, End, Absolute Start columns'`, `'passes photo_id when --video-id provided'`
  </action>
  <verify>
    <automated>cd /Users/steffenchristensen/23/twentythree-cli && pnpm --filter twentythree-cli exec vitest run src/commands/webinar/__tests__/metrics.test.ts src/commands/webinar/__tests__/clips.test.ts src/commands/webinar/__tests__/highlights.test.ts --reporter=dot 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - packages/twentythree-cli/src/commands/webinar/metrics.ts contains `m.formated ?? m.value` (not m.formatted)
    - packages/twentythree-cli/src/commands/webinar/metrics.ts contains `apiClient.GET('/live/metrics'`
    - packages/twentythree-cli/src/commands/webinar/metrics.ts table headers are `['Metric', 'Value']`
    - packages/twentythree-cli/src/commands/webinar/clips.ts contains `apiClient.GET('/live/clips'`
    - packages/twentythree-cli/src/commands/webinar/clips.ts contains `c.photo_id` in row mapping
    - packages/twentythree-cli/src/commands/webinar/highlights.ts contains `apiClient.GET('/live/highlights'`
    - packages/twentythree-cli/src/commands/webinar/highlights.ts contains `h.relative_start_time` in row mapping
    - All three test files exist and vitest runs without error
  </acceptance_criteria>
  <done>Metrics renders two-column key-value table using formated (one t). Clips and highlights render their respective tables. All three support --json. Test stubs pass.</done>
</task>

<task type="auto">
  <name>Task 2: Webinar list-formats, log, repeat commands with tests</name>
  <files>
    packages/twentythree-cli/src/commands/webinar/list-formats.ts
    packages/twentythree-cli/src/commands/webinar/log.ts
    packages/twentythree-cli/src/commands/webinar/repeat.ts
    packages/twentythree-cli/src/commands/webinar/__tests__/list-formats.test.ts
    packages/twentythree-cli/src/commands/webinar/__tests__/log.test.ts
    packages/twentythree-cli/src/commands/webinar/__tests__/repeat.test.ts
  </files>
  <read_first>
    packages/twentythree-cli/src/commands/webinar/metrics.ts
    packages/twentythree-cli/src/commands/webinar/create.ts
    packages/twentythree-cli/src/lib/output.ts
    packages/twentythree-cli/src/lib/base-command.ts
    packages/twentythree-cli/src/lib/term-map.ts
  </read_first>
  <action>
**1. `webinar/list-formats.ts`** — WEB-09. No webinar ID required.
- Class name: `WebinarListFormats`
- `static description = 'List available webinar formats'`
- No args (this is a workspace-level query, not webinar-specific)
- API call: `this.apiClient.GET('/live/list-formats', { params: { query: {} } })`
- Table headers: `['Key', 'Name']`
- Row mapping:
  ```typescript
  const rows = formats.map((f: any) => [
    String(f.key ?? ''),
    String(f.name ?? ''),
  ])
  ```
- Empty state: `'No formats available.'`
- Summary: `${formats.length} format${formats.length === 1 ? '' : 's'}`
- Breadcrumbs: `[{ domain }, { resource: 'webinar' }]` (no id — workspace-level)

**2. `webinar/log.ts`** — WEB-10
- Class name: `WebinarLog`
- `static description = 'Retrieve the event log for a webinar'`
- Args: `id` (string, required, description: 'Webinar ID')
- API call: `this.apiClient.GET('/live/log', { params: { query: { live_id: Number(args.id) } } })`
- Table headers: `['Event', 'Start', 'End']`
- Row mapping — concatenate date + time fields:
  ```typescript
  const rows = events.map((e: any) => [
    String(e.event ?? ''),
    [e.start_time__date, e.start_time__time].filter(Boolean).join(' '),
    [e.end_time__date, e.end_time__time].filter(Boolean).join(' '),
  ])
  ```
- Empty state: `'No log events found.'`
- Summary: `${events.length} event${events.length === 1 ? '' : 's'}`

**3. `webinar/repeat.ts`** — WEB-11. This is a POST, not a GET.
- Class name: `WebinarRepeat`
- `static description = 'Duplicate a webinar and schedule the copy at a new date/time'`
- Args: `id` (string, required, description: 'Webinar ID')
- Flags: `--date` (string, required: true, description: 'Schedule date/time for the new webinar (ISO 8601)')
- Body construction:
  ```typescript
  const body: Record<string, unknown> = {
    live_id: Number(args.id),
    schedule_start_time: flags.date,
  }
  ```
- POST to `/live/repeat` with Content-Type x-www-form-urlencoded
- Extract new webinar ID: `const newLiveId = (repeatData as any)?.data?.live_id`
- Success output (per CONTEXT.md decision 4):
  ```
  this.log('Webinar duplicated and scheduled')
  this.log(`ID:    ${newLiveId}`)
  this.log(`Admin: https://${this.activeWorkspace.domain}/manage/webinar/${newLiveId}`)
  ```
- JSON: `{ ok: true, data: repeatData, summary: 'Webinar duplicated and scheduled', breadcrumbs: [{ domain }, { resource: 'webinar', id: String(newLiveId) }] }`

**4. Test files** — Three test files with stubs:
- list-formats: `'renders two-column Key, Name table'`, `'does not require a webinar ID argument'`
- log: `'concatenates date and time fields for Start and End columns'`, `'returns JSON with log events'`
- repeat: `'sends POST /live/repeat with live_id and schedule_start_time'`, `'prints new webinar ID and admin URL'`, `'--date flag is required'`
  </action>
  <verify>
    <automated>cd /Users/steffenchristensen/23/twentythree-cli && pnpm --filter twentythree-cli exec vitest run src/commands/webinar/__tests__/list-formats.test.ts src/commands/webinar/__tests__/log.test.ts src/commands/webinar/__tests__/repeat.test.ts --reporter=dot 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - packages/twentythree-cli/src/commands/webinar/list-formats.ts contains `apiClient.GET('/live/list-formats'`
    - packages/twentythree-cli/src/commands/webinar/list-formats.ts has NO required `id` arg
    - packages/twentythree-cli/src/commands/webinar/list-formats.ts table headers are `['Key', 'Name']`
    - packages/twentythree-cli/src/commands/webinar/log.ts contains `apiClient.GET('/live/log'`
    - packages/twentythree-cli/src/commands/webinar/log.ts contains `start_time__date` and `start_time__time` concatenation
    - packages/twentythree-cli/src/commands/webinar/repeat.ts contains `apiClient.POST('/live/repeat'`
    - packages/twentythree-cli/src/commands/webinar/repeat.ts contains `schedule_start_time: flags.date`
    - packages/twentythree-cli/src/commands/webinar/repeat.ts contains `manage/webinar/` admin URL with newLiveId
    - packages/twentythree-cli/src/commands/webinar/repeat.ts contains `Webinar duplicated and scheduled`
    - All three test files exist and vitest runs without error
  </acceptance_criteria>
  <done>List-formats renders Key/Name table without webinar ID. Log concatenates date+time fields. Repeat sends POST /live/repeat with schedule_start_time and outputs new webinar ID + admin URL. All support --json. Test stubs pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args to API query params | Webinar ID from user flows into live_id query/body params |
| API response to terminal | Metrics, clips, log data rendered in tables |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-12 | Spoofing | all read-only commands | mitigate | All extend AuthenticatedCommand — anonymous mode rejected |
| T-04-13 | Information Disclosure | webinar/metrics | accept | Metrics are read-only aggregate data; no PII |
| T-04-14 | Tampering | webinar/repeat | accept | --date is passed through to API; server validates date format |
</threat_model>

<verification>
1. `pnpm --filter twentythree-cli exec vitest run src/commands/webinar/__tests__/ --reporter=dot` — all 11 webinar test files pass
2. `grep "formated" packages/twentythree-cli/src/commands/webinar/metrics.ts` — confirms single-t spelling
3. `grep "schedule_start_time" packages/twentythree-cli/src/commands/webinar/repeat.ts` — confirms correct field name
</verification>

<success_criteria>
All six commands exist and function: metrics renders key-value table with formated (one t), clips/highlights/log render multi-column tables, list-formats needs no ID, repeat sends POST and prints new ID + admin URL. All support --json. All test stubs pass.
</success_criteria>

<output>
After completion, create `.planning/phases/04-category-webinar-core/04-04-SUMMARY.md`
</output>
