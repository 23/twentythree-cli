---
phase: 18-package-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 18-package-foundation/01
files_modified:
  - packages/twentythree-skills/skills/SKILL.md
autonomous: true
requirements:
  - SKILL-01

must_haves:
  truths:
    - "An AI agent reading skills/SKILL.md alone can (a) configure auth with twentythree auth credentials, (b) discover any command's metadata via --agent, and (c) pick the right topic from the 22-group resource index"
    - "The file opens with YAML frontmatter containing name: twentythree, a description, triggers, invocable, argument-hint, allowed-tools: Bash(twentythree *), and compatibility"
    - "The body covers auth setup, multi-workspace switching, command syntax, global flags, --agent self-discovery with example output, key invariants, resource index for all 22 groups, meta/utility commands (auth, workspace, doctor, autocomplete), and 2 common workflows (upload+publish, webinar setup)"
    - "Terminology mapping note calls out CLI → API differences (video→photo, category→album, live→webinar)"
    - "pnpm --filter twentythree-skills test still exits 0 after the rewrite (frontmatter keys name + description remain valid)"
  artifacts:
    - path: "packages/twentythree-skills/skills/SKILL.md"
      provides: "Full shell SKILL.md per D-03: auth + syntax + --agent + resource index + allowed-tools"
      min_lines: 150
      contains: "allowed-tools: Bash(twentythree *)"
  key_links:
    - from: "packages/twentythree-skills/skills/SKILL.md frontmatter"
      to: "Claude Code allowed-tools enforcement"
      via: "YAML key"
      pattern: "^allowed-tools: Bash\\(twentythree \\*\\)$"
    - from: "packages/twentythree-skills/skills/SKILL.md auth section"
      to: "twentythree-cli auth commands"
      via: "code-block prerequisite instructions"
      pattern: "twentythree auth credentials"
    - from: "packages/twentythree-skills/skills/SKILL.md resource index"
      to: "22 resource group topic names"
      via: "markdown table listing each topic"
      pattern: "\\| `(video|webinar|analytics|audience|action|category|collector|comment|player|poll|spot|tag|thumbnail|webhook|app|presentation|protection|session|setting|site|openupload|user)` \\|"
---

<objective>
Replace the body of `packages/twentythree-skills/skills/SKILL.md` with a complete, agent-ready skill file per D-03. The file must be immediately useful — an AI agent should be able to authenticate, discover commands via `--agent`, and pick the right topic from the resource index without Phase 19 reference files.

Purpose: Satisfies SKILL-01 (root SKILL.md ~150-200 lines with auth setup, command syntax, resource index for all 22 groups, `--agent` docs, `allowed-tools: Bash(twentythree *)`).

Output: Rewritten `skills/SKILL.md` with expanded frontmatter and ~150-200 lines of body content covering auth, syntax, self-discovery, invariants, 22-group resource index, meta commands (auth, workspace, autocomplete, doctor), and 2 workflow examples.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-package-foundation/18-CONTEXT.md
@.planning/phases/18-package-foundation/18-RESEARCH.md
@.planning/phases/18-package-foundation/18-PATTERNS.md
@.planning/phases/18-package-foundation/18-VALIDATION.md
@.planning/phases/18-package-foundation/18-01-SUMMARY.md
@packages/twentythree-skills/skills/SKILL.md
@packages/twentythree-cli/src/commands

<interfaces>
<!-- Existing frontmatter to preserve name: twentythree (must match installer target dir) -->
<!-- Current file (after plan 01 move) has this frontmatter verbatim: -->

```yaml
---
name: twentythree
description: |
  TwentyThree CLI skills for AI agents.
  Commands for managing videos, categories, webinars, and all TwentyThree resources.
triggers:
  - twentythree
  - video platform
  - webinar management
invocable: true
argument-hint: "<command> [flags]"
---
```

<!-- --- agent flag live output shape (sampled from video:list --agent, per 18-RESEARCH.md) -->
```json
{
  "command": "video:list",
  "description": "List videos in the active workspace",
  "flags": [{"name":"limit","type":"option","required":false,"default":null,"description":"..."}],
  "examples": ["<%= config.bin %> video list"],
  "api_endpoint": "GET /photo/list",
  "auth_scope": "read",
  "output_shape": {"type":"table","columns":["ID","Title","Duration","Status","Published","Updated"]},
  "side_effects": "none"
}
```

<!-- Canonical 22 resource groups (per REQUIREMENTS.md SKILL-02, 18-RESEARCH.md) — must all appear in the resource index -->
```
action, analytics, app, audience, category, collector, comment, openupload,
player, poll, presentation, protection, session, setting, site, spot, tag,
thumbnail, user, video, webhook, webinar
```

<!-- Meta/utility topics NOT in the 22 (documented separately in "Meta Commands" section) -->
```
auth       (twentythree auth credentials | twentythree auth status)
workspace  (twentythree workspace list | twentythree workspace use <domain>)
autocomplete (twentythree autocomplete bash|zsh)
doctor     (twentythree doctor — diagnostics)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rewrite skills/SKILL.md with full D-03 content</name>
  <files>packages/twentythree-skills/skills/SKILL.md</files>
  <read_first>
    - packages/twentythree-skills/skills/SKILL.md (the placeholder moved by plan 01 — read to see current frontmatter)
    - .planning/phases/18-package-foundation/18-CONTEXT.md (D-03 defines what the SKILL.md must contain)
    - .planning/phases/18-package-foundation/18-RESEARCH.md (has the full body outline under "skills/SKILL.md body outline (~200 lines target)" and the expanded frontmatter under "skills/SKILL.md frontmatter structure")
    - .planning/phases/18-package-foundation/18-PATTERNS.md (Pattern Assignments → skills/SKILL.md has the expanded frontmatter to use)
    - packages/twentythree-cli/src/commands (22 topic directories — ls to confirm each group has commands; the resource index table names derive from these)
  </read_first>
  <behavior>
    - File is rewritten — the old body ("Skills content is a work in progress...") is replaced entirely
    - Frontmatter still starts and ends with `---` fences, preserves `name: twentythree` exactly (agentskills.io spec: name must match installer target directory)
    - Frontmatter adds `allowed-tools: Bash(twentythree *)` as a top-level key (enables Claude Code to pre-approve all CLI calls)
    - Frontmatter expands `description:` (block scalar), `triggers:` (list), and adds `compatibility:` noting twentythree-cli as a required global install
    - Body has H2 sections in this order: Prerequisites: Authentication → Command Syntax → Self-Discovery: --agent Flag → Key Invariants → Resource Index → Meta Commands → Common Workflows → Diagnostics
    - Resource index is a markdown table with one row per resource group, listing topic name in backticks, representative verbs, and "use for" description
    - All 22 groups appear in the resource index: action, analytics, app, audience, category, collector, comment, openupload, player, poll, presentation, protection, session, setting, site, spot, tag, thumbnail, user, video, webhook, webinar
    - Meta Commands section documents auth, workspace, autocomplete, doctor (not part of the 22)
    - Common Workflows section has at least 2 named workflows: "Upload and Publish a Video" and "Webinar Setup" with executable `twentythree ...` code blocks
    - Auth section shows `twentythree auth credentials` as step 1 and `twentythree workspace use` for multi-workspace switching
    - `--agent` flag documented with a real JSON output example showing api_endpoint, auth_scope, output_shape, side_effects, flags
    - Terminology mapping note: video→photo, category→album, webinar→live (explains why api_endpoint values differ from CLI topic names)
    - File is between 150 and 250 lines total
    - `pnpm --filter twentythree-skills test` still exits 0 after the rewrite (validate-skills checks `name` and `description` frontmatter keys; both present)
  </behavior>
  <action>
Use the Write tool to replace `packages/twentythree-skills/skills/SKILL.md` with the EXACT content below. This overwrites the placeholder body while preserving the `name: twentythree` frontmatter key (required by agentskills.io spec).

Do NOT trim sections. Do NOT abbreviate the resource index. Do NOT convert the markdown code fences to text. Ship all 22 resource rows exactly as listed.

````markdown
---
name: twentythree
description: |
  Full TwentyThree video platform CLI. Use when the user asks to upload or manage
  videos, run webinars, query analytics, manage audiences, configure players,
  create categories, manage tags, spots, thumbnails, webhooks, collectors, polls,
  presentations, or any TwentyThree platform operation. Covers 235+ API commands
  across 22 resource groups plus meta commands (auth, workspace, autocomplete, doctor).
  Every command supports --json for machine-readable output and --agent for
  self-describing metadata (api_endpoint, auth_scope, output_shape, side_effects).
triggers:
  - upload video
  - manage videos
  - webinar
  - live event
  - analytics
  - twentythree
  - video platform
  - TwentyThree CLI
invocable: true
argument-hint: "<topic> <verb> [flags]"
allowed-tools: Bash(twentythree *)
compatibility: Requires twentythree-cli installed globally (npm install -g twentythree-cli). Node.js >=22.
---

# TwentyThree CLI

> Terminal access to the full TwentyThree video platform API — videos, webinars, analytics, audiences, and every related resource. 235+ commands across 22 resource groups.
>
> Always use `--json` in agentic contexts for structured output. Always run `twentythree <command> --agent` before calling an unfamiliar command to discover its flags, API endpoint, auth scope, and side effects.

## Prerequisites: Authentication

Before any command, configure credentials once per workspace:

```bash
twentythree auth credentials
```

Prompts interactively for:
- **Domain** — your workspace domain (e.g. `company.video.twentythree.com`)
- **Bearer token** — copy from `Settings → API` inside your TwentyThree workspace admin

Credentials are stored in the OS keychain (macOS Keychain, Windows Credential Manager, Linux Secret Service) — never in plaintext files.

Verify auth is working:

```bash
twentythree auth status
```

### Multi-Workspace

The CLI supports multiple workspaces simultaneously:

```bash
twentythree workspace list                   # Show all configured workspaces
twentythree workspace use <domain>           # Set the active workspace
twentythree <command> --workspace <domain>   # One-off override for a single call
```

## Command Syntax

```
twentythree <topic> <verb> [flags]
```

Global flags available on every command:

| Flag | Purpose |
|------|---------|
| `--json` | Machine-readable JSON output — always use in agentic contexts |
| `--agent` | Return machine-readable command metadata (no API call made) |
| `--workspace <domain>` | Target a specific workspace for this call only |

## Self-Discovery: The `--agent` Flag

Before calling any command you haven't used recently, introspect it:

```bash
twentythree video upload --agent
```

Returns JSON:

```json
{
  "command": "video:upload",
  "description": "Upload a video file",
  "flags": [
    { "name": "title", "type": "option", "required": true, "description": "..." },
    { "name": "category-id", "type": "option", "required": false, "description": "..." }
  ],
  "examples": ["twentythree video upload ./file.mp4 --title \"Demo\""],
  "api_endpoint": "POST /photo/redeem-upload-token",
  "auth_scope": "write",
  "output_shape": { "type": "key-value" },
  "side_effects": "creates"
}
```

Key fields returned by `--agent`:

- **`api_endpoint`** — the underlying REST endpoint (note terminology mapping below)
- **`auth_scope`** — one of `anonymous`, `none`, `read`, `write`, `admin`, `super`
- **`output_shape`** — `{ type: "table", columns: [...] }`, `{ type: "key-value" }`, or `{ type: "none" }`
- **`side_effects`** — `none`, `creates`, `updates`, or `destructive`
- **`flags`** — the complete flag list with types, defaults, and required-ness

Always check `auth_scope` and `side_effects` before write/admin operations.

## Key Invariants

- **Use `--json` in agentic contexts.** Human-formatted tables are the default; agents should always request JSON.
- **File uploads use chunked upload automatically.** Never construct multipart requests directly — `twentythree video upload <file>` handles chunking under the hood.
- **Terminology mapping** — the CLI uses product-domain names while the API uses legacy names:
  - CLI `video` ↔ API `photo`
  - CLI `category` ↔ API `album`
  - CLI `webinar` ↔ API `live`
  - The `api_endpoint` field in `--agent` output shows the actual API path.
- **After upload or create, the CLI prints the new resource ID and its admin URL.** Use the ID for follow-up updates (e.g. setting thumbnail, publishing).
- **On persistent errors, run `twentythree doctor`** to diagnose auth, connectivity, and dependency issues.

## Resource Index

All 22 resource groups. Every topic supports `--agent`, `--json`, and `--workspace`.

| Topic | Representative verbs | Use for |
|-------|---------------------|---------|
| `video` | `upload`, `list`, `get`, `update`, `delete`, `replace`, `frame`, `transcoding-progress` | Video file management, upload, metadata, thumbnails |
| `webinar` | `create`, `list`, `get`, `update`, `delete`, `repeat`, `highlights`, `clips`, `metrics`, `log` + attachment/mail/queued-video/recording/room/section/series/speaker/transcription subtopics | Live events, scheduling, recordings, attendee comms |
| `analytics` | `conversions`, `live`, `usage`, `video` subtopics (many verbs each) | Reporting, viewer data, playback metrics, conversion tracking |
| `audience` | `list`, `create`, `get`, `update`, `delete` + segment ops | Audience segmentation and targeting |
| `category` | `list`, `create`, `get`, `update`, `delete` | Content organization (API: albums) |
| `action` | `list`, `create`, `get`, `update`, `delete` + subtypes | Interactive overlays, CTAs inside videos |
| `collector` | `list`, `create`, `delete` | Lead capture forms |
| `comment` | `list`, `create`, `get`, `update`, `delete` | Video comments moderation |
| `player` | `list`, `create`, `get`, `update`, `delete` | Player configuration and theming |
| `poll` | `list`, `create`, `get`, `update`, `delete` | In-video polls |
| `spot` | `list`, `create`, `get`, `update`, `delete` | Hotspot annotations on videos |
| `tag` | `list`, `create` | Content tagging |
| `thumbnail` | `list`, `create`, `get`, `update`, `delete` | Video thumbnail management |
| `webhook` | `list`, `create`, `get`, `update`, `delete` | Event webhooks |
| `app` | `list`, `thumbnail`, `create`, `get`, `update`, `delete` | App/integration management |
| `presentation` | `list` + page/setting subtopics | Presentation content |
| `protection` | `list`, `create`, `delete` | Access protection |
| `session` | `list`, `get` | Viewer session data |
| `setting` | `get` | Workspace settings |
| `site` | `list`, `search` | Site-level operations |
| `openupload` | `list`, `create`, `delete` | Open upload tokens |
| `user` | `list`, `create`, `get`, `update`, `delete` | User management |

## Meta Commands

These are not in the 22 resource groups — they are CLI-local utilities:

| Topic | Commands | Purpose |
|-------|----------|---------|
| `auth` | `credentials`, `status` | Configure and verify bearer-token auth |
| `workspace` | `list`, `use` | Multi-workspace selection |
| `autocomplete` | `bash`, `zsh` | Shell completion (`twentythree autocomplete bash \| source`) |
| `doctor` | (top-level: `twentythree doctor`) | Diagnose auth, connectivity, dependency issues |

## Common Workflows

### Upload and Publish a Video

```bash
# 1. Upload (chunked upload is automatic)
twentythree video upload ./video.mp4 --title "Product Demo" --json
#    → prints { "id": "<video-id>", "admin_url": "..." }

# 2. Assign to a category (API: album)
twentythree video update <video-id> --category-id <cat-id> --json

# 3. Set a thumbnail at second 5
twentythree thumbnail create --video-id <video-id> --time 5 --json

# 4. Publish
twentythree video update <video-id> --published 1 --json
```

### Webinar Setup

```bash
# 1. Create the webinar (API: live)
twentythree webinar create --title "Q2 Kickoff" --scheduled-at "2026-05-01T14:00:00Z" --json
#    → prints { "id": "<webinar-id>", "admin_url": "..." }

# 2. Fetch room URL and stream key
twentythree webinar get <webinar-id> --json

# 3. Start an associated session when the event begins
twentythree session list --webinar-id <webinar-id> --json
```

## Diagnostics

```bash
twentythree doctor        # Check auth, connectivity, Node version, keychain access
twentythree --version     # Print CLI version
twentythree <cmd> --help  # Human-readable help for any command
twentythree <cmd> --agent # Machine-readable metadata for any command (preferred for agents)
```

If a command fails unexpectedly, in order:
1. `twentythree auth status` — confirm credentials are present and the workspace matches
2. `twentythree <cmd> --agent` — confirm required flags and auth scope
3. `twentythree doctor` — catch environmental issues

See [twentythree-cli on npm](https://www.npmjs.com/package/twentythree-cli) for installation, and the GitHub repo for docs and issue reporting.
````

After writing, run `pnpm --filter twentythree-skills test` to confirm the validator still exits 0 (it checks that `name:` and `description:` frontmatter keys are present; both remain present and non-empty in the rewritten file).

If the validator fails, the most likely cause is accidental removal of the `---` fence or the `name:` line during the rewrite — re-check the frontmatter against the content above.
  </action>
  <verify>
    <automated>test -f packages/twentythree-skills/skills/SKILL.md && lines=$(wc -l < packages/twentythree-skills/skills/SKILL.md) && test "$lines" -ge 150 && test "$lines" -le 260 && grep -q '^name: twentythree$' packages/twentythree-skills/skills/SKILL.md && grep -q '^allowed-tools: Bash(twentythree \*)$' packages/twentythree-skills/skills/SKILL.md && grep -q 'twentythree auth credentials' packages/twentythree-skills/skills/SKILL.md && grep -q 'twentythree workspace use' packages/twentythree-skills/skills/SKILL.md && grep -q '\-\-agent' packages/twentythree-skills/skills/SKILL.md && grep -q 'api_endpoint' packages/twentythree-skills/skills/SKILL.md && grep -q 'auth_scope' packages/twentythree-skills/skills/SKILL.md && grep -q 'side_effects' packages/twentythree-skills/skills/SKILL.md && grep -q 'output_shape' packages/twentythree-skills/skills/SKILL.md && for g in action analytics app audience category collector comment openupload player poll presentation protection session setting site spot tag thumbnail user video webhook webinar; do grep -q "\`$g\`" packages/twentythree-skills/skills/SKILL.md || { echo "MISSING RESOURCE GROUP: $g"; exit 30; }; done && grep -q 'Upload and Publish' packages/twentythree-skills/skills/SKILL.md && grep -q 'Webinar Setup' packages/twentythree-skills/skills/SKILL.md && grep -q 'twentythree doctor' packages/twentythree-skills/skills/SKILL.md && grep -qE 'video.*photo|photo.*video' packages/twentythree-skills/skills/SKILL.md && pnpm --filter twentythree-skills test</automated>
  </verify>
  <acceptance_criteria>
    - `packages/twentythree-skills/skills/SKILL.md` exists and is between 150 and 260 lines (`wc -l` in that range)
    - File starts with `---` fence (line 1 is `---`)
    - Frontmatter contains `name: twentythree` (exact string on its own line)
    - Frontmatter contains `allowed-tools: Bash(twentythree *)` (exact string on its own line)
    - Frontmatter contains `invocable: true`
    - Frontmatter contains `argument-hint: "<topic> <verb> [flags]"` (or equivalent with quoted value)
    - Frontmatter contains `compatibility:` key referencing `twentythree-cli` and `npm install -g`
    - Body contains section heading `## Prerequisites: Authentication`
    - Body contains `twentythree auth credentials` (the prerequisite command)
    - Body contains `twentythree auth status` (the verification command)
    - Body contains `twentythree workspace use` (multi-workspace switch command)
    - Body contains `## Self-Discovery: The \`--agent\` Flag` (or equivalent heading with `--agent`)
    - Body shows example JSON output for `--agent` with fields `api_endpoint`, `auth_scope`, `output_shape`, `side_effects`, `flags`
    - Body contains `## Resource Index` section
    - Resource index contains ALL 22 groups as backtick-quoted topic names: `action`, `analytics`, `app`, `audience`, `category`, `collector`, `comment`, `openupload`, `player`, `poll`, `presentation`, `protection`, `session`, `setting`, `site`, `spot`, `tag`, `thumbnail`, `user`, `video`, `webhook`, `webinar` — verify each with a `grep -q "\`$group\`"` (automated verify runs this loop)
    - Body contains `## Meta Commands` section documenting `auth`, `workspace`, `autocomplete`, `doctor`
    - Body contains `## Common Workflows` section with subsections `### Upload and Publish a Video` and `### Webinar Setup`
    - Body contains terminology mapping note (grep matches `video` paired with `photo` somewhere in the file, e.g. `CLI \`video\` ↔ API \`photo\``)
    - Body contains diagnostics section referencing `twentythree doctor`
    - `pnpm --filter twentythree-skills test` exits 0 (validator still passes on rewritten file)
  </acceptance_criteria>
  <done>
    `skills/SKILL.md` is a complete, agent-ready skill file: expanded frontmatter with `allowed-tools`, body covers auth + syntax + `--agent` + invariants + 22-group resource index + meta commands + 2 workflows + diagnostics. Validator still exits 0.
  </done>
</task>

</tasks>

<verification>
After the task completes:

```bash
# 1. Validator still passes
pnpm --filter twentythree-skills test
# Expected: validate-skills: OK, exit 0

# 2. File size is within target range
wc -l packages/twentythree-skills/skills/SKILL.md
# Expected: 150-260 lines

# 3. Frontmatter has all required keys
grep -E '^(name|description|triggers|invocable|argument-hint|allowed-tools|compatibility):' packages/twentythree-skills/skills/SKILL.md
# Expected: 7 matches

# 4. All 22 resource groups present in the resource index
for g in action analytics app audience category collector comment openupload player poll presentation protection session setting site spot tag thumbnail user video webhook webinar; do
  grep -q "\`$g\`" packages/twentythree-skills/skills/SKILL.md && echo "OK $g" || echo "MISS $g"
done
# Expected: 22 OK lines, 0 MISS lines

# 5. Key sections present
grep -cE '^## (Prerequisites: Authentication|Command Syntax|Self-Discovery|Key Invariants|Resource Index|Meta Commands|Common Workflows|Diagnostics)' packages/twentythree-skills/skills/SKILL.md
# Expected: 8

# 6. Manual quality read — open the file and confirm it reads well end-to-end
```
</verification>

<success_criteria>
- `packages/twentythree-skills/skills/SKILL.md` is between 150 and 260 lines
- Frontmatter preserves `name: twentythree` and adds `allowed-tools: Bash(twentythree *)`, `compatibility:`
- Body has 8 H2 sections covering auth, syntax, --agent, invariants, resource index, meta commands, workflows, diagnostics
- Resource index table lists all 22 resource groups
- Upload-and-publish and webinar-setup workflows are each documented with executable code blocks
- Terminology mapping (video↔photo, category↔album, webinar↔live) is explicitly stated
- `pnpm --filter twentythree-skills test` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/18-package-foundation/18-02-SUMMARY.md` documenting:
- Final frontmatter shape (list of keys)
- Final body section outline (list of H2 headings)
- Line count
- Any minor content divergence from the planned text (if any)
- Confirmation that validate-skills still exits 0
</output>
