---
phase: 12-readmes-changelog
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - CHANGELOG.md
autonomous: true
requirements:
  - README-03

must_haves:
  truths:
    - "A developer can see what shipped in v1.0 and v1.1 by reading CHANGELOG.md"
    - "CHANGELOG follows Keep a Changelog format"
  artifacts:
    - path: "CHANGELOG.md"
      provides: "Release history"
      contains: "## [1.0.0]"
  key_links:
    - from: "CHANGELOG.md"
      to: "ROADMAP.md phases"
      via: "feature-level entries derived from phase goals"
      pattern: "\\[1\\.[01]\\.0\\]"
---

<objective>
Write CHANGELOG.md at the repo root with entries for v1.0 and v1.1.

Purpose: Developers and users can see what shipped in each release at a glance, following the standard Keep a Changelog format.

Output: `CHANGELOG.md` at repo root.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-readmes-changelog/12-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write CHANGELOG.md</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - .planning/PROJECT.md (what shipped in v1.0 -- 219 commands, auth, upload, etc.)
    - .planning/ROADMAP.md (phase goals for v1.0 and v1.1 milestones)
    - .planning/phases/12-readmes-changelog/12-CONTEXT.md (D-09: Keep a Changelog format, feature-level bullets)
  </read_first>
  <action>
Create `CHANGELOG.md` at the repo root using [Keep a Changelog](https://keepachangelog.com) format per D-09.

**Structure:**

```
# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

## [1.1.0] - 2026-04-16

### Added

- Endpoint coverage audit script verifying all 235 OpenAPI endpoints are covered
- 18 analytics sub-series commands (video, live, and usage analytics -- timeseries and totals)
- Command reference docs generated with `oclif readme --multi` (244 commands across 24 topics)
- Getting Started guide covering auth setup, workspace selection, and first command
- API Spec Upgrade guide documenting the `pnpm update-api-spec` workflow
- Root README with quickstart, command overview table, and terminology mapping
- npm package README with install instructions and link to full docs
- This CHANGELOG

### Changed

- Package.json updated with `repository`, `bugs`, `homepage`, `keywords`, `author` fields
- `files` array updated to include `/docs` and `/README.md` in published tarball
- `prepack` script added to run full build before pack/publish

## [1.0.0] - 2026-04-16

### Added

- 219 hand-authored commands covering all 235 TwentyThree API endpoints across 22 resource groups
- Multi-workspace authentication with OS keychain storage via `@napi-rs/keyring`
- Automatic bearer token refresh with file-lock concurrency protection
- Interactive workspace discovery and selection via `/api/2/user/tokens`
- Chunked upload engine (100 MB chunks, 5-way parallelism, resume on failure) for videos, webinar attachments, actions, and open uploads
- Domain-only mode for anonymous endpoint access
- `--agent` flag on every command outputting machine-readable metadata for AI agent consumption
- `doctor` command for credential, connectivity, and token validation checks
- Terminology mapping (`photo` to `video`, `album` to `category`, `live` to `webinar`) via `term-map.ts`
- URL normalization resolving relative API response URLs to full workspace URLs
- OpenAPI spec stored locally with `pnpm update-api-spec` regeneration workflow
```

**Date for both entries:** `2026-04-16` (v1.0 shipped that day; v1.1 is the current milestone completing same day).

**Formatting rules:** ATX headings, blank line between all block elements, no emojis. Feature-level bullets, not commit-level (per D-09).

**Note on v1.1 entry:** Include "Root README" and "npm package README" bullets and "This CHANGELOG" since the CHANGELOG is being created as part of v1.1. This is standard practice -- the changelog documents itself.
  </action>
  <verify>
    <automated>grep -c "## \[1.0.0\]" CHANGELOG.md && grep -c "## \[1.1.0\]" CHANGELOG.md && grep -c "Keep a Changelog" CHANGELOG.md</automated>
  </verify>
  <acceptance_criteria>
    - CHANGELOG.md exists at repo root
    - CHANGELOG.md contains `# Changelog` as first heading
    - CHANGELOG.md contains `Keep a Changelog` link
    - CHANGELOG.md contains `Semantic Versioning` link
    - CHANGELOG.md contains `## [Unreleased]` section
    - CHANGELOG.md contains `## [1.1.0] - 2026-04-16` section
    - CHANGELOG.md contains `## [1.0.0] - 2026-04-16` section
    - v1.1 section contains `### Added` and `### Changed` subsections
    - v1.0 section contains `### Added` subsection
    - v1.0 section mentions "219" (hand-authored commands)
    - v1.0 section mentions "keychain" or "keyring" (credential storage)
    - v1.0 section mentions "chunked upload" or "Chunked upload"
    - v1.1 section mentions "endpoint coverage audit" or "Endpoint coverage audit"
    - v1.1 section mentions "prepack" (package hygiene)
    - Entries are feature-level bullets, not commit-level
    - CHANGELOG.md does not contain any emoji characters
  </acceptance_criteria>
  <done>CHANGELOG.md exists at repo root with Keep a Changelog format, Unreleased section, v1.1 entry (endpoint audit, docs, READMEs, package hygiene), and v1.0 entry (full CLI with auth, 219 commands, upload engine, agent support). Feature-level bullets throughout.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CHANGELOG content to readers | Users read release history to understand changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-04 | Information Disclosure | CHANGELOG.md | mitigate | No credentials, internal URLs, or security vulnerability details in changelog entries. Only feature-level descriptions. |
| T-12-05 | Tampering | keepachangelog.com link | accept | External reference link to well-known documentation standard. Low risk. |
</threat_model>

<verification>
- `head -10 CHANGELOG.md` shows title and format attribution
- `grep -c "## \[" CHANGELOG.md` returns 3 (Unreleased, 1.1.0, 1.0.0)
- `grep "### Added" CHANGELOG.md` appears in both version sections
</verification>

<success_criteria>
- CHANGELOG.md at repo root with Keep a Changelog format
- v1.0 entry covers initial CLI release with auth, commands, upload engine, agent support
- v1.1 entry covers endpoint audit, package hygiene, documentation, READMEs
- Feature-level bullets, not commit-level
</success_criteria>

<output>
After completion, create `.planning/phases/12-readmes-changelog/12-02-SUMMARY.md`
</output>
