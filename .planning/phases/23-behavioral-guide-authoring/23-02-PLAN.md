---
phase: 23-behavioral-guide-authoring
plan: 02
type: execute
wave: 2
depends_on:
  - 23-01-PLAN.md
files_modified:
  - packages/twentythree-skills/skills/SKILL.md
  - packages/twentythree-skills/skills/reference/video.md
  - packages/twentythree-skills/skills/reference/webinar.md
autonomous: false
requirements:
  - GUIDE-02
  - GUIDE-03

must_haves:
  truths:
    - "SKILL.md has a ## Behavioral Guide section that appears before ## Resource Index"
    - "SKILL.md Behavioral Guide section contains a link to guide.md"
    - "webinar.md has at least one new > **Note:** callout inside the ## Commands section (after the file-header note)"
    - "video.md has at least one new > **Note:** callout inside the ## Commands section"
    - "Inline notes forward-reference guide.md using ../guide.md relative path (not verbatim rule text)"
    - "node bin/add.js --project from package root lists guide.md in copied file output"
  artifacts:
    - path: "packages/twentythree-skills/skills/SKILL.md"
      provides: "Behavioral Guide section linking to guide.md, placed before Resource Index"
      contains: "## Behavioral Guide"
    - path: "packages/twentythree-skills/skills/reference/video.md"
      provides: "Inline > **Note:** callouts at relevant command sections"
      contains: "> **Note:**"
    - path: "packages/twentythree-skills/skills/reference/webinar.md"
      provides: "Inline > **Note:** callouts at webinar create and webinar list"
      contains: "> **Note:**"
  key_links:
    - from: "packages/twentythree-skills/skills/SKILL.md"
      to: "packages/twentythree-skills/skills/guide.md"
      via: "markdown hyperlink in Behavioral Guide section"
      pattern: "\\[guide\\.md\\]\\(guide\\.md\\)"
    - from: "packages/twentythree-skills/skills/reference/video.md"
      to: "packages/twentythree-skills/skills/guide.md"
      via: "relative link ../guide.md in inline Note callouts"
      pattern: "\\.\\./guide\\.md"
    - from: "packages/twentythree-skills/skills/reference/webinar.md"
      to: "packages/twentythree-skills/skills/guide.md"
      via: "relative link ../guide.md in inline Note callouts"
      pattern: "\\.\\./guide\\.md"
---

<objective>
Update SKILL.md with a "Behavioral Guide" section placed before the Resource Index table, and add > **Note:** forward-reference callouts to video.md and webinar.md at the relevant command sections.

Purpose: Agents encounter the behavioral guide link before scanning the command index. Point-of-use notes in reference files surface at exactly the moment an agent is about to make a mistake — at the command they're about to run.

Output:
- SKILL.md: new ## Behavioral Guide section inserted before line 124 (## Resource Index)
- video.md: 1-3 inline Note callouts at video upload (admin_url), video list (analytics flag if verified), video get (thumbnail preference)
- webinar.md: 1-2 new inline Note callouts at webinar create (access defaults + admin_url) and webinar list (analytics flag if verified) — the existing no-webinar-get note at the file header is already present, no change needed there
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/23-behavioral-guide-authoring/23-CONTEXT.md
@.planning/phases/23-behavioral-guide-authoring/23-PATTERNS.md
@packages/twentythree-skills/skills/SKILL.md
@packages/twentythree-skills/skills/reference/video.md
@packages/twentythree-skills/skills/reference/webinar.md
@.planning/phases/23-behavioral-guide-authoring/23-01-SUMMARY.md

<interfaces>
<!-- guide.md exists after Plan 01. Key content the executor needs to know: -->
<!-- guide.md is at: packages/twentythree-skills/skills/guide.md -->
<!-- From SKILL.md (at skills/SKILL.md), the link to guide.md is: [guide.md](guide.md) -->
<!-- From reference files (at skills/reference/*.md), the link to guide.md is: [guide.md](../guide.md) -->
<!-- SKILL.md ## Resource Index currently begins at line 124 -->
<!-- SKILL.md ## Key Invariants section ends just before line 124 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert Behavioral Guide section into SKILL.md</name>
  <files>packages/twentythree-skills/skills/SKILL.md</files>

  <read_first>
    - packages/twentythree-skills/skills/SKILL.md (MUST read current state — find exact line where ## Resource Index appears; confirm ## Key Invariants ends just before it)
    - .planning/phases/23-behavioral-guide-authoring/23-PATTERNS.md (exact section template at "SKILL.md (UPDATE — insert Behavioral Guide section)" pattern block)
    - .planning/phases/23-behavioral-guide-authoring/23-CONTEXT.md (D-07: section must appear before Resource Index table)
  </read_first>

  <action>
Read SKILL.md to find the exact line number where `## Resource Index` appears (currently expected around line 124). The new section must be inserted IMMEDIATELY before that line — not after it, not at the end of the file.

Insert the following block immediately before the `## Resource Index` heading (add a blank line before and after):

```markdown
## Behavioral Guide

Before executing multi-step workflows, read [`guide.md`](guide.md) for cross-cutting rules.
The guide covers two categories:

- **Correctness Rules** — must-follow rules that prevent API errors (object type differentiation, no `webinar get`, webinar creation defaults, timezone handling, admin link construction)
- **Preference Rules** — best-practice rules that improve output quality (thumbnails from listing responses, analytics via listing flags, filtering/sorting on listing endpoints)

> See [`guide.md`](guide.md) for the full rule list with examples.
```

The result must be:
1. `## Behavioral Guide` appears before `## Resource Index`
2. `## Key Invariants` remains unchanged above `## Behavioral Guide`
3. `## Resource Index` and the table below it remain unchanged
4. The link `[guide.md](guide.md)` uses a relative path (no `../` — SKILL.md is at the same level as guide.md inside `skills/`)
  </action>

  <verify>
    <automated>grep -n "## Behavioral Guide\|## Resource Index\|## Key Invariants" packages/twentythree-skills/skills/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep "^## Behavioral Guide" packages/twentythree-skills/skills/SKILL.md` returns a match
    - `grep -n "## Behavioral Guide" packages/twentythree-skills/skills/SKILL.md` shows a line number LOWER than the line number of `## Resource Index`
    - `grep "\[guide\.md\](guide\.md)" packages/twentythree-skills/skills/SKILL.md` returns a match (relative link, no ../)
    - `grep "^## Key Invariants" packages/twentythree-skills/skills/SKILL.md` still returns a match (section not deleted)
    - `grep "^## Resource Index" packages/twentythree-skills/skills/SKILL.md` still returns a match (section not deleted)
  </acceptance_criteria>
  <done>SKILL.md contains ## Behavioral Guide section with a link to guide.md, positioned between ## Key Invariants and ## Resource Index.</done>
</task>

<task type="auto">
  <name>Task 2: Add inline Note callouts to video.md and webinar.md</name>
  <files>
    packages/twentythree-skills/skills/reference/video.md
    packages/twentythree-skills/skills/reference/webinar.md
  </files>

  <read_first>
    - packages/twentythree-skills/skills/reference/video.md (MUST read current state — locate ### video upload, ### video list, ### video get sections and find the Auth scope line immediately after each heading to find the insertion point)
    - packages/twentythree-skills/skills/reference/webinar.md (MUST read current state — locate ### webinar create, ### webinar list sections; note the existing no-webinar-get block at lines 11-13 is already present and must NOT be modified)
    - .planning/phases/23-behavioral-guide-authoring/23-PATTERNS.md (exact Note format, relative link format, which commands to annotate)
    - packages/twentythree-skills/skills/guide.md (read the verified flag name used in PR-2 to use the same flag name in inline notes)
    - .planning/phases/23-behavioral-guide-authoring/23-01-SUMMARY.md (check what flag names were actually verified in Plan 01)
    - .planning/REQUIREMENTS.md (GUIDE-02: one note per affected command, no verbatim rule repetition, forward-reference only)
  </read_first>

  <action>
**video.md changes — insert notes at these three locations:**

**Location 1: After `### video upload` Auth scope line**
After the line `**Auth scope:** write  **Side effects:** creates  **Output:** key-value (id + admin_url)`, insert:
```markdown
> **Note:** The `--json` response includes `data.admin_url` — read it from the response directly. Do not construct admin URLs manually. See [guide.md](../guide.md) for the Admin Link Construction rule.
```

**Location 2: After `### video list` Auth scope line**
After the line `**Auth scope:** read  **Side effects:** none  **Output:** table (...)`, insert:
```markdown
> **Note:** For analytics and thumbnail data, prefer including them in the listing call rather than making separate command calls. See [guide.md](../guide.md) for Preference Rules.
```
EXCEPTION: If Plan 01 verified that `--include-analytics` exists on `video list`, use the verified flag name: `> **Note:** Use \`twentythree video list --<verified-flag> --json\` to include analytics data in a single call. See [guide.md](../guide.md) for the Analytics via Listing Flags rule.`

**Location 3: After `### video get` Auth scope line**
After the line `**Auth scope:** read  **Side effects:** none  **Output:** key-value`, insert:
```markdown
> **Note:** Thumbnail URLs are included in `video list --json` output — prefer the listing response when you already have it. See [guide.md](../guide.md) for the Thumbnails from Listing Response rule.
```

**webinar.md changes — insert notes at these locations:**

**IMPORTANT:** The existing no-webinar-get block at the top of webinar.md (lines 11-13) covers CR-2. Do NOT modify or remove it. New notes go inside the `## Commands` section only.

**Location 1: After `### webinar create` Auth scope line**
After the line `**Auth scope:** write  **Side effects:** creates  **Output:** key-value (id + admin_url)`, insert a note about access defaults AND admin_url:
```markdown
> **Note:** Set access visibility explicitly when creating webinars — defaults may not match your intent. The `--json` response includes `data.admin_url`; read it from the response rather than constructing URLs. See [guide.md](../guide.md) for Webinar Creation Defaults and Admin Link Construction rules.
```
If Plan 01 verified a specific flag name for `open_p` (e.g. `--open-p`), include it: `Use \`--<verified-flag>\` to set public/private access.`

**Location 2: After `### webinar list` Auth scope line**
After the line `**Auth scope:** read  **Side effects:** none  **Output:** table (...)`, insert:
```markdown
> **Note:** To retrieve a specific webinar, use `--search "<title>"` and filter client-side — there is no `webinar get` command. See [guide.md](../guide.md).
```
If Plan 01 verified that `--include-analytics` (or equivalent) exists on `webinar list`, also add: `For analytics data, use \`twentythree webinar list --<verified-flag> --json\` to include it in one call.`

**Formatting rules for ALL new notes:**
- Use `> **Note:**` opener (not `> **Warning:**`)
- One-to-two lines max
- End with `See [guide.md](../guide.md)` or `See [guide.md](../guide.md) for <rule name>.`
- Do NOT restate the full rule — forward-reference only (D-06)
- Relative path is `../guide.md` (reference files are one level deeper than guide.md)
- Place note IMMEDIATELY after the Auth scope line, before any flag table or bash block
  </action>

  <verify>
    <automated>grep -n "> \*\*Note:\*\*" packages/twentythree-skills/skills/reference/video.md packages/twentythree-skills/skills/reference/webinar.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "> \*\*Note:\*\*" packages/twentythree-skills/skills/reference/video.md` returns `3` (upload, list, get sections)
    - `grep "\.\./guide\.md" packages/twentythree-skills/skills/reference/video.md` returns at least 3 matches
    - `grep "\.\./guide\.md" packages/twentythree-skills/skills/reference/webinar.md` returns at least 2 matches
    - `grep "There is no.*webinar get" packages/twentythree-skills/skills/reference/webinar.md` still returns a match (existing no-webinar-get block preserved)
    - `grep "admin_url" packages/twentythree-skills/skills/reference/video.md` returns a match in the new Note after video upload
    - No note in video.md or webinar.md contains verbatim rule text longer than one sentence — all notes forward-reference guide.md
    - `grep "webinar list --search" packages/twentythree-skills/skills/reference/webinar.md` returns a match (in or near webinar list Note)
  </acceptance_criteria>
  <done>
    video.md has 3 new > **Note:** callouts (after video upload, video list, video get Auth scope lines), each ending with a ../guide.md link.
    webinar.md has 2 new > **Note:** callouts (after webinar create and webinar list Auth scope lines), each ending with a ../guide.md link.
    The existing no-webinar-get block at the webinar.md file header is untouched.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification — review all Phase 23 changes</name>
  <what-built>
    guide.md (Plan 01), SKILL.md Behavioral Guide section, video.md inline notes, webinar.md inline notes.
  </what-built>
  <how-to-verify>
1. Confirm guide.md is at the skills/ root:
   ```bash
   ls packages/twentythree-skills/skills/guide.md
   ```

2. Confirm guide.md rule count:
   ```bash
   grep -c "^### " packages/twentythree-skills/skills/guide.md
   # Expected: 8
   ```

3. Confirm SKILL.md section order (Behavioral Guide must appear before Resource Index):
   ```bash
   grep -n "## Behavioral Guide\|## Resource Index\|## Key Invariants" packages/twentythree-skills/skills/SKILL.md
   # Behavioral Guide line number must be lower than Resource Index line number
   ```

4. Confirm installer picks up guide.md:
   ```bash
   cd packages/twentythree-skills && node bin/add.js --project 2>&1 | grep guide
   # Should show guide.md in the copied file list
   ```

5. Read guide.md and confirm flag names are real (not placeholder text like `--<verified-flag>`):
   ```bash
   cat packages/twentythree-skills/skills/guide.md
   ```

6. Spot-check the webinar.md no-webinar-get block is still present at the file header:
   ```bash
   head -20 packages/twentythree-skills/skills/reference/webinar.md
   ```
  </how-to-verify>
  <files>packages/twentythree-skills/skills/guide.md, packages/twentythree-skills/skills/SKILL.md, packages/twentythree-skills/skills/reference/video.md, packages/twentythree-skills/skills/reference/webinar.md</files>
  <action>Run the verification commands in how-to-verify and confirm output matches expectations. Approve if all checks pass.</action>
  <verify><automated>grep -c "^### " packages/twentythree-skills/skills/guide.md</automated></verify>
  <done>Human confirms: guide.md has 8 rules, SKILL.md Behavioral Guide appears before Resource Index, installer lists guide.md, flag names are concrete (no placeholder text).</done>
  <resume-signal>Type "approved" if everything looks correct, or describe any issues found.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plan 01 output → Plan 02 | Plan 02 reads guide.md and the 23-01-SUMMARY.md to know which flag names were actually verified |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-03 | Tampering | SKILL.md section insertion | mitigate | Executor must read current SKILL.md before edit to find exact insertion line; incorrect insertion point would place Behavioral Guide after Resource Index, defeating its purpose |
| T-23-04 | Information Disclosure | Inline notes | accept | Forward-reference notes contain no credentials or PII; worst case is a broken relative link |
| T-23-05 | Denial of Service | webinar.md existing no-webinar-get block | mitigate | Acceptance criteria includes a grep check confirming the existing block was not removed or modified |
</threat_model>

<verification>
```bash
# SKILL.md has Behavioral Guide before Resource Index
grep -n "## Behavioral Guide\|## Resource Index" packages/twentythree-skills/skills/SKILL.md
# Behavioral Guide line < Resource Index line

# SKILL.md link is relative (no ../)
grep "\[guide\.md\](guide\.md)" packages/twentythree-skills/skills/SKILL.md

# video.md has 3 new Note callouts
grep -c "> \*\*Note:\*\*" packages/twentythree-skills/skills/reference/video.md

# webinar.md inline notes present (in addition to the file-header block)
grep -n "> \*\*Note:\*\*" packages/twentythree-skills/skills/reference/webinar.md

# All reference file notes use correct relative path
grep "\.\./guide\.md" packages/twentythree-skills/skills/reference/video.md
grep "\.\./guide\.md" packages/twentythree-skills/skills/reference/webinar.md

# guide.md picked up by installer
cd packages/twentythree-skills && node bin/add.js --project 2>&1 | grep guide
```
</verification>

<success_criteria>
- SKILL.md ## Behavioral Guide section exists and appears before ## Resource Index
- SKILL.md Behavioral Guide section contains [guide.md](guide.md) link (relative, no ../)
- video.md has 3 inline > **Note:** callouts (upload, list, get) with ../guide.md links
- webinar.md has 2 new inline > **Note:** callouts (webinar create, webinar list) with ../guide.md links
- webinar.md existing no-webinar-get block at file header is unchanged
- node bin/add.js --project from packages/twentythree-skills lists guide.md in output
- Human verification checkpoint passed
</success_criteria>

<output>
After completion, create `.planning/phases/23-behavioral-guide-authoring/23-02-SUMMARY.md`
</output>
