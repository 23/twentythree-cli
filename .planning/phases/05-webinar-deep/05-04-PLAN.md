---
phase: 05-webinar-deep
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/twentythree-cli/src/commands/webinar/series/list.ts
  - packages/twentythree-cli/src/commands/webinar/series/create.ts
  - packages/twentythree-cli/src/commands/webinar/series/update.ts
  - packages/twentythree-cli/src/commands/webinar/series/delete.ts
  - packages/twentythree-cli/src/commands/webinar/series/metrics.ts
  - packages/twentythree-cli/src/commands/webinar/series/recurrences.ts
  - packages/twentythree-cli/src/commands/webinar/series/apply-recurrence.ts
  - packages/twentythree-cli/src/commands/webinar/series/skip-recurrence.ts
  - packages/twentythree-cli/src/commands/webinar/series/cancel.ts
  - packages/twentythree-cli/src/commands/webinar/series/set-ondemand.ts
  - packages/twentythree-cli/src/commands/webinar/series/mapped-objects.ts
  - packages/twentythree-cli/src/commands/webinar/series/upload-thumbnail.ts
autonomous: true
requirements:
  - WEB-19

must_haves:
  truths:
    - "User can list, create, update, and delete webinar series"
    - "User can view series metrics and recurrences"
    - "User can apply and skip recurrences"
    - "User can cancel a series and set it to on-demand"
    - "User can view mapped objects for a series"
    - "User can upload a series thumbnail via chunked upload"
  artifacts:
    - path: "packages/twentythree-cli/src/commands/webinar/series/create.ts"
      provides: "Series create with name required"
      contains: "live_series/create"
    - path: "packages/twentythree-cli/src/commands/webinar/series/upload-thumbnail.ts"
      provides: "Chunked upload for series thumbnail"
      contains: "live_series_id"
    - path: "packages/twentythree-cli/src/commands/webinar/series/delete.ts"
      provides: "Series delete with delete_associations_p flag"
      contains: "delete_associations_p"
    - path: "packages/twentythree-cli/src/commands/webinar/series/cancel.ts"
      provides: "Series cancel with cancel_associations_p flag"
      contains: "cancel_associations_p"
  key_links:
    - from: "series/upload-thumbnail.ts"
      to: "uploadChunked"
      via: "chunked engine with tokenFieldName: 'live_series_id'"
      pattern: "tokenFieldName.*live_series_id"
---

<objective>
Create all 12 webinar series subcommands.

Purpose: Deliver WEB-19 (full series lifecycle including recurrence management, on-demand, mapped objects, and thumbnail upload).
Output: 12 series commands covering CRUD, recurrences, metrics, and chunked thumbnail upload.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-webinar-deep/05-RESEARCH.md

<interfaces>
From packages/twentythree-cli/src/lib/base-command.ts:
```typescript
export abstract class AuthenticatedCommand<T extends typeof Command> extends BaseCommand<T> {
  protected async fetchWebinarToken(webinarId: string | number): Promise<string>
  protected apiClient: ReturnType<typeof createApiClient>
  protected apiBaseUrl: string
  protected activeWorkspace: WorkspaceEntry
  protected printWorkspaceHeader(): void
}
```

From packages/twentythree-cli/src/lib/output.ts:
```typescript
export function formatJsonOutput(params: JsonOutputParams): JsonOutput
export function renderTable(headers: string[], rows: string[][]): Table
export function formatApiError(error: unknown): string
export function parseBoolParam(primary: boolean | undefined, alt: string | undefined): boolean | undefined
export const EXIT_ERROR = 1
export const EXIT_CANCELLED = 2
```

From packages/twentythree-cli/src/upload/chunked-upload.ts:
```typescript
export function uploadChunked(params: ChunkedUploadParams): Promise<ChunkedUploadResult>
```

From packages/twentythree-cli/src/upload/types.ts:
```typescript
export const DEFAULT_CHUNK_SIZE: number
export const DEFAULT_CONCURRENCY: number
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create series list, create, update, delete commands</name>
  <files>
    packages/twentythree-cli/src/commands/webinar/series/list.ts
    packages/twentythree-cli/src/commands/webinar/series/create.ts
    packages/twentythree-cli/src/commands/webinar/series/update.ts
    packages/twentythree-cli/src/commands/webinar/series/delete.ts
  </files>
  <read_first>
    packages/twentythree-cli/src/commands/webinar/list.ts
    packages/twentythree-cli/src/commands/webinar/create.ts
    packages/twentythree-cli/src/commands/webinar/delete.ts
    packages/twentythree-cli/src/lib/output.ts
  </read_first>
  <action>
**series/list.ts:**
Create `WebinarSeriesList` extending `AuthenticatedCommand`.

- `static description = 'List webinar series'`
- `static enableJsonFlag = true`
- Flags: `...AuthenticatedCommand.baseFlags`
- No args — workspace-scoped listing
- NOTE: series list does NOT require a token param
- Use `fetchAllPages` for pagination: `this.apiClient.GET('/live/series/list', { params: { query: { p: page, size } } })`
- Import `fetchAllPages` from `'../../../lib/pagination.js'`
- Table: `renderTable(['ID', 'Name', 'Status', 'Created'], series.map(s => [String(s.live_series_id ?? ''), applyCliTerms(String(s.name ?? '')), String(s.status ?? ''), String(s.creation_date_ansi ?? '')]))`
- Count line: `chalk.dim(\`\${series.length} series\`)`

**series/create.ts:**
Create `WebinarSeriesCreate`.

- `static description = 'Create a webinar series'`
- Flags: `...AuthenticatedCommand.baseFlags`, `name` (string, optional), `description` (string, optional)
- Interactive fallback: if `!flags.name && !this.jsonEnabled()`, prompt for name via `text({ message: 'Series name' })`
- Flag mode: error if `--name` missing
- POST to `/live/series/create` with body `{ name, description? }` as any
- Extract `live_series_id` from response: `(data as any)?.data?.live_series_id ?? (data as any)?.live_series_id`
- Success: `chalk.green('Series created')`, print ID
- Admin URL: `https://${this.activeWorkspace.domain}/manage/webinar/series/${seriesId}`

**series/update.ts:**
Create `WebinarSeriesUpdate`.

- Args: `id` (string, required, 'Series ID')
- Flags: `...AuthenticatedCommand.baseFlags`, `name` (string, optional), `description` (string, optional)
- CRITICAL: uses `live_series_id`, NOT `live_id`
- Build body: only include fields where `flags.X !== undefined`
- POST to `/live/series/update` with body `{ live_series_id: Number(args.id), ...definedFields }`
- Success: `chalk.green('Series updated')`

**series/delete.ts:**
Create `WebinarSeriesDelete`.

- Args: `id` (string, required, 'Series ID')
- Flags: `...AuthenticatedCommand.baseFlags`, `delete-associations` (boolean, optional, allowNo, description: 'Also delete associated webinars')
- Confirmation: `confirm({ message: \`Delete series \${args.id} from \${this.activeWorkspace.domain}? This cannot be undone.\` })`
- POST to `/live/series/delete` with body `{ live_series_id: Number(args.id), delete_associations_p: flags['delete-associations'] ? 1 : 0 }` (only include delete_associations_p if flag defined)
- Success: `chalk.green(\`Series \${args.id} deleted\`)`
  </action>
  <verify>
    <automated>cd /Users/steffenchristensen/23/twentythree-cli && pnpm --filter twentythree-cli exec tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `series/list.ts` uses `fetchAllPages` for pagination
    - `series/create.ts` has interactive fallback for `name`
    - `series/create.ts` prints admin URL with `/manage/webinar/series/`
    - `series/update.ts` uses `live_series_id` in body
    - `series/delete.ts` has `delete_associations_p` flag and confirmation prompt
  </acceptance_criteria>
  <done>4 series CRUD commands compile, list paginates, create has interactive fallback, delete supports delete-associations flag</done>
</task>

<task type="auto">
  <name>Task 2: Create series metrics, recurrences, apply-recurrence, skip-recurrence, cancel, set-ondemand, mapped-objects commands</name>
  <files>
    packages/twentythree-cli/src/commands/webinar/series/metrics.ts
    packages/twentythree-cli/src/commands/webinar/series/recurrences.ts
    packages/twentythree-cli/src/commands/webinar/series/apply-recurrence.ts
    packages/twentythree-cli/src/commands/webinar/series/skip-recurrence.ts
    packages/twentythree-cli/src/commands/webinar/series/cancel.ts
    packages/twentythree-cli/src/commands/webinar/series/set-ondemand.ts
    packages/twentythree-cli/src/commands/webinar/series/mapped-objects.ts
  </files>
  <read_first>
    packages/twentythree-cli/src/commands/webinar/series/list.ts
    packages/twentythree-cli/src/commands/webinar/series/delete.ts
    packages/twentythree-cli/src/lib/output.ts
  </read_first>
  <action>
**series/metrics.ts:**
Create `WebinarSeriesMetrics`.

- `static description = 'Get metrics for a webinar series'`
- Args: `id` (string, required, 'Series ID')
- `this.apiClient.GET('/live/series/metrics', { params: { query: { live_series_id: Number(args.id) } } })`
- Display metrics in key-value table: `renderTable(['Metric', 'Value'], ...)`

**series/recurrences.ts:**
Create `WebinarSeriesRecurrences`.

- `static description = 'List recurrences for a webinar series'`
- Args: `id` (string, required, 'Series ID')
- `this.apiClient.GET('/live/series/recurrences', { params: { query: { live_series_id: Number(args.id) } } })`
- Table: columns based on recurrence data

**series/apply-recurrence.ts:**
Create `WebinarSeriesApplyRecurrence`.

- `static description = 'Apply a recurrence to a webinar series'`
- Args: `id` (string, required, 'Series ID')
- Flags: `...AuthenticatedCommand.baseFlags`, `recurrence-id` (string, required: false)
- Interactive fallback for `--recurrence-id`
- NOTE: This is a GET endpoint per API types
- `this.apiClient.GET('/live/series/apply-recurrence', { params: { query: { live_series_id: Number(args.id), recurrence_id: Number(flags['recurrence-id']) } } })`
- Action: `chalk.green('Recurrence applied')`

**series/skip-recurrence.ts:**
Create `WebinarSeriesSkipRecurrence`.

- `static description = 'Skip a recurrence for a webinar series'`
- Args: `id` (string, required, 'Series ID')
- Flags: `...AuthenticatedCommand.baseFlags`, `recurrence-id` (string, required: false), `skipped` (boolean, required: true, allowNo: true, description: 'Set skipped (--skipped) or unskipped (--no-skipped)')
- Interactive fallback for `--recurrence-id`
- NOTE: This is a GET endpoint per API types
- `this.apiClient.GET('/live/series/skip-recurrence', { params: { query: { live_series_id: Number(args.id), recurrence_id: Number(flags['recurrence-id']), skipped_p: flags.skipped ? 1 : 0 } } })`
- Action: `chalk.green(\`Recurrence \${flags.skipped ? 'skipped' : 'unskipped'}\`)`

**series/cancel.ts:**
Create `WebinarSeriesCancel`.

- `static description = 'Cancel a webinar series'`
- Args: `id` (string, required, 'Series ID')
- Flags: `...AuthenticatedCommand.baseFlags`, `cancel-associations` (boolean, optional, allowNo, 'Also cancel associated webinars')
- Confirmation: `confirm({ message: \`Cancel series \${args.id} on \${this.activeWorkspace.domain}? This cannot be undone.\` })`
- POST to `/live/series/cancel` with body `{ live_series_id: Number(args.id), cancel_associations_p: flags['cancel-associations'] ? 1 : 0 }` (only if flag defined)
- Success: `chalk.green(\`Series \${args.id} cancelled\`)`

**series/set-ondemand.ts:**
Create `WebinarSeriesSetOndemand`.

- `static description = 'Set a webinar series to on-demand'`
- Args: `id` (string, required, 'Series ID')
- Flags: `...AuthenticatedCommand.baseFlags`, `update-associations` (boolean, optional, allowNo)
- POST to `/live/series/set-ondemand` with body `{ live_series_id: Number(args.id), update_associations_p: flags['update-associations'] ? 1 : 0 }` (only if flag defined)
- Action: `chalk.green('Series set to on-demand')`

**series/mapped-objects.ts:**
Create `WebinarSeriesMappedObjects`.

- `static description = 'List mapped objects for a webinar series'`
- Args: `id` (string, required, 'Series ID')
- `this.apiClient.GET('/live/series/mapped-objects', { params: { query: { live_series_id: Number(args.id) } } })`
- Table: columns from response (likely includes object IDs and types)
  </action>
  <verify>
    <automated>cd /Users/steffenchristensen/23/twentythree-cli && pnpm --filter twentythree-cli exec tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - All 7 files use `live_series_id` (NOT `live_id`)
    - `series/cancel.ts` has `cancel_associations_p` flag and confirmation prompt
    - `series/apply-recurrence.ts` and `series/skip-recurrence.ts` use GET (not POST)
    - `series/set-ondemand.ts` has `update_associations_p` flag
    - All files have `enableJsonFlag = true`
  </acceptance_criteria>
  <done>7 series management commands compile, all use live_series_id, cancel has confirmation, recurrence endpoints use correct HTTP method</done>
</task>

<task type="auto">
  <name>Task 3: Create series upload-thumbnail command</name>
  <files>
    packages/twentythree-cli/src/commands/webinar/series/upload-thumbnail.ts
  </files>
  <read_first>
    packages/twentythree-cli/src/commands/webinar/upload-image.ts
    packages/twentythree-cli/src/upload/chunked-upload.ts
    packages/twentythree-cli/src/upload/types.ts
  </read_first>
  <action>
Create `WebinarSeriesUploadThumbnail` extending `AuthenticatedCommand`.

- `static description = 'Upload a thumbnail for a webinar series'`
- `static enableJsonFlag = true`
- Args: `id` (string, required, 'Series ID'), `file` (string, required, 'Path to image file')
- Flags: `...AuthenticatedCommand.baseFlags`, `chunk-size` (integer, default: DEFAULT_CHUNK_SIZE), `concurrency` (integer, default: DEFAULT_CONCURRENCY)
- In `run()`:
  1. Parse, `printWorkspaceHeader()`
  2. Validate file: `await stat(args.file)` — error if not found
  3. Create inline `ProgressBar` class (copy from upload-image.ts pattern — `render(bytesUploaded, total, speed)` and `finish()`)
  4. CRITICAL: `tokenFieldName: 'live_series_id'` — the upload token value is the series ID
  5. Call `uploadChunked({ filePath: args.file, uploadToken: String(args.id), tokenFieldName: 'live_series_id', uploadUrl: \`\${this.apiBaseUrl}live/series/upload-thumbnail\`, bearerToken: this.activeWorkspace.bearer_token || undefined, chunkSize: flags['chunk-size'], concurrency: flags.concurrency, onProgress(bytesUploaded, total) { const elapsed = (Date.now() - startTime) / 1000; const speed = elapsed > 0 ? bytesUploaded / elapsed : 0; bar.render(bytesUploaded, total, speed); } })`
  6. `bar.finish()`
  7. Success: `chalk.green('Series thumbnail uploaded')`, print series ID
  8. Admin URL: `https://${this.activeWorkspace.domain}/manage/webinar/series/${args.id}`
  9. JSON: `formatJsonOutput({ ok: true, data: result, summary: 'Series thumbnail uploaded', breadcrumbs: [{ domain }, { resource: 'series', id: args.id }] })`

Import from: `'../../../lib/base-command.js'`, `'../../../lib/output.js'`, `'../../../lib/term-map.js'`, `'../../../upload/chunked-upload.js'`, `'../../../upload/types.js'`; import `stat` from `'node:fs/promises'`
  </action>
  <verify>
    <automated>cd /Users/steffenchristensen/23/twentythree-cli && pnpm --filter twentythree-cli exec tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `series/upload-thumbnail.ts` uses `tokenFieldName: 'live_series_id'` (NOT 'live_id' or 'upload_token')
    - `series/upload-thumbnail.ts` uses `uploadChunked` from chunked-upload module
    - File validates existence via `stat(args.file)`
    - Progress bar displays byte counts (not token values)
    - Admin URL includes `/manage/webinar/series/`
  </acceptance_criteria>
  <done>Series upload-thumbnail compiles, uses chunked engine with 'live_series_id' tokenFieldName, shows progress bar</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI -> API | Series IDs, recurrence IDs sent to API |
| File system -> upload | Thumbnail image uploaded via chunked engine |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-14 | Information Disclosure | series/upload-thumbnail.ts | mitigate | Progress bar shows only byte counts, never series token |
| T-05-15 | Tampering | series/upload-thumbnail.ts | mitigate | tokenFieldName explicitly set to 'live_series_id' |
| T-05-16 | Information Disclosure | all commands | mitigate | applyCliTerms() on all error messages |
| T-05-17 | Repudiation | series/delete.ts, series/cancel.ts | mitigate | Confirmation prompts include domain |
</threat_model>

<verification>
All 12 commands compile: `pnpm --filter twentythree-cli exec tsc --noEmit`
Series upload-thumbnail uses `tokenFieldName: 'live_series_id'`
Delete/cancel have confirmation prompts
Recurrence endpoints use correct HTTP methods (GET)
</verification>

<success_criteria>
- 12 series commands exist and compile
- CRUD with interactive fallback on create
- Upload-thumbnail uses chunked engine with 'live_series_id'
- Delete and cancel have confirmation prompts with domain
- All commands support --json
</success_criteria>

<output>
After completion, create `.planning/phases/05-webinar-deep/05-04-SUMMARY.md`
</output>
