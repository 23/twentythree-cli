---
phase: 21-skills-npm-publish
plan: 02
type: execute
wave: 2
depends_on:
  - 21-01
files_modified: []
autonomous: false
requirements:
  - NPM-01
  - NPM-04

must_haves:
  truths:
    - "User has verified NPM_TOKEN scope covers twentythree-skills package"
    - "User has confirmed release.yml structure is correct before pushing tag"
  artifacts: []
  key_links: []
---

<objective>
Human verification checkpoint: confirm NPM_TOKEN has publish access for `twentythree-skills`, review the release.yml structure, and prepare for the `skills-v1.0.0` tag push.

Purpose: The dry-run step in CI (NPM-04) catches token scope issues, but verifying locally before the first tag push saves a failed CI round-trip. This checkpoint also ensures the user reviews all changes before the irreversible npm publish.
Output: User confirmation that config is correct and token is ready.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-skills-npm-publish/21-CONTEXT.md
@.planning/phases/21-skills-npm-publish/21-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify release configuration and NPM_TOKEN scope</name>
  <files>.github/workflows/release.yml, packages/twentythree-skills/package.json, packages/twentythree-skills/README.md</files>
  <read_first>
    - .github/workflows/release.yml
    - packages/twentythree-skills/package.json
    - packages/twentythree-skills/README.md
  </read_first>
  <action>
Present the user with a verification checklist for the three files modified in Plan 01. The user must confirm:

1. **release.yml structure** — `on.push.tags` lists both `v*` and `skills-v*`; `publish` job has `if: "!startsWith..."` guard; `publish-skills` job has `permissions: id-token: write` and 7 steps (checkout, pnpm-setup, node-setup, install, validate, dry-run, publish); `smoke-test` still has `needs: publish`

2. **NPM_TOKEN scope** — User runs `cd packages/twentythree-skills && npm publish --dry-run` locally. If 403: create a new Granular Access Token on npmjs.com scoped to both packages, update `NPM_TOKEN` secret in GitHub repo settings.

3. **package.json** — version is 1.0.0, publishConfig.access is public, keywords array has 11 entries

4. **README** — no occurrence of `twentythree-skills add` remains; canonical form is `npx twentythree-skills`
  </action>
  <verify>
    <automated>node -e "const p=JSON.parse(require('fs').readFileSync('packages/twentythree-skills/package.json','utf8')); console.assert(p.version==='1.0.0'); console.assert(p.publishConfig?.access==='public'); console.assert(p.keywords.length===11); console.log('Config verified')" && grep -q 'publish-skills:' .github/workflows/release.yml && echo "Workflow verified"</automated>
  </verify>
  <acceptance_criteria>
    - User confirms release.yml structure is correct
    - User confirms `npm publish --dry-run` succeeds from packages/twentythree-skills (or has updated NPM_TOKEN)
    - User confirms package.json version is 1.0.0 with correct publishConfig and keywords
    - User confirms README has canonical bare invocation form
  </acceptance_criteria>
  <done>User has approved all configuration changes and verified NPM_TOKEN has publish access for twentythree-skills</done>
  <resume-signal>Type "approved" to confirm all checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local env -> npm registry | Local dry-run uses same token as CI to verify access |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-06 | Spoofing | NPM_TOKEN dry-run | mitigate | Local dry-run verifies token scope before CI tag push; prevents wasted CI runs with invalid token |
</threat_model>

<verification>
User has reviewed all three modified files and confirmed NPM_TOKEN has publish access for twentythree-skills.
</verification>

<success_criteria>
- User approves all configuration changes
- NPM_TOKEN scope verified (dry-run succeeds locally or token updated)
- Ready for `skills-v1.0.0` tag push
</success_criteria>

<output>
After completion, create `.planning/phases/21-skills-npm-publish/21-02-SUMMARY.md`
</output>
