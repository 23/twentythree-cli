---
phase: 11-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/twentythree-cli/README.md
  - packages/twentythree-cli/docs/commands/README.md
autonomous: true
requirements:
  - DOCS-01

must_haves:
  truths:
    - "docs/commands/ contains per-topic markdown files generated by oclif readme --multi"
    - "docs/commands/README.md is a handwritten index listing all 24 topics with one-line descriptions"
    - "Generated files cover all 244 commands across 24 topics at depth 2"
  artifacts:
    - path: "packages/twentythree-cli/docs/commands/README.md"
      provides: "Handwritten topic index"
      contains: "Command Reference"
    - path: "packages/twentythree-cli/docs/commands/video.md"
      provides: "Generated video topic page"
    - path: "packages/twentythree-cli/docs/commands/auth.md"
      provides: "Generated auth topic page"
  key_links:
    - from: "docs/commands/README.md"
      to: "docs/commands/*.md"
      via: "markdown links to per-topic files"
      pattern: "\\[docs\\]\\(.*\\.md\\)"
---

<objective>
Generate the complete command reference documentation using `oclif readme --multi --nested-topics-depth 2` and write a handwritten topic index at `docs/commands/README.md`.

Purpose: DOCS-01 requires per-topic markdown files generated by oclif, plus a navigable entry point for the command reference.
Output: `docs/commands/` directory with ~49 generated markdown files and one handwritten `README.md` index.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-documentation/11-CONTEXT.md
@.planning/phases/11-documentation/11-RESEARCH.md
@.planning/phases/11-documentation/11-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate command reference with oclif readme</name>
  <files>packages/twentythree-cli/README.md, packages/twentythree-cli/docs/commands/ (49 generated files)</files>
  <read_first>
    - packages/twentythree-cli/package.json (oclif config block, topicSeparator, bin name)
    - packages/twentythree-cli/oclif.manifest.json (verify it exists — oclif readme reads this)
  </read_first>
  <action>
1. Create the `docs/commands/` directory:
   ```bash
   mkdir -p packages/twentythree-cli/docs/commands
   ```

2. Create a stub `packages/twentythree-cli/README.md` (oclif readme requires this file to exist; Phase 12 will overwrite it):
   ```
   # TwentyThree CLI

   <!-- usage -->

   <!-- commands -->
   ```
   The file MUST contain the `<!-- usage -->` and `<!-- commands -->` marker comments — oclif replaces content between these markers.

3. Run oclif readme from the package directory:
   ```bash
   cd packages/twentythree-cli && node_modules/.bin/oclif readme --multi --nested-topics-depth 2 --output-dir docs/commands --readme-path README.md
   ```
   IMPORTANT: Use a RELATIVE path for `--output-dir` (`docs/commands`, NOT an absolute path). oclif resolves `--output-dir` relative to `process.cwd()` and absolute paths cause doubled path segments.
   IMPORTANT: Run from inside `packages/twentythree-cli/` so oclif can find the manifest.

4. Verify the output:
   - Count generated `.md` files in `docs/commands/` — expect approximately 49 files (24 top-level topic .md files + ~25 sub-topic .md files in subdirectories)
   - Spot-check `docs/commands/video.md` exists and contains command documentation
   - Spot-check `docs/commands/auth.md` exists and contains command documentation
  </action>
  <verify>
    <automated>cd /Users/steffenchristensen/23/twentythree-cli/packages/twentythree-cli && test -f docs/commands/video.md && test -f docs/commands/auth.md && test -f docs/commands/doctor.md && find docs/commands -name "*.md" | wc -l | xargs test 40 -lt</automated>
  </verify>
  <acceptance_criteria>
    - packages/twentythree-cli/docs/commands/video.md exists and contains markdown content
    - packages/twentythree-cli/docs/commands/auth.md exists and contains markdown content
    - packages/twentythree-cli/docs/commands/doctor.md exists and contains markdown content
    - packages/twentythree-cli/docs/commands/ contains at least 40 .md files (expect ~49)
    - packages/twentythree-cli/README.md contains a `<!-- commands -->` block populated by oclif
  </acceptance_criteria>
  <done>oclif readme has generated per-topic markdown files in docs/commands/ covering all 244 commands across 24 topics at depth 2</done>
</task>

<task type="auto">
  <name>Task 2: Write handwritten commands index (docs/commands/README.md)</name>
  <files>packages/twentythree-cli/docs/commands/README.md</files>
  <read_first>
    - packages/twentythree-cli/docs/commands/ (list directory to confirm generated files exist)
    - .planning/phases/11-documentation/11-PATTERNS.md (topic list with verified descriptions at Pattern 2)
  </read_first>
  <action>
Create `packages/twentythree-cli/docs/commands/README.md` with the following exact content structure:

Heading: `# Command Reference`

Opening line: `TwentyThree CLI provides 244 commands across 24 topics.`

Then a GFM table with columns: `Topic`, `Description`, `Reference`. Each row links to the generated `.md` file for that topic. Use the following verified topic list (all 24 topics plus `doctor` as a standalone command):

| Topic | Description | Link target |
|-------|-------------|-------------|
| `action` | Create a new CTA action on a video or webinar | `action.md` |
| `analytics` | Get conversion analytics data | `analytics.md` |
| `app` | Create a new app integration | `app.md` |
| `audience` | List audience companies | `audience.md` |
| `auth` | Configure domain and bearer token for a TwentyThree workspace | `auth.md` |
| `category` | Manage categories -- list, create, update, and delete | `category.md` |
| `collector` | Block a collector from a video or webinar | `collector.md` |
| `comment` | Add a comment to an object | `comment.md` |
| `doctor` | Check CLI credentials, connectivity, and token validity | `doctor.md` |
| `openupload` | List open upload tokens in the active workspace | `openupload.md` |
| `player` | Delete a player from the active workspace | `player.md` |
| `poll` | Create a new poll for a webinar | `poll.md` |
| `presentation` | List available presentation page link locations | `presentation.md` |
| `protection` | Apply protection to content | `protection.md` |
| `session` | Get a session access token | `session.md` |
| `setting` | Update workspace settings (key=value pairs) | `setting.md` |
| `site` | Get site settings for the active workspace | `site.md` |
| `spot` | Get details of a specific spot | `spot.md` |
| `tag` | List tags in the active workspace | `tag.md` |
| `thumbnail` | Manage thumbnail templates -- list, add, update, delete, duplicate, data, and manage files | `thumbnail.md` |
| `user` | Create a new user | `user.md` |
| `video` | Manage videos -- upload, list, update, delete, and more | `video.md` |
| `webhook` | List available webhook event types | `webhook.md` |
| `webinar` | Manage webinars -- create, list, update, delete, and more | `webinar.md` |
| `workspace` | List all configured workspaces | `workspace.md` |

The Reference column contains relative markdown links: `[docs](video.md)` format.

Note: `doctor` is a standalone command (not a topic with sub-commands). Link it to `doctor.md` — there is no `doctor/` subdirectory.

No emojis. ATX headings only. Blank line between heading and table.
  </action>
  <verify>
    <automated>cd /Users/steffenchristensen/23/twentythree-cli && grep -q "Command Reference" packages/twentythree-cli/docs/commands/README.md && grep -q "video.md" packages/twentythree-cli/docs/commands/README.md && grep -q "doctor.md" packages/twentythree-cli/docs/commands/README.md && grep -c "|" packages/twentythree-cli/docs/commands/README.md | xargs test 25 -lt</automated>
  </verify>
  <acceptance_criteria>
    - packages/twentythree-cli/docs/commands/README.md contains "# Command Reference"
    - packages/twentythree-cli/docs/commands/README.md contains "244 commands across 24 topics"
    - packages/twentythree-cli/docs/commands/README.md contains links to all 24 topic .md files plus doctor.md (25 entries total)
    - packages/twentythree-cli/docs/commands/README.md contains "video.md", "auth.md", "doctor.md", "webinar.md", "workspace.md"
    - File uses GFM pipe-table syntax with Topic, Description, and Reference columns
  </acceptance_criteria>
  <done>Handwritten index at docs/commands/README.md lists all 24 topics plus doctor with one-line descriptions and links to generated files (per D-03)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries in this plan. All work is static markdown generation from an existing manifest file. No user input, no network calls, no secrets.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | I (Information Disclosure) | docs/commands/*.md | accept | Generated docs expose command names and flags — this is intentional public documentation |
</threat_model>

<verification>
1. `find packages/twentythree-cli/docs/commands -name "*.md" | wc -l` returns >= 40 (expect ~49 generated + 1 handwritten = ~50)
2. `grep -q "Command Reference" packages/twentythree-cli/docs/commands/README.md` exits 0
3. `head -5 packages/twentythree-cli/docs/commands/video.md` shows oclif-generated content
</verification>

<success_criteria>
- docs/commands/ directory exists with ~49 generated markdown files covering all 244 commands
- docs/commands/README.md is a handwritten index with 25 entries (24 topics + doctor)
- All generated files are linked from the index
</success_criteria>

<output>
After completion, create `.planning/phases/11-documentation/11-01-SUMMARY.md`
</output>
